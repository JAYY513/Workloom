# Keep line endings stable across Windows/macOS/Linux checkouts: devsys
# writes LF files (.devsys/, skill files), and without this rule Windows
# Git warns about CRLF conversion on every status/diff (#342).
* text=auto
