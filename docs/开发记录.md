# Workloom 开发记录（问题 · 偏差 · 决策 · 遗留）

> 用途：实施过程中出现的问题点、与冻结方案的偏差、spec 阶段拍板决定、留给后续里程碑的事项，统一记在这里。
> 任务系统（http://localhost:3422）与 git 历史是流水账，本文档是长期索引 —— 任务很长，结论不能只留在会话里。
>
> 维护方式：每完成一个里程碑步骤，把该步骤的结论追加到对应小节。条目格式：
> `[类别] 背景/现象 → 处理或决定 → 位置/引用`。类别：`[问题]`（缺陷、坑、环境限制）、
> `[偏差]`（与方案或实施计划不一致且被接受）、`[决策]`（契约与拍板）、`[遗留]`（后续里程碑接手）。
>
> 事实来源顺序：方案与实施计划（`docs/原始文档/`，冻结）> 本文档 > 任务 artifacts。

## M0.1 语言与工程骨架（提交 758c688、5777008）

- [决策] 实现语言 Go 1.26（实施计划决策门 D1）：单文件分发、标准库自带文件锁与子进程管理、三平台交叉编译。
- [决策] 行尾统一 LF，状态文件由仓库根 `.gitattributes` 固定（提交 5777008），避免跨机器合出幽灵 diff。
- [问题] Windows 上 `core.autocrlf` 默认值会污染状态文件行尾；`.gitattributes` 已兜底，手工把 `.devsys/` 拷到别的仓库时仍要留意。

## M0.2 CLI 骨架与 `devsys init`（提交 1a8581f）

- [偏差] 方案 §14.3 目录树未列 `.devsys/.gitignore`，但 §14.2 要求 `local/`、`.cache/` 不提交 → init 生成 `.devsys/.gitignore`。
- [偏差] 实施计划 M0.2 产出清单漏写 `workflows/`（§14.3 有；§7 工作流存 `.devsys/workflows/<id>.md`）→ 以 §14.3 为准，init 创建全部 14 个目录。
- [决策] 忽略规则放在 `.devsys/.gitignore` 而非仓库根：随项目走，换设备后仍生效。
- [决策] 退出码契约 0 成功 / 1 内部 / 2 用法 / 3 前置，由测试固定 → `internal/cli/cli.go`。
- [问题] 冒烟时误把临时目录当真实项目，在真实用户注册表写入一条记录；已清理，此后所有冒烟固定用 `DEVSYS_CONFIG_DIR` 隔离注册表。
- [遗留] `internal/minyaml` 是临时序列化实现 → 已由 M0.3 替换并删除。

## M0.3 文本存储基元（提交 2d5db1b）

- [决策] 采用 vendored `gopkg.in/yaml.v3` v3.0.1：本机直连 `proxy.golang.org` 实测超时，vendor 后 `GOPROXY=off` 也能构建；同步删除 `internal/minyaml`。
- [问题] Windows 标准库 `syscall` 不导出 `LockFileEx` → 用 `syscall.NewLazyDLL("kernel32.dll")` 动态调用，不引入锁库 → `internal/storage/lock_windows.go`。
- [问题] Windows 字节区间锁会阻塞其他句柄读取被锁区间，超时诊断读不到持有者 → 锁区间挪到偏移 1 GiB，锁文件头部保持可读 → `internal/storage/lock.go`。
- [问题] Windows `os.Rename` 覆盖被其他句柄打开的文件会 `Access is denied`（Go 的 `os.Open` 不带 `FILE_SHARE_DELETE`）→ 原子写对目标占用做 20–100ms 退避重试 5 次；读路径坚持「读即读即关」。
- [决策] 事务协议：journal（意图 + payload 摘要）→ `commit.yaml` 原子落盘即提交点 → 逐 op 应用 → 清理；恢复按事务 id 排序、幂等重放（Put 比摘要、append 比「前置长度 + 区间摘要」）；恢复失败拒绝读写但允许只读诊断。
- [决策] JSONL 断尾：末字节非 `\n` 即不完整尾部；追加要求「精确前置长度 + 尾部完整」，不完整时阻断追加。
- [问题] 本机无 gcc，`go test -race` 不可用（`cgo: gcc not found`）；并发结论来自多进程测试（4 进程同信号 CAS、跨进程锁超时、8 并发追加不交错）。
- [遗留] 断电持久性未验证且代码与文档均不宣称；仅验收「进程中断 + 单文件原子性」。
- [遗留] macOS/Linux 仅交叉编译通过、未实跑（锁走 `syscall.Flock` 分支）。

## M0.4 严格配置解析（提交：`feat(m0.4): 严格配置解析…`，见 `git log`）

- [决策] M0.4 白名单 = M0.2 占位键集：`project.yaml` 认 `schema_version/id/name/created_at`，`config.yaml` 只认 `schema_version`；方案 §5.1 的完整 Project 模型留给 M1.1，config 业务键留给后续里程碑（扩展时同步白名单 + README + 本文档）。
- [决策] 状态文件（`state/current.yaml`、`state/milestones.yaml`）在 M0.4 只校验 `schema_version`，其余键放行，严格模型归 M1.1。
- [决策] 错误规范：`Problem{file,line,field,reason}` 渲染为 `文件:行号: 字段: 原因`；一次收齐全部问题，不做首错即停。
- [决策] 版本不可解释原则：`schema_version` 缺失或不受支持时，对该文件只报版本问题并跳过键校验，避免用未知 schema 误报。
- [决策] 新增退出码 `4 = invalid managed state`；写命令（当前只有 `init`）在任何改动前校验既有受管文件，未知版本拒绝写入并提示迁移（迁移命令属 M9.2）。
- [决策] 只读诊断面新增 `devsys config check`（普通读取：不加锁、不触发恢复），未知版本时仍可运行 —— 方案 §14.1「仅允许只读诊断」的可观察落点。
- [问题] `yaml.Node` 对第二份文档给出的行号是内容首行而不是 `---` 行 → 改取 DocumentNode 的行号，才能指向文档分隔符本身。
- [遗留] `config.yaml` 尚无业务键；M3（工作流/门禁）、M5（知识）、M6（执行）引入配置项时需同步白名单、README 与本文档。
- [遗留] 迁移命令（如 `devsys migrate`）属 M9.2；当前对未知版本只报错、不动手。

## M1.1 领域类型与序列化

- [决策] `internal/domain` 统一八类模型，config/init/CLI 直接使用 domain.Project，无重复 Project 类型或兼容别名。schema_version 统一为 1；旧最小 Project 字段保持可读。
- [决策] 扩展 Project 白名单并递归定位 scope/current_state/milestones 类型与未知字段；两个 state 文件转为严格领域文档。config.yaml 业务键保持后置。
- [问题] yaml.v3 默认把 nil slice 编成 []，往返丢失 null 语义 → 领域编码补正 YAML 节点，保留 nil/empty 区分；底层稳定编码仍复用 storage。
- [决策] Artifact 用 previous_id 回链前一版本记录；Event 包含 schema_version/id/project_id/type/subject/time/actor/related/content/reply_to，subject 与 related 均为 type/id 引用。
- [验收] 八类模型与两个 state 文档各 YAML/JSON 往返（20 子用例）、稳定字节、nullable false/null/empty 与拒绝未知版本/字段通过；go test ./...、go vet ./...、构建通过。真实隔离 CLI init → --json config check → init 返回 0，最后 created: nothing。

## M1.2–M1.6 领域存储与检索

- [决策] ID 分配统一模式：写事务内目录扫描 `max+1` + `PutYAML ExpectAbsent` CAS，冲突重扫重试≤5；WorkItem `<前缀>-<N>`（前缀大写归一）、Run `run-<YYYYMMDD>-<N>`（按日分序）、记录类 `decision|finding|artifact-<N>`（按类分序）。无全局计数器。
- [决策] 乐观更新契约：caller 读快照原始字节传入 Update，写事务内以 `ExpectHash(HashBytes(快照))` 守卫；storage.Tx 新增 `ReadForExpect`（写锁内读当前字节，供 NewArtifactVersion 等推守卫）。教训：写事务内重读后自造守卫永不冲突；st.Read 不可嵌套于 st.Write（锁超时）。
- [历史测量] 当前 Windows 环境曾测得 1000 文件串行扫描约 3.1s，16 worker 并行扫描约 0.6–0.7s；具体系统开销来源未确证，不能仅凭 Node/Go 对比归因于 Defender。每文件独立槽位保证确定性顺序。原 2s 单元测试硬断言已由任务 #130 移除，改为正确性断言与独立 benchmark。
- [决策] 事件流 ID=`ev-<UTC时间戳.微秒>-<4hex>` 无计数器；分片按 ev.Time 的 UTC 年月（12 月事件 1 月写仍落 12 月分片）；`reply_to` 仅限 comment、悬空/二级回复在聚合时报错。
- [决策] 检索排除 `.cache/`、`local/`；`search` 无命中 exit 0（`"total":0`）；缺/多关键字 usage=2。CLI 退出码契约由 TestSearchReportsMatchesAndExcludes 固定。
- [遗留] search 为逐行子串扫描，M5.1 知识页面解析接入时按页面 front matter 增强过滤；性能数字随硬件变化，升级时重测。
- [决策 #130] `TestSearchThousandFilesResponsive` 保留千文件命中数量、路径、行号、文本检查及耗时日志，不按墙钟时间失败，不跳过 Windows，也不增加预热来让单元测试过关。2s 仅作指定环境下的交互性能目标，不是跨机器保证。
- [测量 #130] `BenchmarkSearchThousandFiles/FreshFiles` 每轮新建文件后首次扫描；`RepeatedFiles` 一次不计时扫描后复用同一批文件。建文件、结果校验、清理不计时；不清空 OS 缓存，不能称为冷缓存基准。命令：`go test ./internal/search -run '^$' -bench BenchmarkSearchThousandFiles -benchmem -benchtime=3x -count=1`。本机 Windows/amd64、i7-13700H：FreshFiles 654.009ms/op、1658616 B/op；RepeatedFiles 12.055ms/op、1649922 B/op，各 3 次。固定 Go 版本、文件规模、机器及负载再作比较；当前不据此归因或宣称算法优化。
- [验收 #130] 最终指定测试扫描 754.546ms，正确性通过；`go test ./... -count=1` 全量通过。未改生产搜索实现；相对路径和大小写转换优化须先取得瓶颈证据。

## M1.7 里程碑剧本与快照

- [决策] `scripts/smoke-m1.ps1` 与 `.sh` 从源仓库根构建 CLI/helper，在全新临时 Git 项目运行，隔离 `DEVSYS_CONFIG_DIR`。避免在临时项目执行 `go run` 导致 Go 模块无法定位。
- [验收] Windows PowerShell 与 Git Bash 均跑通创建 draft 任务 → 决策/发现/评论回复 → Artifact 三版本 → Run 上下文快照 → ReadSnapshot/Update 为 done → config check/search → 回读验证。M1 仅持久化状态字段，不声称实现 M2 状态转换或门禁。
- [验收] Git 暂存 13 个 `.devsys/` 文件，helper 对每个文件验证 UTF-8、无 NUL，并拒绝暂存 `local/` 与 `.cache/`。真实运行快照位于 `docs/examples/m1-devsys/`。
- [决策] 默认及失败路径清理临时工作区；成功的 `-Keep`/`--keep` 保留项目，删除构建产物和临时注册表。PowerShell 恢复调用者 cwd、注册表环境变量及终端编码。
- [边界] 本次 Bash 使用 Windows Git Bash，非 Linux/macOS 实机验收；系统 `bash.exe` 指向 WSL 且其中无 Go。此前脚本备份与临时构建文件在收尾删除。

## M2.1–M2.5 状态机、调度态与对账（提交见 `git log`）

- [决策] 九状态保守图（`internal/domain/state.go`）：draft→backlog→ready→in_progress→review/verification→done；允许的前跳（ready→review/verification、in_progress→verification）在图内注释；blocked 经 `PreviousStatus` 恢复原状态，终态仅 repair 可动。
- [决策] 状态写入唯一路径 `workitem.Transition` / `ApplyRepair`；普通 `Update` 拒绝改状态字段，`Create` 初始状态限定 `{draft,backlog,ready,blocked}`；每次转换要求 actor+reason 并写 `status_changed` 事件，与状态同一 `storage.Write`。
- [决策] `ApplyRepair` 要求非空 `Confirmation` 与 `Guard func(*storage.Tx) error`：Guard 在写事务内复核全部证据（文件 sha256 / 缺失断言），CLI/MCP 无法绕过；这是唯一允许降低完成度的路径。
- [决策] 调度态与 §6.4 映射表落 `WorkItem.SchedulingState`，租约独立文件 `.devsys/scheduling/<id>.yaml`（`domain.SchedulingLease`）；每次领取生成新 32-hex token（调用方传 Token 被忽略），后写校验 owner+token+未过期，`ForExpired`/`ForOrphan` 释放不回归状态。
- [决策] 租约活跃时元数据写必须走 `UpdateClaimed`、Run 写必须走 `FenceRunUpdate`（事务内复核租约与快照）；`Start`（blocked/retry_queued→in_progress）与 `QueueRetry`（确定性 next_attempt_at = now + min(base·2^(attempt-1), 1h)）落地调度态其余取值。
- [问题] Claim 初版在事务外创建 Run（失败者残留 Run、同分片二次 append 违反 tx.claim）→ 改为 `run.CreateTx` 事务内创建、领取事件合并为单条 `claimed`。
- [决策] doctor/status 真只读：`storage.Inspect` 不 O_CREATE、不自动恢复，待恢复事务优先报告且不输出业务事实；无锁文件（从未有写者）时退回 `InspectUnlocked` 保守直读并如实标注，不谎报「无活动」。
- [决策] `recover` = 先 `storage.Recover` 确定性恢复，再按策略释放过期/孤儿领取（每次释放同事务写 `lease_recovered` 事件）；幂等：二次运行为空、无重复事件；缺失 workitem 的租约只报告不释放。
- [问题] `reconcile` 目录缺失判定用 `==` 比 `fs.ErrNotExist` 对包装错误恒假 → 改 `errors.Is`（影响「目录不存在=空列表」契约）。
- [问题] 不可解析的调度文件曾被静默丢弃 → `InspectionReport.UnreadableLeases` 显式报告；过期与孤儿同时成立时先归 `expired`（时间确定事实），修复了 `ReleasedExpired=0` 的误归类。
- [决策] repair 摘要 = sha256(按 workitem+kind 排序的提案与证据集)，不含墙钟；`--apply` 重新 dry-run 后校验摘要，逐提案一个写事务并在事务内复核证据，漂移即拒绝且不写文件；一个明确引用的缺失产物即足以提议 done→in_progress。
- [问题] CLI `--expect` 初版把 64-hex 版本当原始字节传给 CAS（必然冲突）→ 改为读快照校验哈希后传原始字节（fail-closed：哈希不符即拒绝），并容忍首尾空白（Git Bash 输出 CR）。
- [决策] M2.5 交付 `scripts/smoke-m2.ps1` / `.sh`（领取 → 模拟崩溃 → doctor → recover → 确认修复）与真实进程中断回归 `internal/workitem/m2_crash_test.go`：子进程经 `storage.TxnStageHookForTest` 在指定阶段 `os.Exit`；按提交点语义断言——journal-durable 崩溃 = 事务丢弃、领取不可见、可重新领取；committed 崩溃 = 决定已定、完整重放、其余领取者被 fencing 拒绝，读者永不观察半提交。
- [问题] M2.5 初测断言把「commit 后崩溃仍可见领取」误判为泄漏；经 `TestCrashRecoveryMatrix` 语义核对后按提交点分支重写，属测试假设错误而非实现缺陷。
- [问题] PowerShell 版剧本初版漏 `git init`，且 Windows 下 `bash` 需显式调用 Git Bash（系统 `bash.exe` 指向 WSL、其中无 Go）；PS1 文件需 UTF-8 BOM，均已在脚本内备注/修复。
- [验收] 两剧本一次通过（含 doctor 不改锁文件哈希断言）；`go test ./... -count=1` 全绿、gofmt/vet 净；`go build ./...` 通过。
- [遗留] repair 推断目前基于持久化记录（workitem/run/artifact/lease）；方案 §15.4 所述「代码与 Git 历史」证据在缺少锚点（如完成提交关联字段、M3 门禁产物）前不实现，避免无证据猜测——设计原则「先推断、再确认」优先。

## M3.1 策略文件格式与解析

- [决策] 策略文件 `.devsys/workflows/<id>.md` = YAML front matter + 提示词正文；front matter 从首行 `---` 到下一个整行 `---`。不设 schema_version（§5.3 示例只有 `version`，视为策略自身版本，必填 ≥1）；`id` 必填且必须等于文件名；BOM/CRLF 容忍。
- [决策] 未知键（任意层级）= warning（记录、加载成功、`workflow check` 仍 exit 0），结构/类型/必填/交叉引用错误 = error（该文件 Policy 为 nil，exit 4）——实施计划 M3.1「未知键按方案策略处理（记录但不崩）」的落点；一次收齐全部 issue 并按行号排序。
- [决策] schema 拍板：`steps[].type` 词表开放（仅形状 `^[a-z][a-z0-9_-]*$`，避免发明规范未定义的枚举）；`steps[].required` 默认 true；`when` 在 M3.1 只要求非空字符串（语法与字段白名单归 M3.6）；gates 键与 `exempt_stages` 取值 = 工作项九状态（`domain.AllStatuses()`）；hooks 名 = §4.8 四类（未知名按未知键记 warning）；`on_reject` ∈ {`block`, `regress:<status>`}，缺省 `block`。
- [决策] CLI `devsys workflow check`（只读：不加锁、不恢复、不写盘）与 `config check` 同族：exit 0/3/4；warning 打印 `warning: <文件:行号: 字段: 原因>` 但退出码仍 0；`--json` 输出 `policies[]`（file/id/name/version）+ `warnings[]`。
- [问题] front matter 内「第二文档」可达：行 `--- # 注释` 不等于闭合行 `---`，但被 YAML 解码器当作文档分隔 → 保留第二文档拒绝路径（测试固定）。
- [验收] gofmt/vet 净；`go test ./... -count=1` 全绿；真实二进制：三份示例 3 policies exit 0；坏策略逐条定位 `workflows/broken.md:2: id: required`、`:3: version: expected integer, got !!str` exit 4；未知键 warning（`warn-demo.md:5 future_key`、`steps[0].extra`）exit 0；非 `.md` 文件 warning `ignored` exit 0。
- [遗留] 示例策略 `when` 引用的字段白名单待 M3.6 条件求值器定稿；若白名单与示例字段不一致，届时同步示例与 README。

## M3.2 模板渲染与变量引用

- [决策] 模板语法 `{{name}}`（点分标识符，括号内允许空白）；无转义、无控制流：孤立 `}}`、未闭合 `{{`、空名、非法名在加载期报 error（field=`body`，行号=文件绝对行，`workflow check` 可观察）。
- [决策] 渲染严格失败：缺失变量 → `RenderError{kind: unknown_variable, variables[]}`（去重升序）；失败不返回部分输出、绝不静默空串（实施计划 M3.2 验收）。错误分三类 `unknown_variable | template_syntax | env_undefined`，`errors.As` 取结构化字段。
- [决策] `$VAR`/`${VAR}` 由 `ExpandEnv` 在使用时解析（未定义一次列全；`$$` 为字面 `$`；`$` 后非名字符如 `$5` 原样保留）。Parse 与 Policy 原样保留 `$VAR` 文本，加载期不解析、workflow 包无写盘路径——「密钥不落盘」的可实现边界，消费方（M6 hook 执行）负责不持久化解析结果。
- [验证] gofmt/vet 净；`go test ./... -count=1` 全绿；真实二进制：三示例仍 exit 0；`workflows/bad-template.md:9: body: unclosed {{` exit 4。
- [遗留] 渲染变量来源（`{{workitem.id}}` 等名字的供应契约与字段映射）归 M4.4/M5.6；示例策略正文暂不含模板引用，待供应契约定稿后再补。
- [决策] 独立评审回合（M3Reviewer）修订两处、拒绝两处、保留一处：① `workflows/` 目录缺失从 warning 升为 error（对齐 `config check` 对缺失受管文件的处理，exit 4）；② 未知 hook 名从 warning 改为 error——值域（封闭枚举：status/hook 名）失败一律 fail-closed，防止拼错门禁/钩子被静默丢弃；未知**键名**仍是 warning（前向兼容，实施计划原文仅约束键）。③ 拒绝「未知 status 在 gates/per_status 应记 warning」：同属值域，静默丢门禁是 fail-open 风险。④ 第二文档报错行号保留在异常分隔行 `--- # …`（该行即根因），不改为正文首行。⑤ CRLF 闭合行后空行保留为正文内容（verbatim 语义），新增测试钉死。
- [验证] 修订后复跑：`go test ./internal/workflow/... ./internal/cli/... -count=1` 绿；全量 `go test ./... -count=1` 绿；二进制：`.devsys/` 存在但缺 `workflows/` → exit 4；`--quiet` 仍打印 `warning: workflows/notes.txt` 且无成功横幅；未知 hook 名 `hooks.before_rund` → exit 4。

## M3.3 门禁、钩子与限额字段

- [决策] 门禁语义 = 进入阶段时检查 `gates.stages[<目标状态>]`（`exempt_stages` 直接放行、未声明不检查）：验收「缺少必需产物时推进被拒绝」+「审批前不放行」（进入 in_progress 前要求审批）的自然落点；缺失项按声明序输出（artifact 名 → 数量 → 评论 → 审批）。
- [决策] 审批证据在 M3.3 恒 false（fail-closed），M3.7 接消费查询；任务声明了 `workflow` 而策略缺失/非法 → 拒绝（exit 4），不静默跳过——与 M3.5 的分界：M3.3 无缓存、无回退，热载/LKG 到 M3.5 再谈。
- [决策] 领取质量门 = 确定性文本评分（标题长度/描述长度与结构/验收标准/具体引用/验收语言，满分 100，无模型调用）；阈值 `quality_gate.min_score`，未声明则永不拦截；拦截返回得 0 分组件作为改进项。
- [决策] 记录传播在完成事务内：decision/finding 的 `RelatedWorkItems` 命中完成任务 → `decision://<id>`/`finding://<id>` 追加到父任务与直接兄弟的 `context_refs`（同事务 CAS、不改对方状态、已存在则跳过 → 幂等）；无 parent 不广播。
- [问题] 同一事务内多次 `tx.AppendJSONL` 到同一分片违反「一文件一写」契约（M2.1 Claim 同款教训）→ 新增 `storage.Tx.AppendJSONLRaw`（多行 payload，校验空行与终止符）+ `events.AppendBatchTx`（按 shard 分组、一次 append）；状态变更事件与传播事件同批提交，保持原子。
- [验收] gofmt/vet 净；`go test ./... -count=1` 全绿；`scripts/smoke-m3.sh`（Git Bash；PowerShell 版收尾补）四段 PASS：done 门禁拦截（exit 4 + `artifact "test-results" is required`）→ 补 artifact+comment 放行 → 父/兄弟 `context_refs` + `record_propagated` 各 1、状态 draft 不变、child done → 低质量 claim 拦截（exit 4 + `quality_gate.min_score`/改进项）→ 改进后领取成功。
- [遗留] CLI 内「策略查找 + 证据收集」是目前门禁/质量门的唯一实现，M4.2 提取为 CLI/MCP 共用应用服务；hook 执行与 concurrency/limits 调度约束归 M6。
- [决策] 独立评审回合（M3Reviewer2，无 blocker/major）处置：① 事件 ID 随机后缀 2 字节 → 4 字节（注释声称 four random bytes 与实现不符；同事务多事件共享微秒，后缀承担去重）；② `Tx.AppendJSONLRaw` 拒绝含 CR 的 payload 并补 storage 单测（正常调用方走 MarshalJSONL，此为防御面）；③ 传播父子自环（`parent_id == self`）改为语义化错误并回滚完成（此前落到 storage「staged twice」断言）；④ 门禁证据的 TOCTOU 窗口在 `checkTransitionGate` 注释显式声明（证据在事务外收集，M4.2 升级为应用服务）；⑤ 策略重扫开销留给 M3.5 热载缓存（不在 M3.3 造第二套缓存）。
- [验证] 修订后复跑：`go test ./internal/storage/... ./internal/workitem/... ./internal/events/... -count=1` 绿（含新增 AppendJSONLRaw 两例、自环回滚一例）；全量 `go test ./... -count=1` 绿；`scripts/smoke-m3.sh` 四段 PASS。

## M3.4 就绪门与下一步优先级

- [决策] 判定语义：FAIL = 存在待恢复事务（§15.4：不输出业务事实，修复项给出 `devsys recover` 命令）；CONCERNS = 任一风险信号；PASS = 无。风险七类与顺序：过期租约 → 孤儿领取 → 不可读调度文件 → 滞留审查 → 阻塞 → 元数据非法 → 检查受限。
- [决策] 「过期」采用 M2.3 的租约过期语义；知识新鲜度（方案 §12.5）在 M5 作为附加风险接入（记遗留）。「滞留审查」= review/verification 且 `updated_at ≤ now − 24h`（`ReviewStaleAfter` 常量，Now 注入保证可测）。
- [决策] next 六级（§7.4 固定顺序，先命中先返回）：`recover_claim`（过期/孤儿租约按 id 序）→ `review`（review/verification，updated_at 最早）→ `start`（ready，dispatch 排序 priority 降序 → created_at 升序 → id）→ `start_backlog`（created_at 升序 → id）→ `milestone_review`（`project.yaml` milestones 首个非 done/completed）→ `report_done`。blocked 只作风险、不进候选。
- [决策] `devsys next` 只读：经 `reconcile.Doctor` 收集风险；仅在检查可信（InspectionOK 且无 pending）时列举 workitems——既不在不可信状态输出业务事实，也不为只读命令创建锁文件。判定在数据里，exit 恒 0（评估成功），便于脚本分支。
- [问题] 首版测试按索引断言「阻塞」风险细节，实际风险顺序中 stale 在 blocked 之前 → 改为按 kind 查找（测试假设错误，非实现缺陷）。
- [验收] gofmt/vet 净；全量 `go test ./... -count=1` 全绿；真实二进制：init 后（无任务）next=PASS/report_done；一个 ready 任务 → `next: start WLM-1`；`--json` 形状正确；人类输出无估算词汇。CLI 集成测试覆盖四类风险（过期/孤儿/阻塞/滞留审查）与 `next=recover_claim`。
- [遗留] 知识新鲜度风险信号（M5.3）与审批等待信号（M3.7 pending approval）接入 next；`blocked_by` 依赖先决的候选排除归 M6.4 派发。

## M3.5 策略热载入与 last-known-good

- [决策] 缓存位置 `.devsys/.cache/workflows/<id>.md`（本地、可删除可重建、不提交；`.cache/` 已在 ignore/search 排除表）；内容 = 最近一次成功解析的原文。`workflow.Resolve(ctx, root, id)` 为统一读取入口：每次重新 Parse（不信 mtime），成功后 best-effort 刷新快照（内容相同不写），失败回退快照并携带当前失败 `Issue`；无快照即 fail closed；缓存自身损坏视为缺失。
- [决策] 「只阻塞派发」落点：claim（新任务派发）在当前策略非法/缺失时拒绝（错误含首个定位问题与 LKG 说明），即使快照可用也拒绝；transition 门禁允许用快照评估既有任务（既有推进不被配置错误卡死）并打印 `warning: … using last-known-good`；next 增 `invalid_policy` 只读风险（不写缓存）；workflow check 不变。
- [问题] 路径拼接风险：Resolve 先按策略 id 形状（`^[a-z][a-z0-9-]*$`）校验并拒绝 `../`、大写、斜杠等（测试固定），再进行文件读写。
- [问题] 测试夹具缺 `.devsys/local/` 令缓存写入静默失败（best-effort 吞错）——真实项目由 init 创建 local/；夹具补齐 local/ 后通过（缓存写失败本就不阻塞 Resolve，是设计而非掩盖）。
- [验收] gofmt/vet 净；全量 `go test ./... -count=1` 全绿；真实二进制：好策略 claim 成功（落快照）→ 坏策略：`next` exit 0 且报 `invalid_policy`（逐条定位）、`claim` exit 4 提示原因、`transition` 按 LKG 放行并打印 warning → 恢复策略后 claim 成功。
- [遗留] M6 dispatch tick 应消费 `Resolve`（而不是直接 `Load`）；`--watch` 场景的刷新频率随之自然发生。
- [决策] 独立评审回合（M3Reviewer4）处置：① **P1 修复**：transition 在 gate 失败时也会先打印 LKG notice 再返回错误（此前 `return err` 吞掉提示，与「操作者可见」冲突）——notice 打印移到 err 判断之前；补回归（gate 不满足 + 策略非法 → stdout 含 last-known-good、stderr 含缺失证据）。② **采纳**：`next` 的 `invalid_policy` 风险补「工作项引用缺失策略」交叉核对（只读，按文件存在性判定；引用非法但存在的策略由文件级错误覆盖）——`work item WLM-N references missing policy "X"`。③ **拒绝/记录**：`--json next` 的 `ok` 保持「命令执行成功」语义（readiness 看 `verdict`，README 已说明）；`refreshLastKnownGood` 继续 best-effort 静默（缓存不承载正确性，degraded 信号等 M5 知识层统一可观察面）；缺失分支 Issue 无 line/field 是合理呈现（整文件级问题）。
- [验证] 修订后复跑：workflow/next/cli 包绿；全量 `go test ./... -count=1` 绿；二进制坏策略路径回归（claim 拒绝、transition 放行 + warning；gate 失败时 warning 同在）。

## M3.6 工作流实例与步骤推进

- [决策] 实例操作只改 `workflow` 字段 + 事件（同一写事务）：**不写工作项状态、不写调度态/租约/scheduling 目录**。§5.3「步骤 status 与工作项状态映射」按「进入要求」实现——目标步骤声明 `status` 时当前状态不匹配即拒绝（不隐式改状态）；避免实例操作变更执行语义，M4/M6 如需要再评估。
- [决策] 实例模型：`domain.WorkflowInstance` 增 `paused`（本步唯一领域模型变更，向后兼容）；终态哨兵 `done`/`cancelled`；`pause/resume/cancel` 只动实例字段与事件。
- [决策] 条件求值：`<field> <op> <literal>`（`== != < > in`）与 `<field> exists`；白名单 7 字段与类型规则（`< >` 仅 int、`in` 仅 string、`exists` 仅 `workitem.parent_id`）；不支持复合表达式；未知字段/操作符/类型不匹配在**加载期**报错（M3.1 遗留清零）；候选按声明序、首个满足者胜出。
- [决策] 拒绝面：`workitem.WorkflowStepError{Step, Reason, Allowed[]}`（含每个候选的 when 与 satisfied）；CLI exit 4 并逐条渲染允许候选；`start` 为派发类（坏策略拒绝），推进/`next` 允许 LKG + warning。
- [问题] CLI 首版把 workitem id 作为位置参数、flags 在后 → Go `flag` 在首个位置参数处停止解析（全部用法错误）——统一改 `--id`（与 workitem 命令族一致）。
- [验收] gofmt/vet 净；全量 `go test ./... -count=1` 全绿；二进制剧本：start→next（候选+next 提示）→跳步 exit 4（允许列表）→自动推进→pause 拒绝推进（exit 4）→resume→推进至 done；事件序 `workflow_started/step_completed/paused/resumed` 完整；`.devsys/scheduling` 不存在。
- [遗留] 失败/重试分支需要新的显式布尔字段（白名单扩展），示例策略已删去 `verification_failed` 类分支；步骤级门禁与审批联动（`approval_points` 消费）归 M3.7 与 M4。
- [决策] 独立评审回合（M3Reviewer5；无 blocker，2 major + 5 minor 全部处置）：① `in` 字面量支持逗号后空格（重拼 `parts[2:]` 再按逗号切分）；② 字符串字面量剥离成对单/双引号（`''` 表空串）、引号不平衡报策略错误、`in` 成员去重（重复报错）；③ `--to` 指向当前步（自环）显式拒绝，自动模式跳过自环候选；④ **租约 fencing**：`workflowWrite` 在租约活跃时要求调用方提供当前 owner+token（与 `UpdateClaimed` 同契约），CLI 增 `--owner/--token`——防止他人推进运行中任务的实例；⑤ `workflow next` 在 paused 时不输出 `next:` 建议（候选仍展示）；⑥ 信号路径尽力解析策略文件用于错误定位；测试负例补齐（paused next / 重复 pause / 重复 resume / done 后 pause|cancel / 自环 / 租约 / 终态）。
- [验证] 修订后复跑：workflow 1.149s、workitem 14.471s、cli 8.257s 绿；全量 `go test ./... -count=1` 绿；二进制：`workitem.status in ready, review` 命中（空格+引号解析）；无凭据 `step-complete` exit 4 `lease active`，带 `--owner/--token` 成功且 `scheduling=claimed` 不变。

## M3.7 审批服务

- [决策] `domain.Approval` 增 `stage` 与 `requested_status` 两字段（§4.9 的「门禁阶段 + 离开阶段即作废」语义在 §5.8 模型里无处可存）；其余字段沿用 §5.8。存储 `.devsys/approvals/<id>.yaml`，ID `approval-<N>` 目录扫描 + `ExpectAbsent` CAS（与 record 同形）；所有写入在事务内重读 + `ExpectHash` 守卫。
- [决策] 消费语义：`approval.ConsumeTx` 在**推进事务内**复核 approved/未消费/同任务/同阶段/`requested_status == 当前状态`，不符即整单回滚；`workitem.TransitionRequest.Guard` 改签名为 `func(*storage.Tx) ([]*domain.Event, error)`，Guard 事件与状态事件**同批提交**（一事务一分片一次 append；否则 `staged twice`）。
- [决策] 拒绝语义：先 `Decide(rejected)`（comment=理由 + `approval_rejected` 事件），再按策略 `on_reject`（Resolve、允许 LKG）处置：默认 `blocked`；`regress:<status>` 回退；处置失败返回明确错误（审批已拒绝、状态未动，不静默）。`status_changed` 的理由含 `approval <id> rejected: <reason>`。
- [决策] `next` 接入：新增风险 `pending_approval`；优先级 2 候选扩为「review/verification 或存在 pending 审批」，推荐理由为 `waiting for <approval-id>`（M3.4 遗留关闭）。
- [问题] 首版把消费事件沿用 `events.AppendTx` → 与状态事件同分片二次 append 违反 tx 契约（M3.3 同款）——改为 Guard 返回事件、`changeStatus` 统一 `AppendBatchTx`；`reconcile` 的修复 Guard 同步改签名（返回 nil 事件）。
- [验收] gofmt/vet 净；全量 `go test ./... -count=1` 全绿；二进制：门禁拒绝（exit 4 + `an approved, unconsumed approval is required`）→ request → approve → transition 放行且 `consumed_at` 写入 + `approval_consumed` 事件（各 1 次）→ 离开阶段后旧批准不匹配（拒绝再次进入）→ 重新请求后放行；reject 链：`approval_rejected` + 工作项 blocked + `status_changed` 内容含 `approval-1 rejected: risk too high`。
- [遗留] scope=action 审批的执行侧消费（§16.2 危险操作出现时接入）；withdraw 状态未暴露 CLI。
- [决策] 独立评审回合（M3Reviewer6；无 blocker，2 P1 + 7 minor 全处置）：① P1「离开后回到原状态旧批准仍可用」→ `domain.Approval` 增 `invalidated_at`；`workitem.changeStatus` 在同事务内执行 `approval.InvalidateForStatusTx`（离开的状态名下未消费审批全部失效，`approval_invalidated` 事件并入同批）；`MatchingGate`/`ConsumeTx`/`DecideTx` 均拒绝失效审批。② P1「拒绝处置与决定非原子」→ reject 改为在转换事务的 Guard 内 `DecideTx`，状态变更与审批拒绝同事务提交，失败整单回滚（审批保持 pending，错误说明 nothing was changed）。③ 新增 `storage.Tx.Staged(rel)` 并在失效 pass 中跳过本事务已暂存的文件（消费+失效同写一审批会触发 `staged twice`）。④ 消费/状态事件共享同一 `Now`。⑤ 重复未决请求拒绝（同 workitem+stage+requested_status）。⑥ `approval request` 对未声明 `require_approval` 的阶段打印 warning；`approval list --status` 校验取值（usage 2）。⑦ 测试补齐：并发 Decide 恰一胜者、失效审批拒决定/拒消费+事件计数、重复请求拒绝、next pending 单测、CLI 三例（回退失效/原子拒绝/坏 status）。
- [验证] 修订后复跑：approval 7.584s、workitem 39.815s、next 2.507s、cli 31.843s、reconcile 4.518s、storage 12.674s 全绿；全量 `go test ./... -count=1` 绿；二进制：ready→backlog→ready 后旧批准被拒且 `invalidated_at` 落盘、未声明门禁的请求有 warning、`--status foo` exit 2、对失效审批的 reject 原子失败（approval 仍 pending、工作项未动）。

## M4.1 MCP Server 骨架

- [决策] 传输与生命周期：stdio 逐行 JSON-RPC（无 Content-Length 帧）；stdout 仅协议、诊断走 `Log`（CLI 传 stderr）；支持协议版本 2025-06-18/2025-03-26/2024-11-05，客户端版本不支持时回最新支持版（由客户端决定去留）；`initialize` 三个必填参数（protocolVersion/capabilities/clientInfo.name+version）缺失即 -32602 且**不置位初始化**；未初始化请求 -32002（`ping` 除外，生命周期内任意点合法）；通知一律不响应；批数组 -32600（2025-06-18 已移除批处理）。
- [决策] 错误分层：协议级失败用 JSON-RPC error（-32700 仅语法非法 JSON；合法 JSON 但形状不符 -32600；未知方法 -32601；未知工具/参数不符 schema -32602 附 problems 定位；服务端 bug 如坏 schema -32603）；工具执行失败用结果 `isError:true` + `{code,message,problems?}`，code 对齐 CLI 退出码分类（usage/precondition/invalid/internal），M4.5 固化两表映射。
- [决策] profile 分级（方案 §8.6）：session/executor/reviewer/admin，默认 session+executor；`--profile` 逗号列表、未知名 exit 2（拼错不静默放宽/收紧）；可见性=与所选集合相交；空 Profiles=不可见（fail-closed——忘记声明不会泄漏到 admin 面）。
- [决策] 输入校验：极简 JSON Schema 子集（object/properties/required/additionalProperties/type/enum/items/minItems/minimum/maximum）；未知关键字/未知 type 一律拒绝（拼错如 `requierd` 不得静默放宽）；数值用 `big.Rat` 精确比较（1.0/1e3 按 JSON Schema 算整数、1e999 仍受 maximum 约束、超 float64 精度的整数不误判）；参数 UseNumber 解码、尾随数据拒绝；显式 `arguments:null` 按类型错误拒绝（仅缺省时补 `{}`）。
- [问题] serve 的 `--json`/`--quiet` 写在子命令后落到子 flag 集报「未定义标志」——注册空标志后两种位置统一为同一句用法错误。
- [验收] gofmt/vet 净；`go test ./internal/mcp/ ./internal/cli/ -count=1` 绿；全量 `go test ./... -count=1` 绿；脚本化管道（initialize→initialized→tools/list→health）stdout 全为合法 JSON-RPC；真实客户端 codex-cli 0.144.1 经 `-c mcp_servers.devsys.*` 注入（不落盘用户配置）连接并调用 `health`，取回项目 JSON（root/project.id/devsys.present）。
- [遗留] resources/prompts 能力、HTTP 传输、认证不在本步（按计划后置）；`instructions` 文案需与 M4.6 wire 同源对齐。
- [决策] 独立评审：子代理评审两次均因模型配额 429 失败（M4Reviewer1/M4Review1b），改用 slow 模型 completion 独立评审。处置：**误报 1 条**（`new(expr)` 在 Go 1.26 合法，编译与测试全绿为证）；**采纳修复 7 条**：① 合法 JSON 形状不符由 -32700 改 -32600（`json.Valid` 分流）；② initialize 必填参数校验（-32602 且不置位）；③ `id:null`/非标量 id → -32600（MCP 禁止 null id；不再当通知静默丢弃）；④ integer/enum 数值语义（big.Rat：1.0/1e3 通过、超 float64 精度不误判、enum 数值比较）；⑤ 坏 schema 改 -32603（服务端 bug 不伪装成客户端错误）；⑥ 空 Profiles 改 fail-closed；⑦ `toolResult` 序列化失败记日志。测试补：形状不符、null/bool/数组 id、initialize 缺参、显式 null arguments、坏 schema、整数与 enum 数值语义、超 float64 数值。
- [验证] 修复后复跑：mcp+cli 包绿、vet 净；真实客户端与脚本管道复验通过。

## M4.1a 采用官方 MCP Go SDK

- [决策] 用户指示「不熟悉的代码找源码/库，不必从零写」后，把 M4.1 自研的 JSON-RPC/协议层替换为官方 `github.com/modelcontextprotocol/go-sdk`（v1.8.0，经 goproxy.cn 拉取；`go mod vendor` 保持离线构建，`GOFLAGS=-mod=vendor go build ./...` 通过）。SDK 负责：逐行 stdio 帧、initialize 与协议版本协商（含 2025-06-18 及更新版）、`tools/list`/`tools/call`、结构体推断的输入 schema（`additionalProperties:false`）与输入校验、错误包装、read-only 注解。删除自研 `rpc.go`/`schema.go`/`tool.go`/`schemas.go`/`helpers.go` 及其测试。
- [决策] 保留本地职责：profile 分级改为**注册期过滤**（未暴露的工具不可按名调用，比事后过滤更严格）；错误分类仍是本地四类（usage/precondition/invalid/internal）+ `{code,message,problems?,notice?}` 结构化 payload（与 CLI 同形）。
- [问题] SDK 服务器在 stdin EOF 时会取消在途请求（自研实现逐行同步响应，EOF 后仍能拿到输出）——脚本化管道若一次性写入并立即关闭会丢响应；测试与剧本改为「保持管道打开 + 真实 SDK 客户端会话」（`IOTransport` + `NewInMemoryTransports`）。
- [验收] codex-cli 0.144.1 经 `-c mcp_servers.devsys.*` 注入连接 SDK 版服务器：调用 `health` 与 `workitem_list` 返回 `{"count":0,"workitems":[]}`；`workitem_create` 写入后 CLI `workitem list` 读到同一条 WLM-1。全量 `go test ./... -count=1` 绿、gofmt/vet 净、vendored 构建通过。

## M4.2 工具集：项目/工作项/工作流/审批

- [决策] **共享应用服务 `internal/app`**：CLI 的业务逻辑（门禁/质量门/审批消费/版本守卫/事务恢复/LKG 回退/依赖校验）整体迁入，CLI 变薄渲染层；MCP 工具是同一服务的适配器——「任一入口不能绕过约束」由构造保证而非约定。错误分类 `app.Error{Kind,Message,Problems}`：Kind 沿用 CLI 展示名（usage/precondition/invalid/workflow/gate/quality/workitem/approval），`Class()` 收敛到四类，CLI 映射退出码（2/3/4/1）、MCP 映射 code（usage/precondition/invalid/internal）。
- [决策] 工具面（§8.2）：`project_{list,get,status,blueprint_get,create,update,state_update}`、`workitem_{list,get,next,create,update,transition,claim,release,start,block,complete,comment,add_dependency,remove_dependency}`、`workflow_{list,get,start,step_next,step_complete,pause,resume,cancel}`、`approval_{list,get,request,decide}`。profile：查询=session；创建/推进/领取/工作流/审批请求=executor；审批决定=reviewer；项目元数据与状态=admin。
- [决策] **MCP 写工具一律要求 `expect`**（schema 层必填，`workitem_create` 与 append-only 的 `workitem_comment` 除外）：agent 必须「先读后写」；CLI 保留 `[--expect <hash>]` 既有可选语义（已被 cli_test 与文档钉住，不改）。两侧共用同一校验路径，哈希过期/畸形一律拒绝（fail closed）。
- [决策] CLI 补齐：`project list|get|status|blueprint|update|state-update`、`workitem list|update|release|start|block|complete|comment|dep add|remove`、`workflow list|get`、`approval get`；`workitem block/complete` = 走门禁的 transition 到 blocked/done。既有命令输出契约不变（cli_test 47.9KB 全绿）。
- [问题] 评审发现「审批被不需要它的门禁消费」：`gateEvidence` 无条件返回匹配审批，transition 随后消费，与 `approval request` 的「nothing will consume this approval」警告自相矛盾（M3.7 遗留）。修复：仅当目标阶段声明 `require_approval` 时才消费；其余情况审批保持 pending。
- [问题] 评审修复（其余采纳）：`workitem update` 空补丁拒绝；`workitem start` 回读真实状态而非硬编码 in_progress；项目/状态文件错误不再归类为 workitem；`samePath` 仅在 Windows/macOS 大小写不敏感；MCP `workitem_update` 补 `constraints`；transition/step-complete 拒绝时把 LKG `notice` 一并返回（`failNotice`）。
- [遗留] 已知窗口（记录，不在本步关闭）：① 门禁证据在事务外收集（TOCTOU）——要关闭需要 tx 级证据读取 API（记入 M6 前的候选）；② 依赖目标存在性校验在事务外（目标当前无删除路径）；③ `workitem_comment` 为 append-only 事件，不设版本守卫（无丢失更新语义）。
- [验收] gofmt/vet 净；全量 `go test ./... -count=1` 绿（含 mcp 包 SDK 会话测试：profile 过滤、只读注解、结构化错误、schema 拒绝未知参数；cli 包端到端：MCP 创建 → CLI 读取同一版本哈希）；codex 真实客户端：`workitem_get` 读哈希 → `workitem_update` 带 expect 更新成功。
- [决策] 评审来源：子代理评审因模型配额 429 不可用，改用 slow 模型 completion 独立评审（输入 app 层 + mcp 接线 + 工具样例，约 75KB）；处置见上（1 条误报：`Resolve` 契约已保证「有 Issue 必有 Policy」，返回 nil Policy 仅出现在未声明实例时）。

## M4.3 工具集：运行/记录/产物/知识

- [决策] 本步完成面：记录（`decision_{list,get,create,approve}`、`finding_{list,get,create,resolve}`、`event_{list,record}`、`artifact_{list,get,register,update,history}`）、运行（`run_{list,get,log,create,update,heartbeat}`）、上下文（`context_{get,for_workitem,refresh,compact}`）、`knowledge_status`。CLI 同步补齐 `decision|finding|event|artifact|run|context|knowledge status` 七个命令族（薄渲染层）。工具总数 58。
- [决策] 记录写入需要版本守卫（`decision approve`/`finding resolve`/`artifact update`/`run update` 的 `expect` 必填）；append-only 的 `event_record` 与 `workitem_comment` 不设守卫（无丢失更新语义）。`artifact_update` = 新版本追加（旧文件永不改写），`run_create` 为独立运行记录（不携带租约；claim 路径创建的 run 才带凭据），provenance（actor/reason）以 `run_created` 事件落 append-only 流。
- [决策] 上下文装配：`context_get` 给出项目事实/进度计数/readiness/下一步 + 最近记录与事件的**引用**（`decision://id` 等），正文按需读取；`context_refresh` 额外展开策略与 LKG notice；`context_compact` 去掉引用列表。不可信检查（pending 事务或无锁）时**不输出计数**并给出 notice（空 map 会被读成「没有工作项」，属于捏造事实）。
- [决策] 偏差（显式记录）：`run_verify`/`run_complete`/`run_fail`/`run_cancel` 归 M6.3/M6.6（Run 生命周期与完成校验涉及工作区不变量、重试队列与调度态）；`knowledge_{search,get_module,get_file,get_dependencies,get_related,refresh,validate}` 归 M5（索引层与生成器契约 §12.6）。`knowledge_status` 按降级语义实现：如实报告「无索引层、查询降级到记录/事件工具」，M5 落地时替换。
- [问题] `record.NewArtifactVersion` 的既有契约是「用当前版本覆盖 next 的字段」，因此 `artifact update` 的补丁会被吞掉（状态仍是 draft）——新增 `record.AppendArtifactVersion(ctx, id, expected, mutate)`：事务内读当前版本、校验 caller 快照、在副本上应用补丁、`ExpectAbsent` 写新文件并回链 `previous_id`。
- [决策] 评审回合（slow 模型 completion；子代理 429 不可用）处置：**采纳 2 blocker + 4 major + minors**：① `AppendArtifactVersion` 冲突重试会同父分叉——改为「同父已有后继 → `ErrSuperseded`（线性版本链）」，并把 caller 快照校验移进写事务（此前 `checkExpect` 只是事务外咨询检查，TOCTOU）；② run 读取改原子 `run.ReadSnapshot`（此前 `Get` + 单独读字节会撕裂版本）；③ 上下文计数仅在检查可信时输出；④ `ContextForWorkitem` 的 workflow 评估失败改为 notice（不再静默省略）；⑤ `EventList` 校验 subject_type/subject_id 配对与负数 limit；⑥ `EventRecord` 校验 related 引用形状。
- [验收] gofmt/vet 净；全量 `go test ./... -count=1` 绿；M4.3 验收测试：CLI 创建 decision → MCP `decision_get` 返回同一 ID 与版本哈希（反向亦同）、`context_get`/`context_for_workitem` 能定位记录引用、run 建/读/守卫更新/日志、artifact v1→v2 回链、`knowledge status` 降级输出。

## M4.4 会话接口 agent_session_start

- [决策] 一次调用给全（方案 §8.3）：`session`（回显调用方声明的 harness/agent_id/workspace/intent，**不持久化**——启动会话不得改变项目状态）、`project`（id/name/current_phase）、`state`（summary/risks/blockers/next_focus）、`current_workitems`（未关闭事项，priority 降序再按最近活动）、`recommended_next_action`（§7.4 建议 + 可直接执行的命令）、`context`（项目摘要 + 决策/发现/产物引用 + 最近事件）。精简模式 `--compact`：引用上限 3、省略事件列表。
- [决策] 命令只在可执行时给出：无工作项 id 的 `start`/`start_backlog` 不给带空 `--id` 的命令；`review` 指向具体事项时给 `workitem get <id>`，否则给队列列表命令。
- [决策] 诚实性：readiness 检查不可信（pending 事务或无锁）时 `current_workitems` 保持空数组并给 notice，风险逐条进 notices。
- [验收] gofmt/vet 净；全量 `go test ./... -count=1` 绿；CLI `session start`（含 `--compact`）与 MCP `agent_session_start` 返回同一推荐（type=start、目标 id、命令含 id）；compact 模式省略事件列表。
- [决策] 评审（slow 模型 completion，无 blocker）采纳 2 major + 4 minor：`current_workitems` 恒为数组（不再 null）；无 id 不生成命令；milestone 类动作取 `MilestoneID`；`review` 命令与 id 指向同一对象；compact 上限统一为 3；事件省略语义写入注释。

## M4.5 文件交换协议与退出码

- [决策] 退出码表固化：0 成功 / 1 内部 / 2 用法 / 3 前置条件 / 4 受管状态不可信；知识层 0/10/11（0 新鲜、10 过期、11 缺失，方案 §12.5）在 `knowledge status`（M5）落地前**预留**并在 README 标注。表由 `TestExitCodeContract` 钉住（成功/用法/前置条件/无效状态四类实测）。
- [决策] 交换格式：`--json` = 单文档信封（成功 `{ok:true,...}`；失败写 stderr `{ok:false,error:{code,kind,message,problems}}`）；`--jsonl` = 一行一条记录（7 个列表命令：workitem/decision/finding/event/artifact/run/approval list），字段与信封内同名；两者互斥（同时给出 → exit 2）。实现为 CLI 层 `writeJSONL[T]`，不触碰应用服务。
- [决策] 只读文件交换：`.devsys/**` 即事实来源（稳定键序、原子替换、版本哈希），脚本可直接读取；写入仍必须经 CLI/MCP（应用服务持有版本守卫/门禁/事务）；未来导入命令同样必须走该服务（README 明示）。
- [验收] `scripts/exchange-demo.sh`（Git Bash 实跑通过）：原始文件解析 → `--jsonl` 两行记录 → `--json` 信封 → 退出码 0/2/3/4 分支断言；新增 `internal/cli/exchange_test.go`（jsonl 一行一记录、互斥标志、退出码表）。
- [遗留] 知识层状态码（10/11）待 M5.3 实现；JSONL 目前只覆盖列表命令（单对象命令用 `--json`），如后续需要单对象 JSONL 再统一。

## M4.6 AGENTS.md 管理块（devsys wire）

- [决策] 标记区间 `<!-- devsys:begin | managed by `devsys wire`; edits inside this block are overwritten -->` … `<!-- devsys:end -->`；块内容为事实性纪律（状态目录、CLI/MCP 入口、先读后写、结构化输出与退出码）。块**外**内容——包括其他工具的管理块（如 repowiki）——逐字节保留；块内以我们为准（标记已声明）。
- [决策] 识别规则从严：标记必须是**整行**且**不在围栏代码块内**；重复标记、缺一半、end 在 begin 之前一律 exit 4 拒绝且不写盘（猜测哪半是我们的会毁掉手写内容）。CRLF 文件按原行尾写回；写入为临时文件 + rename（含 fsync），并保留原文件权限位。
- [决策] `--dry-run` 只输出差异预览（公共前缀/后缀裁剪，只显示真正变化的区域），不落盘；`--json` 给出 `{path,changed,created,diff}`。
- [验收] `TestWireIsIdempotentAndPreservesHandwritten`：连跑三次字节一致（第二次起报 already wired），手写段落与 repowiki 块原样保留；`TestWireDryRunPreviewsWithoutWriting`：dry-run 不创建文件且输出 `+` 行；`TestWireRejectsMalformedBlock`：缺 end → exit 4 且文件不变；`TestWireIgnoresQuotedMarkers`：围栏内引用的标记不被误认；`TestWireKeepsLineEndingsAndMode`：CRLF 一致 + 二次运行 no-op（权限断言仅 POSIX）。
- [决策] 评审（default 模型 completion，1 major + 4 minor 全部处置）：① 保留原文件权限（此前硬编码 0644）；② 预览只显示变化区域（此前会把块之后的全部内容标成删/增）；③ 围栏代码块内的标记不再被误认（此前会把示例正文当管理块覆盖）；④ CRLF 一致性与幂等（此前混用行尾、CRLF 块每次都会重写）；⑤ 临时文件 fsync。

## M4 收尾（剧本 / repowiki / 提交）

- [剧本] `scripts/smoke-m4.sh` + `scripts/smoke-m4.ps1`：覆盖 MCP 面（profile 过滤、health、decision 往返、`agent_session_start`、错误分类）、CLI/MCP 读写一致、退出码 0/2/3 分支、`--jsonl` 一行一条、`wire` 三次幂等 + 手写保留、`knowledge status` 降级。MCP 侧由 `scripts/m4helper` 用官方 SDK 作客户端（真实 stdio 传输）驱动，替代早期「人工 codex 会话」检查——可重复、无模型依赖。
- [验证] 双平台实跑通过（Git Bash 12.3s / PowerShell 14.1s）；全量 `go test ./... -count=1` 绿；`gofmt -l` 无输出；`go vet ./...` 净；`GOFLAGS=-mod=vendor go build ./...` 通过（离线）。
- [偏差] codex 0.144.1 实连复验：M4.1/M4.1a 期间完成（health / workitem_list / create / update with expect）；收尾当日追加 `agent_session_start` 复验时代理网关 503（环境故障，非代码），该路径由 `m4helper` 在剧本中确定性覆盖。
- [流程] repowiki 增量刷新至 M4 基线（validate 0 errors）；提交 `feat(m4)`（实现 + 剧本 + 文档）与 `docs(repowiki)`（wiki 产物），沿用路径提交、排除 AGENTS.md 与临时产物。

## M6.1 Harness Adapter 接口与 Shell Adapter

- [接口] `internal/harness`：方案 §9.2 九方法的 Go 映射（Name/Probe/Capabilities/Prepare/Command/Start + `Session` 句柄承载 stream_output/stop/collect_result）；`Capabilities` 八项按 §9.2 命名；包内不碰 `.devsys` 状态（纯执行层），由应用服务接线。
- [Shell 适配器] argv 直通（不做 shell 解释）、逐行 stdout/stderr 带流标记与时间戳、超长行按 64 KiB 分块且**按 rune 边界切分**（不丢字节、不产生乱码）、CRLF 归一；超时与取消终止整棵进程树（POSIX `Setpgid` + 进程组 SIGKILL；Windows `taskkill /T /F`，失败回退 `Process.Kill`）。
- [落盘] `.devsys/runs/<run-id>.jsonl`（§14.2 指定位置）：`start`/`output`/`exit` 记录，控制记录与每 128 行 fsync；崩溃断尾由 `storage.RepairTornTail` 截断并在流内写 `repair` 记录（不静默丢证据）——这是对 events 分片「断尾即拒绝追加」纪律的有意偏离，理由：运行流必须保持可追加，断尾记录本身从未成立为一条记录。
- [服务] `app.RunExec`：读 run（缺失=前置条件）→ 适配器 Prepare/Command/Start → 边写流边转发 sink → 结束后按既有版本守卫写回 run（commands/logs/非零退出说明进 result.errors）→ 事件 `run_exec_started`/`run_exec_finished`。`storeError` 补 `run.ErrNotFound` → 前置条件分类。
- [CLI] `devsys run exec --id <run> --actor <a> --reason <r> [--timeout 30s] [--json] -- <command...>`；Ctrl-C 经 `signal.NotifyContext` 触发进程树终止；`--json` 时命令输出镜像到 stderr，stdout 只承载信封。
- [决策] 退出码语义：`run exec` 的码描述 devsys 操作（0/1/2/3/4），被跑命令的退出码作为证据写入 JSONL 与 `result.errors`，不混用；领域模型不新增 exit_code 字段（保持方案 §5.4 的 result 形状）；本步不新增 MCP 工具（执行由 M6.4 dispatch 驱动，避免给 agent 绕过调度的自执行入口）。
- [验证] `internal/harness` 9 例（双流顺序/退出码非错误/长行分块与 rune 边界/超时杀树/Stop 杀树/缺失可执行文件/参数与工作区校验/探测与能力）；`internal/cli` 6 例（验收：命令输出镜像 + 流记录 + run 证据 + 事件；非零退出；用法与前置条件不落盘；超时杀树；`--json` stdout 纯净；断尾修复留痕）。

## M6.2 工作区管理与不变量

- [背景] 本任务由 `opencode-main` 起草 spec/plan 并实现到一半（`internal/workspace/{key,path,git,hook,workspace}.go` + key/path 测试），停在 `hook.go` 的编译错误（`tailBuffer` 缺 `WriteByte`）且 49 分钟无进展；经用户确认后由本会话接手收尾（补编译修复、集成测试、config/init、app 服务、CLI、文档），接口与语义沿用其 spec/plan（钩子集合以方案 §4.8 为准：`after_create`/`before_run`/`after_run`/`before_remove`）。
- [交付] `internal/workspace`：目录名净化 + 哈希后缀（`a/b` 与 `a_b` 不撞名）、工作区根（`workspace_root`，默认 `.devsys/workspaces`）、路径不变量（根内、拒绝 `..` 与符号链接逃逸、Windows 大小写不敏感比较）、git worktree 创建/复用/回收（失败回收 worktree + 目录 + 本次创建的分支）、四个生命周期钩子（走 M6.1 的 harness 适配器，统一超时、输出尾部 4 KiB、进程树终止）。`devsys worktree prepare|remove|list`。
- [接线] `app.WorkspacePrepare/Remove/List`（事件 `workspace_created`/`workspace_reused`/`workspace_removed`，`--run` 时守卫写 Run.workspace）；`run exec` 启动前校验工作区不变量并跑 `before_run`（致命：不 spawn、写 exit 形状记录 + `workspace_hook_failed` 事件 + 终态 failed）与 `after_run`（仅记录，不影响结果）。
- [配置] `.devsys/config.yaml` 新增可选键 `workspace_root`（白名单 + 文档；旧文件仍合法）；`devsys init` 生成的 `.devsys/.gitignore` 增 `workspaces/`，仓库根 `.gitignore` 同步。
- [验证] workspace 18 例（净化/哈希/碰撞/长度/逃逸/符号链接/大小写 + 创建复用（after_create 计数）/失败回收可重试/超时致命/移除钩子失败仍清理/脏工作区需 --force/幂等移除/List/配置根）；CLI 5 例（prepare 创建+绑定+事件、复用不重跑钩子、失败回收、移除与列表、越界拒绝、run exec 在工作区内且 before_run 致命）。

## M6.3 Run 生命周期与多轮续跑

- [装配] `internal/prompt`：首轮全量（任务简报 + 策略正文严格渲染 + 就绪 + 上下文指针 + 汇报协议），续跑只发「上一轮以来变化 + 未完成项」；纯函数、确定性 Hash（可离线重放并与流中 `prompt_hash` 对照）；模板缺变量即错误，绝不静默空串。变量集合：`workitem.{id,title,type,status}`、`project.{id,name}`、`run.id`、`step`。
- [轮次] 轮次是证据不是状态：`round` 记录写在 run 事件流里，下一轮号 = 流中最大 n + 1（重启/多进程安全）；轮数上限取 `limits.max_attempts`（方案 §5.3「轮数上限」在 M3.1 的落点），超限 exit 3 且不写 start。提示词正文落 `.devsys/local/runs/<id>/round-N.md`（不提交、可重放），子进程 env 增 `DEVSYS_RUN_ID`/`DEVSYS_ROUND`/`DEVSYS_PROMPT_FILE`；run 的 `input_context_refs` 记引用集。
- [生命周期] 阶段按 §4.8 推进（`building_prompt → launching_agent → streaming_turns → finishing`），每次推进守卫写 run.phase + 流内 `phase` 记录；**干净轮次不结束尝试**（退出码 0 → 流内 `status: succeeded` + `continuation_due_at` 缺省 30s，run 保持 `running`，供 M6.4 判定续跑），非零/超时/取消 → `failed`/`timed_out`/`canceled`；`run complete|fail|cancel` 显式收尾（守卫 + `run_finished` 事件，重复收尾被拒）；MCP 增 `run_complete`/`run_fail`/`run_cancel`（executor profile）。
- [决策] 轮次不进领域模型（`domain.Run` 保持 §5.4 形状，轮次与提示词哈希落在证据流）；`run verify`（claim head SHA 完成校验）归 M6.6；提示词正文不落受管状态（派生数据不进事实来源）。
- [验证] prompt 6 例 + app 8 例 + MCP 3 例 + CLI 5 例（首轮 full 与子进程 env、次轮 continuation 不重发简报且带「变化/未完成」、重放 Hash 与流中一致、轮数上限拒绝、`complete` 守卫与重复收尾拒绝）。

## M6.4 调度 tick、派发与阻塞

- [计划层] `internal/dispatch`（纯函数）：排序（priority 降序 → created_at → 标识符）、并发闸门（全局 + 按状态，多策略取最严，未声明缺省 1）、`blocked_by`（依赖需 done/cancelled，未知依赖视为未解决）、到期重试（`next_attempt_at` 未到即跳过）、`Max` 上限；跳过原因分类（not_dispatchable/lease_active/retry_not_due/blocked_by/max_reached/cap_global/cap_status）便于操作者定位。
- [编排层] `app.Dispatch`：`reconcile.Recover`（与 recover 同实现）→ 计划 → 逐个启动（`WorkitemClaim`（owner=dispatch）→ `WorkspacePrepare`（绑定 run）→ 子进程 `devsys run exec …`（`dispatch_command` 经宿主 shell；stdout/stderr 落 `.devsys/local/runs/<id>.exec.log`，`Process.Release()` 不等待）→ `run_dispatched` 事件）。被拒候选只记 notice 并**让出名额**给下一个候选（容量约束的是「在飞的尝试」，不是「被拒的候选」）。
- [命令来源] `.devsys/config.yaml` 新增可选键 `dispatch_command`（D1）；未配置且有候选 → exit 3 并列出候选数，不静默空转；M6.7 的适配器按 `assigned_harness` 选择后将取代它。
- [安全] `defaultSpawner` 在测试二进制内拒绝启动（`testing.Testing()`）：测试里 spawn 会把测试二进制当尝试跑，从而递归 spawn——本步开发中真的踩到过（695 个 `cli.test.exe` 进程），因此加护栏并在测试中注入 spawner。
- [坑] 顶层命令（无子命令）的 `rest` 不含命令名，`fs.Parse(rest)` 才是对的（`dispatch` 初版按子命令写法 `rest[1:]` 把第一个 flag 吞掉了）；`workitem update` 不接受 `--actor/--reason`。
- [验证] dispatch 计划层 8 例（排序/全局与按状态上限/依赖阻塞与取消视为解决/未知依赖/跳过原因/Max/空输入/取最严）；app 6 例（顺序启动 + 幂等 + 工作区绑定 + 事件、cap 拒绝、依赖不领取、dry-run 无副作用、缺命令拒绝、坏候选不拖队列）；CLI 4 例（dry-run 计划、缺命令 exit 3、用法、只读命令不派发）。实机剧本：cap 1 下首个 tick 启动高优先级（真实子进程，attempt 成功 `round=1`）、重复 tick 幂等、释放领取后队列推进、dry-run 零副作用、`run_dispatched` 事件 2 条。

## M6.5 重试、退避与停滞检测

- [退避] `internal/retry`（纯函数）：`Delay(base, max, attempt, seed)` = `min(base·2^(attempt-1), max)` + 派生抖动（延迟的 0–19%，夹在上限内）；`base` 缺省 30s（策略无 base 键），`max` 取 `limits.backoff_max_seconds`（缺省 1h）且是**硬上限**——上限小于 base 是合法的「尽快重试」。抖动由 sha256(标识#尝试) 派生，重启后同一失败得到同一时刻。
- [重排] `workitem.RetryOptions.Delay` 允许调用方传入算好的退避（默认仍走内建倍增）；`domain.StatusRetryQueued` 常量补上（原先散落字面量）。tick 在计划前扫尾（恢复之后、计划之前，符合 §4.8 的 tick 顺序）：失败/超时/停滞 → `QueueRetry`（`retry_queued` + `next_attempt_at` + 事件）；尝试用尽（`limits.max_attempts`，缺省 1）→ 释放领取 + `retry_exhausted` 事件；取消 → 只释放（人的决定不自动重试）。
- [停滞] 运行中尝试的进展证据=其事件流最后一条记录的时间（无记录则用 StartedAt）；超过 `limits.stall_threshold_seconds`（缺省 30 分钟）→ 先 `RunFinish(stalled)` 记终态，再按退避重排。`--dry-run` 不扫尾（扫尾写状态）。
- [计划层] `retry_queued` 且到期的项成为候选（此前只认 `ready`）；未到期仍报 `retry_not_due`。
- [验证] retry 3 例（可复现且按标识区分、倍增与上限、退化边界）；app 4 例（失败→重排且到期后可派发、用尽→释放 + 事件、停滞→终态 stalled + 重排 + dry-run 不动、进展中的尝试不动）；全量套件绿。

## M6.6 完成校验与人工复核

- [claim head] 工作区绑定到 run 时记录工作区 HEAD 到 `Run.Claim.HeadSHA`（`bindWorkspaceToRun`；读不到 HEAD 即报错——绑定一个无法校验的 run 比失败更糟）。dispatch 与人工两条路都经过它。
- [校验] `app.RunVerify`（只读）与 `RunFinish` 的 succeeded 路径共用 `verifyCompletion`：分支 SHA（有工作区时 `refs/heads/<branch>`，否则当前 HEAD）与 claim head 比对；不同才算推进。`verification.advanced` / `head_sha_at_complete` 按结论落盘。
- [拒绝] 未推进 / git 报错 / 缺 claim head → 拒绝完成：run 保持非终态、写 `verification.advanced=false`、工作项转 `review`（先释放领取——被领取的项拒绝他人推进，复核者不该需要 agent 的 token）、事件 `completion_refused`（含两侧 SHA），返回前置条件错误（exit 3，理由可读）。
- [放行] `--force --by <reviewer>`：`verified_by` + 事件 `completion_overridden`；缺 `--by` 是用法错误（例外必须可追溯到人）。
- [只读] CLI `run verify --id <run> [--json]` + MCP `run_verify`（session profile、read-only 注解）；`run_complete` MCP 工具增 `force`/`by`。
- [影响] 既有测试里「无工作区直接 complete」的用例改为 `--force --by`（新契约下无证据的完成需要复核者显式接受）。
- [验证] app 6 例（拒绝且转 review + 事件、提交后通过且 head 落盘、force 留名、force 缺 by、无证据拒绝、verify 只读）+ CLI 1 例 + MCP 1 例；全量套件绿。

## M6.7 Harness 适配：Codex / OpenCode / Claude Code

- [适配器] `internal/harness/cli.go`：`cliAdapter` 组合 Shell 适配器（执行、流式、超时、进程树终止复用一份实现），只实现 `build_command` 与能力声明；`Codex()/OpenCode()/ClaudeCode()` + `ByName`/`Names`。参数形态按各 CLI 真实接口（codex `exec --json -s workspace-write -C <dir> -`；opencode `run --format json --dir <dir> <prompt>`；claude `-p --output-format stream-json --dangerously-skip-permissions`），续跑参数模板由 `Request.SessionID` 触发。
- [stdin] Shell 适配器增 `Request.Stdin`/`Command.Stdin`：起进程后写入并关闭（写失败只进流，不算启动失败）。codex/claude 的提示词走 stdin（避免命令行长度上限），opencode 走位置参数（无 stdin 入口，文档注明长度风险）。
- [探测] `Probe` 跑 `<bin> --version`（10s 超时）：未安装 → `Installed=false` + 说明，调用方拒绝（前置条件错误）；**绝不静默回退**到别的 Harness。`run exec --harness shell` 明确指向「用 `-- <command>` 或 dispatch_command」，因为 shell 适配器不消费提示词。
- [接线] `RunExecRequest.Harness/Model`（适配器 `build_command`，提示词用 M6.3 装配文本；未给则走 argv 路径）+ `recordAgent`（把 harness/model 写进 run 的 `agent` 字段，验收靠它比对）；dispatch 按 `assigned_harness` 启动 `run exec --harness <name>`（未声明回退 `dispatch_command`）；CLI `run exec --harness/--model`（与 `--` 命令互斥）。
- [工作区汇报] CLI 支持 `DEVSYS_PROJECT_ROOT` 覆盖项目根：工作区是 git worktree，自带 `.devsys/` 副本，agent 在工作区里跑 `devsys` 时靠这个变量把汇报写回项目状态（有测试）。
- [实跑] 真实 codex 0.144.1 与 opencode 1.18.21 各启动一次（适配器命令行被真实 CLI 接受、JSONL 流进 run 事件流）：两次都因**提供方后端不可用**失败（codex 代理 503、opencode 本地网关 socket closed），run 记录如实为 failed + 完成校验拒绝——**环境偏差，非适配器缺陷**。claude 本机未安装 → 探测/拒绝路径已验证（exit 3 + 说明），三个适配器只完成 2 个实跑 + 1 个未安装，偏差入档。
- [确定性验收] 用桩 `codex`（PATH 上的脚本）走完整链路：适配器建命令 → 提示词经 stdin 到达（stub 报 730 字节）→ 工作目录=工作区 → 提交 → `run verify` 报 advanced → `run complete` 通过 → run 记录 `agent.harness=codex`。这补上了「三个 Harness 各完成一次同规格任务」在提供方不可用时的可验证部分。
- [验证] harness 适配器单测（argv 形态含 stdin/位置参数与 model、缺提示词报错、能力声明不虚报、Probe 对已装/未装的结论、stdin 真被进程读回）+ app 3 例（不可用/未知名/互斥、dispatch 用 assigned_harness）+ CLI 1 例（DEVSYS_PROJECT_ROOT）。

## M6.8 一致性自检（Symphony SPEC §17）

- [产出] `docs/M6-一致性自检.md`：按 SPEC §17.1–§17.8 逐条给结论（通过 / 替换 / 偏离 / 不适用）+ 证据（代码位置或测试名），并对每条偏离写明理由与影响；末尾汇总 6 类偏离。
- [结论要点] §17.1 工作流/配置：除「外部 tracker 键」与「`codex.command`」外全部通过或已替换（严格模板渲染、LKG 热载入、并发归一化均有测试）。§17.2 工作区与安全：**全条通过**（确定性 key、哈希抗碰撞、根内不变量含符号链接、钩子语义、cwd 与越界拒绝）。§17.3 tracker：整节不适用（方案 §10.7 已决定替换为 `.devsys` 事实来源）。§17.4 编排：排序/依赖/对账幂等/续跑/停滞/重试条目通过；偏离 4 条（退避基准 30s、槽位耗尽不写重排事件、运行中尝试不被状态变化强制停止、终态不自动清理工作区）。§17.5 客户端：CLI 适配器而非 app-server 协议 → 协议级条目不适用，cwd/超时/stderr 分离/沙箱策略通过。§17.6 可观测：读面只读且不影响正确性通过；证据写失败中止尝试为**有意偏离**。§17.7 CLI 生命周期：退出码与错误可见性通过，无位置参数/`WORKFLOW.md` 为偏离。§17.8 真实集成：如实记为「受环境阻塞未通过」（提供方 503/socket closed + claude 未安装），确定性链路用桩 harness 覆盖。
- [验收] 清单每条有结论；偏离均带理由与后续归属（其中 3 条属「方案 §10.7 已决定的替换」，2 条为有意设计选择，1 条为环境阻塞）。

## M6 收尾（剧本 / 指派可写 / repowiki / 提交）

- [剧本] `scripts/smoke-m6.sh` + `scripts/smoke-m6.ps1`：用桩 codex（PATH 上的脚本，读 stdin + 在工作区提交）覆盖适配器链路，无需模型提供方；七段检查见 README 的 M6 剧本节。双平台实跑通过（Git Bash 10.3s / PowerShell 10.0s）。
- [补口] `workitem update --assigned-harness/--assigned-agent`（CLI + MCP 字段 + app 请求），空值清除（`nil` 单一表示）；此前 `assigned_harness` 只能手改受管状态——dispatch 读它却没人能设它，属 M6.7 的接口缺口，此处补齐并补测试。
- [走查发现] 剧本首版三处自身错误（`workitem create` 输出是多行、`assigned_harness` 无 setter、路径未加引号）已随剧本修正；产品侧只补了 setter 一项。
- [流程] repowiki 增量刷新至 M6 基线（validate 0 errors）；提交随各里程碑落地（M6.1→M6.8 + 收尾，均为路径提交）。

## 全局遗留与跨里程碑事项

- [遗留] `-race` 检测器在本机不可用（无 gcc）；有编译环境时补跑 `CGO_ENABLED=1 go test -race ./...`。
- [遗留] macOS/Linux 实机未验证（仅交叉编译）；M8 设备接力前至少各跑一次全量测试。

- [遗留] 用户级注册表只存路径与标识（§14.4）；跨设备同时领取明确不支持，设备接力前必须停写。
- [决策] 仓库内 `AGENTS.md` 长期处于 git 暂存区未提交：历次里程碑提交均按路径提交（`git commit -- <paths>`）以排除它，避免把环境文件混入产品历史。
