---
status: stable
type: module
dimension: architecture
triggers:
  - 架构设计
  - 命令分发
  - 错误传递
  - 退出码链
  - 渲染层
  - workflow 分发
  - approval 分发
  - next 分发
description: CLI→领域分层、错误传递链、配置校验器与项目注册表之间的依赖方向；M3 新增 workflow/approval/next 三条分发链与 CLI→internal/{workflow,approval,next} 依赖边
generated: true
source_commit: cf7b256
generator: repowiki-gen
---

# 项目接入与配置 · 架构设计

## 内部结构

```
cmd/devsys/main.go          # 进程入口，转发 + 退出码
   ↓
internal/cli/cli.go         # 全局开关 + 命令分发 + codedError + render
   ↓
   ├── internal/config/*    # 受管元数据校验（config.Load / Diagnose）
   ├── internal/project/*   # init 的项目侧实现
   ├── internal/registry/*  # 用户级项目注册表
   ├── internal/version/*   # 构建版本标识
   └──
       ↓ 仅作为调用方
   ┌───────────────────────────────────────────────────────────┐
   │ M1：storage / workitem / events / run / record / search  │
   │ M2：reconcile（doctor/recover/repair）                   │
   │ M3：workflow / approval / next                           │
   └───────────────────────────────────────────────────────────┘
```

CLI 层只做三件事：

1. 解析全局开关与位置参数；
2. 按顶层命令 + 子命令分发给对应的 `runXxx` 函数；
3. 调 `render` 把结果或错误按人/机两种格式写到 stdout/stderr，把退出码交给操作系统。

## 命令分发

[internal/cli/cli.go:144-180](file://internal/cli/cli.go#L144-L180) 用 `switch cmd` 分发顶层命令：

```go
case "init":          return render(stderr, opts, runInit(stdout, opts, rest))
case "config":        return render(stderr, opts, runConfig(stdout, opts, rest))
case "search":        return render(stderr, opts, runSearch(stdout, opts, rest))
case "workitem":      return render(stderr, opts, runWorkitem(stdout, opts, rest))
case "doctor":        return render(stderr, opts, runDoctor(stdout, opts, rest))
case "recover":       return render(stderr, opts, runRecover(stdout, opts, rest))
case "repair":        return render(stderr, opts, runRepair(stdout, opts, rest))
case "workflow":      return render(stderr, opts, runWorkflow(stdout, opts, rest))   // M3
case "approval":      return render(stderr, opts, runApproval(stdout, opts, rest))   // M3
case "next":          return render(stderr, opts, runNext(stdout, opts, rest))       // M3
```

每条子命令通过独立的 `runXxx` 函数实现，例如 `runWorkflow` 进一步把 `check | start | next | step-complete | pause | resume | cancel` 分发给对应子函数（[internal/cli/cli.go:458-476](file://internal/cli/cli.go#L458-L476)）。

## 错误传递链

```
storage.ErrXxx / domain.TransitionError / workflow.Issue / approval.ErrXxx
   ↓
工作流/审批/工作项的 runXxx 函数通过 errors.Is / 类型断言取 sentinel
   ↓
codedError{code, kind, message, problems?}
   ↓
render 决定人/机两种输出
   ↓
stderr JSON: {ok:false, error:{code, kind, message, problems?}}
```

### codedError

[internal/cli/cli.go:33-44](file://internal/cli/cli.go#L33-L44) 定义退出码常量；[internal/cli/cli.go:103-114](file://internal/cli/cli.go#L103-L114) 定义 `codedError{kind, code}` 与构造器（`errUsage` / `errPrecondition` / `errInvalid` / `errInternal`）。

| kind | 触发 | code |
|---|---|---|
| `usage` | `errUsage(...)`：参数缺失 / 未知选项 / 子命令不存在 | 2 |
| `precondition` | `errPrecondition(...)`：非 git 仓库 / `.devsys/` 不存在 / `--expect` 不匹配 / `--confirm` 摘要不符 | 3 |
| `invalid` | `errInvalid(...)`：`config.Problems` 非空 / 状态机非法转换 / 修复摘要不符 / workflow 策略非法 / approval 错误 | 4 |
| `internal` | `errInternal(...)`：读/写系统调用失败 | 1 |

M2 起扩展 `invalid` 的子类型：`kind="workitem"`（[internal/cli/cli.go:477-482](file://internal/cli/cli.go#L477-L482)）。
M3 起又扩展：`kind="workflow"`（[internal/cli/cli.go:522-543](file://internal/cli/cli.go#L522-L543)）、`kind="approval"`（[internal/cli/cli.go:1218-1232](file://internal/cli/cli.go#L1218-L1232)）。

`codedError` 的 JSON 形式（`--json` 模式）：

```json
{
  "ok": false,
  "error": {
    "code": 4,
    "kind": "invalid",
    "message": "workflow step \"implement\" refused: no transition condition is satisfied",
    "problems": null
  }
}
```

调用脚本应当只信 `code` 字段做分支，不要解析 `message`。

## 渲染层

[internal/cli/cli.go:178-198](file://internal/cli/cli.go#L178-L198) `render` 决定输出形态：

- `--json`：成功 / 错误信封都走单行 JSON（成功 stdout，错误 stderr）。
- 非 `--json`：成功用人类可读文本（受 `--quiet` 控制），错误走 stderr。

`--quiet` 仅抑制成功输出，错误与 warning 仍然写 stderr——这是 `doctor` / `repair --dry-run` / `workflow check` 等诊断命令可见性约束。

## 写命令前置校验

`devsys init` 在落盘前先调 `config.Load(root)` 校验现有受管元数据；M2 起 `workitem create` 同样先校验（[internal/cli/cli.go:392-411](file://internal/cli/cli.go#L392-L411)）。`config.Load` 返回 `*Project` 与 `Problems`：存在未知 schema_version / 未知键 / 类型错误 → 拒绝写盘。

M3 起 `workitem transition` / `claim` / `workflow start` / `step-complete` / `pause` / `resume` / `cancel` / `approval request` 全部走 `expectedSnapshot` 校验 `--expect`；`workflow check` 不做 `--expect`（只读命令）。

## 依赖关系

| CLI 模块 | 调用领域包 | 关键依赖 |
|---|---|---|
| `runInit` | `internal/project`, `internal/storage` | `config.Load`、`AtomicWrite` |
| `runSearch` | `internal/search` | `Search(devsysDir, query)` |
| `runConfig` | `internal/config` | `Diagnose` / `Load` |
| `runWorkitem` | `internal/workitem`, `internal/storage`, `internal/domain` | `Create` / `Get` / `Transition` / `Claim` + `expectedSnapshot` |
| `runDoctor` / `runRecover` / `runRepair` | `internal/reconcile`, `internal/storage`, `internal/workitem` | `Doctor` / `Recover` / `RepairDryRun` / `RepairApply` |
| `runWorkflow` | `internal/workflow`, `internal/workitem`, `internal/storage`, `internal/domain` | `Load` / `Resolve` + `WorkflowStart` / `WorkflowNext` / `WorkflowStepComplete` / `WorkflowPause/Resume/Cancel` |
| `runApproval` | `internal/approval`, `internal/workitem`, `internal/storage`, `internal/domain` | `Request` / `Decide` / `List` + `rejectStageGate`（在 `Transition` 的 Guard 内调 `DecideTx(rejected)`） |
| `runNext` | `internal/reconcile`, `internal/workitem`, `internal/approval`, `internal/workflow`, `internal/next`, `internal/storage`, `internal/config` | `Doctor` + `workitem.List` + `approval.List` + `workflow.Load` + `config.Diagnose` + `next.Evaluate` |

CLI 层**不**做业务校验（不重做状态机九态图、不重做条件白名单、不重做 CAS 守卫），只做参数校验（必填、类型、形状）与 `--expect`/`--confirm` 解析。

## M3 命令族详解

### `devsys workflow`

`runWorkflowCheck`（[internal/cli/cli.go:388-452](file://internal/cli/cli.go#L388-L452)）：

- `workflow.Load(cwd)` 解析所有 `.devsys/workflows/*.md`。
- `error-severity` issue 升为 `config.Problem` 并通过 `errInvalid(problems)` → exit 4。
- `warning-severity` issue 走 `warning:` 行打印，`--quiet` 仍保留。
- `--json` 输出 `{ok, root, policies[], warnings[]}`。

`runWorkflowStart` / `WorkflowNext` / `WorkflowStepComplete` / `WorkflowSignal`（pause/resume/cancel）通过 `workflowPolicy(ctx, root, wi, policyID, requirePolicy)` 取当前或 LKG 策略，要求 `policy != nil`（start 时要求，next/step-complete 时允许 LKG fallback），然后调 `items.WorkflowStart/Next/StepComplete/Signal`。所有实例操作都强制 `--actor` / `--reason` / `--expect`；租约活跃时强制 `--owner` / `--token`。

### `devsys approval`

`runApprovalList` / `runApprovalRequest` / `runApprovalDecide`（approve / reject）：

- `Request` 调 `approval.New(root).Request(ctx, opts)`，含重复请求检查（`findOpenApproval`）。
- `Approve` 走独立事务 `Decide(ctx, id, opts{Approve: true})`。
- `Reject` 区分 scope：`scope=stage_gate` 走 `rejectStageGate`（在 `workitem.Transition` 的 Guard 内同时完成 `DecideTx(rejected)` 与状态变更）；`scope=action` 走独立事务 `Decide(ctx, id, opts{Approve: false})`。

`rejectStageGate`（[internal/cli/cli.go:942-976](file://internal/cli/cli.go#L942-L976)）：

1. 调 `workitem.ReadSnapshot` 取工作项当前字节作为 `--expect`。
2. 按策略 `on_reject` 决定目标状态（默认 `blocked`，`regress:<status>` 回退）。
3. 构造 `TransitionRequest{TargetStatus: target, Guard: <DecisionTx closure>}`。
4. `items.Transition(ctx, wi.ID, req, raw)`：Guard 内 `DecideTx(rejected)` 失败 → 整单回滚，工作项不动，审批保持 pending。

### `devsys next`

`runNext`（[internal/cli/cli.go:1323-1437](file://internal/cli/cli.go#L1323-L1437)）：

1. `reconcile.Doctor` 收集待恢复事务 + 过期/孤儿/不可读租约 + `InspectionOK`。
2. 仅在 `InspectionOK && len(pending) == 0` 时列举 `workitems` 与 `pending approvals`（不可信状态不输出业务事实，§15.4）。
3. `config.Diagnose` 收集元数据问题；`workflow.Load` 收集策略问题与「工作项引用缺失策略」交叉核对。
4. `next.Evaluate(in)` 输出 `Report{Verdict, Reasons, Risks, Fixes, Next}`。
5. exit 恒 0；`--json` 输出完整结构。

## 主要源码位置

| 路径 | 职责 |
|---|---|
| [`cmd/devsys/main.go`](../../../cmd/devsys/main.go) | 进程入口，转发 + 退出码 |
| [`internal/cli/cli.go`](../../../internal/cli/cli.go) | 命令分发 + `codedError` + `render` |
| [`internal/cli/cli.go:33-44`](../../../internal/cli/cli.go#L33-L44) | 退出码常量 |
| [`internal/cli/cli.go:103-114`](../../../internal/cli/cli.go#L103-L114) | `errUsage` / `errPrecondition` / `errInvalid` / `errInternal` 构造器 |
| [`internal/cli/cli.go:178-198`](../../../internal/cli/cli.go#L178-L198) | `render` |
| [`internal/cli/cli.go:388-735`](../../../internal/cli/cli.go#L388-L735) | `runWorkflow` 家族 |
| [`internal/cli/cli.go:741-976`](../../../internal/cli/cli.go#L741-L976) | `runApproval` 家族与 `rejectStageGate` |
| [`internal/cli/cli.go:1206-1232`](../../../internal/cli/cli.go#L1206-L1232) | `evidenceForTransition` / `approvalError` |
| [`internal/cli/cli.go:1323-1437`](../../../internal/cli/cli.go#L1323-L1437) | `runNext` |
| [`internal/config/config.go`](../../../internal/config/config.go) | `config.Load` / `Diagnose` / `Problem` / `Problems` |
| [`internal/config/validate.go`](../../../internal/config/validate.go) | YAML 行号定位 + 字段校验 |
| [`internal/domain/models.go`](../../../internal/domain/models.go) | 受管领域记录类型 |

## 与其他层的关系

- [Schema 与错误契约](./Schema与错误契约.md) — `config.Problem` / 退出码 / `--expect` / `--confirm` 完整定义
- [编码规范](./编码规范.md) — CLI 与领域包之间的错误传递规则
- [特殊配置与命令](./特殊配置与命令.md) — `DEVSYS_CONFIG_DIR` 与 `smoke-m*` 端到端验证入口
