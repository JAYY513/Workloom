---
status: stable
type: module
dimension: tech_stack
triggers:
  - Go 版本
  - 唯一外部依赖
  - vendor 怎么构建
  - 平台锁
  - 行尾规则
  - schema_version
  - workflow 包
  - 共享应用服务
  - x/term
  - 进程树终止
  - 内核 JobObject
  - 工作区怎么建
  - 适配器怎么选
  - dispatch tick
  - prompt 怎么组装
  - workflow 模板
  - VisibleTools
  - WorkflowTemplatesFS
  - embed.FS
  - BlueprintArtifactID
  - MCPSnippet 三参
  - setup 编排
  - mcpinstall
  - Makefile 两个名字
  - 别名 devsys
description: Go 1.26、`gopkg.in/yaml.v3` 与 `github.com/modelcontextprotocol/go-sdk v1.8.0` 与 `golang.org/x/term`（均 vendored）、平台原生锁调用、UTF-8 行尾、vendor 模式离线构建、M3 起新增 internal/{workflow,approval,next} 三个包，M4 起新增 internal/{app,mcp} 与 scripts/{m4helper,smoke-m4.*,exchange-demo.sh}，M6 起新增 internal/{harness,workspace,dispatch,retry,prompt} 与 scripts/{smoke-m6.*}，M7 起 scripts/{smoke-m7.*}（无新增依赖），process tree 终止由 proc_unix/proc_windows 平台 split 实现；M7.1 增 internal/view 包（无新增外部依赖，复用 storage / reconcile / config / workflow / approval / next / knowledge）作为视图域只读装配入口；本轮（ca58d27→19b9149）：无新增外部依赖；新增 internal/cli/setup.go 与 internal/app/{setup.go,mcpinstall.go}（纯标准库 + 既有包），Go 版本与 vendored 依赖不变，`Makefile` 的 build/install 改为主命令 + 兼容别名两个产物
generated: true
source_commit: 705cb18
---

# 技术栈

## 语言与工具链

- **Go 1.26**（`go.mod: go 1.26`）。M4 仍在该版本下，新增 Go SDK 依赖；M6 在该版本下新增 `golang.org/x/term`。
- M1 起 vendored `gopkg.in/yaml.v3` v3.0.1。
- **M4 新增 vendored** `github.com/modelcontextprotocol/go-sdk` v1.8.0（[internal/mcp/server.go:1-130+](file://internal/mcp/server.go#L1-L130)）。SDK 拥有 Model Context Protocol 的帧、生命周期、版本协商、`tools/list` / `tools/call` 形态、工具输入校验；本仓库只决定哪些工具暴露与共享错误分类。
- **M6 新增 vendored** `golang.org/x/term`（[go.mod](file://go.mod)）。`internal/harness.shell.go:Probe` 用 `term.IsTerminal(fd int) bool` 报告 host shell 是否可达 TTY；**不**用于 process tree 终止（POSIX 走 `syscall.Kill(-pid, sig)` + `Setpgid`，Windows 走 `golang.org/x/sys/windows` 的 `CreateJobObject` / `TerminateJobObject`）。
- 平台原生文件锁：POSIX `syscall.Flock`（`internal/storage/lock_unix.go`）；Windows `syscall.NewLazyDLL("kernel32.dll")` 动态调用 `LockFileEx`（`internal/storage/lock_windows.go`）。
- 行尾统一 LF：仓库根 `.gitattributes` 固定；M0.1 决策（原始依据见 git 历史中的 docs/开发记录.md）。

## 模块结构

```text
internal/cli/                   # M4 薄化为渲染层；M6 进一步承载执行层 CLI 适配；M7.1 增 workspace view 视图域
internal/cli/                   # M4 薄化为渲染层；M6 进一步承载执行层 CLI 适配；M7.1 增 workspace view 视图域；P1 增 roots.go 子目录解析 + wire --check|--skill|--print-mcp；P2 增 workloom prime + --latest
  cli.go                        # 全局开关 + 命令分发 + codedError + render + writeJSONL + toCoded + appService(委托 resolveService)
  records.go                    # M4 新增：decision / finding / event / artifact / run 族 CLI 适配；M6 增 run exec|prompt|verify|complete|fail|cancel；P2 增 runPrime（session start --compact 别名）+ outputSession
  worktree.go                   # M6 新增：worktree prepare|remove|list（§4.8 执行工作区）
  dispatch.go                   # M6 新增：dispatch --once|--watch|--dry-run|--max
  workspace.go                  # M7.1 新增 workspace view；M7.4 扩 {view,build,serve}：runWorkspace 24 / runWorkspaceBuild 44 / build JSON 信封 83-92 / runWorkspaceView 114 / view JSON 信封 135-140 / renderWorkspaceView 149 / hint 行 199-222
  diagnostics.go                # M4 拆分：CLI 保留的只读诊断（search / config check / doctor / recover / repair）
  mcp.go                        # M4 新增：workloom mcp serve；P1 增 --tier core|standard（默认 core，[internal/cli/mcp.go:153-154](file://internal/cli/mcp.go#L153-L154)）；v0.1.12 增 probe: 报告行（[internal/cli/mcp.go:113-115](file://internal/cli/mcp.go#L113-L115)）
  setup.go                      # v0.1.9 新增：runSetup（workloom setup [--template quick-fix]）——flag 解析 + 人类/--json 渲染
  roots.go                      # P1 新增：resolveRoot / resolveService —— DEVSYS_PROJECT_ROOT 优先，否则从 cwd 向上找最近的 .devsys/；找不到回退 cwd（让前置条件错误继续指出操作者所在目录）
internal/app/                   # Service + Error + Session/Context/Project/Workitem/Workflow/
                                # Approval/Decision/Finding/Artifact/Event/Run/Next/Wire/KnowledgeStatus
                                # M6 增 runexec/runverify/runstream + dispatch + workspace + prompt
                                # P1 增 wirecheck + skill + mcpsnippets（P1 子目录解析 + wire 增强）
                                # v0.1.9 增 setup.go（Setup 八步编排）+ mcpinstall.go（MCP 客户端注册，默认 dry-run）
                                # v0.1.12 增 mcpcommand.go（注册命令解析）+ mcpprobe.go（写入后启动探针，无新增外部依赖）

internal/registry/              # 用户级项目注册表
internal/version/               # 构建标识
internal/domain/                # 领域模型 + SchemaVersion + 序列化器

internal/mcp/                   # 官方 Go SDK server + 60+ 工具的 profile 过滤注册 + toolError；M6 增 run_prompt/run_verify/run_complete/run_fail/run_cancel；P1 增 TierCore / TierStandard / DefaultTier / ParseTier（[internal/mcp/server.go:37-60](file://internal/mcp/server.go#L37-L60)），与 profile 合取决定暴露集

# M6 新增：执行层（harness/workspace/dispatch/retry/prompt — 纯执行 / 纯函数）
internal/harness/               # Adapter / Session / Shell / Codex / OpenCode / ClaudeCode + proc_unix / proc_windows
internal/workspace/             # Ensure / Remove / List / RunHook + Key / Path / Git
internal/dispatch/              # Plan / ResolveCaps（纯函数）
internal/retry/                 # Delay（base/max + 派生 jitter）
internal/prompt/                # Assemble（首轮全 brief / 续轮 delta）

# 写入路径（仅作调用方）
internal/storage/               # 原子写、锁、事务、JSONL、序列化（含 M3 AppendJSONLRaw/Staged；M4 Run.ReadSnapshot 由 storage.Store.Read 共享锁取字节）
internal/workitem/              # 工作项 + 状态机 + 租约 + Guard + 传播 + 工作流实例
internal/events/                # JSONL 事件流 + AppendBatchTx
internal/run/                   # Run 记录 + M4 ReadSnapshot 显式 API
internal/record/                # decision / finding / artifact（含 ListArtifacts 与 M4 update.go：ReadDecision/ReadFinding/ReadArtifact + UpdateDecision/UpdateFinding + AppendArtifactVersion 含 ErrSuperseded）
internal/search/                # 文本扫描
internal/reconcile/             # doctor / recover / repair

# M3 新增应用逻辑
internal/workflow/              # 策略解析 + 条件求值 + 门禁/质量门 + LKG + 模板 + 环境变量
internal/approval/              # 审批请求/决定/消费生命周期
internal/next/                  # 就绪判定 + §7.4 推荐动作
```

## 构建

```sh
# 离线构建
GOFLAGS=-mod=vendor GOPROXY=off go build ./...

# Makefile（v0.1.9 起 build 同时产主命令与别名；install 两个名字都装；v0.1.11 增 npm-publish）
make build         # bin/workloom[.exe] + bin/devsys[.exe]
make install       # ~/.local/bin/{workloom,devsys}
make npm-publish   # 发布 dist/npm 下已打包的 tgz（默认平台集 win32-x64）

# 发布构建（注入版本；模块路径 v0.1.7 起规范化为 github.com/JAYY513/Workloom）
go build -mod=vendor -ldflags "-X github.com/JAYY513/Workloom/internal/version.Version=0.1.0 -X github.com/JAYY513/Workloom/internal/version.Commit=$(git rev-parse --short HEAD)" -o bin/workloom.exe ./cmd/workloom

# M4 端到端辅助（构建 m4helper）
go build -mod=vendor -o bin/m4helper.exe ./scripts/m4helper
```

直装（v0.1.7 起，无需 clone）：`go install github.com/JAYY513/Workloom/cmd/workloom@latest`；release 资产直装脚本 `install.sh` / `install.ps1` 自 v0.1.6 起随 GitHub Release 发布（不再依赖仓库内 scripts/ 副本）。

npm 分发层（**v0.1.11 起已发布到 registry**）：`@kaki317/workloom`（wrapper，bin 只有 `workloom`）+ 平台包 `@kaki317/workloom-<os>-<cpu>`，平台包走 `optionalDependencies`、**无** postinstall 下载；模板在 `npm/workloom/package.json` 与 `npm/platforms/*/package.json`，都带 `publishConfig.access = "public"`（scoped 包默认 restricted，缺这项首次发布会被拒，[npm/workloom/package.json:6-8](file://npm/workloom/package.json#L6-L8)、[npm/platforms/win32-x64/package.json:6-8](file://npm/platforms/win32-x64/package.json#L6-L8)）；wrapper 模板另有 `keywords` 与 `files: ["bin/workloom.js","README.md"]`（[npm/workloom/package.json:12-23](file://npm/workloom/package.json#L12-L23)），[npm/workloom/README.md](file://npm/workloom/README.md) 是 npm 包页的门面（v0.1.12 新增，中英对照写清「不下载、无 postinstall、当前只发 Windows x64」）。打包仍是 `scripts/pack-npm.sh`（只 `npm pack`、**不发布**，[scripts/pack-npm.sh:2-4](file://scripts/pack-npm.sh#L2-L4)）；发布是独立步骤 `scripts/publish-npm.sh` / `make npm-publish`（平台包先、wrapper 后；`npm view` 探测到同版本则跳过、可重跑，[scripts/publish-npm.sh:62](file://scripts/publish-npm.sh#L62)、[scripts/publish-npm.sh:83](file://scripts/publish-npm.sh#L83)、[Makefile:30-32](file://Makefile#L30-L32)）。当前只发 Windows x64（`@kaki317/workloom-win32-x64`），其余平台包只随 Release 附发。**v0.1.13 起**发布由 Release CI 自动完成——`release.yml` 声明 `id-token: write` 并用 Node 24，新增 `Publish npm packages` 步骤调 `scripts/publish-npm.sh --dir dist/npm`（位于 Pack 之后、`gh release create` 之前，[.github/workflows/release.yml:118-137](file://.github/workflows/release.yml#L118-L137)）；npm 走 OIDC trusted publishing（无长期凭据、仓库无 `NPM_TOKEN`），发布失败拦下整个 Release（fail closed）。

Windows：`$env:GOPROXY='off'`。

## 平台差异

通过 `//go:build` 标签隔离：

- `internal/storage/fsync_unix.go` / `fsync_windows.go` — Windows 下 `fsyncDir` 为 no-op。
- `internal/storage/lock_unix.go` / `lock_windows.go` — `lockContended` 与 `transientRenameError` 分别识别平台错误码。
- **`internal/harness/proc_unix.go` / `proc_windows.go`**（M6）— 跨平台 `killProcessTree(cmd *exec.Cmd) error` 同签名实现：POSIX 走 `Setpgid + kill -PGID`；Windows 走 `JobObject + TerminateJobObject`。
- `scripts/smoke-m{4,6}.sh` 在 `MINGW*` / `MSYS*` 运行时把 `/c/Program Files/Go/bin` 顶到 `PATH` 头部（与 M1–M3 同款）。

## 验证边界


- `-race` 检测器在本机不可用（无 gcc）；有编译环境时补跑 `CGO_ENABLED=1 go test -race ./...`。
- macOS/Linux 仅交叉编译、未实机运行。
- 进程中断验收不证明断电持久性（[internal/storage/errors.go](file://internal/storage/errors.go)）。
- **M6** 真实集成画像：codex 0.144.1 与 opencode 1.18.21 各启动一次（命令行被真实 CLI 接受、JSONL 入 run 事件流），两次均因提供方后端不可用失败（codex 代理 503、opencode 本地网关 socket closed）；claude 本机未安装（探测/拒绝路径已验）。**按 SPEC 要求，未通过的真实集成明确记为未通过/跳过**，确定性链路用桩 harness 覆盖（stdin 投递 → 工作区 → 提交 → 完成校验通过）。
- **M7.1** 视图只读副本验收：整树 `chmod -R 0444` 后跑两次 `--json` 必**同字节**（无墙钟字段），输出不被 `local/` / `.cache/` / `workspaces/` 影响，且 `.devsys/local/` 与 `.devsys/.cache/` 视图层从未触达（`local/lock` 不被创建、`storage.Inspect` 在 `ErrLockUnavailable` 时退 `storage.InspectUnlocked` 并把快照标 `advisory_unlocked`）。
- **M7.2–M7.4** 站点离线零命中外部资源；view/build 输出逐字节一致；非 loopback serve 返回 405；M7.4 站点横幅展示 runtime / Stage / Caps / Updated。

## 与其他层的关系

- [架构设计](./架构设计.md) — 模块依赖方向
- [特殊配置与命令](./特殊配置与命令.md) — `DEVSYS_CONFIG_DIR` / `DEVSYS_PROJECT_ROOT` 与 `smoke-m*` 端到端验证入口
- [共享应用与 MCP 概述](../共享应用与MCP/概述.md) — M4 共享应用服务与 MCP 服务的入口
- [执行层概述](../执行层/概述.md) — M6 执行层五子包 + 适配器协议
- [视图层 · 概述](../视图层/概述.md) — M7.1 `internal/view` 模型 / 装配 / 来源契约

## 本批（M9 #336/#337 提交 362b637 / 7e08e3d）

- 仓库根新增 `templates.go`（package workloom），用 `//go:embed docs/examples/workflows/*.md` → `WorkflowTemplatesFS embed.FS` 暴露四份示例策略（`quick-fix` / `feature-development` / `architecture-change` / `reference-template`）。docs 目录仍是唯一源，不搬家；嵌入仅给 `internal/app/workflow_init.go` 的 `WorkflowTemplates()` / `WorkflowInitTemplate(ctx, id)` 用。无新增外部依赖。
- `internal/mcp/server.go` 新增导出 `VisibleTools(cfg Config) []string`——与 `NewServer` 同一 profile ∧ tier 过滤；profile × tier 是合取。
- `internal/app/mcpsnippets.go` 的 `MCPSnippet(name, devsysBin, projectRoot)` 三参——`filepath.ToSlash` 归一 Windows 路径分隔符，JSON 用 `encoding/json` 编字符串（避免手写 escape），TOML 正斜线；`DEVSYS_CONFIG_DIR` 已设时把 `env` 注入 codex/claude/opencode 三种 harness 的片段。无新增依赖。

## 本批（v0.1.9，ca58d27→19b9149）

- **无新增外部依赖**：`go.mod` 的 Go 版本与 vendored 依赖（`gopkg.in/yaml.v3`、`github.com/modelcontextprotocol/go-sdk`、`golang.org/x/term`、`golang.org/x/sys`）不变；新增代码只用标准库（`encoding/json`、`flag`、`os/exec`、`path/filepath`）+ 既有内部包。
- **新增模块**：`internal/cli/setup.go`（[internal/cli/setup.go:17-58](file://internal/cli/setup.go#L17-L58)）、`internal/app/setup.go`（[internal/app/setup.go:43-198](file://internal/app/setup.go#L43-L198)）、`internal/app/mcpinstall.go`（[internal/app/mcpinstall.go:69-184](file://internal/app/mcpinstall.go#L69-L184)）；`internal/cli/mcp.go` 扩 `install` 子命令（[internal/cli/mcp.go:54-118](file://internal/cli/mcp.go#L54-L118)）。
- **新增分发层**（不在 Go 代码里）：`npm/workloom/package.json` + `npm/platforms/*` 平台包 + `scripts/pack-npm.sh`——包装包 `@kaki317/workloom`，bin 只有 `workloom`，平台包走 `optionalDependencies`、**无** postinstall 下载；`scripts/pack-npm.sh` 只 `npm pack`、不 `npm publish`。
- **构建**：`Makefile` 的 `BIN` 改为 `bin/workloom[.exe]`，`ALIAS_BIN := $(subst workloom,devsys,$(BIN))`；`build` 同时构建 `./cmd/workloom` 与 `./cmd/devsys`，`install` 把两个名字都装到 `~/.local/bin`（[Makefile:1-21](file://Makefile#L1-L21)）。`scripts/build-release.sh` 继续只交叉编译 `./cmd/workloom`（别名不再单独发布资产）。
- **平台差异不变**：仍只有 storage 的 fsync/lock 与 harness 的 proc 两处 `//go:build` split；本轮新增代码无平台分支（`mcpinstall` 的路径解析用 `filepath`，输出统一 `filepath.ToSlash`）。
- **本批（#398）**：`workloom --help` 顶层命令表补回 `init` 与 `sync status` 两行——两条命令一直可路由、`setup` 与 M8.1 接力判定都依赖它们，此前只是命令表漏列；纯文案、无行为变更，本页命令面不变。
- **本批（v0.1.10）**：补丁版本发布（`--help` 顶层命令表修复，无行为变更）；本页口径不变，`source_commit` 跟进至 `21a9e71`。
- **本批（v0.1.11 / npm 首发）**：npm 分发层已发布到 registry（`@kaki317/workloom@0.1.11` + `@kaki317/workloom-win32-x64@0.1.11`，当前只发 Windows x64）；包名由 `@jayy513/workloom` 更正为 `@kaki317/workloom`，新增 `scripts/publish-npm.sh` 与 `make npm-publish`（[Makefile:30-32](file://Makefile#L30-L32)），wrapper 与 6 个平台包模板都加 `publishConfig.access = "public"`；Go 版本与 vendored 依赖不变，`source_commit` 跟进至 `238e30c`。
- **本批（v0.1.12 / MCP 接入闭环）**：**无新增外部依赖**——`internal/app/mcpcommand.go` 与 `internal/app/mcpprobe.go` 只用标准库（`encoding/json`、`os/exec`、`path/filepath`、`log/slog`、`time`）与既有 vendored 的官方 Go SDK（探针用它的 client + `CommandTransport` 走一次 `initialize` + `ListTools`，SDK 日志丢弃，[internal/app/mcpprobe.go:55](file://internal/app/mcpprobe.go#L55)）。npm 包装包门面补齐：`npm/workloom/README.md`（新）+ `keywords` + `files`（[npm/workloom/package.json:12-23](file://npm/workloom/package.json#L12-L23)），打包测试断言 tarball 含 `package/README.md`（[npm_pack_test.go:118-121](file://npm_pack_test.go#L118-L121)）——registry packument 的 `readme` 字段由此非空。`source_commit` 跟进至 `7cdd918`。
- **本批（v0.1.13 / #414+#416）**：**无新增外部依赖**——`attemptSpawnCommand` 复用既有 `ResolveMCPCommand`（`exec.LookPath` 作参数注入），新增测试 `internal/app/dispatch_spawn_test.go` 固定「npm 树形态与 MCP 注册一致 / 非 npm 路径原样透传」。分发侧（CI）改 npm OIDC trusted publishing：`release.yml` 声明 `id-token: write`、`Set up Node` 用 `node-version: 24`、新增 `Publish npm packages` 步骤调 `scripts/publish-npm.sh --dir dist/npm`（[.github/workflows/release.yml:118-137](file://.github/workflows/release.yml#L118-L137)）；全程无 `NODE_AUTH_TOKEN` / `NPM_TOKEN`。Go 版本与 vendored 依赖不变，`scripts/build-release.sh` 的 checksums 生成强制 `sha256sum -t`（[scripts/build-release.sh:50](file://scripts/build-release.sh#L50)）。
