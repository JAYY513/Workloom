---
status: stable
type: module
dimension: overview
triggers:
  - 执行层
  - M6 执行层
  - harness 适配器
  - 适配器接口
  - workspace 工作区
  - worktree
  - dispatch tick
  - 派发排序
  - retry 退避
  - 重试抖动
  - prompt 组装
  - 提示词首轮/续轮
  - §4.8
  - §9.2
  - §9.3
  - §15.4
  - codex 适配器
  - opencode 适配器
  - claude 适配器
  - shell 适配器
  - 进程树终止
description: M6 执行层（internal/harness + internal/workspace + internal/dispatch + internal/retry + internal/prompt）的模块定位、五子包职责边界与卡片导航；适配器抽象 + Shell/三个 CLI harness + 路径净化与根内不变量 + 派发 tick + 可复现退避 + 提示词首轮/续轮装配；M8.2 `Dispatch` tick 顶端加 merge 门控（recover 前 + dry-run 同门，blocked 走 exit 3 precondition）；M8.4 Contrabass 作为可选执行后端：`scripts/contrabass/{export,import}.py` 把 devsys 工作项镜像到看板卡片，回流只走 `devsys workitem transition|comment` CLI；`.devsys/` 是唯一事实来源，看板是投递副本，看板侧字段名 [UNVERIFIED]。
generated: true
source_commit: 2b8b8ce
generator: repowiki-gen
---

# 执行层

本模块是 M6 里程碑的**执行层**：把 .devsys/ 中的领域记录（WorkItem/Run/Policy）与真实进程执行打通——为每一次 attempt 提供一个**适配器抽象**（Adapter）、一个**隔离执行目录**（Workspace）、一份**派发计划**（Plan）、一条**可复现的重试退避**（Delay）、一段**可复现的提示词文本**（Prompt），并在 `internal/app` 统一接线到 CLI/MCP 入口。

## 模块定位

- **做什么**：在 `internal/harness` 暴露 §9.2 的 `Adapter` 接口与 `Session` 接口（Capabilities / Availability / Workspace / Request / Command / Line / Result）+ Shell 基线适配器 + 三个 CLI 适配器（codex / opencode / claude），统一流式输出、行缓冲、stdin 投递、进程树终止与超时取消；在 `internal/workspace` 提供 Key/Path/Git/Hook + Ensure/Remove/List + RunHook（生命周期钩子通过 shell 适配器执行，保留相同流/超时/进程树语义）+ HookEnv；在 `internal/dispatch` 暴露 §4.8/§7.4 的纯函数 `Plan`（priority 降序 → created_at 升序 → id 决胜 + 状态白名单 + blocked_by + 全局/状态并发）+ `ResolveCaps`；在 `internal/retry` 给出 §15.4 的 `Delay(base, max, attempt, seed)`——指数增长 + 派生抖动（不随机、可复现）；在 `internal/prompt` 给出 `Assemble(Input)`——首轮全 brief，续轮 delta + remaining，sectioned 渲染，hash 稳定。
- **不做什么**：不写 .devsys/ 状态（由 `internal/app` 调 `storage.Store.Write`）；不实现审批/门禁/工作流策略（属 [可靠文本存储](../可靠文本存储/概述.md)）；不实现 MCP 工具族（属 [共享应用与 MCP](../共享应用与MCP/概述.md)）；不暴露 CLI 子命令（`internal/cli` 调用 `internal/app`）。

## 职责边界

| 子包 | 目录 | 职责 |
|---|---|---|
| Harness 适配器 | `internal/harness/` | `Adapter` / `Session` / `Capabilities` / `Availability` / `Workspace` / `Request` / `Command` / `Line` / `Result` 抽象；Shell 适配器（argv + 流式 + 进程树终止）；三个 CLI 适配器（codex / opencode / claude）；`Names()` / `ByName(name)` 注册表 |
| Workspace | `internal/workspace/` | `Key`（净化 + 哈希后缀，§4.8）；`Root` / `Validate`（路径净化与根内不变量，含 symlink 逃逸）；`Ensure` / `Remove` / `List`（git worktree 生命周期）；`Hook` / `RunHook` / `HookEnv`（after_create/before_run/after_run/before_remove 通过 harness shell 执行） |
| 派发 | `internal/dispatch/` | 纯函数 `Plan(in Input) Report`（§4.8/§7.4 排序 + 门禁 + 并发 + blocked_by）；`ResolveCaps(policies []Caps) Caps`（最严合并 + 缺失即默认 1）；常量 `DefaultGlobalCap = 1` |
| 重试退避 | `internal/retry/` | `Delay(base, max, attempt, seed)` 指数 + 派生抖动（jitter 取 seed+attempt 派生 ≤ 20% × delay，clamp 至 max）；常量 `DefaultBase = 30s` / `DefaultMax = 1h` / `DefaultStallThreshold = 30m` |
| 提示词组装 | `internal/prompt/` | `Assemble(in Input) (Prompt, error)` 首轮全 brief / 续轮 delta + remaining；首轮无挂载工作流策略时写入 `noPolicyBody` 兜底文案、`len(Remaining)>0` 时附加「工作流步骤」section；`Prompt.Text` + `Hash` + `Refs`；常量 `protocol` 报告契约（devsys CLI/MCP 路径统一，含 `--by` / `workitem block` / `approval request`） |

五个子包**全部纯函数 / 纯执行**——`internal/harness` 不知道 .devsys/、Run、事件；`internal/workspace` 不知道 WorkItem 状态机；`internal/dispatch` 不写不发起；`internal/retry` 无状态；`internal/prompt` 无副作用。**接线由 `internal/app`** 完成：Service 把 plan → claim → workspace → spawn → prompt → stream → finish 串成一次 attempt，再把每一步落到 storage 与事件流。

**M8 新增**：
- M8.2 `internal/app.Dispatch` tick 顶端 `mergeConflict(root)` 门控（`PorcelainStatus` 找 unmerged 路径 + `MergeHeads` 找未完成 merge，recover 前 + dry-run 同门，blocked 走 exit 3 precondition；探针失败降级为 notice，不伪装为 conflict）。
- M8.4 Contrabass 作为可选执行后端：`scripts/contrabass/export.py`（九态→六态，幂等重跑同字节） + `import.py`（保守子集回流，只调 `devsys workitem transition|comment`）；`.devsys/` 是唯一事实来源，看板卡片是投递副本——没有第二事实来源，字段名 [UNVERIFIED]，细节见 `scripts/contrabass/README.md`。

## 卡片导航

- [架构设计](架构设计.md) — 五子包依赖方向、适配器↔适配器组合、Plan 的纯函数契约、Retry 与 Prompt 的可复现边界
- [技术栈](技术栈.md) — Go 1.26、`golang.org/x/term`（vendored，process tree 跨平台 syscall）、harness 平台差异（proc_unix.go / proc_windows.go）
- [编码规范](编码规范.md) — 适配器接口稳定性、Plan 输入不可变、Retry seed 与 attempt 一致性、Prompt section 顺序稳定
- [特殊配置与命令](特殊配置与命令.md) — `config.workspace_root` / `config.dispatch_command` 配置项、HookEnv 变量、CLI `worktree / dispatch / run exec|prompt|verify|complete|fail|cancel` 命令族与典型剧本
- [适配器协议契约](适配器协议契约.md) — Adapter 接口、Session 接口、Shell/Codex/OpenCode/ClaudeCode 实现差异、Capabilities/Availability 探针契约、stdin 投递与进程树终止（custom dimension: `adapter_protocol`）
