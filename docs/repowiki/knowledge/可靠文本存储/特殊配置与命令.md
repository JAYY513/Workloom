---
status: stable
type: module
dimension: setup
triggers:
  - 怎么跑 devsys
  - DEVSYS_CONFIG_DIR
  - vendor 构建
  - GOPROXY off
  - smoke-m1
  - smoke-m2
  - smoke-m3
  - m3helper
  - 冒烟剧本
  - 工作流示例
description: 隔离运行、离线构建、冒烟剧本入口与示例快照；M3 新增 smoke-m3.{sh,ps1}、scripts/m3helper 与 docs/examples/workflows/*.md 三份示例策略。
generated: true
source_commit: cf7b256
generator: repowiki-gen
---

# 特殊配置与命令 · 可靠文本存储

本卡汇集运行、构建、冒烟与示例相关的不在主命令族的配置项与命令。

## 隔离运行

### `DEVSYS_CONFIG_DIR`

所有冒烟剧本与隔离测试通过该环境变量把「全局注册表」隔离到临时目录，避免污染用户配置：

```sh
# Bash
export DEVSYS_CONFIG_DIR="$workspace/config"

# PowerShell
$env:DEVSYS_CONFIG_DIR = 'C:/Temp/workloom-registry'
```

### `GOPROXY=off` 离线构建

`vendor/` 已提交；干净机器设 `GOPROXY=off` 即可离线构建（[README.md:120-126](file://README.md#L120-L126)）。

```sh
# Bash / PowerShell / cmd
GOFLAGS=-mod=vendor GOPROXY=off go build ./...
```

PowerShell：`$env:GOPROXY='off'`。

## 构建与发布标识

```sh
go build -mod=vendor -ldflags "-X workloom/internal/version.Version=0.1.0 -X workloom/internal/version.Commit=$(git rev-parse --short HEAD)" -o bin/devsys.exe ./cmd/devsys
```

未注入版本的本地构建保留 `Version=0.1.0-dev`、`Commit=unknown`；`devsys --version` 在 `Commit=="unknown"` 时只输出 `Version`（[internal/version/version.go:13-23](file://internal/version/version.go#L13-L23)）。这是构建标识能力，不表示已有自动发布流水线。

## 冒烟剧本

### `smoke-m1.{ps1,sh}`（M1.7 完整端到端）

两个剧本从仓库根构建 CLI 与 helper，在新临时 Git 项目中跑：初始化 → 创建任务 → 决策/发现/Artifact 三版本 → 评论回复 → Run + ContextSnapshot → ReadSnapshot/Update → config check/search → 回读验证。Go 与 Git 必须在 PATH。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-m1.ps1 -Keep
```

```sh
bash scripts/smoke-m1.sh --keep
```

成功结束两个脚本都打印 `PASS: M1 create -> record -> complete; all tracked state is UTF-8 text.`。`-Keep` / `--keep` 额外打印 `KEPT_PROJECT=<path>` 用于手工回读。

`scripts/smoke-m1-helper.go` 同时承担 seed（无 `--verify`）与回读（`--verify`）两件事：seed 按工作流顺序调用 `workitem.Create` → `events.Append` → `record.*` → `run.Create` → `events.Append(comment + reply)`；`--verify` 用 `ReadSnapshot` / `GetArtifact` / `ArtifactHistory` / `Comments` 反向验证，并逐文件 UTF-8 + 非 NUL 校验。

Bash 版本检测 `uname -s` 命中 `MINGW*` / `MSYS*` 时自动把 `/c/Program Files/Go/bin` 顶到 `PATH` 头部。

### `smoke-m2.{ps1,sh}`（M2 状态机与对账端到端）

```sh
bash scripts/smoke-m2.sh --keep
```

完整链路：领取 → 模拟崩溃 → `doctor` → `recover` → 确认修复。M2 通过 `storage.TxnStageHookForTest` 在真实子进程内注入崩溃点，验证提交两侧语义（commit 前丢弃 / commit 后幂等重放）。

### `smoke-m3.{ps1,sh}`（M3 工作流/门禁/审批端到端）

```sh
bash scripts/smoke-m3.sh --keep
```

七段 PASS（[scripts/smoke-m3.sh](file://scripts/smoke-m3.sh)）：

1. 策略就位：`devsys init` + `cp docs/examples/workflows/quick-fix.md .devsys/workflows/` + `m3helper setup`（parent + sibling + child，child 在 verification 步、quick-fix policy、链接 decision + finding）。
2. done 门禁拦截：`workitem transition --to done` 缺 `test-results` artifact 与 comment → exit 4 + `artifact "test-results" is required`。
3. 补 artifact + comment → 推进 done，child 状态 done、父与兄弟 `context_refs` 包含 `decision://<id>` + `finding://<id>` 各 1、`record_propagated` 事件 1 条、父与兄弟 status 不变。
4. 质量门拦截：`m3helper lowq` 创建 thin 任务（标题"fix"、描述"x"）→ `workitem claim` exit 4 + `quality_gate.min_score` 与改进项（标题过短等）；`m3helper improve` 补足后领取成功。
5. last-known-good：成功 claim 落快照 → 注入坏策略（缺 `id` / `version: "x"`）→ claim 拒绝（exit 4 + `is invalid` + `last-known-good`），`next` 报 `invalid_policy` 风险，`workflow next` 含 `last-known-good` 提示；恢复策略后 claim 成功。
6. 工作流实例：创建 thin 策略（`flow.md`：inspect → implement 两条边）→ `workflow start` → `workflow next` 列出 `to=implement` 候选 → `--to nope` exit 4 + `not a declared transition` + `allowed next steps` → `--to implement` 成功 → `workflow pause` → `step-complete` exit 4 + `paused` → `workflow resume` → 推进；`.devsys/scheduling/` 哈希前后一致（实例操作不动调度态）。
7. 审批门禁：thin 策略（`approval-flow.md`：in_progress 阶段 `require_approval: true`）→ `workitem transition --to in_progress` exit 4 + `an approved, unconsumed approval is required` → `approval request --stage in_progress` → `approval approve --id approval-1` → 重提 transition 成功 + `consumed_at` 写入 + `approval_consumed` 事件 → `approval reject --id approval-2` 触发 `workitem.Transition` 的 Guard 把工作项转 `blocked`（`on_reject=block`）+ `status_changed` 内容含 `approval-2 rejected: risk too high`。

### `m3helper`（`scripts/m3helper/main.go`）

为 `smoke-m3.*` 准备的命令 helper，按 KEY=VALUE 打印：

- `setup <root>` — 创建 parent + sibling + child + decision + finding，挂 quick-fix policy。
- `lowq <root>` — 创建 thin 任务，挂在 quick-fix policy 的 implement 步（触发质量门）。
- `evidence <root> <child>` — 创建 `test-results` artifact + 一条 comment（满足 done 门禁）。
- `improve <root> <id>` — 把 thin 任务改成能过质量门的版本（标题 + 描述 + acceptance_criteria）。
- `status <root> <id>` — 打印 `ID=<id> STATUS=<s> REFS=<comma-sep context_refs>`。
- `propagated <root> <id>` — 打印 `PROPAGATED=<n>`（subject=workitem id 的 `record_propagated` 事件数）。

构建：`go run ./scripts/m3helper <subcommand> <root> [args]`。

## 示例策略（`docs/examples/workflows/`）

三份示例策略可直接 `cp` 到 `.devsys/workflows/` 跑端到端：

| 文件 | 用途 | 关键声明 |
|---|---|---|
| `quick-fix.md` | 简单 Bug / 小改 / 文档 | `inspect → implement → verify → done`，done 门禁 `require_artifacts: [test-results]`，`quality_gate.min_score: 40`，`on_reject: block` |
| `feature-development.md` | 功能开发 | 5 步（inspect / clarify / plan / implement / verify），plan 与 verify 阶段有 `require_artifacts` + `require_comment`，done 阶段 `require_artifacts: [test-results]`，`concurrency.global: 4`，`concurrency.per_status: {in_progress: 2}`，`quality_gate.min_score: 55` |
| `architecture-change.md` | 架构变更 | 5 步含 `decide`（`when: workitem.approval_required == false` 放行），`approval_points: [architecture_change]`，in_progress 阶段 `require_approval: true`，done 阶段 `require_artifacts: [test-results, decision]`，`hooks.before_run: git fetch --prune`，`on_reject: regress:ready` |

`id` 必填且必须等于文件名（除 `.md`）；`input.required / optional` 是声明的输入 token（实现侧未消费，归 M4.4/M5.6）；正文是提示词模板，可用 `{{name}}` 引用（变量供应契约定稿前不写示例引用）。

## 示例 `.devsys/` 快照

`docs/examples/m1-devsys/` 是 13 文件的通过验收 `.devsys/` 快照（不含 `local/`、`.cache/`、用户注册表），可整体复制到新 Git 仓库的 `.devsys/`，再在该仓库执行 `devsys init` 补齐目录与注册表；项目 ID 为 `demo-project`，用于真实项目需一致替换示例 ID 与内容。

## 验证边界

- `-race` 检测器在本机不可用（无 gcc，[docs/开发记录.md:35](file://docs/开发记录.md#L35)）；有编译环境时补跑 `CGO_ENABLED=1 go test -race ./...`。
- macOS/Linux 仅交叉编译、未实机运行（[docs/开发记录.md:37](file://docs/开发记录.md#L37)）。
- 进程中断验收不证明断电持久性（[internal/storage/errors.go](file://internal/storage/errors.go)）。

## 与其他层的关系

- [架构设计](架构设计.md) — `Store.Write` / `Inspect` / `Recover` 的契约
- [事务与恢复](事务与恢复.md) — `TxnStageHookForTest` 与中断注入
