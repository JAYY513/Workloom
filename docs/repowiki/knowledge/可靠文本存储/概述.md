---
status: stable
type: module
dimension: overview
triggers:
  - 领域存储
  - WorkItem 存不了
  - 事件流怎么写
  - Run 记录在哪
  - 决策记录怎么查
  - 状态机
  - 租约
  - 对账
  - 修复
  - 工作流策略
  - 门禁
  - 审批
  - next 就绪判定
  - 适配器协议契约
  - 运行事件流
  - .devsys/runs/<id>.jsonl
  - run_exec_started
  - run_finished
  - 阶段事件
  - 提示词落盘
  - 适配器接线
  - 适配器注册
  - OS 文件锁
  - LockFile 原语
  - 锁文件
  - InvalidFile
  - InspectionReport.InvalidFiles
  - Doctor 不可读工作项
  - readAllWorkitems 解码错误
  - buildProposals 解码错误
  - RepairDryRun fail-closed
  - workitem.Store.List 空集合
description: 项目内领域记录（WorkItem/事件/Run/记录/审批）的文件布局、ID 分配与访问纪律；M3 工作流策略/门禁/质量门/审批/next；M4 显式化 ReadSnapshot + 守卫 Update（record/update.go、run/ReadSnapshot），artifact 版本链线性、防分叉（ErrSuperseded）；M5 新增 `internal/storage.LockFile`（OS 独占锁原语，被知识层 `LockRefresh` 复用）与方案 §15.2 同源；M6 在 .devsys/runs/<id>.jsonl 引入运行事件流（streamRecord + 行级 sync + 断尾重建 + 阶段事件 run_exec_started/run_finished/record_prompt），Run 记录与 RunExec/RunVerify/RunFinish 接入 service；M8 在对账侧把 git 未合并路径/合并残留列为人工 proposal（note_unmerged_paths，devsys 不自动合），在归档侧新增 `internal/archive` 把事件与 run 流保守搬到 .devsys/archive/（events|runs/manifest.yaml，单事务 read-copy-delete + manifest CAS，读侧双树合并，搜索排除 archive，删除由手动 git 负责）；b89ffae 起 next/project status 对 ready 工作项用与 claim 同源的质量门（新风险 kind `quality_blocked`），`retry_queued` 等待加入 `retry_pending`；本批（M#337，commit 362b637）reconcile 新增 `InvalidFile{Path, Err}` 类型与 `InspectionReport.InvalidFiles`，`readAllWorkitems` / `buildProposals` 收集解码错误继续扫其余工作项文件，`Doctor` 填充 `InvalidFiles` 并把 Note 追加「one or more work item files are unreadable; the state cannot be fully trusted」，`RepairDryRun` 有 invalid 时拒绝 `cannot plan repairs: …`（保持 fail-closed），`workitem.Store.List` 空集合返回非 nil `[]`，CLI doctor 在人类模式打 `INVALID  <path>  <err>` 行、`--json` 编码后同样 `exitWithCode(4)`（exit 4 语义），next/prime/project status 同路 `Invalidf(KindInvalid)`。
generated: true
source_commit: 2b8b8ce
generator: repowiki-gen
---

# 可靠文本存储 · 概述

本模块覆盖 `.devsys/` 项目内**领域数据的落盘层**与**M3 工作流/审批/就绪判定三层应用逻辑**、**M4 显式化的读快照与守卫更新路径**：任务（WorkItem）、事件流、Run 记录、决策/发现/工件记录、审批记录、工作流策略文件、就绪判定引擎，以及基于文本扫描的关键字检索。它建立在 [存储基元](存储错误语义.md) 的事务与锁之上，向上提供 Go API 与 `internal/app` 包装的共享应用服务，检索另有 CLI 入口；不提供 MCP 工具（由 `internal/mcp` 在 M4.2 之后独立暴露，见 [共享应用与 MCP](../共享应用与MCP/概述.md)）。

## 模块定位

- **做什么**：把领域记录写成稳定键序的 YAML/JSONL 文本文件；分配全局唯一 ID；提供乐观并发更新与不可变版本链；M3 起在工作项之上承接工作流策略加载与 last-known-good 缓存、门禁与质量门、审批生命周期、就绪判定与 next 优先级推荐；M4 起把「读 + 守卫写」模式显式化为 `ReadSnapshot` / `ReadDecision` / `ReadFinding` / `ReadArtifact` / `ReadRun` 与对应的 `UpdateDecision` / `UpdateFinding` / `AppendArtifactVersion` 守卫更新方法。
- **不做什么**：不直接暴露 MCP 工具（由 `internal/mcp` 通过 `internal/app` 间接调用）；不执行 hook（M6）、不做任务派发（M6）。

M2 起在可靠文本存储之上又承接了**调度**与**对账/修复**两块职责：状态机九态图与保守转换（`internal/domain/state.go`）、租约文件 `.devsys/scheduling/<id>.yaml` 下的领取/心跳/释放/重试（`internal/workitem/claim.go`），以及只读检查 / 恢复 / 确认式修复（`internal/storage/inspect.go` + `internal/reconcile/`）。M3 起又承接：

- **工作流策略层** `internal/workflow/`：解析 `.devsys/workflows/<id>.md`（YAML front matter + 提示词正文），校验步骤/转换/门禁/钩子/限额/质量门；评估白名单条件；渲染模板与展开环境变量；last-known-good 缓存（`.devsys/.cache/workflows/`）。
- **审批服务** `internal/approval/`：持久化 `.devsys/approvals/approval-<N>.yaml`，实现 request → decide → consume 生命周期；consume 永远在转换事务的 Guard 内完成，失败即整单回滚。
- **就绪判定** `internal/next/`：纯函数 `Evaluate` 根据待恢复事务、过期/孤儿租约、滞留审查、阻塞、元数据/策略问题、待决审批等风险输出 PASS/CONCERNS/FAIL 判定，并按 §7.4 优先级给出唯一推荐动作。

三者仍全部落在 storage 的事务与锁之上，自身不引入新的锁类型。

M4 起在领域模块层又新增**读快照与守卫更新**的显式 API（`internal/record/update.go` 与 `internal/run/run.go`）：

- `record.ReadDecision / ReadFinding / ReadArtifact` 通过共享泛型 `readSnapshot[T]` 返回解码对象与原 YAML 字节；应用服务把字节哈希为 `version` 字段返回给 CLI/MCP。
- `record.UpdateDecision / UpdateFinding` 接受调用方持有的 `expected []byte`，写锁内 `tx.ReadForExpect` + `ExpectHash` 守卫；并发写 → `storage.ErrConflict`。
- `record.AppendArtifactVersion` 拒绝**并发分叉**——若 `currentID` 已有任何子版本，立即返回 `ErrSuperseded`，要求 re-read head 再 append。
- `run.ReadSnapshot` 同形返回 Run 解码对象与原字节，支撑 `app.RunUpdate` 的 `expect` 守卫路径。

| Run | `internal/run/` | `run-<YYYYMMDD>-<N>`，携带 §11.3 上下文快照；M4 提供 `ReadSnapshot(ctx, id) (*Run, []byte, error)` 与 `app.RunUpdate` 的 expect 守卫路径；**M6** 由 `internal/app.runStream` 维护 `.devsys/runs/<id>.jsonl`（行级流 + 阶段事件 + 断尾重建） |

| 子层 | 目录 | 职责 |
|---|---|---|
| WorkItem | `internal/workitem/` | 一任务一文件，`<前缀>-<序号>` ID，CAS 分配；M2 状态机转换、租约领取/释放/重试；M3 转换事务的 Guard 钩子（供审批消费）、记录传播（propagate）、工作流实例操作；M4 经 `app.WorkitemUpdate` 等方法把读快照 + 守卫更新暴露给 CLI/MCP |
| 事件流 | `internal/events/` | JSONL 按月分片追加，评论一级回复；M2 提供 `AppendTx` 让调度在同一事务里追加事件；M3 新增 `AppendBatchTx`（按 shard 分组、一次 append）支撑状态事件 + 传播事件 + 失效事件同批提交 |
| Run | `internal/run/` | `run-<YYYYMMDD>-<N>`，携带 §11.3 上下文快照；M4 提供 `ReadSnapshot(ctx, id) (*Run, []byte, error)` 与 `app.RunUpdate` 的 expect 守卫路径 |
| 记录 | `internal/record/` | decision/finding/artifact 一记录一文件；Artifact 版本链；M3 新增 `ListArtifacts` 给门禁证据收集；M4 引入 `internal/record/update.go` 的 `ReadDecision / ReadFinding / ReadArtifact` 共享 `readSnapshot[T]` 泛型 + `UpdateDecision / UpdateFinding`（守卫来自读取字节）+ `AppendArtifactVersion`（线性版本链，防分叉 `ErrSuperseded`） |
| 检索 | `internal/search/` | 无索引文本扫描，排除 `local/`、`.cache/` |
| 只读检查 | `internal/storage/inspect.go` | 共享项目锁下的 `Inspect` / `InspectUnlocked`，**不**创建锁文件、**不**触发恢复 |
| 对账/修复 | `internal/reconcile/` | Doctor（只读）/ Recover（释放过期/孤儿租约）/ RepairDryRun + RepairApply（确认式） |
| 工作流策略 | `internal/workflow/` | M3 新增：front matter 解析、严格 located 校验、条件求值白名单、门禁与质量门、last-known-good 缓存、模板渲染与环境变量展开 |
| 审批 | `internal/approval/` | M3 新增：审批记录持久化、请求/决定/消费生命周期、消费在转换事务 Guard 内、离开阶段即作废 |
| 就绪判定 | `internal/next/` | M3 新增：纯函数 `Evaluate`，PASS/CONCERNS/FAIL 与 §7.4 推荐动作；接入待决审批与策略问题作为风险 |

## 本批（M#337，commit 362b637）

M#337 在对账层和工作项存储引入「不可读工作项文件」作为一类新的失败可见信号，并把 CLI / next / prime / project status 收敛到统一的 exit 4 语义：

- `internal/reconcile/reconcile.go` 新增类型 `InvalidFile{Path, Err}`（`String()` = `path: err`），`InspectionReport` 增字段 `InvalidFiles []InvalidFile`（json/yaml tag `invalid_files,omitempty`）。
- `readAllWorkitems` / `buildProposals` 走 `storage.Inspect` 扫 `.devsys/workitems/` 时把单个解码失败的文件记为 `InvalidFile` 并继续扫其余文件，避免一个坏文件把整棵树隐藏（[internal/reconcile/reconcile.go:964-1003](file://internal/reconcile/reconcile.go#L964-L1003)）；`buildProposals` 把 invalid 列表合并进自己的返回值传给 `Doctor`（[internal/reconcile/reconcile.go:889-894](file://internal/reconcile/reconcile.go#L889-L894)）。
- `Doctor` 填充 `rep.InvalidFiles = invalid` 并把 `rep.Note` 追加 `one or more work item files are unreadable; the state cannot be fully trusted`（[internal/reconcile/reconcile.go:226-230](file://internal/reconcile/reconcile.go#L226-L230)）。
- `RepairDryRun` 有 invalid 时直接 `fmt.Errorf("cannot plan repairs: %s", …)` 拒绝，保持 fail-closed——在不可信树上构造的 plan 不可信（[internal/reconcile/reconcile.go:324-333](file://internal/reconcile/reconcile.go#L324-L333)）。
- `internal/workitem/workitem.go`：`Store.List` 把 `items` 初始化为 `[]*domain.WorkItem{}`，空集合返回非 nil `[]`——下游可直接 `len(...) == 0` / 序列化 `[]`，免去 nil 判断（[internal/workitem/workitem.go:131-164](file://internal/workitem/workitem.go#L131-L164)）。
- CLI 渲染：`internal/cli/diagnostics.go` 在 doctor 人类模式打 `INVALID  <path>  <err>` 行；`--json` 编码完整 report 后若 `len(InvalidFiles) > 0` 同样 `exitWithCode(CodeInvalid)`；exit 4 语义在 JSON / 人类两种形态下保持一致（[internal/cli/diagnostics.go:133-172](file://internal/cli/diagnostics.go#L133-L172)）。
- `internal/app/next.go`：`Service.Next` 在 `len(doc.InvalidFiles) > 0` 时直接 `Invalidf(KindInvalid, nil, "next: managed state unreadable: <file>: <err>…", ≤3 条 + `+N more`)；`next` / `prime` / `project status` 同路——不可信状态下不让 next 给出推荐动作（[internal/app/next.go:24-46](file://internal/app/next.go#L24-L46)）。

不可读工作项文件不是 `doctor` 的「提案」而是「证据不可信」信号；与 git 冲突的 `ProposalNoteUnmerged`（M8.2）不同——后者在 dry-run plan 里出现，apply 全 rejected；本批的 `InvalidFiles` 让 doctor / repair / next 在出口上达成一致的 fail-closed 行为。

## 卡片导航
- [运行事件流](运行事件流.md) — **M6** `.devsys/runs/<id>.jsonl` 的 streamRecord 字段表、行级 sync（syncEvery=128 + 控制记录 + 末次）、断尾重建、nextRound / lastRound / lastExit 推断、§4.8 阶段事件与 `run_exec_started` / `run_finished` / `record_prompt` / `review_routed` / `review_accepted` 类型
- [对账与修复](对账与修复.md) — Doctor / Recover / RepairDryRun / RepairApply 流程与证据契约
- [领域记录与事件](领域记录与事件.md) — 四个领域存储模块的接口与不变量
- [读快照与守卫更新](读快照与守卫更新.md) — M4 `ReadSnapshot` 泛型与 `UpdateDecision / UpdateFinding / AppendArtifactVersion`（含 `ErrSuperseded`）
- [架构设计](架构设计.md) — 存储基元、领域持久化与调用方之间的分层与依赖方向
- [状态机与调度](状态机与调度.md) — 工作项九状态机、调度态与文件级领取租约的落点
- [编码规范](编码规范.md) — 命名、错误、事务与并发模型、测试约定；M3 工作流 / 审批 / next 的编码规则
- [事务与恢复](事务与恢复.md) — 事务日志、断尾检测与恢复协议（存储基元）
- [存储错误语义](存储错误语义.md) — 错误分类与调用方处理约定；**M5** 增 `ErrLocked` 与 `storage.LockFile` OS 独占锁原语（被知识层 `LockRefresh` 复用，方案 §15.2 同源）
- [特殊配置与命令](特殊配置与命令.md) — 隔离运行、冒烟剧本与示例快照
