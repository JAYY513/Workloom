---
status: stable
type: module
dimension: overview
triggers:
  - 领域存储
  - WorkItem 存不了
  - 事件流怎么写
  - Run 记录在哪
  - 决策记录怎么查
description: 项目内领域记录（WorkItem/事件/Run/决策/工件）的文件布局、ID 分配与访问纪律。
generated: true
source_commit: 2735f62
generator: repowiki-gen
---

# 可靠文本存储 · 概述

本模块覆盖 `.devsys/` 项目内**领域数据的落盘层**：任务（WorkItem）、事件流、Run 记录、决策/发现/工件记录，以及基于文本扫描的关键字检索。它建立在 [存储基元](存储错误语义.md) 的事务与锁之上，向上提供 Go API，检索另有 CLI 入口；不提供业务状态机。

## 模块定位

- **做什么**：把领域记录写成稳定键序的 YAML/JSONL 文本文件；分配全局唯一 ID；提供乐观并发更新与不可变版本链。
- **不做什么**：不理解状态机（M2）、不做门禁（M3）、不提供 CLI 领域命令（M4，`search` 除外）。

## 职责边界

| 子层 | 目录 | 职责 |
|---|---|---|
| WorkItem | `internal/workitem/` | 一任务一文件，`<前缀>-<序号>` ID，CAS 分配 |
| 事件流 | `internal/events/` | JSONL 按月分片追加，评论一级回复 |
| Run | `internal/run/` | `run-<YYYYMMDD>-<N>`，携带 §11.3 上下文快照 |
| 记录 | `internal/record/` | decision/finding/artifact 一记录一文件；Artifact 版本链 |
| 检索 | `internal/search/` | 无索引文本扫描，排除 `local/`、`.cache/` |

物理布局见 [架构设计](架构设计.md)；错误语义见 [存储错误语义](存储错误语义.md)。

## 卡片导航

- [架构设计](架构设计.md) — 每类数据的物理布局、ID 策略与并发写路径
- [领域记录与事件](领域记录与事件.md) — 四个领域存储模块的接口与不变量
- [事务与恢复](事务与恢复.md) — 事务日志、断尾检测与恢复协议（存储基元）
- [存储错误语义](存储错误语义.md) — 错误分类与调用方处理约定
- [编码规范](编码规范.md) — 本模块已验证的代码惯例
- [特殊配置与命令](特殊配置与命令.md) — 隔离运行、冒烟剧本与示例快照
