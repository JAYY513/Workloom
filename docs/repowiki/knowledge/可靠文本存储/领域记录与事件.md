---
status: stable
type: module
dimension: domain_records
triggers:
  - WorkItem 接口
  - 事件流 Append
  - Run 读写
  - Decision/Finding/Artifact 接口
  - Artifact 版本链
  - M2 状态机/领取/租约 API
description: 四个领域存储模块的 API、CAS 分配、乐观更新、Artifact 不可变版本链与 M2 调度入口
generated: true
source_commit: 85b0de7
generator: repowiki-gen
---

# 可靠文本存储 · 领域记录与事件

本卡描述 M1/M2 在 storage 基元之上暴露给上层的四类领域存储模块及其公开契约。所有变更都经过 `internal/storage` 的独占事务协议；模块本身**不**持锁、不直接 `fsync`、不写 `.devsys/local/`。

## 公共形态

四个模块都遵循同一形态：

- `New(root string) *Store` 绑定项目根（必须含 `.devsys/`，否则 `storage.ErrNotInitialized`）。
- 私有方法 `(s *Store) store() (*storage.Store, error)` 复用 `storage.Open`（[internal/workitem/workitem.go:48](file://internal/workitem/workitem.go#L48)、[internal/events/events.go:42](file://internal/events/events.go#L42)、[internal/run/run.go:42](file://internal/run/run.go#L42)、[internal/record/record.go:57](file://internal/record/record.go#L57)）。
- 暴露 `ctx context.Context` 作为第一个参数（编码规范约定）。
- 包级 sentinel `ErrNotInitialized = storage.ErrNotInitialized`（[internal/workitem/workitem.go:28](file://internal/workitem/workitem.go#L28)、[internal/events/events.go:26](file://internal/events/events.go#L26)）让调用方用 `errors.Is` 跨越包边界判定根状态。
- 序列化通过 `domain.EncodeYAML` / `domain.DecodeYAML`，自动注入 `schema_version: 1` 与 `null`/`[]` 区分（[internal/domain/serialize.go:16-71](file://internal/domain/serialize.go#L16-L71)）。

## WorkItem — `internal/workitem/`

| 项 | 落点 |
|---|---|
| 目录 | `.devsys/workitems/<id>.yaml`（[internal/workitem/workitem.go:23](file://internal/workitem/workitem.go#L23)） |
| ID 模式 | `<PREFIX>-<N>`，前缀必须以大写字母开头、只含 `[A-Z0-9_]`（[internal/workitem/workitem.go:43](file://internal/workitem/workitem.go#L43)、[workitem.go:57](file://internal/workitem/workitem.go#L57)）；调用方传 `prefix` 会被 `strings.ToUpper` 归一化 |
| 公开 API | `New`、`Create(ctx, *WorkItem, prefix) (id string, err error)`、`Get(ctx, id)`、`List(ctx)`、`Update(ctx, *WorkItem, expected []byte)`、`ReadSnapshot(ctx, id)`、`ValidID(id)`；M2 在 state.go / claim.go 增补 `Transition`、`ApplyRepair`、`Claim`、`Heartbeat`、`Release`、`Start`、`QueueRetry`、`UpdateClaimed`、`FenceRunUpdate`、`LeaseInspection`、`ListLeases` |
| 包级错误 | `ErrNotInitialized`、`ErrNotFound`、`ErrConflict`、`ErrBadPrefix`、`ErrInvalidInput`、`ErrLeaseTokenMismatch`、`ErrAlreadyClaimed`、`ErrNotClaimed`、`ErrLeaseExpired`（[internal/workitem/workitem.go:27-32](file://internal/workitem/workitem.go#L27-L32)、[internal/workitem/state.go:189-193](file://internal/workitem/state.go#L189-L193)） |

不变量：

- **CAS 分配**：`Create` 在写事务内 `nextNumber` 扫目录 `max+1`，再用 `tx.PutYAML(..., storage.ExpectAbsent())` 写入；冲突（`storage.ErrConflict`）即 `continue` 重扫，最多 5 次（[internal/workitem/workitem.go:55-97](file://internal/workitem/workitem.go#L55-L97)）。
- **Create 初始状态白名单**：传入的 `wi.Status` 必须落在 `domain.UnclaimedInitial = {draft, backlog, ready, blocked}` 之内（[internal/workitem/workitem.go:67-69](file://internal/workitem/workitem.go#L67-L69)、[internal/domain/state.go:123-128](file://internal/domain/state.go#L123-L128)）；空值默认 `draft`，其他状态（含 `done` / `cancelled` 等终态）一律返回 `ErrInvalidInput`，初态校验与 §6.1 状态机共享同一张表。
- **乐观更新**：`Update` 把 `expected []byte` 喂给 `storage.ExpectHash(storage.HashBytes(expected))`（[internal/workitem/workitem.go:186-188](file://internal/workitem/workitem.go#L186-L188)）；并发写直接 `ErrConflict`，**永不**被静默覆盖（方案 §15.2）。
- **`ReadSnapshot` 配对**：`ReadSnapshot`（[internal/workitem/workitem.go:225-251](file://internal/workitem/workitem.go#L225-L251)）返回 `(wi, data, err)`；`data` 是 raw file bytes，read-modify-write 流程必须把它原封不动传回 `Update`。
- **列表稳定序**：`List` 按 `(prefix, numeric suffix)` 二元组升序，**不**依赖目录枚举顺序或 mtime（[internal/workitem/workitem.go:129-160](file://internal/workitem/workitem.go#L129-L160)）。
- **SchemaVersion 默认**：零值（`wi.SchemaVersion == 0`）时在写事务内改写为 `domain.SchemaVersion`（[internal/workitem/workitem.go:84-86](file://internal/workitem/workitem.go#L84-L86)）。
- **Update 拒绝 Status 字段变更**：写入前比对 `expected` 中的 `Status` 与 `wi.Status`，不一致直接 `ErrInvalidInput`，强制走 `Transition` / `ApplyRepair`（[internal/workitem/workitem.go:173-175](file://internal/workitem/workitem.go#L173-L175)、[workitem.go:197-206](file://internal/workitem/workitem.go#L197-L206)）。
- **租约活跃时阻断普通 Update**：当 `expected` 解码出非空的 `LeaseOwner` 或 `LeaseToken` 时，`Update` 返回 `ErrInvalidInput`，强制使用 `UpdateClaimed` 提供 owner+token 才能改 metadata（[internal/workitem/workitem.go:179-181](file://internal/workitem/workitem.go#L179-L181)、[workitem.go:211-220](file://internal/workitem/workitem.go#L211-L220)）。

### M2 调度与状态机 API 一览

`internal/workitem/state.go` 与 `internal/workitem/claim.go` 在 storage 基元之上暴露调度、状态机与租约相关的入口。下表是 M2 对外契约的精炼摘要，详细语义（九状态、租约字段、Claim 不变量）见 [状态机与调度](状态机与调度.md) 与 [对账与修复](对账与修复.md)。

| 方法 | 出处 | 关键不变量 |
|---|---|---|
| `Transition(ctx, id, TransitionRequest, expected)` | [internal/workitem/state.go:46-51](file://internal/workitem/state.go#L46-L51) | `req.AllowRepair` 必须为 false；按 `domain.AllowedFrom + IsResumeLegal` 校验，`blocked` 走 `PreviousStatus` 恢复；写锁内重读 `expected` 字节做 CAS；`status_changed` 事件与状态变更同事务落盘 |
| `ApplyRepair(ctx, id, TransitionRequest, expected)` | [internal/workitem/state.go:53-59](file://internal/workitem/state.go#L53-L59) | 必须 `req.AllowRepair && len(req.Confirmation) > 0 && req.Guard != nil`；guard 在写锁内重跑；走 `legalRepairTransition`，可打破 §6 图（终态仅 repair 可动） |
| `TransitionRequest` 公共字段 | [internal/workitem/state.go:18-29](file://internal/workitem/state.go#L18-L29) | `TargetStatus / Actor / Reason / RunID / Token / PreviousStatus / Confirmation / AllowRepair / Now / Guard`；`valid()` 要求 target/actor/reason 非空 |
| `Claim(ctx, id, ClaimOptions)` | [internal/workitem/claim.go:179-326](file://internal/workitem/claim.go#L179-L326) | 每次新生成 32-hex token（`crypto/rand` 16 字节）；lease 文件 `ExpectAbsent` CAS，并发单赢家；同事务通过 `run.CreateTx` 创建 Run；与状态写 `status=in_progress / scheduling_state=claimed`；append **一条** `claimed` 事件 |
| `Heartbeat(ctx, id, HeartbeatOptions)` | [internal/workitem/claim.go:331-408](file://internal/workitem/claim.go#L331-L408) | owner+token 必须匹配当前 lease；过期（`now >= lease.LeaseUntil`）返回 `ErrLeaseExpired`；更新 `HeartbeatAt / LeaseUntil`，同时写 lease 与 workitem，append `lease_heartbeat` 事件 |
| `Release(ctx, id, ReleaseOptions)` | [internal/workitem/claim.go:414-536](file://internal/workitem/claim.go#L414-L536) | 默认要 owner+token；`ForExpired` 要求 `lease.LeaseUntil < now`，`ForOrphan` 要求 Run 文件缺失，二者绕过 owner/token；按 `SchedulingStateMapping` 写入 unclaimed/released；同事务关闭关联 Run（`status=finished`，`finished_at=now`）并 `tx.Delete(leaseRel)`；emit `lease_released` / `lease_recovered` 事件 |
| `Start(ctx, id, StartOptions)` | [internal/workitem/claim.go:541-665](file://internal/workitem/claim.go#L541-L665) | 仅 `blocked` 或 `retry_queued` 可走；目标 `in_progress`；保留旧 lease（resume）或发新 lease；缺少 lease 时按 Claim 路径同事务创建 Run |
| `QueueRetry(ctx, id, RetryOptions)` | [internal/workitem/claim.go:670-764](file://internal/workitem/claim.go#L670-L764) | 当前 status 必须是 `in_progress`；owner+token 必须匹配；`next_attempt_at = now + min(base · 2^(attempt-1), 1h)`（默认 `base=1min`）；清空 lease 字段、状态 `retry_queued`、`scheduling_state=retry_queued`，删除 lease 文件 |
| `UpdateClaimed(ctx, id, owner, token, mutate, expected)` | [internal/workitem/claim.go:770-819](file://internal/workitem/claim.go#L770-L819) | owner+token 必须匹配；`mutate` 写完后强制重置 status / scheduling_state / 所有 lease 字段（fence 兜底）；仅写 workitem，不动 lease，不 append 事件 |
| `FenceRunUpdate(ctx, workitemID, owner, token, mutate, expected)` | [internal/workitem/claim.go:889-929](file://internal/workitem/claim.go#L889-L929) | 在 lease 与 Run 同事务内重读，owner+token 必须匹配；`mutate(&domain.Run)` 后用 `ExpectHash` 写回；缺少 lease 返回 `ErrNotClaimed` |
| `LeaseInspection(ctx, id)` | [internal/workitem/claim.go:823-856](file://internal/workitem/claim.go#L823-L856) | 读 lease 文件返回 `domain.SchedulingLease`；缺失返回 `ErrNotClaimed`；读路径不走 `Write`，可被 reconcile 等只读调用方使用 |
| `ListLeases(ctx)` | [internal/workitem/claim.go:860-883](file://internal/workitem/claim.go#L860-L883) | 枚举 `.devsys/scheduling/` 下所有 `*.yaml`，仅保留匹配 `^[A-Z][A-Z0-9_]*-[0-9]+$` 的 id，返回 `(WorkitemID, Path)` 列表，供 reconcile 遍历 |

包级常量：

- `SchedulingDirName = "scheduling"`（[internal/workitem/claim.go:47](file://internal/workitem/claim.go#L47)），与 `storage.SchedulingDirName` 同名约定。
- `DefaultLeaseDuration = 5 * time.Minute`（[internal/workitem/claim.go:52](file://internal/workitem/claim.go#L52)），claim/heartbeat 都默认走它。
- `SchedulingStateMapping` 是 `workitem.Status → scheduling_state` 的 §6.4 表（[internal/workitem/claim.go:57-67](file://internal/workitem/claim.go#L57-L67)）。

## Events — `internal/events/`

| 项 | 落点 |
|---|---|
| 目录 | `.devsys/events/<YYYY-MM>.jsonl`，**按 UTC 年月分片**（[internal/events/events.go:45-47](file://internal/events/events.go#L45-L47)） |
| 行格式 | 一行 JSON：`{"id":..., "type":..., "subject":{"type":...,"id":...}, "time":..., "reply_to":null|"id", "content":...}`，JSONL 严格解码 |
| ID 派生 | `ev-<UTC 时间戳.微秒>-<4hex>`，事件时间 + `crypto/rand` 2 字节；**无全局计数器**（[internal/events/events.go:114-121](file://internal/events/events.go#L114-L121)） |
| 公开 API | `New`、`Append(ctx, *Event)`、`Read(ctx, Filter)`、`Comments(ctx, subject) ([]CommentThread, error)`；`ShardRel(t)` / `AppendTx(tx, ev)` 导出供 M2 在写锁内拼装复合事务 |
| 包级错误 | `ErrNotInitialized`、`ErrIncompleteTail`（来自 storage）、`ErrReplyNotFound`、`ErrReplyNotOneLevel`、`ErrBadTime`（[internal/events/events.go:25-31](file://internal/events/events.go#L25-L31)） |

不变量：

- **`ShardRel` 导出**：`ShardRel(t time.Time) string`（[internal/events/events.go:45-47](file://internal/events/events.go#L45-L47)）把 UTC 时间映射到 `events/<YYYY>-<MM>.jsonl`，供状态机/领取事务复用同一分片派生规则，保证事件与状态写到同一 shard。
- **分片按事件时间**：`Append` / `AppendTx` 用事件自身的 UTC `t.Year()/Month()` 派生 shard 路径；12 月事件 1 月写仍落 12 月分片（[internal/events/events.go:53-82](file://internal/events/events.go#L53-L82)）。
- **`AppendTx` 在写锁内复用**：`AppendTx(tx *storage.Tx, ev *domain.Event)`（[internal/events/events.go:90-110](file://internal/events/events.go#L90-L110)）负责 ID/SchemaVersion/UTC 时间填充、`Type` / `Subject` / `ReplyTo` 校验、shard 派生；M2 的 `changeStatus` / `Claim` / `Heartbeat` / `Release` / `QueueRetry` 都在自己持有的 `tx` 里调它，保证事件与状态、lease 同事务落地、不会出现「先写事件后写 workitem」的双写风险。
- **JSONL 追加协议**：`Append` 用 `st.Write(... tx.AppendJSONL(ShardRel(ev.Time), ev) ...)`（[internal/events/events.go:79-81](file://internal/events/events.go#L79-L81)）；中断恢复由 storage 层事务协议处理，事件层不重复断尾判定。
- **reply-to 一级规则**：写入校验 `ev.ReplyTo != nil && ev.Type != "comment"` 拒绝（[internal/events/events.go:69-71](file://internal/events/events.go#L69-L71)、[events.go:106-108](file://internal/events/events.go#L106-L108)）；聚合阶段 `Comments` 再校验：
  - `parent.Subject != ev.Subject` → `ErrReplyNotFound`（[internal/events/events.go:278-281](file://internal/events/events.go#L278-L281)）；
  - `parent.ReplyTo != nil`（二级回复）→ `ErrReplyNotFound`（[internal/events/events.go:282-284](file://internal/events/events.go#L282-L284)）；
  - 因此线程树深度恒为 2。
- **Filter 零字段匹配**：未设的时间与 `Subject` 边界都视为不限；`Subject` 必须 type+id 都匹配；`Type` 是精确匹配（[internal/events/events.go:123-154](file://internal/events/events.go#L123-L154)）。
- **读排序跨月**：`Read` 用 `listShards` 按月份升序遍历，再每 shard 时间升序（[internal/events/events.go:159-180](file://internal/events/events.go#L159-L180)、[events.go:182-209](file://internal/events/events.go#L182-L209)）。
- **必要字段校验**：`ev.Subject.Type` 与 `ev.Subject.ID` 不能为空（[internal/events/events.go:66-68](file://internal/events/events.go#L66-L68)、[events.go:103-105](file://internal/events/events.go#L103-L105)）。

## Run — `internal/run/`

| 项 | 落点 |
|---|---|
| 目录 | `.devsys/runs/<id>.yaml`（[internal/run/run.go:23](file://internal/run/run.go#L23)） |
| ID 模式 | `run-<YYYYMMDD>-<N>`，8 位日期 + 序号；`ValidID` 锚定 `^run-[0-9]{8}-[0-9]+$`（[internal/run/run.go:31](file://internal/run/run.go#L31)） |
| 公开 API | `New`、`Create(ctx, *Run) (id string, err error)`、`Get`、`List`、`Update(ctx, *Run, expected)`、`ReadSnapshot(ctx, id)`、`ValidID(id)`；`CreateTx(tx, *Run)` 供 M2 `Claim` 在已持锁事务内联创建 |
| 包级错误 | `ErrNotFound`、`ErrBadID`（[internal/run/run.go:26-29](file://internal/run/run.go#L26-L29)）；冲突复用 `storage.ErrConflict` |

不变量：

- **CAS 分配同形态**：写事务内 `nextNumber(st, timeBase(r.StartedAt))` → `run-<YYYYMMDD>-<N>` → `PutYAML(..., ExpectAbsent())`；冲突 ≤ 5 次（[internal/run/run.go:51-92](file://internal/run/run.go#L51-L92)）。
- **`CreateTx` 服务调度态**：`CreateTx(tx, r)`（[internal/run/run.go:99-125](file://internal/run/run.go#L99-L125)）要求 `r.ID == ""`，由 helper 派生并写回；`Claim` 调它保证 lease + run + workitem 三件套在同一 `st.Write` 事务里写完，并发 `Claim` 失败者只会看到 lease CAS 失败（不会留下孤儿 Run）。
- **默认字段填充**：`SchemaVersion == 0` → `domain.SchemaVersion`；`Attempt == 0` → `1`；`Status == ""` → `"running"`（[internal/run/run.go:71-79](file://internal/run/run.go#L71-L79)、[run.go:115-123](file://internal/run/run.go#L115-L123)）。
- **乐观更新同 WorkItem**：`Update` 用 `ExpectHash(HashBytes(expected))`；并发写抛 `storage.ErrConflict`（[internal/run/run.go:194-202](file://internal/run/run.go#L194-L202)）。
- **`ReadSnapshot` 同 WorkItem**：[internal/run/run.go:158-186](file://internal/run/run.go#L158-L186)。
- **排序稳定**：`List` 按 `(date, numeric suffix)` 二元组升序（[internal/run/run.go:188-219](file://internal/run/run.go#L188-L219)）；`listIDs` 用 `id[4:12]` 切日期（[internal/run/run.go:233](file://internal/run/run.go#L233)）。
- **ContextSnapshot**：Run 记录携带 `domain.ContextSnapshot`（方案 §11.3），记录 run 启动时的项目状态 / 工件 / 决策版本（[internal/domain/models.go:112-122](file://internal/domain/models.go#L112-L122)），便于重建视图与排查 stale-context。
- **M2 Run 关闭路径**：`Release` 找到 `lease.RunID` 对应的 Run 文件后在同一事务里写 `status=finished, finished_at=now` 并追加 `run_finished` 到事件 `Related`（[internal/workitem/claim.go:509-526](file://internal/workitem/claim.go#L509-L526)）；`FenceRunUpdate` 是 lease 活跃时唯一允许的 Run 改写入口（[internal/workitem/claim.go:889-929](file://internal/workitem/claim.go#L889-L929)）。

## Record — `internal/record/`

`record` 是三种记录家族的统一入口：`KindDecision`、`KindFinding`、`KindArtifact`（[internal/record/record.go:26-30](file://internal/record/record.go#L26-L30)）；目录 = `string(Kind) + "s"`（[internal/record/record.go:32](file://internal/record/record.go#L32)）。

| Kind | 目录 | ID 模式 |
|---|---|---|
| `decision` | `.devsys/decisions/` | `^decision-[0-9]+$` |
| `finding` | `.devsys/findings/` | `^finding-[0-9]+$` |
| `artifact` | `.devsys/artifacts/` | `^artifact-[0-9]+$` |

正则映射见 [internal/record/record.go:42-46](file://internal/record/record.go#L42-L46)。

| 公开 API | 说明 |
|---|---|
| `New(root)` | 绑定根 |
| `CreateDecision(ctx, *Decision) (id, err)` | 见下 |
| `CreateFinding(ctx, *Finding) (id, err)` | 同上 |
| `CreateArtifact(ctx, *Artifact) (id, err)` | 同上；初始 `Version = 1`、`PreviousID = nil` |
| `GetDecision` / `GetFinding` / `GetArtifact` | 解码单条 |
| `ListDecisions` / `ListFindings` | 按 ID 升序 |
| `NewArtifactVersion(ctx, currentID, next) (newID, err)` | 不可变版本追加 |
| `ArtifactHistory(ctx, id) ([]*Artifact, error)` | 沿 `previous_id` 反向遍历，新→旧 |
| `Kind.ValidID(id) bool` | 各 Kind 的格式校验 |

不变量：

- **统一 CAS 入口**：`create(ctx, kind, v, setID func(int))` 收口三种记录的 CAS 分配；`setID` 闭包负责写 `v.ID` / `v.SchemaVersion`（[internal/record/record.go:124-153](file://internal/record/record.go#L124-L153)）；上限 5 次冲突。
- **Artifact 版本链不可变**：`NewArtifactVersion`（[internal/record/record.go:280-325](file://internal/record/record.go#L280-L325)）写锁内：
  1. `tx.ReadForExpect(artifacts/<currentID>.yaml)` 取当前字节 → `DecodeYAML` 到 `cur`；
  2. `*next = *cur` 浅拷贝全部字段；
  3. 重置 `next.ID = newID`、`next.SchemaVersion = domain.SchemaVersion`、`next.Version = cur.Version+1`、`next.PreviousID = &currentID`；
  4. `tx.PutYAML(artifacts/<newID>.yaml, next, ExpectAbsent())` 写**新文件**。
  现有版本文件**永不**被改写。
- **`ReadForExpect` 来源**：`storage.Tx.ReadForExpect` 在写锁内安全重读（[internal/storage/txn.go:167-180](file://internal/storage/txn.go#L167-L180)）；调用方无需自己再开 `st.Read`——那会与 `st.Write` 的独占锁冲突。
- **历史回放**：`ArtifactHistory`（[internal/record/record.go:329-351](file://internal/record/record.go#L329-L351)）从 `id` 沿 `PreviousID` 链反查；`seen[cursor]` 检测环 → `ErrBadID`；`PreviousID` 格式非法 → `ErrBadID`。
- **包级错误**：`ErrNotFound`、`ErrBadID`、`ErrBadKind`、`ErrConflict = storage.ErrConflict`（[internal/record/record.go:34-40](file://internal/record/record.go#L34-L40)）。
- **空目录容忍**：`listIDs` 用 `os.ReadDir`，目录不存在时返回 `nil`（[internal/record/record.go:89-93](file://internal/record/record.go#L89-L93)）；与 run 的同名行为一致（[internal/run/run.go:208-210](file://internal/run/run.go#L208-L210)）。

## 跨模块契约小结

| 契约 | 出处 | 含义 |
|---|---|---|
| `ctx context.Context` 首个参数 | 所有四个模块 | 与 storage 协议保持一致；调用方负责 cancel |
| `ErrNotInitialized = storage.ErrNotInitialized` | workitem、events | 让上层用 `errors.Is` 一处识别未初始化根 |
| 写前 `SchemaVersion == 0` → `domain.SchemaVersion` | workitem、events、run、record | 保证落地 YAML 永远带 `schema_version: 1` |
| CAS 上限 5 次 | workitem、run、record.create、record.NewArtifactVersion | 极端竞争放弃而非死循环；调用方需自行决定重试策略 |
| 列表排序按 `(kind, numeric)` 稳定 | workitem、run、record | 不依赖文件系统枚举顺序 |
| JSONL 断尾归 storage.ErrIncompleteTail | events | 事件层不做额外断尾判定，避免双重协议 |
| `Update` 拒绝改 `Status` / 拒绝 lease 活跃期写入 | workitem | 状态变更走 Transition / ApplyRepair；带租约改 metadata 走 UpdateClaimed；避免绕过 fencing |
| 状态/lease/event 同事务 | workitem.state、workitem.claim、events.AppendTx | `Claim` / `Heartbeat` / `Release` / `QueueRetry` / `changeStatus` 都在同一 `st.Write` 内写 workitem + 必要时 lease/Run + 事件；事件统一经 `events.AppendTx`，shard 由 `events.ShardRel` 派生 |

## 相关卡

- [状态机与调度](状态机与调度.md) — 九状态图、Transition / ApplyRepair 字段语义、`SchedulingState` 五值与 §6.4 映射、租约字段、Claim / Heartbeat / Release / Start / QueueRetry 的详细不变量。
- [对账与修复](对账与修复.md) — `ApplyRepair` 的 `Confirmation` + `Guard` 落地、`internal/reconcile/` 下的 doctor / recover / repair 与本卡 API 的协作。
