---
status: stable
type: module
dimension: reconciliation
triggers:
  - doctor
  - recover
  - repair
  - 对账
  - 修复
  - 摘要不符
  - 确认式修复
  - 推断→确认→重写
  - 过期租约
  - 孤儿租约
description: devsys doctor / recover / repair 的流程与契约；doctor 只读、recover 确定性事务恢复 + 释放过期/孤儿租约、repair 推断→确认→重写 + 摘要回放 + Guard 在事务内复核证据；M3 Guard 改签名为返回事件；M8.1 sync status 复用 reconcile.LeaseAudit 做接力前的四态租约审计（active/expired/orphan/unreadable），pending transactions 时整体 defer；M8.2 冲突人工项 ProposalNoteUnmerged 复用 porcelain unmerged + MergeHeads 探针，git 失败降级为 plan note，apply 永远全 rejected 零写。
generated: true
source_commit: 52294b0
generator: repowiki-gen
---

# 对账与修复

本卡描述 `devsys doctor` / `recover` / `repair` 三条命令的内部实现、契约边界与 M3 起 `RepairApply` 的 Guard 签名变更（`func(*storage.Tx) ([]*domain.Event, error)`，事件并入提交批）。

## 职责分层

| 层 | 入口 | 物理动作 |
|---|---|---|
| `doctor` | `devsys doctor` / `internal/reconcile.Doctor` | 只读扫描（`storage.Inspect`），**不**创建锁文件、**不**触发事务恢复、**不**释放租约、**不**写盘 |
| `recover` | `devsys recover --actor … --reason …` | `storage.Recover`（确定性事务恢复）+ 释放过期/孤儿租约（写 `lease_recovered` 事件）；幂等 |
| `repair --dry-run` | `devsys repair --dry-run --actor … --reason …` | 收集 proposals + 计算确定性 `digest`（不含墙钟） |
| `repair --apply --confirm <digest>` | 同上 + `--confirm` | 重跑 `dry-run` → 比对 `digest` → `ApplyRepair` 在事务内用 Guard 重核证据 → 失败整单回滚 |

## `doctor`：只读对账

### 入口

`reconcile.Doctor(ctx, root, reconcile.Options{})` ([internal/reconcile/reconcile.go:142-208](file://internal/reconcile/reconcile.go#L142-L208))。CLI 层在 `devsys doctor` ([internal/cli/cli.go:1442-1484](file://internal/cli/cli.go#L1442-L1484)) 调用。

### 流程

1. `storage.InspectUnlocked` 列出 `.devsys/local/txn/` 目录与 `scheduling/` 目录，调用 callback 收集信息。
2. **不在 callback 内调 `storage.Read`**——这是只读路径的硬约束（[internal/reconcile/reconcile.go:1-20](file://internal/reconcile/reconcile.go#L1-L20)）。
3. 解析 lease YAML：通过 `reconcile_io.listSchedulingEntries` + 直接 `os.ReadFile` + `domain.DecodeYAML` 读取租约字节——但**只**在 `Inspect` 的 callback 闭包内调用，物理上等价于「共享锁下读」但不走 `storage.Read`。
4. 判定租约状态：
   - `LeaseUntil < now` → 过期租约
   - `WorkItemID` 不存在 → 孤儿租约
   - 不可读 YAML → 不可读租约
5. 收集 repair proposals（状态漂移：工作项 `Status` 与 `SchedulingState` 不一致）。
6. 缺锁文件 → `InspectionOK = false`，附 `InspectionNote`；有待恢复事务 → `InspectionOK = false`。

### 输出

```go
type InspectionReport struct {
    Root                string
    InspectionOK        bool
    Note                string
    PendingTransactions []storage.PendingTxn
    ExpiredLeases       []LeaseProbe
    OrphanLeases        []LeaseProbe
    UnreadableLeases    []LeaseProbe
    Proposals           []Proposal
}
```

### 退出码与行为边界

- `CodeInvalid = 4` 当 `InspectionReport.Proposals` 非空但 caller 是只读命令时**不**报错——doctor 是诊断工具，仅打印 proposals。
- 待恢复事务存在时 `InspectionReport.Orphans` 留空并附恢复提示（[internal/reconcile/reconcile.go:155-208](file://internal/reconcile/reconcile.go#L155-L208)）——不在不可信状态输出业务事实。
- `--json` 输出完整结构。

## `recover`：确定性恢复 + 释放过期租约

### 入口

`reconcile.Recover(ctx, root, opts)` ([internal/reconcile/reconcile.go:209-280](file://internal/reconcile/reconcile.go#L209-L280))。CLI 层 `devsys recover --actor … --reason …` ([internal/cli/cli.go:559-592](file://internal/cli/cli.go#L559-592))，强制 `--actor` / `--reason`。

### 流程

1. `storage.Recover` 完成所有 `.devsys/local/txn/` 的事务重放（commit 前丢弃 / commit 后幂等重放）。
2. 释放过期租约：`workitem.Release`，写 `lease_recovered` 事件。
3. 释放孤儿租约（同上）。
4. 不可读租约**只报告不释放**——避免破坏未知语义的状态。
5. **引用缺失 workitem 的租约只报告不释放**——避免破坏 workitem 状态图。

### 幂等性

`workitem.Release` 是 `ExpectHash` 守卫；重复执行（`ErrConflict`）跳过——多次 recover 不会产生重复 `lease_recovered` 事件。

## `repair`：推断 → 确认 → 重写

### 入口

- `reconcile.RepairDryRun(ctx, root, opts)` ([internal/reconcile/reconcile.go:282-355](file://internal/reconcile/reconcile.go#L282-L355))：只读扫描，输出 `Plan{Proposals, Digest}`。
- `reconcile.RepairApply(ctx, root, plan, opts)` ([internal/reconcile/reconcile.go:357-513](file://internal/reconcile/reconcile.go#L357-L513))：在事务内用 Guard 重核证据后写盘。

### DryRun 输出

```go
type Plan struct {
    Proposals []Proposal
    Digest    string  // sha256-hex of proposals，不含墙钟
}

type Proposal struct {
    WorkItemID string
    From       string  // 推断的当前状态
    To         string  // 修复目标状态
    Evidence   []Evidence
}

type Evidence struct {
    Kind   string  // lease / run / artifact
    Path   string  // 相对路径
    SHA256 string
    State  string  // present / absent
}
```

`Digest = sha256(encode(proposals))`，墙钟不参与——相同证据输入产生相同 digest。

### Apply 流程（M3 Guard 签名变更）

`reconcile.RepairApply` 在写事务内：

1. **重跑 `RepairDryRun`** 取得当前 `Plan.Digest`。
2. 用 caller 提供的 `--confirm` 覆盖 `Plan.Digest`（即 caller 的 digest 必须与当前 digest 严格相等，否则 `ErrDigestMismatch`）。
3. 对每个 proposal：
   - 写事务内 `tx.ReadForExpect(workitemRel(id))` 取当前字节
   - 构造 `TransitionRequest{TargetStatus: To, Confirmation: digest, AllowRepair: true, Guard: <evidence guard>}`
   - `workitem.ApplyRepair(ctx, id, req, raw)`
4. **`changeStatus` 内的 Guard** 是 M3 新签名：`func(*storage.Tx) ([]*domain.Event, error)`。`reconcile` 的证据 Guard 不返回事件（仅复审）；`applyRepair` 路径在 `changeStatus` 内 `req.Guard != nil` 但 guard 不产出事件（[internal/workitem/state.go:128-134](file://internal/workitem/state.go#L128-L134)）。

### 错误与退出码

- 摘要不符：`reconcile.ErrDigestMismatch` → CLI 升为 `CodePrecondition = 3`，错误提示 `run `devsys repair --dry-run` again`。
- Guard 证据漂移：`changeStatus` 内的 Guard 返回错误 → 整单回滚；摘要无意义——因为 digest 在事务内重算。
- 修复成功：返回 `RecoveryReport{Repaired: [...], Proposals: [...]}`。

## Guard 签名：M2 → M3 演化

| 路径 | M2 | M3 |
|---|---|---|
| `reconcile.RepairApply` Guard | `func() error` | `func(*storage.Tx) ([]*domain.Event, error)` |
| `approval.DecideTx` (reject 路径) 编排 | n/a | 通过 `workitem.Transition` 的 Guard 调 `DecideTx(rejected)`，返回 `approval_rejected` 事件 |
| `approval.ConsumeTx` (approve 路径) | n/a | 通过 `workitem.Transition` 的 Guard 调 `ConsumeTx`，返回 `approval_consumed` 事件 |

`changeStatus` 把 `status_changed` 事件、Guard 事件、失效事件、传播事件一并送入 `events.AppendBatchTx` 一次 append（[internal/workitem/state.go:165-182](file://internal/workitem/state.go#L165-L182)）。

## M3 决策与独立评审处置

[docs/开发记录.md](file://docs/开发记录.md) 记录的 M3Reviewer6 P1（离开后回到原状态旧批准仍可用）由 `approval.InvalidateForStatusTx` 在 `changeStatus` 的离开分支解决；M3Reviewer6 P1（拒绝处置与决定非原子）由 `rejectStageGate` 在 `workitem.Transition` 的 Guard 内同时完成 `DecideTx(rejected)` 与状态变更解决；M3Reviewer6 ③（消费 + 失效同文件触发 `staged twice`）由 `Tx.Staged(rel)` 在失效 pass 跳过已暂存路径解决。

## 调用契约示例

```sh
# 只读对账
bin/devsys.exe doctor                         # 待恢复事务 + 过期/孤儿租约 + 修复提议
bin/devsys.exe --json doctor                  # 完整 JSON

# 显式恢复（事务重放 + 释放过期/孤儿租约）
bin/devsys.exe recover --actor alice --reason "interrupted claim"

# 确认式修复：先推断、再确认、后重写
DIGEST=$(bin/devsys.exe --json repair --dry-run --actor alice --reason audit | jq -r .plan.digest)
bin/devsys.exe repair --apply --confirm "$DIGEST" --actor alice --reason audit
```

## M8.1 接力前的租约审计（被 `sync status` 复用）

`internal/syncstatus/syncstatus.go` 的 `Check` 是 `devsys sync status` 的只读探针（[internal/syncstatus/syncstatus.go:82-199](file://internal/syncstatus/syncstatus.go#L82-L199)），其租约分支**直接调用** `reconcile.LeaseAudit` 而不重写逻辑：

1. 先看 `storage.Diagnose` 的待恢复事务：`len(Pending) > 0` → `CodePendingTransactions` 阻塞器，并把 `st.LeasesDeferred = true` + `st.Note = "lease audit deferred: pending transactions make the lease snapshot partial"`（[internal/syncstatus/syncstatus.go:165-171](file://internal/syncstatus/syncstatus.go#L165-L171)）。理由：pending 的事务材料**可能**还要 apply，截一段租约快照会比「不报」更误导。
2. 无 pending 时按四态分流到 `st.Leases` 与 `Blockers`：
   - `lp.Owner == "<unreadable>"` → `CodeUnreadableLeases`（[internal/syncstatus/syncstatus.go:179-181](file://internal/syncstatus/syncstatus.go#L179-L181)）
   - `lp.ReasonKind == "orphan"` → `CodeOrphanLeases`（[internal/syncstatus/syncstatus.go:182-184](file://internal/syncstatus/syncstatus.go#L182-L184)）
   - `lp.Expired || lp.ReasonKind == "expired"` → `CodeExpiredLeases`（[internal/syncstatus/syncstatus.go:185-187](file://internal/syncstatus/syncstatus.go#L185-L187)）
   - 其它 → `CodeActiveLeases`（[internal/syncstatus/syncstatus.go:188-193](file://internal/syncstatus/syncstatus.go#L188-L193)）
3. `HandoffReady = len(Blockers) == 0`。

`sync status` 的出口约束是**只读**：不 fetch、不创建锁、不触发 recover、不写任何东西（[internal/syncstatus/syncstatus.go:1-9](file://internal/syncstatus/syncstatus.go#L1-L9)）；写入侧的修复仍走 `recover` / `repair --apply`。对账分叉不自动选赢家：sync status 只把分叉报成 blocker，由人/执行命令族（dispatch 阻塞见 `执行命令族`）决定下一步。

## M8.2 冲突人工项（`ProposalNoteUnmerged`）

`reconcile.RepairDryRun` 在原 proposals 之外追加 `conflictNotes` 探针（[internal/reconcile/reconcile.go:306-314](file://internal/reconcile/reconcile.go#L306-L314)）。探针本身不破坏只读路径——与 `sync status` 共享底层 `internal/knowledge` 的 `PorcelainStatus` + `MergeHeads`（[internal/reconcile/reconcile.go:327-364](file://internal/reconcile/reconcile.go#L327-L364)）：

1. `knowledge.PorcelainStatus(root)` 读 porcelain v1 的 unmerged 项；`knowledge.MergeHeads(root)` 读 `MERGE_HEAD` 等合并机械残留。
2. 任一调用失败 → `conflictNotes` 返回 `(nil, "git conflict probe unavailable: ...")`；`RepairDryRun` 把这条 message 接续到 `plan.Note`（[internal/reconcile/reconcile.go:300-314](file://internal/reconcile/reconcile.go#L300-L314)）——**不会**让 `RepairDryRun` 自身报错；测试环境常见于非 git checkout。
3. 成功时每条 unmerged 路径 / 每个 merge head 合成一条 `Proposal`：`Kind = ProposalNoteUnmerged`（[internal/reconcile/reconcile.go:49](file://internal/reconcile/reconcile.go#L49)）、`Note = "human-only"`、`Evidence = [{Kind: EvidenceAbsent, Path: "git:<path>", Note: "human resolution required"}]`。
4. sort by `Description` 稳定输出（[internal/reconcile/reconcile.go:360-362](file://internal/reconcile/reconcile.go#L360-L362)）。

形态特征（**只描述 proposal，不写 tick 流程**）：

- `Kind` 是独立枚举值 `ProposalNoteUnmerged = "note_unmerged_paths"`（[internal/reconcile/reconcile.go:45-49](file://internal/reconcile/reconcile.go#L45-L49)）；冲突路径在 working tree 中、不在 storage-managed 树内，故 `Evidence` 只能给 `Kind: EvidenceAbsent`、`Path: "git:" + path`，**不**携带 SHA256。
- `Note: "human-only"` 是显式语义标签，让后续 apply 与 dispatch（执行命令族）据此阻塞。
- description 形如 `unmerged path <path>: resolve with git (keep both sides explicit), then \`devsys repair --dry-run\`` 或 `unfinished merge machinery <name>: finish or abort the merge with git first`。

### Apply 全 rejected（零写）

`RepairApply` 在循环里第一个分支就是 `ProposalNoteUnmerged`（[internal/reconcile/reconcile.go:501-509](file://internal/reconcile/reconcile.go#L501-L509)）：把 `Evidence[0].Path` 去掉 `git:` 前缀得到人类可读路径，加 `: requires human resolution; no automatic merge` 推到 `rep.Rejected`，`continue`。**没有** `tx.Put*`、**没有** 走 `Guard`、**没有** 走 `ApplyRepair`——这是硬约束：devsys 不自动合并、不写侧车、不按时间戳选赢家。`digest` 必须包含这些 proposal 才能让 apply 进入循环，但循环本身把它们从写路径里完全跳过；dispatch 端的合并冲突门控见 `执行命令族`。

### Digest 第三键

`ComputeDigest` 把每个 proposal 序列化为 `P|Kind|WorkItemID|RunID|From|To|Description|` + 逐条 `E|Kind|Path|SHA256|Note|`（[internal/reconcile/reconcile.go:419-432](file://internal/reconcile/reconcile.go#L419-L432)），`canonicalProposals` 的排序键是 `(Kind, WorkItemID, firstEvidence.Path)`——第三键 `firstEvidence.Path` 只为 disambiguate 当前单条 evidence 的 `note_unmerged_paths`（[internal/reconcile/reconcile.go:434-465](file://internal/reconcile/reconcile.go#L434-L465)）；未来若有多 evidence 且首路径相同的 kind，需要再加第四键。注释原话："every current kind emits at most one proposal per (kind, workitem) with its own path. A future multi-evidence kind sharing one first path needs a fourth key."

## 与其他层的关系
- [架构设计](架构设计.md) — `storage.Inspect` / `Recover` 的物理边界
- [状态机与调度](状态机与调度.md) — `ApplyRepair` 与 Guard 的契约
- [领域记录与事件](领域记录与事件.md) — `RepairApply` 写入的事件类型
