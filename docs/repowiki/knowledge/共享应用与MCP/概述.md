---
status: stable
type: module
dimension: overview
triggers:
  - 共享应用服务
  - 共享服务怎么调
  - MCP 怎么开
  - devsys mcp
  - profile
  - 健康检查
  - 退避怎么用
  - 工作区服务
  - 适配器接线
  - 完成校验
  - 人工复核
  - run verify
  - run complete
description: M4 共享应用服务（internal/app）与 MCP 服务（internal/mcp，基于官方 Go SDK）的模块定位、职责边界与卡片导航；M6 在 Service 上新增 runexec/runverify/runstream + dispatch + workspace + prompt 五块业务接线，MCP tools_run 增 run_prompt/run_verify/run_complete/run_fail/run_cancel 五个工具，profile 仍为 session/executor/reviewer/admin 四种
  - agents 入口
  - devsys wire
description: M4 共享应用服务（internal/app）与 MCP 服务（internal/mcp，基于官方 Go SDK）的模块定位、职责边界与卡片导航
generated: true
source_commit: 997c5f8
generator: repowiki-gen
---

# 共享应用与 MCP · 概述

本模块是 M4 里程碑的接入层：把 CLI 与 MCP 两类入口共用的业务规则（输入校验、门禁、质量门、审批、版本守卫、事务、AGENTS.md 写入）收敛到**同一份 Go 应用服务** `internal/app`，让任意入口都不能绕过其它入口已经强制的不变量。`internal/mcp` 在该服务之上以官方 Go SDK 暴露 profile 过滤的工具族。两层都不引入新的锁或新的存储后端——它们都是 `可靠文本存储` 与 `项目接入与配置` 既有原语的薄适配。

## 模块定位

- **做什么**：
  - `internal/app.Service` 提供 30 余个业务方法，涵盖 `Session` / `Context` / `Project` / `Workitem` / `Workflow` / `Approval` / `Decision` / `Finding` / `Artifact` / `Event` / `Run` / `Next` / `Wire` / `KnowledgeStatus` 等领域；写方法统一接收 `expect`（来自对应 `Get*` 的 `Version`）并通过 `Tx` 走 `Store.Write` 的乐观并发。
  - `internal/mcp` 把每个工具方法映射到 SDK server 的注册项，按 profile 过滤暴露；工具处理器是 4–6 行的薄适配——业务规则全在 `internal/app`。
  - CLI（`internal/cli`）在 M4 后只做 flag 解析、`render`、`toCoded` 把 `app.Error` 翻译为退出码，并把业务分发迁出到 `internal/app`。
- **不做什么**：
  - 不实现业务规则（门禁、审批消费、传播、状态机、读取守卫、记录版本链）——这些都在 `internal/{workflow,approval,workitem,record,run,...}`，由 `app.Service` 调用。
  - 不引入新锁或新事务形态——所有写路径走 `storage.Store.Write`、所有读路径走 `Store.Read`。
  - 不实现知识层（`knowledge_status` 始终返回 `unavailable`，因为 M5 才是知识层的到达里程碑）。

## 职责边界

| 层 | 目录 | 职责 |
|---|---|---|
| 错误分类 | `internal/app/app.go` | `Kind`（展示类型：usage / precondition / internal / invalid / workflow / gate / quality / workitem / approval）+ `Class()`（归一到四类，对应退出码与 MCP 工具错误码）+ `Usagef / Preconditionf / Internalf / Invalidf` 构造器；`Classify` 把任意 error 包装为 `*Error` |
| 服务骨架 | `internal/app/app.go` | `Service{Root, Now}`、`New(root)`、`metadata` / `project` / `items` / `now` 私有助手 |
| 运行 | `internal/app/run.go` | `RunList / Get / Create / Update / Heartbeat / Log / Finish`；M6 增 `RunVerify` / `RunExec` / `RunPrompt` / `bindWorkspaceToRun` / `recordExecOutcome` / `verifyCompletion` / `refuseCompletion` |
| 上下文 | `internal/app/context.go` | `ContextGet / ContextForWorkitem / ContextRefresh / ContextCompact`：项目 + 计数 + 判决 + 近期 decision/finding/artifact/event 引用（compact 模式裁短） |
| 派发 | `internal/app/dispatch.go` | **M6 新增** `Dispatch / DispatchRequest / DispatchReport / DispatchedAttempt / SweptAttempt`；`retrySweep` 收尾失败/超时/停滞 attempt，按 `retry.Delay` 派下一次到期；`dispatchOne` 走 claim → workspace prepare → spawn |
| 工作项 | `internal/app/workitem.go` | 14 个方法覆盖 `List / Get / Create / Update / Transition / Claim / Release / Start / Block / Complete / Comment / AddDependency / RemoveDependency / Next`；写方法均带 `Expect`（来自 `Get`） |
| 工作流 | `internal/app/workflow.go` | `WorkflowList / Get / Start / StepNext / StepComplete / Pause / Resume / Cancel` |
| 审批 | `internal/app/approval.go` | `ApprovalList / Get / Request / Approve / Reject`（`approve` / `reject` 合并为 `Decide(approve bool)`） |
| 工作区 | `internal/app/workspace.go` | **M6 新增** `WorkspacePrepare / Remove / List` + `bindWorkspaceToRun`（把 workspace 路径 + claim_head 写到 run）+ `workspaceHooks`（policy hooks → workspace.Hook 映射） |
| 运行 | `internal/app/run.go` | `RunList / Get / Create / Update / Heartbeat / Log`；**M6 增 `RunFinish`**（succeeded / failed / cancel 终态守卫）+ `isTerminalRun` |
| 运行执行 | `internal/app/runexec.go` | **M6 新增** `RunExec` + `runStream` + `recordAgent` + `markRunning` + `finishAttempt` + `terminalStatus` + `recordExecOutcome` + `abortAttempt` + `advancePhase` + `logRelFor` + `execNote/Summary` + `roundCap`；before_run 钩子 + prompt 装配 + spawn + 流式 + 终止守卫 + 阶段事件 |
| 运行验证 | `internal/app/runverify.go` | **M6 新增** `RunVerify` + `verifyCompletion` + `refuseCompletion` + `routeToReview` + `runUpdateRaw`；claim_head vs current_head 比对；advance=false → run failed + workitem review + 事件 review_routed |
| 运行事件流 | `internal/app/runstream.go` | **M6 新增** `readRunStream` + `nextRound / lastRound / lastExit` + `recordedPhases` + `streamRecord` 结构；用于断尾重建与 next round 推断 |
| 提示词装配 | `internal/app/prompt.go` | **M6 新增** `RunPrompt` + `promptVars / workflowID / Step / Paused` + `specText` + `promptChanges` + `promptRemaining` + `recordPromptRefs / eventPromptRefs` + `riskLines` + `writePromptFile` + `firstLine`；首轮全 brief / 续轮 delta，Hash 由 sha256(text) |
| Wire | `internal/app/wire.go` | `Wire(dryRun bool) WireView`：把 devsys 纪律块注入 `AGENTS.md` 的 `<!-- devsys:begin/end -->` 区间；幂等（三次运行字节一致）；区段外字节保留（含其它工具的管理块） |
| 知识状态 | `internal/app/knowledge.go` | `KnowledgeStatus() KnowledgeStatusView`：M4 永远返回 `unavailable`，提示「M5 §12.6 到达」 |
- [架构设计](架构设计.md) — 共享服务 + SDK server 的分层、Profile 过滤与错误传递链；M6 增 dispatch / run exec|prompt|verify|complete|fail|cancel 流程图与依赖方向
- [工具与Profile](工具与Profile.md) — `toolSpec` 注册表、四种 Profile 的暴露面与默认组合；M6 增 run_prompt/run_verify（session）+ run_complete/run_fail/run_cancel（executor）工具表
- [错误语义](错误语义.md) — `app.Error` 的 `Kind/Class/Problems` 与 CLI/MCP 翻译；M6 增 workspace/harness 错误的 precondition / usage 翻译
- [执行命令族](执行命令族.md) — M6 Service 方法清单：WorkspacePrepare/Remove/List + Dispatch（recover→sweep→plan→claim→spawn）+ RunExec（stream 行级 + 阶段记录）+ RunPrompt + RunVerify + RunFinish
- [完成校验与人工复核](完成校验与人工复核.md) — `run verify` 只读 + `run complete` 走守卫 + `--force --by reviewer` 绕过路径 + workitem `review` 状态与 `next.Evaluate` 的 `stale_review` 对接
| 工具错误 | `internal/mcp/tools.go` | `toolError` 结构 + `fail / failNotice / usageFail`：把 `*app.Error` 翻译为 SDK 的 `isError=true` tool result；保留 `notice` 用于 last-known-good 上下文 |

## 卡片导航

- [架构设计](架构设计.md) — 共享服务 + SDK server 的分层、Profile 过滤与错误传递链
- [工具与Profile](工具与Profile.md) — `toolSpec` 注册表、四种 Profile 的暴露面与默认组合
- [错误语义](错误语义.md) — `app.Error` 的 `Kind/Class/Problems` 与 CLI/MCP 翻译
