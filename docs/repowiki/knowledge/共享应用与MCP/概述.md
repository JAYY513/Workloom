---
status: stable
type: module
dimension: overview
triggers:
  - 共享应用服务
  - 共享服务怎么调
  - MCP 怎么开
  - workloom mcp
  - profile
  - 健康检查
  - 退避怎么用
  - 工作区服务
  - 适配器接线
  - 完成校验
  - 人工复核
  - run verify
  - run complete
  - agents 入口
  - workloom wire
  - knowledge 服务接线
  - knowledge status/scan/validate/refresh
  - knowledge_pages / knowledge_generator
  - knowledge 退出码 0/10/11
  - context get --task
  - task 知识上下文
  - Adapter
  - AdapterInstalled
  - knowledgeState
  - Service.generate
  - TaskContext
  - Snapshot
  - contextSnapshot
  - context get --task --path
  - context workitem --path
  - AwaitingGeneration
  - Finalized
  - awaiting generation
  - knowledgeState helper
  - 知识基线
  - skill 目录
  - wire --check
  - wire --skill
  - wire --print-mcp
  - 默认 core 档
  - tier standard
  - default_policy
  - policyForWorkItem
  - claimNotice
  - storeError
  - ApprovalNote
  - QualityBlocks
  - retry_queued
  - project blueprint declared
  - no blueprint declared
  - 技能 frontmatter
  - 工具 tier
  - VisibleTools
  - WorkflowInitTemplate
  - BlueprintArtifactID
  - InvalidFile
  - ClassifyAttempts
  - CreateWorkitemCommand
  - preflightAttempt
  - refuseAttempt
  - workflow init --template
  - project update --blueprint-artifact
  - workloom setup
  - workloom mcp install
  - mcp install --apply
  - workloom 别名
  - 公开仓库
  - npm 包
  - Git 可选能力
  - 目录工作区
  - CompletionCheck.skipped
description: "M4 共享应用服务（internal/app）与 MCP 服务（internal/mcp，基于官方 Go SDK）的模块定位、职责边界与卡片导航；M5 在 Service 上新增 `KnowledgeStatus/Scan/Validate/Refresh` 四方法 + `KindKnowledge` 错误类（归一 `Class=KindInvalid`，CLI exit 4 / MCP `invalid`），并在 `context.go.knowledgeContext` 之上叠加 `KnowledgeContext`（Pages[], Baseline, Degraded, Notice）（`context get --task` 三阶段选页）；MCP `allTools()` 在 knowledge_* 行增 `knowledge_status`/`knowledge_validate`/`knowledge_refresh` 三 session profile 工具，并在 `registerContextForWorkitem` 输入新增 `paths []string` 参数；CLI 增 `workloom knowledge status|scan|validate|refresh` 四族路由与 `CodeStale=10` / `CodeMissing=11` reserved exit code；M5.6 在 Service 上增 `Service.knowledgeState` 复用层（LoadState + Adapter.Import + MergeStates）、`Service.generate` argv/Adapter 分派、`Service.Ta…M8.2 在 Dispatch tick 顶端加 merge 门控（recover 前 + dry-run 同门，blocked 走 exit 3 precondition，探针失败降级 notice）；M8.3 readRunStream 在活流缺失时回落到 `.devsys/archive/runs/<id>.jsonl`，归档只收终态 run（live-wins-or-fallback 严格）。\"；**P1/P2 与外部审查批**：`prime` 别名与 `--latest`、MCP `--tier core|standard`、`wire --check|--skill|--print-mcp`、`policyForWorkItem` 的 `default_policy` 回落（config.yaml 非法时 fail closed）、`Next` 装配 `PolicyIDs`/`QualityBlocks`/`DefaultPolicy`、`claim` 的 `Notice`、`storeError` 的并发冲突出路、`gateEvidence` 的 `ApprovalNote`、`ProjectBlueprint` 未声明蓝图返回 `(nil, nil)`、`project_blueprint_get` 补注册（session 档 / standard tier）本批（#336/#337，dispatch 预检+project 蓝图接入闭环）：dispatchOne claim 前 preflightAttempt（assigned_harness 非空须 harness.ByName 已知，shell 无 dispatch_command 且无 -- 命令 → Usagef；非 shell harness Probe 未安装 → Usagef）；RunExec 新增 refuseAttempt——openRunStream 之前 Usage/Precondition 拒绝统一落账为 run failed + result.errors + run_failed 事件 + 释放租约（abortAttempt 接入同一函数；禁止把空 run 标 finished）；Next 在 len(doc.InvalidFiles)>0 时 Invalidf(KindInvalid)（exit 4）防止基于残缺状态给建议；ProjectUpdate 增 BlueprintArtifactID *string（形态错 Usagef exit 2 / 未注册 Preconditionf exit 3，附 workloom artifact register 出路），写事务内更新 project.yaml.blueprint_artifact_id；MCP project_update 同步增 blueprint_artifact_id（empty string clears，id must already be registered）；internal/mcp 新导出 VisibleTools(cfg) []string，CLI mcp serve 在 0 工具时 errUsage（exit 2，提示 --tier standard）；WorkflowInitTemplate 把 docs/examples/workflows/*.md 通过 //go:embed 落到 .devsys/workflows/<id>.md，CLI 增 workflow init --template <id>；SessionStart 区分 items==nil（未列）与 len==0（空项目）——空项目不再报 \"state unavailable\"，notice 仅在 inspection limited 时给出；MCPSnippet 签名 MCPSnippet(name, cmd MCPCommand, projectRoot)（v0.1.12 起第一/二参由 `devsysBin string` 改为 `cmd MCPCommand`，同批新增 mcpcommand.go / mcpprobe.go：npm 树解析成 node + wrapper 脚本、注册后真起一次 `mcp serve` 的启动探针），filepath.ToSlash(bin)、JSON 用 encoding/json 编字符串、TOML 正斜线、cwd 填真实项目根、DEVSYS_CONFIG_DIR 已设时带 env。；本轮（ca58d27→19b9149，v0.1.9 发布链 + 主命令改名 workloom + Git 可选能力）：仓库转为公开（MIT），默认安装匿名下载 Release 资产、不设 GOPRIVATE（GOPRIVATE / gh auth login 只对私有 fork 有意义）；主命令改名 workloom（入口 cmd/workloom），devsys 是同一二进制的兼容别名（入口 cmd/devsys，install.sh / install.ps1 同时装两个名字）；v0.1.9 起 Release 资产名为 workloom-*，v0.1.8 及更早仍是 devsys-*；internal/app/setup.go + internal/cli/setup.go 新增 workloom setup 一条命令编排接入（init → workflow init --template quick-fix → wire（含 skill）→ config check → wire --check → prime → 蓝图检查 → doctor；幂等、失败即停、已有文件一律保留；不注册 MCP）；internal/app/mcpinstall.go + internal/cli/mcp.go 新增 workloom mcp install（codex / claude / opencode，user / project scope；默认只预览、--apply 才写、--force 只替换已有 devsys 条目；写前 announce 目标路径；JSONC 或 Codex 行内 mcp_servers 表无法安全合并即拒绝并指向 wire --print-mcp；注册条目调用 mcp serve --profile session,executor --tier core，注册名保持 devsys）；Git 从入场券改为可选能力（非 Git 项目走目录工作区，run complete 跳过 Git 证据且不把 Advanced 标 true；wire --check 把 Git 记为能力缺失而非失败）；mcpsnippets 的三种文件形态改为对齐 vendor docs（不再标 [UNVERIFIED]，JSONC 与行内 mcp_servers 表仍拒绝）。"
generated: true
source_commit: 7cdd918
generator: repowiki-gen
---

# 共享应用与 MCP · 概述

本模块是 M4 里程碑的接入层：把 CLI 与 MCP 两类入口共用的业务规则（输入校验、门禁、质量门、审批、版本守卫、事务、AGENTS.md 写入）收敛到**同一份 Go 应用服务** `internal/app`，让任意入口都不能绕过其它入口已经强制的不变量。`internal/mcp` 在该服务之上以官方 Go SDK 暴露 profile 过滤的工具族。两层都不引入新的锁或新的存储后端——它们都是 `可靠文本存储` 与 `项目接入与配置` 既有原语的薄适配。

## 模块定位

- **做什么**：
  - `internal/app.Service` 提供 30 余个业务方法，涵盖 `Session` / `Context` / `Project` / `Workitem` / `Workflow` / `Approval` / `Decision` / `Finding` / `Artifact` / `Event` / `Run` / `Next` / `Wire` / `KnowledgeStatus` 等领域；写方法统一接收 `expect`（来自对应 `Get*` 的 `Version`）并通过 `Tx` 走 `Store.Write` 的乐观并发。
  - `internal/mcp` 把每个工具方法映射到 SDK server 的注册项，按 profile 过滤暴露；工具处理器是 4–6 行的薄适配——业务规则全在 `internal/app`。
  - CLI（`internal/cli`）在 M4 后只做 flag 解析、`render`、`toCoded` 把 `app.Error` 翻译为退出码，并把业务分发迁出到 `internal/app`。
- **不做什么**：
  - 不实现业务规则（门禁、审批消费、传播、状态机、读取守卫、记录版本链）——这些都在 `internal/{workflow,approval,workitem,record,run,...}`，由 `app.Service` 调用。
  - 不引入新锁或新事务形态——所有写路径走 `storage.Store.Write`、所有读路径走 `Store.Read`。
  - 不在 app 层实现知识层算法（选页 / 索引 / 生成器归 `internal/{knowledge,...}`）；`KnowledgeStatus / Scan / Validate / Refresh` 只做封装，页层缺失是受支持的降级（`missing`，退出码 11），不是错误（[internal/app/knowledge.go:78-82](file://internal/app/knowledge.go#L78-L82)）。


  - **M8 新增**：`Dispatch` tick 顶端走 `mergeConflict(root)` 门控（recover 前 + dry-run 同门；blocked 走 precondition / CLI exit 3；探针失败降级 notice，不伪装为 conflict）；`readRunStream` 在活流缺失时回落到 `.devsys/archive/runs/<id>.jsonl`（归档只收终态 run，live-wins-or-fallback 严格）。**本批（#300/#301）**：next 走与 claim 同源的质量门（`next.QualityBlocks` 用 `workflow.Load` 的 plain read 拿 policy 体；ready 项被门挡下的标 `quality_blocked` 风险 + 推荐 `workitem update` 补救命令），工作项 `retry_queued` 也参与门判，新增 `retry_pending` 风险种类；`workitem create` 接受 `--acceptance a,b` 把验收标准在 create 阶段一并落库（避免满足质量门时再发 update）。
  - **本批接入层**：`internal/app/skill.go`（`WriteSkill` 写 `.agents/skills/devsys/SKILL.md` + `references/cli.md` + `references/troubleshooting.md`，全部以 `<!-- devsys-skill -->` marker 标定，hand-written 不覆盖）；`internal/app/wirecheck.go`（`WireCheck` 只读报 go/git/.devsys/registry/AGENTS.md 块/skill/MCP 配置 8 行，**不**写）；`internal/app/mcpsnippets.go`（`MCPSnippet(codex|claude|opencode, devsysBin, projectRoot)` 拼 stdio 命令片段；**本轮**文件形态对齐 vendor docs，不再标 [UNVERIFIED]——Codex `command+args`、Claude `mcpServers command+args`、OpenCode `type local + command array`，`cwd` 取代 `directory`）；`internal/app/wire.go` 把 `<!-- devsys:begin/end -->` 块末尾「结构化输出」一行换成指向 `SKILL.md`，让 wire 注入的纪律块与本批技能目录形成单一源。
  - **本批（#336/#337，dispatch 预检+project 蓝图接入闭环）**：`dispatchOne` claim 前 `preflightAttempt`——`assigned_harness` 非空须 `harness.ByName` 已知（shell 无 `dispatch_command` 且无 `--` 命令 → `Usagef`），非 shell harness `Probe` 未安装 → `Usagef`（拒绝走 notice 不占容量）；`RunExec` 新增 `refuseAttempt`——`openRunStream` 之前 Usage/Precondition 拒绝统一落账为 run `failed` + `result.errors` + `run_failed` 事件 + 释放租约（`abortAttempt` 接入同一函数；禁止把空 run 标 `finished`）；`Next` 在 `len(doc.InvalidFiles)>0` 时 `Invalidf(KindInvalid)`（exit 4）防止基于残缺状态给建议；`ProjectUpdate` 增 `BlueprintArtifactID *string`（形态错 `Usagef` exit 2 / 未注册 `Preconditionf` exit 3，附 `workloom artifact register` 出路），写事务内更新 `project.yaml.blueprint_artifact_id`；MCP `project_update` 同步增 `blueprint_artifact_id`（empty string clears，id must already be registered）；`internal/mcp` 新导出 `VisibleTools(cfg) []string`，CLI `mcp serve` 在 0 工具时 `errUsage`（exit 2，提示 `--tier standard`）；`WorkflowInitTemplate` 把 `docs/examples/workflows/*.md` 通过 `//go:embed` 落到 `.devsys/workflows/<id>.md`，CLI 增 `workflow init --template <id>`；`SessionStart` 区分 `items==nil`（未列）与 `len==0`（空项目）——空项目不再报 "state unavailable"，notice 仅在 inspection limited 时给出；`MCPSnippet` 三参签名 `MCPSnippet(codex|claude|opencode, devsysBin, projectRoot)`，`filepath.ToSlash(bin)`、JSON 用 `encoding/json` 编字符串、TOML 正斜线、`cwd` 填真实项目根、`DEVSYS_CONFIG_DIR` 已设时带 `env`。

  - **本批（v0.1.9，ca58d27→19b9149：v0.1.9 发布链 + 主命令改名 workloom + Git 可选能力）**：主命令改名 `workloom`（入口 `cmd/workloom/main.go`），`devsys` 是**同一二进制的兼容别名**（入口 `cmd/devsys/main.go`，install 脚本同时装两个名字）——机器命令行示例统一用 `workloom …`，而 `.devsys/`、`DEVSYS_*` 环境变量、`.agents/skills/devsys/`、`<!-- devsys-skill -->` 与 MCP 注册名 `devsys`（键 `mcp_servers.devsys` / `mcpServers.devsys` / `mcp.devsys`）**保持原样**；所以「二进制叫 workloom、注册名仍 devsys」不是冲突：`workloom mcp install` 写进客户端配置的条目名是 `devsys`，它调用的命令是 `workloom mcp serve --profile session,executor --tier core`（[internal/app/mcpinstall.go:61-63](file://internal/app/mcpinstall.go#L61-L63)）。
  - 仓库转为**公开仓库（MIT）**：默认安装匿名下载 Release 资产，不需要 `gh auth login`，也**不要设 `GOPRIVATE`**（设了会跳过公共校验和数据库）；`go install github.com/JAYY513/Workloom/cmd/workloom@latest` 是公开模块直装；`gh auth login` / `GOPRIVATE` 只对**私有 fork** 有意义。**v0.1.9 起** Release 资产名是 `workloom-*`（6 平台二进制 + `install.sh` / `install.ps1` + `checksums.txt` + `dist/npm/*.tgz`），**v0.1.8 及更早仍是 `devsys-*`**；npm 包装层包名 `@kaki317/workloom`（[npm/workloom/package.json:2](file://npm/workloom/package.json#L2)）——**已发布**：`@kaki317/workloom@0.1.11` + `@kaki317/workloom-win32-x64@0.1.11`（首发 0.1.10 于 2026-09-22），**当前只发 Windows x64**；darwin / linux / win32-arm64 平台包仍未发布，继续只随 Release 附发 tgz（wrapper 的 `optionalDependencies` 锁同一版本，[npm/workloom/package.json:27-33](file://npm/workloom/package.json#L27-L33)）。
  - **新增 onboarding 编排**：`internal/app/setup.go:43-198` + `internal/cli/setup.go:17` 的 `workloom setup` 一条命令按序 `init` → `workflow init --template quick-fix` → `wire`（默认含 skill）→ `config check` → `wire --check` → `prime` → 蓝图检查 → `doctor`；幂等（已有 policy 记 `Skipped` 并保留）、失败即停、`Ready` 只在硬步骤通过后为 true、只调用既有 Service 方法（不新增第二写路径）；**`setup` 不注册 MCP**（`mcp` 行只作报告），也不声明蓝图。
  - **新增 MCP 客户端注册**：`internal/app/mcpinstall.go:69-184` + `internal/cli/mcp.go:54` 的 `workloom mcp install` 覆盖 codex / claude / opencode 与 `--scope user|project`；**默认只预览（dry-run）**、`--apply` 才写、`--apply --force` 只替换已有 `devsys` 条目；写前 `Announce(client, path)` 打印目标路径；Codex 走 `[mcp_servers.devsys]` 表、claude / opencode 走外科式 JSON 插入（`jsonSetEntry`）；JSONC 或 Codex 行内 `mcp_servers = {...}` 表**拒绝（即便 `--force`）**并指向 `workloom wire --print-mcp <codex|claude|opencode>`；单个客户端失败只把该行标 `failed`，不打断其它客户端。
  - **Git 从入场券改为可选能力（cc5de1f）**：项目身份是「含 `.devsys/` 的目录」而非 git 根（`init` 允许非 Git 目录与 git 子目录、禁止自动 `git init`）；非 Git 项目走**目录工作区**（非 worktree），`run complete` 对该路径**跳过 Git 证据**——`CompletionCheck.Skipped=true`、`Advanced` 保持 false、事件仍为 `run_finished`（verdict `git check not applicable`）；`wire --check` 的 git 行（[internal/app/wirecheck.go:49-54](file://internal/app/wirecheck.go#L49-L54)）在 git 不在 PATH 时 `OK=true` + detail `not on PATH (optional: worktree, sync, knowledge freshness unavailable)`——**能力缺失不等于检查失败**。
  - **本批（v0.1.11 / npm 首发）**：npm 包装层包名 `@jayy513/workloom` → **`@kaki317/workloom`**（登录 scope 是 `kaki317`）并**已发布**：wrapper + `win32-x64` 平台包均为 `0.1.11`（首发 `0.1.10` 于 2026-09-22），**当前只发 Windows x64**，darwin / linux / win32-arm64 仍只随 Release 附发 tgz（补发同版本即可生效）。本模块内只有 `internal/app/mcpsnippets.go` 的**可选** npx 注释同步了包名（[internal/app/mcpsnippets.go:24](file://internal/app/mcpsnippets.go#L24)）——npx 仍是可选补充、**不**替代本地二进制行、冷启动需联网；`MCPSnippet` 三参签名、三种文件形态与 JSONC / 行内 `mcp_servers` 表拒绝语义都不变。
  - **本批（v0.1.12 / MCP 接入闭环）**：新增 `internal/app/mcpcommand.go`（`MCPCommand{Command, Args}` + `ServeArgs` / `MCPCommandFor` / `ResolveMCPCommand`：路径含 `node_modules/` 时向上最多 6 层找 npm wrapper 包，找到且 `node` 在 PATH → 改写成 `node` + wrapper 脚本，否则原样返回；[internal/app/mcpcommand.go:53-74](file://internal/app/mcpcommand.go#L53-L74)）与 `internal/app/mcpprobe.go`（`MCPProbeResult` + `ProbeMCPServer`：真起一次 `mcp serve` 完成 initialize 握手 + `ListTools`，超时 `MCPProbeTimeout = 10s`，SDK 日志丢弃；[internal/app/mcpprobe.go:26-32](file://internal/app/mcpprobe.go#L26-L32)、[:55-82](file://internal/app/mcpprobe.go#L55-L82)）；`MCPSnippet` 的 `devsysBin string` 参数换成 `cmd MCPCommand`（[internal/app/mcpsnippets.go:21](file://internal/app/mcpsnippets.go#L21)），调用方 `wire --print-mcp` 改传 `app.MCPCommandFor(os.Args[0])`（[internal/cli/cli.go:468](file://internal/cli/cli.go#L468)）；`mcp install` 在有客户端被处理时增一次启动探针（`MCPInstallView.Probe`，JSON `probe`，[internal/app/mcpinstall.go:48-55](file://internal/app/mcpinstall.go#L48-L55)）；npm 包装包补 `README.md`（中英对照，写清不下载、无 postinstall、当前只发 Windows x64，[npm/workloom/README.md](file://npm/workloom/README.md)）并把它收进 `files`（[npm/workloom/package.json:12-15](file://npm/workloom/package.json#L12-L15)）。

| 层 | 目录 | 职责 |
|---|---|---|
| 错误分类 | `internal/app/app.go` | `Kind`（展示类型：usage / precondition / internal / invalid / workflow / gate / quality / workitem / approval）+ `Class()`（归一到四类，对应退出码与 MCP 工具错误码）+ `Usagef / Preconditionf / Internalf / Invalidf` 构造器；`Classify` 把任意 error 包装为 `*Error` |
| 服务骨架 | `internal/app/app.go` | `Service{Root, Now}`、`New(root)`、`metadata` / `project` / `items` / `now` 私有助手 |
| 运行 | `internal/app/run.go` | `RunList / Get / Create / Update / Heartbeat / Log / Finish`；M6 增 `RunVerify` / `RunExec` / `RunPrompt` / `bindWorkspaceToRun` / `recordExecOutcome` / `verifyCompletion` / `refuseCompletion` |
| 上下文 | `internal/app/context.go` | `ContextGet / ContextForWorkitem / ContextRefresh / ContextCompact`：项目 + 计数 + 判决 + 近期 decision/finding/artifact/event 引用（compact 模式裁短） |

| 工作项 | `internal/app/workitem.go` | 14 个方法覆盖 `List / Get / Create / Update / Transition / Claim / Release / Start / Block / Complete / Comment / AddDependency / RemoveDependency / Next`；写方法均带 `Expect`（来自 `Get`）。**本批**：`ClaimView.Notice` 承载 `claimNotice` 文案（实例+default_policy 都为空且项目里存在策略文件时输出 `warning:`），`CreateWorkitemRequest` 增 `AcceptanceCriteria` 字段（create 时一并落库），`storeError` 把 `workitem.ErrBadID` 归 `Usagef`（exit 2）/ `storage.ErrConflict` 归 `Invalidf(KindWorkitem,…)` 并附「重新读取重试 / `--latest`」出路，`policyForWorkItem` 走「实例优先 + `default_policy` 回落」（config.yaml 非法 → claim fail closed），`gateEvidence` 经 `approval.InvalidatedFor` 把被 §4.9 voided 的审批填进 `workflow.GateEvidence.ApprovalNote`（失效时间戳 + 重批命令） |
| 审批 | `internal/app/approval.go` | `ApprovalList / Get / Request / Approve / Reject`（`approve` / `reject` 合并为 `Decide(approve bool)`） |
| 派发 | `internal/app/dispatch.go` | **M6 新增** `Dispatch / DispatchRequest / DispatchReport / DispatchedAttempt / SweptAttempt`；`retrySweep` 收尾失败/超时/停滞 attempt，按 `retry.Delay` 派下一次到期；`dispatchOne` 走 claim → workspace prepare → spawn。**M8.2** tick 顶端 `mergeConflict(root)` 门控（recover 前 + dry-run 同门；blocked 走 precondition exit 3，探针失败降级 notice）。 |
| 运行 | `internal/app/run.go` | `RunList / Get / Create / Update / Heartbeat / Log`；**M6 增 `RunFinish`**（succeeded / failed / cancel 终态守卫）+ `isTerminalRun` |
| 运行执行 | `internal/app/runexec.go` | **M6 新增** `RunExec` + `runStream` + `recordAgent` + `markRunning` + `finishAttempt` + `terminalStatus` + `recordExecOutcome` + `abortAttempt` + `advancePhase` + `logRelFor` + `execNote/Summary` + `roundCap`；before_run 钩子 + prompt 装配 + spawn + 流式 + 终止守卫 + 阶段事件 |
| 运行验证 | `internal/app/runverify.go` | **M6 新增** `RunVerify` + `verifyCompletion` + `refuseCompletion` + `routeToReview` + `runUpdateRaw`；claim_head vs current_head 比对；无证据/未推进 → `Advanced=false` + `Reason`，拒绝完成写 `completion_refused` 事件并把 workitem 送 `review`（stage gate 挡住时写 `review_gate_blocked`）；**本轮**非 Git 项目或目录工作区置 `Skipped=true` + Reason `git completion check is not applicable`（不拒绝 `succeeded`，`Advanced` 不置 true） |

| 提示词装配 | `internal/app/prompt.go` | **M6 新增** `RunPrompt` + `promptVars / workflowID / Step / Paused` + `specText` + `promptChanges` + `promptRemaining` + `recordPromptRefs / eventPromptRefs` + `riskLines` + `writePromptFile` + `firstLine`；首轮全 brief / 续轮 delta，Hash 由 sha256(text) |
| Next | `internal/app/next.go` | `Next(ctx) (Report, []*WorkItem, error)`；本批装配 `PolicyIDs`/`QualityBlocks`/`DefaultPolicy`，policy 体走 `workflow.Load` 的 plain read（解析失败的策略仍计入 `present` / 进 `parsed` cache），`next` 零写入；ready 项过 `next.QualityBlocks`（与 claim 同源），被门挡下的报 `quality_blocked` 风险 + 推荐 `workitem update` 补救命令；`retry_queued` 也参与门判，新增 `retry_pending` 风险种类；`default_policy` 未对应到 `.md` 时进 `PolicyProblems` |
| Wire | `internal/app/wire.go` | `Wire(dryRun bool) WireView`：把 devsys 纪律块注入 `AGENTS.md` 的 `<!-- devsys:begin/end -->` 区间；幂等（三次运行字节一致）；区段外字节保留（含其它工具的管理块）；本批块末尾行从「`--json/--jsonl` + 退出码 0/1/2/3/4」改为指向 `.agents/skills/devsys/SKILL.md` |
| 技能目录 | `internal/app/skill.go` | **本批新增** `WriteSkill` 管理 `.agents/skills/devsys/{SKILL.md, references/cli.md, references/troubleshooting.md}` 三文件，统一以 `<!-- devsys-skill -->` marker 标定；hand-written 无 marker 文件不会被覆盖；CLI 参考（[skill.go:64-104](file://internal/app/skill.go#L64-L104)）列 `workloom prime` / `wire --skill|--check|--print-mcp` / `project blueprint` 等本批命令形态 |
| Wire 检查 | `internal/app/wirecheck.go` | **本批新增** `WireCheck() WireCheckView` 只读报 8 行（go / git / .devsys/ / schema / registry / AGENTS.md 块 / skill 文件 / MCP 客户端配置）；**不**写 lock、不 recover、不创建文件；`staleSkillFiles` 按字节比对 marker 文件与本批生成的 body 是否一致；**本轮** git 行改走 `checkGit()`——git 不在 PATH 时仍 `OK=true` 并把 detail 记为能力缺失（`not on PATH (optional: worktree, sync, knowledge freshness unavailable)`） |
| 部署引导 | `internal/app/setup.go` | **本轮新增** `Setup(ctx, template) SetupView`：按序 `init` → `workflow init --template` → `Wire` + `WriteSkill` → `config.Diagnose` → `WireCheck` → `SessionStart` → `ProjectBlueprint` → `reconcile.Doctor`；幂等、失败即停、**不注册 MCP**、不声明蓝图 |
| MCP 注册 | `internal/app/mcpinstall.go` | **本轮新增** `MCPInstall` + `installCodexTOML` / `installClientJSON` / `jsonSetEntry` / `insertIntoJSONObject`：codex / claude / opencode × user / project scope，默认 dry-run；条目调用 `mcp serve --profile session,executor --tier core`（npm 装机时 `command`/`args` 由 `ResolveMCPCommand` 改写成 `node` + wrapper 脚本），注册名仍是 `devsys`；**本批** `MCPInstallView` 增 `Probe *MCPProbeResult`（JSON `probe`）——有客户端被处理时探一次，`--dry-run` 也探但不写盘，`--apply` 探针失败 → `Preconditionf`（exit 3，配置保留） |
| MCP 命令解析 | `internal/app/mcpcommand.go` | **本批新增** `MCPCommand{Command, Args}` + `ServeArgs` / `MCPCommandFor` / `ResolveMCPCommand` / `wrapperCommand` / `isNPMWrapperDir`：路径不含 `node_modules/` 原样返回；含则向上最多 6 层找 wrapper 包（`package.json` 的 `name == @kaki317/workloom` 且存在 `bin/workloom.js`）以及「最近一层 `node_modules` 下的 `@kaki317/workloom`」（**提升布局**）；找到且 `node` 在 PATH → 返回 `node` + wrapper 脚本，否则回退原二进制（[internal/app/mcpcommand.go:53-74](file://internal/app/mcpcommand.go#L53-L74)） |
| 启动探针 | `internal/app/mcpprobe.go` | **本批新增** `MCPProbeResult{OK, Skipped, Tools, DurationMS, Detail}` + `ProbeMCPServer(ctx, cmd, serve, cwd)`：用 SDK client + `CommandTransport` 真起一次 `mcp serve` 完成 initialize 握手并 `ListTools`，超时 `MCPProbeTimeout = 10s`，SDK 日志丢弃（`discardLogger`）；`DEVSYS_MCP_PROBE` 取值 `0`/`off`/`false`/`no`（大小写不敏感）→ `Skipped=true` + Detail `skipped (DEVSYS_MCP_PROBE=0)`，**不 spawn、绝不当作通过** |
| MCP 片段 | `internal/app/mcpsnippets.go` | **本批新增** `MCPSnippet(name, cmd MCPCommand, projectRoot)`（**本批签名**把 `devsysBin string` 换成 `cmd MCPCommand`，[internal/app/mcpsnippets.go:21](file://internal/app/mcpsnippets.go#L21)）拼出 stdio 命令片段；codex 写法用 `-c mcp_servers.devsys.{command,args,cwd}` 验证过（codex-cli 0.144.1）；**本轮**三种文件形态对齐 vendor docs（Codex `command+args`、Claude `mcpServers command+args`、OpenCode `type local + command array`，`cwd` 取代 `directory`），不再标 [UNVERIFIED]，JSONC 与行内 `mcp_servers` 表仍拒绝；未知 harness → `Usagef` exit 2；片段另带**可选** npx 注释（`npx --yes @kaki317/workloom mcp serve --profile session,executor --tier core`——冷启动需联网、**不**替代上面的本地二进制行，[internal/app/mcpsnippets.go:24](file://internal/app/mcpsnippets.go#L24)） |
| 项目蓝图 | `internal/app/project.go` | `ProjectGet/Update` + `StateGet/Update` + `List/Status` + `Blueprint`；本批 `ProjectBlueprint` 在项目未声明蓝图时返回 `(nil, nil)`（CLI `project blueprint` exit 0 + 文案 `no blueprint declared`，MCP `project_blueprint_get` 经 session 档 / standard tier 暴露，`artifact: null`） |
| 任务上下文装配 | `internal/app/context.go: knowledgeContext` | **M5 新增** `KnowledgeContext{Pages[], Baseline, Degraded, Notice}` + `KnowledgePageRef{Path, Description, Type, Status, Match, Reason, Stale}` + `MatchTrigger`/`MatchSources` 常量；三阶段选页（`knowledge.TriggerHit` 子串 → sources `knowledge.Match` → per-page `Evaluate`）；触发词匹配文本走 `knowledge.WorkItemText(item)`（M7.1 把原先内联的 `triggerHit` + lower/Join 抽到知识层，与 `internal/view` 共用一份实现）；无页面层 `Degraded=true` + Notice `degrade to records and events` |
| 工具错误 + tier | `internal/mcp/tools.go` + `internal/mcp/server.go` | `toolError` 结构 + `fail / failNotice / usageFail`：把 `*app.Error` 翻译为 SDK 的 `isError=true` tool result；保留 `notice` 用于 last-known-good 上下文。**本批**：`toolSpec` 加 `tier` 字段（空 = standard，显式 `TierCore` 才进 core 档），`server.go` 引入 `TierCore`/`TierStandard` + `ParseTier`（未知名报错）与 `visibleTier`（core 在 core+standard 都暴露，standard 只在 standard）；`tools_health.go` 把当前 `tier` 写进 `health` 响应；注册逻辑改成 `visible(spec.profiles, cfg.Profiles) && visibleTier(spec.tier, tier)` |

### 卡片详情（本批关键更新）

- [工具与Profile](工具与Profile.md) — `toolSpec` 注册表、四种 Profile 的暴露面与默认组合 + tier 档（core=20 项日常子集，standard=profile 全量）；M6 增 run_prompt/run_verify（session）+ run_complete/run_fail/run_cancel（executor）工具表；本批加 `project_blueprint_get`（session + standard tier）
- [错误语义](错误语义.md) — `app.Error` 的 `Kind/Class/Problems` 与 CLI/MCP 翻译；M6 增 workspace/harness 错误的 precondition / usage 翻译；本批 `storeError` 把 `workitem.ErrBadID` 归 usage（exit 2）/ `storage.ErrConflict` 归 invalid 并附「重新读取重试 / `--latest`」出路
- [执行命令族](执行命令族.md) — M6 Service 方法清单：WorkspacePrepare/Remove/List + Dispatch（recover→sweep→plan→claim→spawn）+ RunExec（stream 行级 + 阶段记录）+ RunPrompt + RunVerify + RunFinish；M8.2 在 Dispatch 顶端加 merge 门控（recover 前 + dry-run 同门，exit 3 precondition）；M8.3 在 readRunStream 加归档回落；本批 `Next` 装配 `PolicyIDs`/`QualityBlocks`/`DefaultPolicy` 并把 ready 项过 `next.QualityBlocks`（与 claim 同源）；**本轮**加 `Setup` 与 `MCPInstall` 两条 onboarding 入口，并给 `RunVerify`/`RunFinish` 补 Git 可选的 `Skipped` 路径
- [完成校验与人工复核](完成校验与人工复核.md) — `run verify` 只读 + `run complete` 走守卫 + `--force --by reviewer` 绕过路径 + workitem `review` 状态与 `next.Evaluate` 的 `stale_review` 对接；本批 `gateEvidence` 在 `require_approval` 缺失且审批被 §4.9 voided 时填 `ApprovalNote`（失效时间戳 + 重批命令），让 reviewer 看到的不只是「还差一个审批」；**本轮**非 Git 项目 / 目录工作区走 `Skipped` 例外：`run complete` 直接成功、`Advanced` 不置 true、事件仍是 `run_finished`（verdict `git check not applicable`）

## 卡片导航

- [架构设计](架构设计.md) — 共享服务 + SDK server 的分层、Profile 过滤、tier 档与错误传递链
- [工具与Profile](工具与Profile.md) — `toolSpec` 注册表、四种 Profile 的暴露面与默认组合 + tier 档（core=20 项日常子集 / standard=profile 全量）；`health` 响应回传当前 tier
- [错误语义](错误语义.md) — `app.Error` 的 `Kind/Class/Problems` 与 CLI/MCP 翻译；M5 增 `KindKnowledge` 翻译；本批 `storeError` 多了 ErrBadID/ErrConflict 的翻译分支
