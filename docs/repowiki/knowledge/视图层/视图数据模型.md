---
status: stable
type: module
dimension: view_model
triggers:
  - view.Model
  - view.SchemaVersion
  - SchemaVersion=1
  - DefaultLimit
  - view.Options
  - view.Project
  - view.Progress
  - view.Item
  - view.Runs
  - view.RunEntry
  - view.Records
  - view.RecordRef
  - view.Knowledge
  - view.PageEntry
  - view.Baseline
  - view.Trust
  - view.Provenance
  - Provenance.Sources
  - 目录以 / 结尾
  - Truncated
  - Total
  - TrustOK
  - TrustAdvisory
  - TrustPending
  - KnowledgeFresh
  - KnowledgeStale
  - KnowledgeMissing
  - KnowledgeUnavailable
  - Degraded
  - Reason
  - JSON 契约
  - omitempty
  - freshnessHint
  - siteData
  - ShowFreshness
  - ModelJSON
  - data/model.json
description: M7.4 视图层数据契约（internal/view/view.go + internal/sitestatic/{static,render}.go + internal/cli/workspace.go）；Model = schema_version/project/trust/baseline/progress/runs/records/knowledge/sources/problems + 每节带 Provenance.Sources（目录以 / 结尾）；列表节带 Truncated 标记 + counts/total；Trust 三态常量（ok/advisory_unlocked/pending_transaction）+ Knowledge 四态（fresh/stale/missing/unavailable）；Baseline 用 *bool 表示 Dirty；SchemaVersion=1、DefaultLimit=50、Options{Now func, Limit int}。M7.4 在 sitestatic 内由 siteData 五派生字段 + freshnessHint 纯函数把 Knowledge 状态映射为页面横幅文案，ModelJSON 与落盘 data/model.json 逐字节一致（无 generated_at），workspace build --json 输出 {ok,out,pages,generated_at,baseline,trust_state} 信封。
generated: true
source_commit: fa2a87d
generator: repowiki-gen
---

# 视图层 · 视图数据模型

视图的**唯一**对外数据契约是 `view.Model`（[internal/view/view.go:62-80](file://internal/view/view.go#L62-L80)）。本卡梳理字段、JSON 编码约定、各节 `Provenance.Sources` 的「目录以 `/` 结尾」语义、列表节的 `Truncated`/`Total` 计数与各状态常量，供 M7.2 静态构建 / M7.3 serve 渲染层做稳定输入。

## 顶层结构

```go
type Model struct {
    SchemaVersion int       `json:"schema_version"`           // 固定 1（const SchemaVersion = 1）
    Project       Project   `json:"project"`                  // 蓝图（必有）
    Trust         Trust     `json:"trust"`                    // 信任态（必有）
    Baseline      Baseline  `json:"baseline"`                 // git 基线（必有）
    Progress      Progress  `json:"progress"`                 // 进度板（必有）
    Runs          Runs      `json:"runs"`                     // 执行时间线（必有）
    Records       Records   `json:"records"`                  // 决策/发现/工件（必有）
    Knowledge     Knowledge `json:"knowledge"`                // 知识新鲜度（必有）
    Sources       []string  `json:"sources"`                  // 顶层 sources 并集（必有）
    Problems      []string  `json:"problems,omitempty"`       // 非致命问题（空时省略）
}
```

| 字段 | JSON key | 必有？ | 编码约定 |
|---|---|---|---|
| `SchemaVersion` | `schema_version` | 是 | 当前固定 `1`（[view.go:25](file://internal/view/view.go#L25)）；升到 2 时顶层判断由调用方承担 |
| `Project` | `project` | 是 | 见 `Project`；`newModel` 预填 `Milestones/Risks/Blockers/NextFocus` 为空 slice |
| `Trust` | `trust` | 是 | 见 `Trust`；`newModel` 预填 `Pending` 为空 slice |
| `Baseline` | `baseline` | 是 | 见 `Baseline` |
| `Progress` | `progress` | 是 | 见 `Progress`；`newModel` 预填 `Counts=map[string]int{}`、`Items=[]Item{}`、`Readiness.Verdict=VerdictPass` |
| `Runs` | `runs` | 是 | 见 `Runs`；`newModel` 预填 `Entries=[]RunEntry{}` |
| `Records` | `records` | 是 | 见 `Records`；`newModel` 预填 `Decisions/Findings/Artifacts=[]RecordRef{}` |
| `Knowledge` | `knowledge` | 是 | 见 `Knowledge`；`newModel` 预填 `Pages=[]PageEntry{}`、`Affected=[]string{}` |
| `Sources` | `sources` | 是 | `[]string`；`newModel` 预填 `[]string{}`；`collectSources` 汇总各节 sources 并 `sortedUnique` |
| `Problems` | `problems` | 否 | `omitempty`：空时省略，避免污染 JSON 信封 |

## Provenance（每节的 sources 字段）

`Provenance`（[view.go:82-86](file://internal/view/view.go#L82-L86)）作为嵌入字段出现在 `Project`、`Progress`、`Runs`、`Records`、`Knowledge` 五节中：

```go
type Provenance struct {
    Sources []string `json:"sources"`
}
```

**关键约定**（[collect.go:96-123](file://internal/view/collect.go#L96-L123)）：

- **路径全部用项目相对 slash 形式**（Windows 也用 `/`），不混用 `\`。
- **`/` 后缀 = 目录**；无 `/` = 文件。例如 `.devsys/runs/`（runs 目录）vs `.devsys/runs/run-1.jsonl`（具体文件）。
- 来源路径为空的字符串一律丢弃（`noteRel/noteRelDir` 都有 `if rel == ""` 早退）。
- 每节 `Sources` 都过 `sortedUnique`，顶层 `Sources` 又过一次——保证「读端记啥就出啥」。

调用方可以通过 `m.Project.Sources`、`m.Knowledge.Sources` 等逐节追溯每一段事实读了哪些文件；顶层 `m.Sources` 是全量并集 + `git` 基线（基线不在 `.devsys/` 下，不进各节 `Provenance.Sources`，但加入顶层）。

## `Options`（Build 的注入参数）

```go
type Options struct {
    Now  func() time.Time  // 可选；零值时 Build 用 time.Now；作为 Build 的时钟同时喂 storage.Open / reconcile.Doctor / next.Evaluate，但绝不写进 JSON
    Limit int              // 可选；零值或负值时用 DefaultLimit；只作用于列表节
}
```

- `Now` 注入的目的是让测试控制时钟（`storage.Open` / `reconcile.Doctor` / `next.Evaluate` 共用同一份）；模型不带 `Now`——这是确定性保证。
- `Limit` 影响的是 `Runs/Records/Knowledge.Pages` 等「列表节」；`Progress.Items` 与 `Project.Milestones` **不**截断（[view.go:27-30](file://internal/view/view.go#L27-L30) 注释：`work items and milestones are never capped — they are the board itself`）。

## 各节字段

### `Project`（蓝图，[view.go:107-130](file://internal/view/view.go#L107-L130)）

| 字段 | JSON key | 必有？ | 来源 |
|---|---|---|---|
| `Provenance` | `sources` | 是 | `internal/view/build.go: project()` 读 `config.ManagedFiles` 列表 + 当前状态 + 里程碑文件 |
| `ID/Name/Description/Status` | `id/name/description/status` | `omitempty` | `config.Metadata.Project.*` |
| `CurrentPhase` | `current_phase` | `omitempty` | `mp.CurrentPhase` |
| `Goals/Constraints/TechStack` | `goals/constraints/tech_stack` | `omitempty` | `mp.Goals/Constraints/TechStack` |
| `Scope` | `scope` | 是（无 `omitempty`） | `domain.Scope` |
| `Milestones` | `milestones` | 是 | `domain.Milestone[]`（`newModel` 预填 `[]`）；`state/milestones.yaml` 覆盖 |
| `Summary` | `summary` | `omitempty` | `current.yaml.Summary` |
| `Risks/Blockers/NextFocus` | `risks/blockers/next_focus` | 是（**无** `omitempty`，`newModel` 预填 `[]`） | `current.yaml.{Risks,Blockers,NextFocus}` |
| `CreatedAt/UpdatedAt` | `created_at/updated_at` | `omitempty` | `*time.Time`，零值时省略（`timePtr`） |
| `Degraded` | `degraded` | `omitempty` | 缺/坏 metadata 时 `true` |
| `Reason` | `reason` | `omitempty` | 降级原因；pending 时写 `pendingReason` |

### `Progress`（进度板，[view.go:132-143](file://internal/view/view.go#L132-L143)）

| 字段 | JSON key | 必有？ | 备注 |
|---|---|---|---|
| `Provenance` | `sources` | 是 | `workitems/*.yaml` |
| `Counts` | `counts` | 是 | `map[string]int`；key 为 `domain.Status*` 常量；`encoding/json` 保证 key 序按字典序 |
| `Items` | `items` | 是 | `[]Item`；按 `id` 升序；**不截断** |
| `Readiness` | `readiness` | 是 | `next.Report`（`Verdict/Reasons/Risks/Fixes/Next`） |
| `Degraded/Reason` | `degraded/reason` | `omitempty` | 至少一个工作项文件损坏时 `true` + 损坏信息 |

`Item` 字段（[view.go:145-167](file://internal/view/view.go#L145-L167)）：ID/Title/Type/Status/Priority/ParentID/Dependencies/Workflow/WorkflowStep/WorkflowPaused/AssignedAgent/AssignedHarness/SchedulingState/LeaseOwner/LeaseUntil/NextAttemptAt/ActiveRunID/PreviousStatus/CreatedAt/UpdatedAt。可选字段有 `omitempty`；`Dependencies` 空时由 `itemFrom` 显式置 `nil`（让 JSON 里 `dependencies` 字段消失而不是出现 `[]`）。

### `Runs`（执行时间线，[view.go:169-199](file://internal/view/view.go#L169-L199)）

```go
type Runs struct {
    Provenance
    Entries   []RunEntry `json:"entries"`
    Truncated bool       `json:"truncated,omitempty"`
}
```

- `Entries`：按 `StartedAt` 倒序、再 `ID` 升序定序。
- `Truncated`：超过 `Limit` 时 `true`；这是 `Runs` 唯一带 `Truncated` 字段的节。
- 排序 + 截断都在 [build.go:387-396](file://internal/view/build.go#L387-L396) 完成，**未走 `capList`**——这里直接 `runs[:b.limit]` 内联（与 `capList` 等价）。
- `RunEntry` 含 `StreamTorn bool`（`omitempty`）——通过 `storage.InspectJSONL` 判定；事件流文件本身追加进 `Runs.Sources`。

### `Records`（记录，[view.go:201-221](file://internal/view/view.go#L201-L221)）

```go
type Records struct {
    Provenance
    Decisions []RecordRef `json:"decisions"`
    Findings  []RecordRef `json:"findings"`
    Artifacts []RecordRef `json:"artifacts"`
    Truncated bool        `json:"truncated,omitempty"`
}
```

- 三个列表各自走 `capList(decisions|findings|artifacts, b.limit)`——任一截断则 `rec.Truncated=true`。
- 顺序：decisions/artifacts 按 `CreatedAt` 倒序再 `ID` 升序；findings 按 `ID` 升序（领域模型无时间戳，M1.5）。
- `RecordRef`（[view.go:210-221](file://internal/view/view.go#L210-L221)）含 `ID/Title/Type/Status/Severity/Version/RelatedWorkItems/CreatedAt`，全部 `omitempty`。

### `Knowledge`（知识层快照，[view.go:223-244](file://internal/view/view.go#L223-L244)）

| 字段 | JSON key | 必有？ | 备注 |
|---|---|---|---|
| `Provenance` | `sources` | 是 | `state.json/snapshot.json` + page roots（目录以 `/` 结尾） |
| `Status` | `status` | 是（无 `omitempty`） | `fresh/stale/missing/unavailable` 四态之一 |
| `Reason` | `reason` | `omitempty` | 裁决语；`missing`/stale/`unavailable` 时填充 |
| `Baseline/Head/Branch` | `baseline/head/branch` | `omitempty` | 三个 commit/branch；与 `Model.Baseline` 互补 |
| `IndexReady` | `index_ready` | 是 | `LoadSnapshot` 成功时 `true` |
| `IndexFiles` | `index_files` | `omitempty` | `snapshot.Stats.TotalFiles` |
| `Generator/Adapter` | `generator/adapter` | `omitempty` | 配置值 + 适配器名 |
| `Pages` | `pages` | 是（无 `omitempty`） | `[]PageEntry`；`newModel` 预填 `[]`；超过 limit 时 `Truncated=true` |
| `Affected/Unverifiable` | `affected/unverifiable` | `omitempty` | 受变更影响的页 + 无 sources/baseline 的页 |
| `ChangedFiles` | `changed_files` | 是（无 `omitempty`） | git 变更文件数（默认 0） |
| `Truncated/Degraded` | `truncated/degraded` | `omitempty` | 列表截断 / 知识层不可用 |

**`missing` vs `unavailable`**：
- `KnowledgeMissing`（[view.go:50](file://internal/view/view.go#L50)）：无 page layer（`report.Pages == 0`，未配置生成器或未生成）→ 方案 §12.6 降级路径。
- `KnowledgeUnavailable`（[view.go:51](file://internal/view/view.go#L51)）：有 page layer 但「新鲜度答不出来」（典型：无 git / git 失败）→ 方案 §12.5「cannot tell ≠ fresh」。

### `Baseline`（git 基线，[view.go:88-97](file://internal/view/view.go#L88-L97)）

```go
type Baseline struct {
    Available    bool   `json:"available"`
    Commit       string `json:"commit,omitempty"`
    Branch       string `json:"branch,omitempty"`
    Dirty        *bool  `json:"dirty,omitempty"`
    ChangedFiles int    `json:"changed_files,omitempty"`
    Reason       string `json:"reason,omitempty"`
}
```

- `Dirty` 是 `*bool`：git 不可用时**留 nil**（不假装 false），否则按真实值。
- `ChangedFiles` 是 git 报告的工作树变更文件数；`Reason` 是 git 失败的原因。

### `Trust`（信任态，[view.go:99-105](file://internal/view/view.go#L99-L105)）

```go
type Trust struct {
    State        string   `json:"state"`
    InspectionOK bool     `json:"inspection_ok"`
    Note         string   `json:"note,omitempty"`
    Pending      []string `json:"pending_transactions"`
}
```

三态常量（[view.go:32-42](file://internal/view/view.go#L32-L42)）：

| 常量 | JSON 值 | 触发条件 |
|---|---|---|
| `TrustOK` | `"ok"` | `storage.Inspect` 在已有锁文件上拿到共享锁，`reconcile.Doctor` 报无待恢复事务 |
| `TrustAdvisory` | `"advisory_unlocked"` | 锁文件不存在（只读副本/从未写过状态），`storage.InspectUnlocked` 降级 |
| `TrustPending` | `"pending_transaction"` | `reconcile.Doctor` 报待恢复事务 **或** `inspectSnapshot` 路径上撞 `*storage.PendingTxnError` |

`InspectionOK` 直接来自 `doc.InspectionOK`（Doctor 完整诊断 OK）；advisory 时显式置 `false`。`Note` 是 Doctor 的诊断说明 + advisory 时附加的「reads were lock-free: no project lock exists (no writer ever ran), so the snapshot is advisory」（[build.go:104-107](file://internal/view/build.go#L104-L107)）。

### `Knowledge` 四态常量（[view.go:44-51](file://internal/view/view.go#L44-L51)）

| 常量 | JSON 值 | 含义 |
|---|---|---|
| `KnowledgeFresh` | `"fresh"` | 所有页面都对得上工作树 |
| `KnowledgeStale` | `"stale"` | 至少一页 affected 或 unverifiable |
| `KnowledgeMissing` | `"missing"` | 无页面层（生成器未配置/未生成） |
| `KnowledgeUnavailable` | `"unavailable"` | 有页面但「新鲜度答不出来」（典型：无 git） |

M7.4 渲染层把这四态映射为页面横幅文案（`freshnessHint` 纯函数，[internal/sitestatic/static.go:171-185](file://internal/sitestatic/static.go#L171-L185)）：`fresh` → 不显示（`ShowFreshness=false`）；`stale` → 标题「知识已过期」+ 命令 `devsys knowledge refresh`；`missing` → 标题「知识页面层缺失」+ 命令 `devsys knowledge refresh --full`；`unavailable` → 标题「知识新鲜度不可判」+ 留空命令。判定与渲染共用一份纯函数，模板不直接分支 `Status` 字符串。

## 渲染派生（不在模型内）

M7.2/M7.3/M7.4 在 `internal/sitestatic` 与 `internal/cli/workspace.go` 内派生了若干字段与纯函数，用于 HTML 渲染与 `workspace build --json` 信封。这些值**不进** `view.Model`，也不出现在 `data/model.json` 里——`ModelJSON` 输出与 `Build` 落盘的 `data/model.json` 逐字节一致。

### `siteData` 的派生字段（[internal/sitestatic/static.go:131-162](file://internal/sitestatic/static.go#L131-L162)）

`siteData` 是 `Build` 喂给 `html/template` 的结构体，模板里**不**做状态串分支（"the model is fact; this is rendering"）。它内嵌 `*view.Model` 之外再带三类派生量：

| 字段 | JSON key | 必有？ | 来源 |
|---|---|---|---|
| `M` | — | 是 | 嵌入 `*view.Model`，模板侧通过 `m.X` 访问 |
| `Title` / `Active` | — | 是 | `Build` 由每页配置写入（驱动 `<title>` 与导航高亮） |
| `Section` / `Sources` | — | 是 | 当前页 `Provenance.Sources`（目录以 `/` 结尾） |
| `GeneratedAt` | — | 是 | `Build` 时刻 `time.Now().UTC().Format(time.RFC3339)`，**只在 siteData**——`data/model.json` 无此键 |
| `ShowBanner` / `IsPending` / `TrustState` / `TrustNote` / `Pending` | — | 是 | 由 `m.Trust.State` 派生（`ShowBanner = State != TrustOK`；`IsPending = State == TrustPending`） |
| `ShowFreshness` | — | 是 | `freshnessHint(m.Knowledge)` 返回首字段；`fresh` 时 `false` |
| `FreshnessTitle` | — | 是 | 同上：stale→「知识已过期」；missing→「知识页面层缺失」；unavailable→「知识新鲜度不可判」 |
| `FreshnessText` | — | 是 | 同上：unavailable 留空；其余两态给出动作引导句 |
| `FreshnessReason` | — | 是 | 直接透传 `m.Knowledge.Reason` |
| `FreshnessCmd` | — | 是 | stale→`devsys knowledge refresh`；missing→`devsys knowledge refresh --full`；unavailable 与 fresh 留空 |
| `ProgressCounts` | — | 是 | `sortedCounts(m.Progress.Counts)`，模板 map 遍历序随机 → Go 内预排序 |
| `RecentRuns` / `RecentDecisions` / `RecentArtifacts` | — | 是 | 三个列表各自 `head5(...)`，固定前 5 条 |
| `WorkflowGroups` | — | 是 | `groupWorkflows(m.Progress.Items)`，按 item-ID 顺序 |
| `PendingApprovals` | — | 是 | `m.Progress.Readiness.Risks` 中 `Kind == next.RiskPendingApproval` 的子集 |

### `freshnessHint` 纯函数（[internal/sitestatic/static.go:171-185](file://internal/sitestatic/static.go#L171-L185)）

```go
func freshnessHint(k view.Knowledge) (show bool, title, text, reason, cmd string)
```

判定复用 `view.Knowledge.Status`（不重读 knowledge 层），按四态开关：

| `k.Status` | `show` | `title` | `text` | `cmd` |
|---|---|---|---|---|
| `KnowledgeFresh` | `false` | `""` | `""` | `""` |
| `KnowledgeStale` | `true` | `知识已过期` | 执行以下命令重新生成受影响页面： | `devsys knowledge refresh` |
| `KnowledgeMissing` | `true` | `知识页面层缺失` | 配置 knowledge_generator 后执行： | `devsys knowledge refresh --full` |
| `KnowledgeUnavailable` | `true` | `知识新鲜度不可判` | `""` | `""` |

「`fresh` 时 `ShowFreshness=false`」意味着模板按该布尔位开关横幅，**整个四态文案在 `siteData` 里都是已经判定好的字符串**——模板不再触碰 `m.Knowledge.Status`。

CLI 文本脸在 M7.4 同样走 `freshnessHint`（CLI 行 JSON 与退出码 0/10/11 **不动**，只追加/改一行文案）。判定与渲染共用同一份纯函数，没有第二份字符串表。

### `ModelJSON` 与 `data/model.json`

```go
// internal/sitestatic/render.go:37-46
func ModelJSON(m *view.Model) ([]byte, error) {
    if m == nil { return nil, fmt.Errorf("sitestatic: nil model") }
    out, err := json.MarshalIndent(m, "", "  ")
    if err != nil { return nil, fmt.Errorf("sitestatic: encode model: %w", err) }
    return append(out, '\n'), nil
}
```

- 输入 `*view.Model`，**只** `json.MarshalIndent`，**不**附 `generated_at`。
- `Build` 落盘 `data/model.json` 调用同一函数；serve 端每请求重新 `Build` 再 `ModelJSON`——两者字节级一致（M7.3 设计契约）。
- 因此「`data/model.json` 不带 `generated_at`」是**字段缺席**而非字段为零；`generated_at` 仅出现在 siteData（HTML 头部）与 `workspace build --json` 信封。

### `workspace build --json` 信封（[internal/cli/workspace.go:83-91](file://internal/cli/workspace.go#L83-L91)）

```go
return json.NewEncoder(stdout).Encode(struct {
    OK          bool          `json:"ok"`
    Out         string        `json:"out"`
    Pages       []string      `json:"pages"`
    GeneratedAt string        `json:"generated_at"`
    Baseline    view.Baseline `json:"baseline"`
    TrustState  string        `json:"trust_state"`
}{OK: true, Out: rel, Pages: pages, GeneratedAt: generatedAt.Format(time.RFC3339), Baseline: model.Baseline, TrustState: model.Trust.State})
```

- `out` 是相对工作区的输出目录字符串（`workspace build --out` 解析结果，`DefaultOutRel = .devsys/dist/site/`）。
- `pages` 是页面相对路径列表，与 `view.Model.Knowledge.Pages` 内容一致（ID/title 形如 `<id>.html`/`index.html`）。
- `generated_at` 与 `siteData.GeneratedAt` **同源**——`Build` 入口一次性取 `generatedAt := b.now()`，HTML 头部与 CLI 信封都来自这一时刻。
- `baseline` 是 `view.Baseline` 整结构（`commit/branch/dirty/changed_files/reason`）；`trust_state` 是字符串 `view.Trust.State` 的值（`ok`/`advisory_unlocked`/`pending_transaction`）。

> 退出码 0/10/11 只属于 `devsys knowledge status`，**不**进 `view.Model.Knowledge.Status`——M7.1 视图层只输出状态字符串，退出码由 CLI 层决定。

## JSON 编码约定汇总

- **`omitempty` 的统一规则**：所有可选字段（`omitempty` 标签）都是「空时省略」，让 `Problems/Risks/Blockers/...` 这种空集合不出现在 JSON 里。
- **slice 预填为空**（`newModel`）：保证「没找到」是 `[]` 不是 `null`——这是确定性 + JSON 信封稳定的基础。
- **目录以 `/` 结尾**：路径字符串最后是 `/` 表示目录，最后是文件名（或不带 `/`）表示文件；这是 `Sources` 字段的语义约定。
- **时间字段**：`Item.CreatedAt/UpdatedAt` 是 `time.Time`（必有）；`Project.CreatedAt/UpdatedAt`、`RecordRef.CreatedAt`、`RunEntry.FinishedAt` 是 `*time.Time`（零值省略）。
- **指针字段**：`Baseline.Dirty`（`*bool`）、`Item.LeaseUntil`（`*time.Time`）、`Item.NextAttemptAt`（`*time.Time`）、`RunEntry.FinishedAt`（`*time.Time`）、`RunEntry.Advanced`（`*bool`）、`RecordRef.CreatedAt`（`*time.Time`）。
- **map 字段**：`Progress.Counts` 是 `map[string]int`，**有 `omitempty`** 不会生效（Go 的 json 包对 map 不判 nil → 总是输出）——但 `newModel` 预填 `map[string]int{}` 而不是 `nil`，保证输出 `{}` 而非 `null`。
- **枚举值**：所有状态字段是字符串常量，渲染层按字符串分支，不要把字符串哈希为整数。
- **hint 纯渲染：JSON 无 hint 键**：`siteData.ShowFreshness/FreshnessTitle/FreshnessText/FreshnessReason/FreshnessCmd` 是渲染层派生量（`freshnessHint` 纯函数），**不进** `view.Model`；`ModelJSON` 只 marshal 模型自身，`data/model.json` 与 `Build` 落盘字节级一致——JSON 信封里也找不到 `show_freshness/freshness_*` 这类键。

## 与其他卡的关系

- [架构设计](架构设计.md) — 装配流程 + `Build` 主入口 + 各节装配子流程 + 依赖方向
- [概述](概述.md) — 模块定位与职责边界 + 与执行工作区 `devsys worktree` 的区别
- [读取纪律与信任语义](读取纪律与信任语义.md) — 三态 `Trust.State` 的语义差与「不可信时不渲染业务事实」的边界
- [特殊配置与命令](特殊配置与命令.md) — `--json` 是给 M7.2/M7.3 渲染层的稳定信封
