---
status: stable
type: module
dimension: coding_conventions
triggers:
  - 编码规范
  - capList
  - 先定序后截断
  - sortedUnique
  - 路径去重
  - 目录以 / 结尾
  - non-fatal problems
  - 非致命问题
  - problems 收集
  - storage.ErrNotInitialized
  - 只有缺 project.yaml 算错误
  - Options.Now 注入
  - 不直接 time.Now
  - time.Now 注入而非直接调用
  - 测试时钟
  - fixedClock
  - testNow
  - omitempty 约定
  - JSON 契约 omitempty
  - 指纹断言
  - fingerprint 断言
  - 双 marshal 断言
  - 只读副本断言
  - TestBuildWritesNothing
  - TestBuildIsDeterministic
  - TestBuildOnAReadOnlyCopy
  - freshnessHint
  - 模板零分叉
  - map 预排序
  - short12
  - ftime
  - dirtyStr
  - siteData 派生
  - sortedCounts
  - groupWorkflows
  - default_policy
  - config.yaml default_policy
  - policyFacts
  - defaultPolicy
  - policyOf
  - PolicyIDs
  - PolicySource
  - QualityBlocks
  - 质量门
  - 与 claim 同源
  - workflow.Load
  - plain parse
  - 不写 LKG
  - workloom
  - devsys 兼容别名
description: M7.4 视图层（internal/sitestatic + render.go + internal/cli）编码规范七条：(1) capList 先定序后截断、(2) sortedUnique 路径去重、(3) 非致命问题进 m.Problems、(4) Options.Now 注入而非直接 time.Now、(5) JSON 契约 omitempty 统一规则、(6) 测试用「指纹/双 marshal/只读副本」断言、(7) 渲染派生放 Go、模板只做开关（freshnessHint + siteData + 模板零分叉 + map 预排序 + short12/ftime/dirtyStr）+ policyFacts/defaultPolicy/policyOf 只读路径：workflow.Load 明文读不调 workflow.Resolve；本轮（ca58d27→19b9149，v0.1.9 发布链 + 主命令改名 workloom + Git 可选能力）：规范条文未变，示例命令与内部输出串同步改为 workloom，CLI 路由锚点随 internal/cli/cli.go 增长下移（帮助文本 cli.go:82 不变、case workspace → cli.go:313-314）。
source_commit: 19b9149
generated: true
generator: repowiki-gen
---

# 视图层 · 编码规范

视图层的代码风格围绕「只读聚合层 + 确定性 + 错误降级 + 渲染派生下放」四件事收敛。本卡梳理七条规范，每条都标注两处以上源码行号依据，便于在 review 时快速对照。

## 1. 列表先定序后截断（`capList` + 内联切片）

**规范**：任何列表节（`Records.Decisions/Findings/Artifacts`、`Knowledge.Pages`，及 `Runs.Entries` 的内联截断）必须**先按文档约定排序**（时间倒序再 ID / 路径字典序 / ID 字典序），再用 `capList[T any]` 或等价的 `list[:limit]` 截断；截断后置 `Truncated=true`。

**依据**：
- `capList`（[internal/view/build.go:844-849](file://internal/view/build.go#L844-L849)）：`if len(list) <= limit { return list, false }; return list[:limit], true`——只切末尾，不改顺序，不重排。
- `Records` 三列表（[internal/view/build.go:566-567, 591-592, 619-620](file://internal/view/build.go#L566-L567,L591-L592,L619-L620)）：`decisions, capped := capList(decisions, b.limit); truncated = truncated || capped`；`findings` 与 `artifacts` 同形。
- `Runs.Entries`（[internal/view/build.go:448-450](file://internal/view/build.go#L448-L450)）：先 `sort.Slice` 按 `StartedAt` 倒序再 `ID` 升序，再 `runs = runs[:b.limit]; r.Truncated = true`（与 `capList` 等价，但内联）。

**反模式**：截断后再排序（结果与 limit 无关，但顺序与 `Truncated` 关系错乱）；用 `map[string]T` 当列表（编码顺序由 Go runtime 决定，不稳定）。

## 2. 路径与 sources 去重排序（`sortedUnique`）

**规范**：所有 `Sources`（顶层 + 各节 `Provenance.Sources`）必须过 `sortedUnique`：去空、去重、`sort.Strings` 后输出。`noteRel/noteRelDir` 入口就丢弃空字符串。

**依据**：
- `sortedUnique`（[internal/view/collect.go:127-139](file://internal/view/collect.go#L127-L139)）：`seen := make(map[string]bool, len(paths))` + `sort.Strings(out)`。
- `collectSources`（[internal/view/build.go:823-832](file://internal/view/build.go#L823-L832)）：五节 `Sources` 拼成 `all`，再 `sortedUnique(all)`。
- 各节装配收尾的 `pr.Sources = b.rd.since(mark)`（[build.go:301](file://internal/view/build.go#L301)、[build.go:454](file://internal/view/build.go#L454)、[build.go:630](file://internal/view/build.go#L630)、[build.go:736](file://internal/view/build.go#L736)）走 `reader.since` → `sortedUnique`（[collect.go:86-88](file://internal/view/collect.go#L86-L88)）。
- 目录以 `/` 结尾（[collect.go:103-105](file://internal/view/collect.go#L103-L105)）：`noteDevsysDir` 强制 `rel + "/"`；`sortedUnique` 不去后缀，目录与同名文件可共存。

## 3. 非致命问题进 `m.Problems`，只有缺 `project.yaml` 整体报错

**规范**：单文件损坏（YAML 解码失败、git 读失败、知识状态异常、工作项坏）→ 走 `b.problem(err)` / `b.problemf(format, args...)` 追加到 `m.Problems`，并在对应节标 `Degraded=true` + `Reason=...`。**只有**缺 `.devsys/project.yaml`（即 `storage.ErrNotInitialized`）让 `Build` 整体报错；其余失败都降级。

**依据**：
- `b.problem/problemf`（[internal/view/collect.go:142-152](file://internal/view/collect.go#L142-L152)）：`b.problems = append(b.problems, err.Error())`。
- `project()` 单文件失败（[internal/view/build.go:226-240](file://internal/view/build.go#L226-L240)）：YAML 失败 `b.problem(err)` + `p.Degraded=true` + `p.Reason=err.Error()`，但装配继续。
- `progress()` 单工作项失败（[internal/view/build.go:261-265](file://internal/view/build.go#L261-L265)）：`b.problem(err)` + `failed = append(failed, err.Error())`，其他工作项照常解码；最后 `pr.Degraded = true; pr.Reason = strings.Join(failed, "; ")`（[build.go:296-299](file://internal/view/build.go#L296-L299)）。
- `Build` 入口的硬错误只有 `storage.ErrNotInitialized`（[build.go:33, 42-45](file://internal/view/build.go#L33-L45)）：`storage.Open` 失败即返回 `err`。
- `reconcile.Doctor` 失败时**回退** `storage.Diagnose`（[build.go:51-66](file://internal/view/build.go#L51-L66)）——Doctor 为生成修复提议会解码全部工作项，一个坏文件曾让整次聚合失败，改为回退到不看工作项的诊断并把限制写进 `problems`，其余节照常渲染（该修复的原始依据见 git 历史中的 docs/开发记录.md）。

## 4. `Options.Now` 注入而非直接 `time.Now()`

**规范**：`Build` 内的时钟**不**直接调 `time.Now()`；用 `Options{Now: func() time.Time}` 注入；零值时再 fallback 到 `time.Now`。`Now` 只喂 `next.Evaluate`（就绪判定租约/审批过期），**不**进入 `view.Model` JSON。

**依据**：
- `Build` 入口的注入（[internal/view/build.go:34-36](file://internal/view/build.go#L34-L36)）：`nowFn := opts.Now; if nowFn == nil { nowFn = time.Now }`。
- `Options.Now` 字段定义（[internal/view/view.go:54-60](file://internal/view/view.go#L54-L60)）：注释「It is never written into the model」。
- `nowFn()` 的调用点：`pendingReadiness(nowFn(), ...)`（[build.go:78](file://internal/view/build.go#L78)、[build.go:96](file://internal/view/build.go#L96)）；`b.now = nowFn()`（[build.go:87](file://internal/view/build.go#L87)）；`next.Evaluate(... Now: b.now ...)`（[build.go:277](file://internal/view/build.go#L277)）。
- 测试固定时钟（[internal/view/view_test.go:30-32](file://internal/view/view_test.go#L30-L32)）：`var testNow = time.Date(2026, 9, 19, 8, 0, 0, 0, time.UTC); func fixedClock() time.Time { return testNow }`。
- `build(t, root)` 测试 helper（[view_test.go:181-188](file://internal/view/view_test.go#L181-L188)）：固定传 `Options{Now: fixedClock}`。

## 5. JSON 契约的 `omitempty` 统一规则

**规范**：视图层 JSON 字段的 `omitempty` 标签遵循「**空值省略**」原则——`""`/`nil`/`0`/`false`/空 slice/空 map 都省略。**slice 字段用 `newModel` 预填 `[]T{}`** 保证「找到 0 项」编码为 `[]` 而非字段消失。指针字段（`*bool`/`*time.Time`）用于「零值 vs 未设置」的区分。

**依据**：
- `Model` 顶层（[view.go:62-80](file://internal/view/view.go#L62-L80)）：`Problems []string \`json:"problems,omitempty"\``，其余字段不带 `omitempty`（必有字段）。
- `Baseline.Dirty *bool`（[view.go:95](file://internal/view/view.go#L95)）：`*bool` 让 `nil` 与 `false` 区分——`Available=false` 时**留 nil**（不假装 false）。
- `Item.LeaseUntil *time.Time` / `NextAttemptAt *time.Time`（[view.go:158-159](file://internal/view/view.go#L158-L159)）：未设值时省略。
- `RunEntry.FinishedAt *time.Time` / `Advanced *bool`（[view.go:190-191](file://internal/view/view.go#L190-L191)）：同上。
- `newModel` 预填（[build.go:117-140](file://internal/view/build.go#L117-L140)）：`Milestones: []domain.Milestone{}`、`Risks: []string{}`、`Items: []Item{}`、`Pages: []PageEntry{}`、`Affected: []string{}`、`Pending: []string{}`、`Entries: []RunEntry{}`。
- `itemFrom` 中 `Dependencies` 空时显式置 `nil`（[build.go:869-871](file://internal/view/build.go#L869-L871)）：让 JSON 里 `dependencies` 字段消失而不是出现 `[]`。
- `runEntry` 中 `Errors` 空时置 `nil`（[build.go:480-482](file://internal/view/build.go#L480-L482)）：同上。


## 6. 测试用「指纹 / 双 marshal / 只读副本」三种断言

**规范**：视图层测试用三类断言保证「零写入 + 确定性 + 只读契约」：
- **指纹断言**（`fingerprint` + `diffFingerprints`）：整树文件 sha256 + 大小 + mtime 三元组比对前后，证明装配不改任何文件。
- **双 marshal 断言**（`json.Marshal` × 2 + `reflect.DeepEqual`）：同一状态两次装配字节级相同。
- **只读副本断言**（`copyTree` + `os.Chmod 0o444`）：整树只读还能装配完整。

**依据**：
- `TestBuildWritesNothing`（[view_test.go:308-318](file://internal/view/view_test.go#L308-L318)）+ `fingerprint`（[view_test.go:486-520](file://internal/view/view_test.go#L486-L520)）+ `diffFingerprints`（[view_test.go:522-537](file://internal/view/view_test.go#L522-L537)）。
- `TestBuildIsDeterministic`（[view_test.go:293-306](file://internal/view/view_test.go#L293-L306)）：`first, _ := json.Marshal(build(t, root)); second, _ := json.Marshal(build(t, root)); reflect.DeepEqual(first, second)`。
- `TestBuildOnAReadOnlyCopy`（[view_test.go:349-378](file://internal/view/view_test.go#L349-L378)）+ `copyTree`（[view_test.go:539-569](file://internal/view/view_test.go#L539-L569)）：复制 + `filepath.WalkDir` + `os.Chmod(path, 0o444)` + `t.Cleanup` 还原。
- `TestBuildWithoutLockFileStaysReadable`（[view_test.go:320-347](file://internal/view/view_test.go#L320-L347)）：fixture 无 lock 文件 → `build` 后 `os.Stat(lock)` 必须 `ErrNotExist` + `fingerprint(before) == fingerprint(after)`。
- `TestBuildPendingTransactionRendersNoBusinessFacts`（[view_test.go:380-412](file://internal/view/view_test.go#L380-L412)）：手工建 `.devsys/local/txn/txn-1/` → `m.Trust.State == TrustPending` + 业务节空 + `Readiness.Verdict == FAIL` + `m.Baseline.Available == true`（基线照常）。
- `TestBuildDegradesOneSectionOnly`（[view_test.go:414-431](file://internal/view/view_test.go#L414-L431)）：写一个坏 YAML 文件 → 仅 `Progress.Degraded == true` + `m.Problems` 含坏路径 + 其他节正常。

## 7. 渲染派生放 Go、模板只做开关（M7.4）

**规范**：站点层（`internal/sitestatic` + `render.go`）的所有「派生量」（freshness 状态、map 顺序、表头截断、commit 缩写、零值渲染、dirty 标记）都必须在 Go 侧算成 `siteData` 的扁平字段；模板只保留 `{{if .ShowFreshness}}` 这类零分叉开关，**不**在模板里做 `switch`/嵌套 `if`/map 迭代排序。展示型辅助函数（`short12` / `ftime` / `dirtyStr`）走 `template.FuncMap` 注册，**不**写成方法。

**依据**：
- `freshnessHint` 纯函数（[internal/sitestatic/static.go:171-185](file://internal/sitestatic/static.go#L171-L185)）：把 `Knowledge.Status` 的 `stale/missing/unavailable/fresh` 四态翻译成 `show/title/text/reason/cmd`，`default` 分支返回 `false, "", "", "", ""`，无副作用、可独立测。
- `siteData` 五派生字段（[internal/sitestatic/static.go:147-154](file://internal/sitestatic/static.go#L147-L154)）：`ShowFreshness`/`FreshnessTitle`/`FreshnessText`/`FreshnessReason`/`FreshnessCmd` 全部在 `newSiteData` 里一次性装好（[static.go:194, 203-207](file://internal/sitestatic/static.go#L194-L207)），模板只 `{{if .ShowFreshness}}<section>…{{.FreshnessCmd}}…</section>{{end}}`。
- `sortedCounts`（[internal/sitestatic/static.go:97-104](file://internal/sitestatic/static.go#L97-L104)）：`map[string]int` → `sort.Slice` 按 K 升序 → `[]kv`；`groupWorkflows`（[static.go:112-128](file://internal/sitestatic/static.go#L112-L128)）按「model 已有顺序」分组而不重排，保证模板迭代稳定。
- `head5`（[internal/sitestatic/static.go:164-169](file://internal/sitestatic/static.go#L164-L169)）：`len(list) > 5 ? list[:5] : list`——只切不排，对应 `RecentRuns/RecentDecisions/RecentArtifacts`。
- `short12`（[internal/sitestatic/static.go:218-226](file://internal/sitestatic/static.go#L218-L226)）/ `ftime`（[static.go:229-245](file://internal/sitestatic/static.go#L229-L245)）/ `dirtyStr`（[static.go:247-255](file://internal/sitestatic/static.go#L247-L255)）：展示函数零外部依赖；`short12` 处理空串→`(none)`、`ftime` 处理 `time.Time` / `*time.Time` 零值→`—`、`dirtyStr` 处理 `*bool == nil`→`unknown`。
- `siteFuncs` 注册（[internal/sitestatic/static.go:257-262](file://internal/sitestatic/static.go#L257-L262)）：`FuncMap{"short12": short12, ...}`，`html/template` 全自动转义。
- **本批（v0.1.9，ca58d27→19b9149）**：七条规范本身未变；只有示例命令与内部输出串随主命令改名同步为 `workloom`（`devsys` 是同一二进制的兼容别名，[cmd/devsys/main.go:1-3](file://cmd/devsys/main.go#L1-L3)），以及 CLI 路由锚点随 `internal/cli/cli.go` 增长 3 行而下移（`case "workspace"` → [cli.go:313-314](file://internal/cli/cli.go#L313-L314)；帮助文本仍在 [cli.go:82](file://internal/cli/cli.go#L82)）。

**反模式**：在模板里 `{{if eq .Knowledge.Status "stale"}}…{{else if eq … "missing"}}…`；把派生计算塞进模板的 `with/range` 嵌套；`map` 直接 `range`（Go map 顺序不稳定）；展示逻辑写方法挂在结构体上导致 `siteData` 必须有方法集、不能走纯值传递。

## 与其他卡的关系

- [概述](概述.md) — 模块定位与「只读聚合层」承诺
- [架构设计](架构设计.md) — `Build`/`builder`/`reader` 装配流程
- [视图数据模型](视图数据模型.md) — 字段与 JSON 契约
- [读取纪律与信任语义](读取纪律与信任语义.md) — 「零写入契约 + 信任态 + 确定性」的展开
