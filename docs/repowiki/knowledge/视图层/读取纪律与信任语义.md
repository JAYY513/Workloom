---
status: stable
type: module
dimension: read_discipline
triggers:
  - 读取纪律
  - 零写入
  - 只读契约
  - read-only
  - storage.Inspect
  - storage.InspectUnlocked
  - ErrLockUnavailable
  - 不建锁
  - 不恢复
  - 不修断尾
  - 不写 .cache
  - 不探测生成器
  - 整树 0444
  - 文件指纹
  - fingerprint
  - TestBuildWritesNothing
  - TestBuildOnAReadOnlyCopy
  - TestBuildWithoutLockFileStaysReadable
  - 未创建 local/lock
  - 信任态
  - trust.state
  - advisory_unlocked
  - pending_transaction
  - 不可信时不渲染业务事实
  - 方案 §15.4
  - emptyBusiness
  - pendingReadiness
  - knowledge missing
  - knowledge unavailable
  - 方案 §12.6
  - 方案 §12.5
  - 确定性
  - 无墙钟字段
  - 双跑逐字节一致
  - 视图与判决
  - freshnessHint
  - 新鲜度横幅
  - hint
  - workloom knowledge refresh
  - workloom doctor
  - workflow.Load
  - workflow.Resolve
  - 不写 LKG
  - 不刷 .cache
  - LKG 缓存
  - lastKnownGood
  - plain parse
  - default_policy
  - config.yaml default_policy
  - policyFacts
  - policyOf
  - defaultPolicy
  - PolicyIDs
  - PolicySource
  - QualityBlocks
  - 质量门
  - quality_blocked
  - 与 claim 同源
  - 与 workloom next 同源
  - 视图与就绪判定同源
  - 无项目默认策略报错
  - workloom
  - devsys 兼容别名
description: M7.1 视图层的零写入契约及其测试验证（TestBuildWritesNothing 文件指纹 / TestBuildOnAReadOnlyCopy 整树 0444 / TestBuildWithoutLockFileStaysReadable 不创建 .devsys/local/lock）+ advisory_unlocked 与 pending_transaction 的差别（快照 vs 判决，方案 §15.4 不可信时不渲染业务事实）+ 知识 missing（§12.6 降级） vs unavailable（§12.5 cannot tell）的区分 + 确定性（模型无墙钟字段、Options.Now 注入、列表先定序后截断、sortedUnique 路径去重、双跑 marshal 逐字节一致）+ M7.4 新鲜度提示（freshnessHint 派生 ShowFreshness/FreshnessTitle/Text/Reason/Cmd，fresh 不显示，stale→refresh、missing→--full、unavailable→空命令，CLI 文本脸只加行 JSON/退出码不动，判定复用 knowledge.Evaluate）+ 政策只读：workflow.Load 明文读且不刷 .devsys/.cache/workflows/（解析后供 next.Evaluate 走 QualityBlocks 复用 claim 判定，policyFacts{ids,parsed,problems} + defaultPolicy + policyOf 给 next.PolicySource，default_policy 指向缺失策略文件时入 PolicyProblems）；本轮（ca58d27→19b9149，v0.1.9 发布链 + 主命令改名 workloom + Git 可选能力）：pending 态文案为 HEAD 实际字符串（修复项为「workloom recover --actor operator --reason "recover interrupted state"」，CLI 提示行为 run workloom doctor to inspect before workloom recover）；零写入契约与三态信任语义不变。
source_commit: 19b9149
generated: true
generator: repowiki-gen
---

# 视图层 · 读取纪律与信任语义

视图层的**第一性原则**是「**只读**」：任何装配都不应留下磁盘痕迹、任何修复动作都不应属于本层。本卡梳理「零写入契约」的精确含义、测试层的三种验证手段、`advisory_unlocked` 与 `pending_transaction` 的语义差、知识节 `missing` 与 `unavailable` 的区别、以及「确定性」在工程上的兑现方式。

## 零写入契约

**包级承诺**（[internal/view/view.go:1-15](file://internal/view/view.go#L1-L15)）：

> The package never writes. Reads go through storage.Inspect — a shared lock on an existing lock file, never created — with storage.InspectUnlocked as the fallback for copies where no writer ever ran (a read-only mount of a fresh checkout). It never recovers transactions, never repairs torn tails, never touches `.devsys/.cache/` and never probes a generator (a probe runs a process).

具体落到代码：
| 不做的动作 | 替代做法 | 位置 |
|---|---|---|
| 不创建 `.devsys/local/lock` | `inspectSnapshot` 先 `st.Inspect`，缺锁文件时退 `st.InspectUnlocked`，**永不**调 `st.Lock` | [collect.go:23-29](file://internal/view/collect.go#L23-L29) |
| 不写 `.devsys/.cache/` | 装配只走 `Inspect`/`InspectUnlocked` + `storage.Reader.Read`；从不调 `storage.Read`（会建锁并写缓存） | 装配全文 |
| 不恢复事务 | `pendingReadiness` 走 `next.Evaluate` 报 FAIL + `workloom recover` 修复项；从不调 `reconcile.Recover` / `storage.RecoverPending` | [build.go:159-165](file://internal/view/build.go#L159-L165) |
| 不修复断尾事件流 | `streamTorn` 仅查 `storage.InspectJSONL` 的 `Complete` 标志位；从不调 `storage.RepairTornTail` | [build.go:489-499](file://internal/view/build.go#L489-L499) |
| 不探测生成器 | 知识层只读 `AdapterByName + Import` 拿适配器**已落盘**的 state；从不调 `Adapter.Probe`（Probe 起进程） | [build.go:742-759](file://internal/view/build.go#L742-L759) |
| 不写 `.devsys/workspaces/` | 装配路径不引用 `internal/workspace`（那是执行工作区） | import 列表 |
| 不写 LKG 缓存（`.devsys/.cache/workflows/`） | 政策读取走 `workflow.Load`（明文，不锁、不写缓存），装配层把每条 id + 已解析的 `*workflow.Policy` 装进 `policyFacts{ids, parsed, problems}`；`defaultPolicy()` 读 `.devsys/config.yaml` 的 `default_policy`；`facts.policyOf(default)` 返回 `next.PolicySource` 给 `next.QualityBlocks` 复用 `claim` 的质量门判定。**绝不**调 `workflow.Resolve`（会写 LKG 快照） | [build.go:321-400](file://internal/view/build.go#L321-L400) |
| `workspace serve` 运行期**零写入** | 每请求现装 `view.Build`，不建快照、不建锁、无缓存；handler 不持可变状态，可并发 | [workspace_serve.go:30-37](file://internal/cli/workspace_serve.go#L30-L37) |


## 三种「零写入」验证手段

测试层用三种独立手段验证「不写」：

### 1. 文件指纹前后比对（`TestBuildWritesNothing`）

`internal/view/view_test.go:308-318` + `fingerprint`（[view_test.go:486-520](file://internal/view/view_test.go#L486-L520)）：

```text
before := fingerprint(root)        # sha256 整文件 + 大小 + mtime
build(root)
after  := fingerprint(root)
要求 before == after（含 lock 文件）
```

`fingerprint` 遍历 root 下每个文件（除 `.git/`），按 `size + mtime + sha256` 三元组记录；任何被改动/新增/删除的文件都会触发 `diffFingerprints` 报错。

**保证**：`Build` 不修改**任何**文件，包括 `.devsys/local/lock`、`.devsys/.cache/*`、运行事件流尾、知识层 state、git 索引。

### 2. 整树只读副本（`TestBuildOnAReadOnlyCopy`）

`internal/view/view_test.go:349-378`：

```text
source := fixture(t)                 # 含 lock 文件的完整 fixture
copyTree(t, source, copyRoot)        # 复制到新目录
filepath.WalkDir(copyRoot, ...)      # 逐文件 os.Chmod(path, 0o444)
t.Cleanup(...)                        # 测试结束 chmod 回 0o644
m := build(t, copyRoot)
要求 m.Progress.Items == 2 && m.Runs.Entries == 1 && m.Knowledge.Status == KnowledgeFresh
```

**保证**：即使整个 `.devsys/` 都是 `0o444`（含锁文件），视图仍能装配完整——`storage.Inspect` 不需要写权限，`InspectUnlocked` 也不需要；任何「悄悄写」都会失败。

### 3. 无锁文件时不创建锁（`TestBuildWithoutLockFileStaysReadable`）

`internal/view/view_test.go:320-347`：

```text
root := projectOnly(t)               # 只 init，没写过状态 → 无 lock 文件
lock := .devsys/local/lock
os.Stat(lock) → 必须 ErrNotExist
before := fingerprint(root)
m := build(t, root)
要求 m.Trust.State == TrustAdvisory && !m.Trust.InspectionOK
要求 m.Trust.Note != ""
要求 m.Project.ID != ""              # 仍渲染业务事实
要求 os.Stat(lock) → 仍 ErrNotExist   # 关键：不创建锁
要求 fingerprint(before) == fingerprint(after)
```

**保证**：缺锁文件时视图不会为了「拿到一个锁」而创建它——`inspectSnapshot` 把 `ErrLockUnavailable` 翻译成「降级到 `InspectUnlocked` + advisory=true」而不是「先建锁再读」。

M7.1 验收期实跑记录：真实 `.devsys` 快照（`docs/examples/m1-devsys/`）两次 `--json` 同 3288 字节；0444 副本上同一模型（除 git 基线）且未创建 `local/lock`（原始依据见 git 历史中的 docs/开发记录.md）。

## 信任态：advisory vs pending

视图把「这一份快照能信多少」分成**三种状态**（[view.go:32-42](file://internal/view/view.go#L32-L42)）：

| 常量 | JSON 值 | 触发 | 业务事实？ | 与 `workloom next` 的差别 |
|---|---|---|---|---|
| `TrustOK` | `"ok"` | `storage.Inspect` 拿到共享锁 + `reconcile.Doctor` 无 pending | 渲染 | 同 `next`：PASS / CONCERNS |
| `TrustAdvisory` | `"advisory_unlocked"` | 锁文件不存在（只读副本/从未写过状态）→ `InspectUnlocked` | **仍渲染** | `next` 更严格：无锁文件即不渲染；视图是快照不是判决（M7.1 偏差 ①） |
| `TrustPending` | `"pending_transaction"` | `reconcile.Doctor` 报 pending **或** `inspectSnapshot` 撞 `*storage.PendingTxnError` | **不渲染**（`emptyBusiness`） | 同 `next`：FAIL + `workloom recover` |

### `advisory_unlocked` 的语义

> 「这一刻的 `.devsys/` 没有写锁保护，所以你看的内容可能正在被改——但版本是确定的（写者必须先 commit）……」

具体行为（[build.go:94-108](file://internal/view/build.go#L94-L108)）：
- `m.Trust.State = "advisory_unlocked"`
- `m.Trust.Note = original + " reads were lock-free: no project lock exists (no writer ever ran), so the snapshot is advisory"`
- `m.Trust.InspectionOK = false`（与 Doctor 报告独立）
- 五节都照常渲染（项目、进度、运行、记录、知识）；baseline 仍读。
- **建议**：渲染层在 `advisory` 时**提示**用户「此视图无锁保护，谨慎对待」，但**不**阻塞渲染。**渲染落点**：静态站与 serve 全站的 `{{define "banner"}}` 段（[static.go:309](file://internal/sitestatic/static.go#L309)）输出 `trust: advisory_unlocked — <note>` 横幅；CLI 文本脸在 `trust:` 行后直接打印同款 note（[workspace.go:217-219](file://internal/cli/workspace.go#L217-L219)），JSON 模型不变。

### `pending_transaction` 的语义

> 「存在待恢复事务，状态文件可能半应用——版本号尚未提交，业务事实不可信……」

具体行为（[build.go:75-82](file://internal/view/build.go#L75-L82) + [build.go:145-154](file://internal/view/build.go#L145-L154)）：
- `m.Trust.State = "pending_transaction"`
- `m.Trust.Pending = pendingIDs(...)`：待恢复事务 ID 列表（来自 Doctor 或 `PendingTxnError.IDs`）。
- `emptyBusiness(m)`：
  - `m.Project.Degraded = true`，`m.Project.Reason = pendingReason`（[build.go:28](file://internal/view/build.go#L28)，文案 `"pending transactions: run `workloom recover` before trusting business state (方案 §15.4)"`）
  - `m.Progress.Degraded = true`，`m.Progress.Reason = pendingReason`
  - `m.Progress.Items = []`，`m.Runs.Entries = []`，`m.Records.Decisions/Findings/Artifacts = []`
  - `m.Knowledge.Status = KnowledgeUnavailable`，`m.Knowledge.Degraded = true`，`m.Knowledge.Reason = pendingReason`
- `m.Progress.Readiness = pendingReadiness(nowFn(), ids)`：走 `next.Evaluate` 产出 `Verdict=FAIL` + `Fixes[0].Command = "workloom recover --actor operator --reason \"recover interrupted state\""`（`recoverCommand` 常量，[build.go:25](file://internal/view/build.go#L25)）。
- **基线仍给**：`m.Baseline` 与 git 状态照常报告——git 与 `.devsys/` 状态独立。
- **建议**：渲染层在 `pending` 时**只渲染** `Trust/Sources/Baseline` 与 `Progress.Readiness.Fixes`，**不**渲染任何业务事实；这是方案 §15.4 的硬约束。**渲染落点**：静态站与 serve 全站的 `banner` 段（[static.go:309](file://internal/sitestatic/static.go#L309)）追加 `存在未完成的事务…先执行 workloom recover…可用 workloom doctor 先查看` 提示行；CLI 文本脸在 `trust:` 行后打印 `pending: <ids>`，再追加 `hint: run workloom doctor to inspect before workloom recover` 提示行（[workspace.go:220-222](file://internal/cli/workspace.go#L220-L222)）。JSON 模型字段不变。

### `workloom next` 与 `view.Model.Trust` 的差别（关键）

| 维度 | `workloom next` | `view.Build` |
|---|---|---|
| `next.Report.Verdict` 在 advisory 下 | **FAIL**（无锁文件即不渲染） | `CONCERNS` 或 `PASS`（业务节照常渲染） |
| `next.Report.Verdict` 在 pending 下 | **FAIL** + recover 修复项 | **FAIL** + recover 修复项（经 `pendingReadiness`） |
| 任务列表 | 只列「可调度的」 | 全部列出（视图不调度） |
| 立场 | 判决（决定下一步做什么） | 快照（呈现当前事实） |

`view.Progress.Readiness` 在 advisory 下与 `workloom next` 的 verdict **可能不同**——这是 M7.1 验收时接受的偏差（原始依据见 git 历史中的 docs/开发记录.md），原因：M7.1 验收要求「只读副本也能出完整视图」。

## 新鲜度提示（M7.4）

视图在 M7.4 加入「数据过期时明确提示」——知识层的判定仍走 `knowledge.Evaluate`，视图**复用**其结论并在渲染侧派生五字段横幅，**不动**模型 schema、不改 CLI 退出码、不改 `--json` 的字节内容。

### 派生字段（[static.go:150-154](file://internal/sitestatic/static.go#L150-L154)）

`siteData` 从 `view.Knowledge` 派生五个渲染字段：

| 字段 | 类型 | 作用 |
|---|---|---|
| `ShowFreshness` | `bool` | 是否显示横幅（fresh 时为 `false`，模板整段被剪掉） |
| `FreshnessTitle` | `string` | 横幅粗体标题（如「知识已过期」） |
| `FreshnessText` | `string` | 引导文案（紧跟 `FreshnessCmd` 之前） |
| `FreshnessReason` | `string` | 来自 `k.Reason` 的事实陈述（如 `n page(s) affected by m changed file(s)`） |
| `FreshnessCmd` | `string` | 修复命令；unavailable 留空（刷新指针无意义） |

派生函数 `freshnessHint(k view.Knowledge)`（[static.go:171-185](file://internal/sitestatic/static.go#L171-L185)）按状态返回 `show/title/text/reason/cmd` 五元组——fresh 走 `default` 返回空，模板上的 `{{if .ShowFreshness}}` 自然短路。

### 状态映射

| `Knowledge.Status` | `ShowFreshness` | 标题 | 命令 |
|---|---|---|---|
| `KnowledgeFresh` | `false` | — | — |
| `KnowledgeStale` | `true` | 知识已过期 | `workloom knowledge refresh` |
| `KnowledgeMissing` | `true` | 知识页面层缺失 | `workloom knowledge refresh --full` |
| `KnowledgeUnavailable` | `true` | 知识新鲜度不可判 | （空——`Cannot tell` 无可修复命令） |

判定本身复用 `knowledge.Evaluate`（[build.go:680-733](file://internal/view/build.go#L680-L733)）：`k.Affected` / `k.Unverifiable` / `k.ChangedFiles` 决定 `KnowledgeStale`，git 故障走 `KnowledgeUnavailable`，无页面层走 `KnowledgeMissing`——视图不重复实现，只消费结论。

### 渲染落点

两处用到同一组派生字段：

1. **全站第二横幅**：[static.go:300](file://internal/sitestatic/static.go#L300) 的 `{{define "freshness"}}` 段紧跟在 trust 横幅（[static.go:309](file://internal/sitestatic/static.go#L309)）之后，由 `base.html` 的 `{{template "freshness" .}}`（[static.go:297](file://internal/sitestatic/static.go#L297)）在每个页面的 `<main>` 之前注入；显示条件 `{{if .ShowFreshness}}`。
2. **知识页内联提示**：[static.go:402](file://internal/sitestatic/static.go#L402) 在知识页 `<h1>知识新鲜度</h1>` 段下再输出一段 `FreshnessText + FreshnessCmd`——配合页内的 `Affected` / `Unverifiable` 列表，把「为什么要刷新、刷新哪几页」在同一页讲完。

### CLI 文本脸（[workspace.go:196-223](file://internal/cli/workspace.go#L196-L223)）

CLI 走**纯渲染增量**：在已有 `knowledge:` 行后追加 `  hint: ...` 一行（最多一行），按状态分：

| 状态 | hint 行 |
|---|---|
| `KnowledgeMissing` | `hint: configure knowledge_generator, then run workloom knowledge refresh --full` |
| `KnowledgeStale` | `hint: run workloom knowledge refresh to regenerate affected pages` |
| `KnowledgeUnavailable`（非 pending） | `hint: run workloom knowledge status to see the underlying error` |
| `KnowledgeUnavailable`（`TrustPending`） | （静默——`recover` 修复项已在 trust 行给出，避免重复） |

**不动的部分**：JSON 模型字段（`Knowledge.Status` / `Reason` / `Affected` / `Unverifiable` / `ChangedFiles`）、退出码、判定逻辑、workspace build/serve 的零写入契约——hint 是**纯文本**增量，`--json` 消费者看不到任何差异。

### `Affected` / `Unverifiable` 与 `--limit` 的关系

关键不变量：层级结论**不受** `--limit` 影响。`capList` 只作用在 `k.Pages`（[build.go:699-702](file://internal/view/build.go#L699-L702)），`k.Affected` / `k.Unverifiable` 在排序前就已收集完整（[build.go:690-694](file://internal/view/build.go#L690-L694)），`k.Truncated` 仅标记「页表被截断」。这意味着：

- 降低 `--limit` 不会让 `KnowledgeStale` 误判成 `KnowledgeFresh`；
- `Reason` 文案里的 `len(Affected) / len(Unverifiable)` 总是全集；
- 模板上的 `<p class="meta">列表已截断，提高 --limit 查看更多。</p>` 只对 `Pages` 出现，对 `Affected` / `Unverifiable` 不出现。


## 知识节 missing 与 unavailable

`Knowledge.Status` 四态中，`missing` 与 `unavailable` 都意味着「不能给 fresh 答案」，但**触发条件与降级语义不同**：

### `KnowledgeMissing`（方案 §12.6）

```text
报告: report.Pages == 0          # 无页面层
  - 生成器未配置（md.Config.KnowledgeGenerator 空）
  - 或生成器未跑过（state.json 还没生成）
Status: KnowledgeMissing
Reason: "无知识页面层：未安装或未配置生成器（方案 §12.6)" (build.go:677-678)
```

这是**支持性降级**：视图不报错，用户/agent 应该去配/跑一次生成器。

### `KnowledgeUnavailable`（方案 §12.5）

```text
报告: report.Pages > 0            # 有页面层
  - 但 knowledge.Evaluate 失败（典型：git 不可用）
Status: KnowledgeUnavailable
Reason: "freshness unavailable: " + err.Error()
Degraded: true
```

这是**问题答不出来**：「cannot tell」≠ fresh——CI 不能因此放过。

> 区分要点：`missing` 是「层不存在」，`unavailable` 是「层存在但问不出来」；前者是配置问题，后者是环境问题。

`pending_transaction` 状态下，`emptyBusiness` 把 `Knowledge.Status` 设为 `KnowledgeUnavailable`（[build.go:151-153](file://internal/view/build.go#L151-L153)）——这里取 `Unavailable` 而非 `Missing`，因为 `.devsys/knowledge/state.json` 与 `.devsys/knowledge/snapshot.json` 在事务存在时可能半应用，不能说「层不存在」。

## 政策只读：plain parse ≠ LKG 解析器

视图层在「质量门该不该放过 ready 工作项」这件事上必须和 `workloom next`/`workitem claim` 同源——但 `workflow.Resolve` 是**写**路径：成功解析时会刷新 `.devsys/.cache/workflows/<id>.md`（[internal/workflow/cache.go:30](file://internal/workflow/cache.go#L30)、[refreshLastKnownGood](file://internal/workflow/cache.go#L93-108)），反复调用 `view.Build` 会在 cache 目录堆积文件，破坏「零写入」契约。视图层走**只读**路径：

```text
workflow.Load(root)               # 明文读 + Parse（无锁、无写）
  → policyFacts{ids, parsed, problems}
  → defaultPolicy()               # 读 b.md.Config.DefaultPolicy
  → policyOf(default)             # 闭包：返回 next.PolicySource
  → next.QualityBlocks(items, src) # 与 workitem claim 同源判定
```

关键代码点（[internal/view/build.go:321-400](file://internal/view/build.go#L321-L400)）：

| 形状 | 作用 | 行号 |
|---|---|---|
| `policyFacts{ids []string, parsed map[string]*workflow.Policy, problems []string}` | 一次解析，多处消费；`ids` 排序后喂 `next.Input.PolicyIDs`，`parsed[id]` 是该 id 的 `*workflow.Policy`（解析失败时 `nil`，错误已入 `problems`），`problems` 是「解析错 + 引用了不存在的 id + default_policy 指向缺失文件」三合一 | [build.go:321-327](file://internal/view/build.go#L321-L327) |
| `(*builder).policyFacts(items)` | `workflow.Load` → 收集 error-severity issues → 跳过非 `.md` 条目 → 把每个 `id` 与 `res.Policy` 配对进 `parsed` map → 对每个引用了不存在 id 的工作项追加 `work item %s references missing policy %q` → 若 `defaultPolicy()` 指向不存在的文件追加 `config.yaml default_policy %q has no .devsys/workflows/%s.md` | [build.go:354-400](file://internal/view/build.go#L354-L400) |
| `(*builder).defaultPolicy()` | 直接读 `b.md.Config.DefaultPolicy`；`md` 或 `Config` 为 nil 时返回 `""`（项目层无声明） | [build.go:329-336](file://internal/view/build.go#L329-L336) |
| `(policyFacts).policyOf(default string) next.PolicySource` | 闭包：每个 `wi` 优先取 `wi.Workflow.ID`，否则用 `default`；`id == ""` 返回 `("", "", nil)`；否则返回 `(id, "workflows/"+id+".md", f.parsed[id])`——把视图的「只读 policy 表」翻译成 `claim` 用的「策略查找器」 | [build.go:338-352](file://internal/view/build.go#L338-L352) |
| `next.Evaluate(next.Input{... PolicyIDs, DefaultPolicy, QualityBlocks: next.QualityBlocks(items, facts.policyOf(...))})` | 把上面的产物喂给 `next.Evaluate`；`next.QualityBlocks`（[internal/next/evaluate.go:125-156](file://internal/next/evaluate.go#L125-L156)）用同一份 `workflow.ScoreQuality` + `policy.QualityGateBlocks` 与 `workitem claim` 算得同样结果，每个被拒的 ready 工作项成为 `rep.Risks` 中一条 `Kind: RiskQualityBlocked` | [build.go:276-297](file://internal/view/build.go#L276-L297) |

**为什么 `default_policy` 也入 `problems`**：`workflow.Load` 不知道 `config.yaml` 的存在；视图层是唯一把「项目级声明」与「工作流目录实际存在性」对照的位置。若 `default_policy` 指向不存在的工作流，视图把这条进 `PolicyProblems`（`next.Input.PolicyProblems`），`next.Evaluate` 把它转成 `RiskInvalidPolicy`，与「工作项引用了不存在的工作流」走同一信号通道——用户看到的「policy 配置问题」是同一类。

**为什么是 plain parse 而非 LKG 解析器**：`workflow.Load` 仅做 `os.ReadDir` + `os.ReadFile` + `workflow.Parse`（[internal/workflow/load.go:31-66](file://internal/workflow/load.go#L31-L66)）；无锁、不调 `Resolve`、不刷 `.cache/`。代价是视图不享受 LKG 的「解析失败也返回上次好值」保护——这是有意取舍：视图既然是只读快照，「读到的就是当前文件的内容」就是事实，不需要 LKG 来掩盖配置错误；LKG 留给 `workitem claim`/`next` 等需要「必须给出可执行策略」的写路径。

**路径保持（含 v0.1.3 失败可见性批次 #336 与后续版本）**：internal/view/build.go 的 `policyFacts`/`defaultPolicy`/`policyOf` 三件套仍是 plain parse 路径——只读 `.devsys/workflows/*.md`、只读 `config.yaml.default_policy`，装配出 `PolicyIDs` / `DefaultPolicy` / `QualityBlocks` 喂 `next.Evaluate`；与 `workloom next` / `workitem claim` 同源判定质量门，但**不**触发 `.devsys/.cache/workflows/<id>.md` 的 LKG 快照写入（写路径留给 `claim` 与 `next`）。`defaultPolicy` 指向缺失文件、工作项引用不存在的 `workflow.id` 仍按既入 `PolicyProblems` 的语义处理，不引入第二种「解析策略」入口。v0.1.3 起 `progress()` 在同一条只读路径上增传 `HasBlueprint`/`DeadAttempts`/`InFlight`：`attemptSignals` 只读 `runs/*.yaml` 与 `runs/*.jsonl`（数行数，[build.go:501-540](file://internal/view/build.go#L501-L540)），复用 `next.ClassifyAttempts` 分类，不引入任何写入。

## 确定性：双跑逐字节一致

视图承诺「同一状态两次构建 marshal 出来逐字节相同」（[internal/view/view.go:13-14](file://internal/view/view.go#L13-L14)）。这一性质由五个工程约束共同保证：

### 1. 模型无墙钟字段

`time.Time` 只出现在两类位置：
- **领域对象本身的时间**：`Item.CreatedAt/UpdatedAt`、`RunEntry.StartedAt/FinishedAt`、`RecordRef.CreatedAt`——这些来自文件内容，不来自 `Now`。
- **可选指针**：`Baseline.Dirty`、`Item.LeaseUntil`、`Item.NextAttemptAt`、`RunEntry.FinishedAt`、`RunEntry.Advanced`、`RecordRef.CreatedAt`——同上。

`Options.Now`（[view.go:54-60](file://internal/view/view.go#L54-L60)）只喂 `next.Evaluate`，**不**进入 `view.Model`。测试用 `Options{Now: fixedClock}` 控制时钟（[view_test.go:32](file://internal/view/view_test.go#L32)），但即便换时钟，模型不变。

### 2. `newModel` 预填所有 slice

`newModel`（[build.go:117-140](file://internal/view/build.go#L117-L140)）把**每一个** slice 都初始化为空（`[]string{}`、`[]domain.Milestone{}`、`[]RunEntry{}`、`[]RecordRef{}` 等）——「没找到」是 `[]` 不是 `null`。这避免了 JSON 编码时 `omitempty` slice 字段在「找到 0 项」时消失带来的差异。

### 3. 列表先定序后截断

每个列表节在截断前先排序：

| 节 | 排序键 |
|---|---|
| `Progress.Items` | `id` 升序（[build.go:269](file://internal/view/build.go#L269)） |
| `Runs.Entries` | `StartedAt` 倒序再 `ID` 升序（[build.go:441-447](file://internal/view/build.go#L441-L447)） |
| `Records.Decisions` | `CreatedAt` 倒序再 `ID` 升序（[build.go:560-565](file://internal/view/build.go#L560-L565)） |
| `Records.Findings` | `id` 升序（[build.go:590](file://internal/view/build.go#L590)） |
| `Records.Artifacts` | `CreatedAt` 倒序再 `ID` 升序（[build.go:613-618](file://internal/view/build.go#L613-L618)） |
| `Knowledge.Pages` | `knowledge.Scan` 内部排序（按 path） |
| `Trust.Pending` | `pendingIDs` 内 `sort.Strings`（[build.go:834-841](file://internal/view/build.go#L834-L841)） |

`capList[T]`（[build.go:844-849](file://internal/view/build.go#L844-L849)）只切末尾，不改顺序；`Truncated` 标志告诉调用方「后面还有」。

### 4. `sortedUnique` 路径去重

`Sources`（顶层 + 各节 `Provenance.Sources`）都过 `sortedUnique`（[collect.go:127-139](file://internal/view/collect.go#L127-L139)）：去空、去重、`sort.Strings`。这保证：
- 同一路径被多次 `noteDevsys` 只出现一次。
- 多节访问同一文件（如 `.devsys/project.yaml` 同时被 `project` 与 metadata problems 记录）只贡献一份。
- 两次装配的 `Sources` 数组按字典序排列，逐字节一致。

### 5. map 字段的键序

`Progress.Counts` 是 `map[string]int`；`encoding/json` 对 map 的编码顺序是**字典序**——这是 Go 标准库保证（`json.Marshal` 内部用 `textMarshaler` + 排序），与具体实现无关。

### 测试断言：`TestBuildIsDeterministic`

`internal/view/view_test.go:293-306`：

```go
first  := json.Marshal(build(t, root))
second := json.Marshal(build(t, root))
reflect.DeepEqual(first, second)   // 字节级断言
```

M7.1 验收期实跑记录：真实 `.devsys` 快照（`docs/examples/m1-devsys/`）两次 `--json` 同 **3288 字节**（原始依据见 git 历史中的 docs/开发记录.md）。

- **本批（v0.1.9，ca58d27→19b9149）**：零写入契约、`advisory_unlocked` / `pending_transaction` 语义与确定性保证都不变；本轮只有 CLI 文案随主命令改名——`pending` 态的修复项仍是 `workloom recover --actor operator --reason "recover interrupted state"`（[build.go:25](file://internal/view/build.go#L25)），CLI 文本脸提示行为「hint: run `workloom doctor` to inspect before `workloom recover`」（[internal/cli/workspace.go:221-222](file://internal/cli/workspace.go#L221-L222)），`devsys` 作为同一二进制的兼容别名仍可用（[cmd/devsys/main.go:1-3](file://cmd/devsys/main.go#L1-L3)）。

## 与其他卡的关系

- [概述](概述.md) — 模块定位与「只读聚合层」承诺
- [架构设计](架构设计.md) — `inspectSnapshot` 共享锁/降级分支 + `pendingReadiness` 提前返回 + `Build` 主入口 + `progress()` 喂 `next.Evaluate` 的入参扩展（PolicyIDs/DefaultPolicy/QualityBlocks）+ `policyFacts`/`policyOf` 装配顺序
- [视图数据模型](视图数据模型.md) — `Trust` 三态常量、`Knowledge` 四态常量、字段 `omitempty` 约定
- [编码规范](编码规范.md) — `Options.Now` 注入 + `capList` 先定序后截断 + `sortedUnique` + `problems` 收集
- [政策只读：plain parse ≠ LKG 解析器](#政策只读plain-parse--lkg-解析器) — 同卡上一节，解释为什么视图用 `workflow.Load` 而非 `workflow.Resolve`
