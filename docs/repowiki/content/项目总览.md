---
status: stable
type: overview
triggers: [项目定位, 当前能力, 总体架构, Workloom, MCP, 共享应用服务, devsys wire, session start, 执行层, 适配器, dispatch, worktree, 知识层, knowledge status, knowledge scan/validate/refresh, context get --task, 0/10/11 退出码, knowledge_pages, knowledge_generator, 视图层, workspace view, 只读视图, advisory_unlocked, pending_transaction]
description: Workloom 当前命令面、项目状态布局、可靠文本存储、M3 工作流/审批/next、M4 共享应用服务与 MCP 接入、CLI 薄化渲染与 AGENTS.md 注入；M5 新增内部知识层（internal/knowledge 八子文件 + internal/storage.LockFile 复用），CLI 进一步承载 knowledge { status | scan | validate | refresh } 四族命令与 context get --task 装配，0/10/11 reserved exit code，配置增 knowledge_pages/knowledge_generator 两键；M6 新增内部执行层（harness/workspace/dispatch/retry/prompt），CLI 进一步承载 worktree/dispatch/run exec|prompt|verify|complete|fail|cancel 七族命令，配置增 workspace_root/dispatch_command 与 DEVSYS_PROJECT_ROOT 环境变量；M7.1 新增视图层（internal/view 只读聚合 + devsys workspace view [--json] [--limit N]，storage.Inspect 共享锁 → 缺锁文件降级 advisory_unlocked，pending 事务按 §15.4 不渲染业务事实）。
source_commit: 463c2d8
generated: true
generator: repowiki-gen
---

# 项目总览

<cite>
**本文引用的文件**
- [README.md:1-20](file://README.md#L1-L20)
- [internal/cli/cli.go:24-56](file://internal/cli/cli.go#L24-L56)
- [internal/cli/cli.go:144-180](file://internal/cli/cli.go#L144-L180)
- [internal/view/view.go:25-105](file://internal/view/view.go#L25-L105)（SchemaVersion / DefaultLimit / Trust 三态 / Model）
- [internal/view/build.go:33-115](file://internal/view/build.go#L33-L115)（Build 只读装配）
- [internal/cli/workspace.go:21-135](file://internal/cli/workspace.go#L21-L135)（workspace view 命令面）
- [internal/knowledge/trigger.go:14-41](file://internal/knowledge/trigger.go#L14-L41)（TriggerHit / WorkItemText）
- [internal/project/init.go:41-123](file://internal/project/init.go#L41-L123)
- [internal/config/config.go:80-113](file://internal/config/config.go#L80-L113)
- [internal/storage/store.go:140-190](file://internal/storage/store.go#L140-L190)
- [internal/workitem/workitem.go:55-92](file://internal/workitem/workitem.go#L55-L92)
- [internal/record/record.go:243-325](file://internal/record/record.go#L243-L325)
- [internal/workflow/policy.go:13-142](file://internal/workflow/policy.go#L13-L142)
- [internal/workflow/condition.go:57-103](file://internal/workflow/condition.go#L57-L103)
- [internal/workflow/gate.go:38-73](file://internal/workflow/gate.go#L38-L73)
- [internal/workflow/cache.go:47-73](file://internal/workflow/cache.go#L47-L73)
- [internal/approval/approval.go:101-176](file://internal/approval/approval.go#L101-L176)
- [internal/approval/approval.go:266-304](file://internal/approval/approval.go#L266-L304)
- [internal/approval/approval.go:485-533](file://internal/approval/approval.go#L485-L533)
- [internal/workitem/state.go:66-188](file://internal/workitem/state.go#L66-L188)
- [internal/workitem/propagate.go:28-99](file://internal/workitem/propagate.go#L28-L99)
- [internal/workitem/workflow_instance.go:108-381](file://internal/workitem/workflow_instance.go#L108-L381)
- [internal/next/evaluate.go:113-181](file://internal/next/evaluate.go#L113-L181)
</cite>

## 目录
1. [引言](#引言)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [快速链接](#快速链接)

## 更新摘要

**变更内容**
- 基于 `4ad8f9e` 刷新：M1 命令与领域存储语义不变；搜索的千文件正确性测试不再按 2s 墙钟时间失败，性能由首次文件扫描与重复扫描两个独立 benchmark 场景记录（[internal/search/search_test.go:102-144](file://internal/search/search_test.go#L102-L144)）。
- 基于 `85b0de7` 刷新：M2 交付工作项九状态机、调度态与文件级领取、只读对账与摘要确认修复，命令面新增 `workitem` / `doctor` / `recover` / `repair`；新增知识卡[状态机与调度](../knowledge/可靠文本存储/状态机与调度.md)与[对账与修复](../knowledge/可靠文本存储/对账与修复.md)。
- 基于 `cf7b256` 刷新：M3 交付工作流策略（`internal/workflow`）、审批服务（`internal/approval`）与就绪判定（`internal/next`）三层应用逻辑，命令面新增 `workflow` / `approval` / `next`；存储新增 `Tx.AppendJSONLRaw`（多行 JSONL payload）与 `Tx.Staged(rel)`（事务内已暂存查询），`events.AppendBatchTx`（按 shard 分组一次 append）支撑状态/传播/失效事件同批提交；工作项转换事务的 `TransitionRequest.Guard` 在事务内复审证据并产出事件，`propagateRecordsTx` 完成事务内广播决策/发现引用到父与直接兄弟，`workflow_instance.go` 五类实例操作只动 `workflow` 字段（不动状态/调度/租约）；`record.ListArtifacts` 给门禁证据收集使用；工作流读取入口 `workflow.Resolve` 解析失败回退 last-known-good 缓存（`.devsys/.cache/workflows/<id>.md`）。既有知识卡全部刷新覆盖 M3 内容。
- **M4（997c5f8）增量刷新**：交付**共享应用服务** `internal/app`（`Service` + `*app.Error{Kind,Message,Problems}` + `Class()` 四类归一 + Session/Context/Project/Workitem/Workflow/Approval/Decision/Finding/Artifact/Event/Run/Next/Wire/KnowledgeStatus 共 30+ 方法）和 **MCP 服务** `internal/mcp`（基于官方 `github.com/modelcontextprotocol/go-sdk v1.8.0`、`Config` + `DefaultProfiles` + `ParseProfiles` + `allTools()` 注册期 profile 过滤的 60+ 工具、`toolError{Code,Message,Problems,Notice}` + `fail / failNotice / usageFail`）。CLI 薄化为「`runXxx` 子命令 + `appService()` + `toCoded`」三层，新增 `decision/finding/event/artifact/run` 完整记录族、`context get|workitem|refresh|compact` 只读聚合、`knowledge status`（M4 恒 `unavailable`，M5 §12.6 达到）、`session start` 单次会话定向、`wire` 把 `<!-- devsys:begin/end -->` 纪律块注入 `AGENTS.md`（幂等）、`mcp serve` stdio 服务；`--jsonl` 列表行流与 `writeJSONL[T]` 泛型。新增 vendored `github.com/modelcontextprotocol/go-sdk v1.8.0`。**M4 不再引入新锁或新存储后端**——所有写路径走 `Store.Write` 与 `tx.AppendJSONLRaw`；artifact 版本链通过 `internal/record/update.go` 显式化为 `readSnapshot[T]` 泛型 + `UpdateDecision / UpdateFinding / AppendArtifactVersion`（线性版本链，防并发分叉 `ErrSuperseded`），`internal/run` 提供 `ReadSnapshot`。CLI 与 MCP 共享同一份 `*app.Error.Class()` → 退出码 / 工具错误码的翻译。**知识（M5）与执行调度（M6）未实现**；`config.yaml` 仍只有 `schema_version`。

- **M6（997c5f8 之后）增量刷新**：交付**执行层** `internal/harness + internal/workspace + internal/dispatch + internal/retry + internal/prompt` 五个新子包，并把执行层接到 `internal/app` 与 CLI/MCP 双入口。**harness** 暴露 §9.2 `Adapter` / `Session` 接口与 §9.3 CLI 形态（Shell + Codex / OpenCode / ClaudeCode），统一流式输出、行级缓冲（64 KiB chunk）、stdin 投递、进程树终止（POSIX 进程组 / Windows JobObject）。**workspace** 提供 `Key`（净化 + 哈希后缀 + keyMax=64 + 16 hex 抗碰撞）、`Root` / `Validate`（根内不变量 + symlink 逃逸解析）、`Ensure` / `Remove` / `List`（git worktree 生命周期）+ `RunHook`（生命周期钩子走 shell 适配器）+ `HookEnv`（`DEVSYS_PROJECT_ROOT` 等环境变量注入）。**dispatch** 是 §4.8/§7.4 纯函数 `Plan`（priority desc → created_at asc → id asc 决胜 + 状态白名单 + blocked_by + 全局/状态并发）+ `ResolveCaps`（最严合并 + 缺失即默认 1）；**retry** 是 §15.4 `Delay(base, max, attempt, seed)`（指数 + 派生 jit…
- **M5（7c3fcde 增量刷新）**：交付**知识层** `internal/knowledge` 八子文件（page 契约 + 校验、扫描与匹配器、索引快照、新鲜度、人工保护、生成器契约、断点续跑与锁、state 映射）与 `internal/storage.LockFile`（OS 独占锁原语，方案 §15.2 同源）。**page.go** 定义 `Page` 结构 + 必填 `status/type/triggers/description/source_commit`（5 个）+ 可选 `dimension/sources/content_hash/generator/generated/protected`；`AuthoredStatuses = {current, stable, draft, unverified, conflicted, deprecated, archived}` 显式排除手写 `stale`/`invalid`（这两态是计算出来的）；`KnownTypes` 之外的类型仅 warning（外部生成器的扩展允许）；`sources` 拒绝对路径 / 盘符 / `\\` / `..` / 空段。**snapshot.go** `BuildSnapshot` 走 `git check-ignore --stdin -z`（`ErrNoGit` = 错误，方案 §12.5 禁止"无 git 就当无忽略"）+ `DefaultExcludedDirectories` + `SecretPatterns`（`.env*`、`.pem`/`.key`/`.p12`/`.pfx`/`.keystore`/`.jks`、SSH 私钥、`credentials*`/`secrets*`、`.npmrc`/`.netrc`/`.htpasswd`）+ `DefaultMaxFileSize = 1 MiB` + 自定义 `.devsys/knowledge/ignore`（不支持 `!` 取反）。**match.go** `Match(pattern, path)` 走 `.gitignore` 同形语法（`*` 段内、`?` 单字符、`**` 跨段；无 `/` 任意深度、尾部 `/` 子树）；`matchCacheSize = 4096` 防无界。**freshness.go** `Evaluate` 三层基线（in-page → state.Mapping → state.Baseline.Commit）+ `recordedMatches` 晋升（生成器记录 hash 与磁盘一致时升到层基线，理由：生成器记录 source_commit 滞后于 state.Baseline.Commit）；`Unverifiable` 仍走 stale（"Cannot tell" ≠ fresh）；`Head` detached 时 branch 字面 `"HEAD"`。**protection.go** `Skips` 双闸（`protected: true` 永久锁 + `content_hash` 与磁盘 hash 不一致 = 手改）+ `--force` 覆盖仍记录 `Overridden=true`。**generator.go** `Scope{FormatVersion=1, Project, Mode, Baseline, Head, Pages}` 写到 `.devsys/knowledge/refresh-scope.json`；`GeneratorCommand` 用 `strings.Fields` 切分 argv（**不走 shell**，避免第二套平台相关解析层）。**run.go** `Checkpoint{Phase ∈ {generating, finished, failed}, Pages[], Completed[], PID, Host, StartedAt, UpdatedAt}` 写到 `.devsys/knowledge/run.json`；`LockRefresh` 走 `storage.LockFile`（进程死亡锁自动释放）；`ResumePending` 逐页比对已写 `content_hash` 与磁盘 hash，未完成页继续 pending。**state.go** 读 `.devsys/knowledge/state.json`（`Baseline{Commit,Branch}` + `Pages{path → PageState{Sources,ContentHash,SourceCommit}}`），由生成器写、层只读。**应用接线** `internal/app.knowledge.go` 四方法 `KnowledgeStatus/Scan/Validate/Refresh` + 四 View + `pageRoots` 合并策略（`md.Config.KnowledgePages` 优先，否则 `ExistingRoots(root, DefaultRoots)`）+ `checkPageRoot`（拒绝不存在 / 项目外）+ `runGenerator`（`limitWriter` 上限 256 KiB）+ `refreshHolder`（holder 提示 `pid+host+started_at`）；`internal/app.context.go.knowledgeContext` 在 `context get --task <id>` 之上叠加 `KnowledgeContext{Pages[], Baseline, Degraded, Notice}`（三阶段选页 trigger → sources → per-page fresh）。**CLI 接线** `internal/cli.records.go` `runKnowledge{Scan,Validate,Status,Refresh}` 四族路由 + `internal/cli.cli.go` 新增 `CodeStale=10` / `CodeMissing=11` reserved exit code 与 `codedExit{code}` 错误类型（stdout 已写报告，退出码承载状态）；`reportRefresh` 把 `--quiet` 下 `Skipped`/`Output`/`affected` 仍可见；`humanBytes`/`shortSHA`/`indexSummary` 辅助函数。**配置接线** `internal/config` 增 `SeverityWarning = "warning"` 常量 + `Problem.Severity` 字段（`Problem.String()` 前缀 `warning: `）+ `KnowledgePages []string` 与 `KnowledgeGenerator string` 两配置键；`internal/config.validate.go` 拆 `kindStrings`（接受 null / 空列表 / 字符串标量列表）与 `kindMilestones` 双路径。**MCP 接线** `internal/mcp.allTools()` 新增 `knowledge_status` / `knowledge_validate` / `knowledge_refresh` 三 session profile 工具，`registerContextForWorkitem` 输入新增 `paths []string` 参数。**新增知识模块** `docs/repowiki/knowledge/知识层/` 共 8 张卡（概述 / 架构设计 / 页面契约与校验 / 索引快照与匹配器 / 新鲜度与基线 / 人工保护 / 生成器契约与适配器 / 断点续跑与锁 / 知识服务接线 / 上下文装配）；既有模块卡（project-access、shared-app-mcp、durable-storage）补 M5 接线。

- **工作流策略**（`internal/workflow`）：`.devsys/workflows/<id>.md`（YAML front matter + 提示词正文）严格 located 解析；条件白名单求值（7 字段、`==` `!=` `<` `>` `in` `exists`）；门禁（artifact 名集、min 数、comment、approval）与质量门（确定性文本评分，满分 100）；模板渲染与环境变量展开；last-known-good 缓存（`.devsys/.cache/workflows/<id>.md`，本地、可重建、不提交）。
- **审批服务**（`internal/approval`）：`.devsys/approvals/approval-<N>.yaml`；请求 → 决定（approved / rejected） → 消费生命周期；消费永远在 `workitem.changeStatus` 的 Guard 内完成（同事务内重读 + `ExpectHash`），离开状态未消费的同源审批自动失效；`scope=stage_gate` 拒绝走 `rejectStageGate` 在 `Transition` 的 Guard 内同时完成决定与状态变更。
- **就绪判定**（`internal/next`）：纯函数 `Evaluate(Input) Report` 输出 PASS/CONCERNS/FAIL 与 §7.4 唯一推荐动作；CLI `devsys next` 只读组装 Input，不可信状态不输出业务事实。

M4 交付**接入层**：

- **共享应用服务**（`internal/app`）：30+ 方法覆盖 Session/Context/Project/Workitem/Workflow/Approval/Decision/Finding/Artifact/Event/Run/Next/Wire/KnowledgeStatus；统一错误模型 `*app.Error{Kind, Message, Problems}` + `Class()` 把 9 种 Kind 归一到 usage / precondition / internal / invalid 四类；写方法统一接受 `expect`（来自对应 `Get*` 的 `Version`）通过 `tx.AppendJSONLRaw` + `tx.Staged(rel)` 走 `Store.Write` 的乐观并发。
- **MCP 服务**（`internal/mcp`）：基于官方 Go SDK `github.com/modelcontextprotocol/go-sdk v1.8.0`（vendored）；`Config` + `DefaultProfiles`（session + executor）+ `ParseProfiles`（未知名字报错）+ `allTools()` 注册期 profile 过滤的 60+ 工具（`tools/list` 决定暴露集，未注册无法以名称调用）；工具错误统一通过 `toolError{Code, Message, Problems, Notice}` + `fail / failNotice / usageFail` 翻译。
- **CLI 薄化**：原 M3 的 `workitemError / approvalError / mapWorkflowError` 三个工具函数被 `toCoded` 单一翻译替代；新增 `decision/finding/event/artifact/run` 完整记录族、`context get|workitem|refresh|compact` 只读聚合、`knowledge status`（M4 恒 `unavailable`，M5 §12.6 达到）、`session start` 单次会话定向、`wire` AGENTS.md 纪律块幂等注入、`mcp serve` stdio 服务；`--jsonl` 列表行流与 `writeJSONL[T]` 泛型。
- **读快照与守卫更新**（`internal/record/update.go`）：共享 `readSnapshot[T]` 泛型 + `UpdateDecision / UpdateFinding / AppendArtifactVersion`（线性版本链，防并发分叉 `ErrSuperseded`）；`internal/run.ReadSnapshot` 同形单态实现。CLI 的 `--expect <64-hex>` 与 MCP 工具的 `expect` 字段在 `internal/app` 内被反解码为原字节传给 storage 层——保证两入口的乐观并发语义一致。

CLI 与 MCP 共享同一份 `*app.Error` 与 `app.New(root)` 入口，约束不会因入口不同而分歧。

- **M7.1（463c2d8 增量刷新）**：交付**视图层** `internal/view`（方案 §17）——`Build(ctx, root, opts)` 从 `.devsys/` 与 git 状态装配 `view.Model`（`project / baseline / trust / progress / runs / records / knowledge / sources / problems`）；每个列表先定序后截断（缺省 `DefaultLimit = 50`，`--limit` 覆盖）并置 `Truncated`，模型无墙钟字段，同一状态两次构建逐字节一致。读取纪律：只走 `storage.Inspect`（既有锁文件上的共享锁，**绝不创建**）→ 缺锁文件时 `storage.InspectUnlocked` 降级并把快照标成 `advisory_unlocked`；**不**恢复事务、**不**修断尾事件流、**不**写 `.devsys/.cache/`、**不**探测生成器。存在 pending 事务时按 §15.4 不渲染业务事实，改为 `trust.state = pending_transaction` + 事务 ID + `devsys recover` 修复项（`next.Evaluate` 的 FAIL 报告）。CLI 新增 `devsys workspace view [--json] [--limit N]`（[internal/cli/workspace.go:21-135](file://internal/cli/workspace.go#L21-L135)）：`--json` 把 `Model` 作为信封 `data`，人类输出「一行一事实」；`workspace`（§17 视图域）与 `worktree`（§4.8 执行工作区）是两个概念。触发词匹配规则从 `internal/app/context.go` 抽到 [internal/knowledge/trigger.go:14-41](file://internal/knowledge/trigger.go#L14-L41)（`TriggerHit` / `WorkItemText`），`context get --task` 与视图层共用一份实现。知识卡新增模块[视图层](../knowledge/视图层/概述.md)（7 张卡）。

章节来源：[README.md:13-32](file://README.md#L13-L32)、[internal/cli/cli.go:144-180](file://internal/cli/cli.go#L144-L180)、[internal/next/evaluate.go:113-181](file://internal/next/evaluate.go#L113-L181)。

## 项目结构
| 区域 | 当前职责 |
|---|---|
| `cmd/devsys/`、`internal/cli/` | M4 起薄化为「渲染层」：flag 解析 + `--json`/`--jsonl`/`render` + `toCoded`；业务分发迁出到 `internal/app` |
| `internal/project/`、`internal/registry/` | 项目目录初始化与用户级项目路径登记 |
| `internal/config/` | 受管元数据文件的校验与问题定位（含嵌套 scope/current_state/milestones） |
| `internal/storage/` | 原子写、序列化、JSONL、锁、版本守卫与可恢复事务；M3 新增 `Tx.AppendJSONLRaw` 与 `Tx.Staged`；**M5** 新增 `LockFile`（OS 独占锁原语，被知识层 `LockRefresh` 复用，方案 §15.2 同源） |
| `internal/workitem/`、`internal/events/`、`internal/run/`、`internal/record/` | WorkItem（含状态机、文件级领取、Guard、记录传播、工作流实例）、事件流（+ `AppendBatchTx`）、Run（M4 起 `ReadSnapshot`）、决策/发现/工件（M4 `internal/record/update.go`：`readSnapshot[T]` 泛型 + `UpdateDecision / UpdateFinding / AppendArtifactVersion` 含 `ErrSuperseded`）的项目内存储与 ID 分配 |
| `internal/reconcile/` | 只读对账、显式恢复与摘要确认的状态修复（M2） |
| `internal/search/` | 无索引文本扫描检索 |
| `internal/workflow/`（M3） | 策略解析、条件求值、门禁/质量门、LKG、模板/环境变量 |
| `internal/approval/`（M3） | 审批生命周期 |
| `internal/next/`（M3） | 就绪判定 + §7.4 推荐 |
| `internal/app/`（M4 新增） | 共享应用服务：Session/Context/Project/Workitem/Workflow/Approval/Decision/Finding/Artifact/Event/Run/Next/Wire/KnowledgeStatus + `*app.Error` 四类分类 |
| `internal/mcp/`（M4 新增） | 基于官方 Go SDK 的 Model Context Protocol 服务：profile 过滤注册 + 60+ 工具的 `toolError` 翻译 |
| `internal/knowledge/`（M5 新增） | 知识层业务规则：Page 契约与 front matter 校验（5 必填 + KnownKeys 字典）、`Scan`/`BuildSnapshot`（git check-ignore + DefaultExcludedDirectories + SecretPatterns + 1 MiB + 自定义 ignore）、Match `.gitignore` 风格语法、`Evaluate`（三层基线 + recordedMatches 晋升）、`Skips`（双闸 + --force 记录 override）、`GeneratorCommand`（argv 不走 shell）、`Checkpoint`/`LockRefresh`（OS 锁 + 续跑）、`LoadState`/`LoadSnapshot` |
| `internal/view/`（M7.1 新增） | 视图域只读聚合层（方案 §17）：`Build` 装配 `view.Model`（project/baseline/trust/progress/runs/records/knowledge + sources/problems）；只走 `storage.Inspect`（既有锁上的共享锁，绝不创建）→ 缺锁文件时 `storage.InspectUnlocked` 降级为 `advisory_unlocked`；pending 事务按 §15.4 不渲染业务事实；每节带来源文件、模型无墙钟字段、列表先定序后截断；调用面是 CLI `devsys workspace view`（M7.2 静态构建 / M7.3 serve 复用同一 `Model`） |
| `internal/version/` | 构建版本标识 |
| `docs/examples/workflows/`（M3） | 三份示例策略（quick-fix / feature-development / architecture-change） |
| `scripts/m4helper`、`scripts/smoke-m4.{sh,ps1}`、`scripts/exchange-demo.sh`（M4 新增） | M4 端到端入口（MCP 表面 / CLI-MCP 写一致 / 会话接口 / 交换协议 / wire 幂等） |
| `scripts/smoke-m5.sh`（M5 新增） | M5 知识层端到端（page 契约 + 索引扫描 + 新鲜度 0/10/11 + 生成器契约 + 人工保护 + 续跑 + 并发拒绝 + 上下文装配 + 降级）：用 stub 生成器（bash 实现 + `.cmd` 包装），覆盖 Page 校验、0/10/11 退出码、生成器 argv/Scope 协议、双闸保护、续跑语义、上下文 trigger/sources 命中 |
章节来源：[README.md:144-157](file://README.md#L144-L157)、[internal/reconcile/reconcile.go:1-19](file://internal/reconcile/reconcile.go#L1-L19)、[README.md:13-32](file://README.md#L13-L32)。

## 架构总览

```mermaid
flowchart TD
    A[devsys 命令入口] --> B[CLI 薄渲染层: flag + toCoded + render]
    B --> C[init]
    B --> D[config check]
    B --> S[search 文本扫描]
    B --> P[project list/get/status/blueprint/update/state-update]
    B --> W[workitem create/get/update/transition/claim/release/start/block/complete/comment/dep]
    B --> WF[workflow check/list/get/start/next/step-complete/pause/resume/cancel]
    B --> AP[approval list/get/request/approve/reject]
    B --> R[doctor / recover / repair]
    B --> DEC[decision list/get/create/approve]
    B --> FIN[finding list/get/create/resolve]
    B --> EV[event list/record]
    B --> AR[artifact list/get/register/update/history]
    B --> RU[run list/get/create/update/heartbeat/log]
    B --> CTX[context get/workitem/refresh/compact\nget --task ID-/workitem-<N>]
    B --> KNO[knowledge status / scan / validate [dir|page.md] / refresh [--full|--affected] [--force]]
    B --> SES[session start]

    MCP --> M[internal/mcp SDK server + 60+ 工具]
    M --> APP[internal/app 共享应用服务]

    C --> APP
    D --> APP
    W --> APP
    WF --> APP
    AP --> APP
    N --> APP
    DEC --> APP
    FIN --> APP
    EV --> APP
    AR --> APP
    RU --> APP
    CTX --> APP
    KNO --> APP
    SES --> APP
    WIR --> APP
    P --> APP

    APP --> ST[storage.Store 共享/独占锁]
    APP --> WI[internal/workitem / events / run / record / search / reconcile]
    APP --> WF[internal/workflow / approval / next]
    APP --> DOM[internal/domain models + serialize]
    APP --> CONF[internal/config Load + Diagnose]
    APP --> PROJ[internal/project + registry + version]

    ST --> RD[恢复未完成事务]
    RD --> CM[一致读取或事务提交]
    ST --> APPJSON[Tx.AppendJSONLRaw/Staged]
    APPJSON --> EVB[events.AppendBatchTx]
    CM --> EVB

    R --> REC[internal/reconcile doctor/recover/repair]
    REC --> ST
    S --> SCH[internal/search 文本扫描]

    B --> WSV["workspace view [--json] [--limit N]"]
    WSV --> VWP["internal/view.Build 五节装配 + trust/baseline/sources"]
    VWP --> ST
    VWP --> WI
    VWP --> WF
    VWP --> CONF
```

图表来源：
- [cmd/devsys/main.go:12-14](file://cmd/devsys/main.go#L12-L14)
- [internal/cli/cli.go:144-180](file://internal/cli/cli.go#L144-L180)（命令分发）
- [internal/cli/cli.go:99-117](file://internal/cli/cli.go#L99-L117)（toCoded 翻译）
- [internal/cli/cli.go:124-133](file://internal/cli/cli.go#L124-L133)（writeJSONL）
- [internal/cli/mcp.go:48-84](file://internal/cli/mcp.go#L48-L84)（mcp serve）
- [internal/cli/workspace.go:21-135](file://internal/cli/workspace.go#L21-L135)（workspace view 与一行一事实输出）
- [internal/view/build.go:33-115](file://internal/view/build.go#L33-L115)（Build 只读装配入口）
- [internal/cli/wire_test.go](file://internal/cli/wire_test.go)（wire 幂等测试）
- [internal/app/app.go:20-129](file://internal/app/app.go#L20-L129)（Service + Error + Class）
- [internal/mcp/server.go:91-130+](file://internal/mcp/server.go#L91-L130)（profile 注册期过滤）
- [internal/mcp/tools.go:24-119](file://internal/mcp/tools.go#L24-L119)（allTools 注册表）
- [internal/storage/store.go:140-190](file://internal/storage/store.go#L140-L190)（事务与锁）
- [internal/storage/txn.go:179-247](file://internal/storage/txn.go#L179-L247)（AppendJSONLRaw / Staged）
- [internal/events/events.go:108-144](file://internal/events/events.go#L108-L144)（AppendBatchTx）
- [internal/record/update.go](file://internal/record/update.go)（readSnapshot[T] + ErrSuperseded）
- [internal/run/run.go:156-185](file://internal/run/run.go#L156-L185)（Run.ReadSnapshot）
- [internal/app/knowledge.go:64-148](file://internal/app/knowledge.go#L64-L148)（KnowledgeStatus 三态裁决）
- [internal/app/knowledge.go:170-330](file://internal/app/knowledge.go#L170-L330)（KnowledgeRefresh 流水线）
- [internal/app/context.go:283-372](file://internal/app/context.go#L283-L372)（knowledgeContext 三阶段选页）
- [internal/knowledge/page.go:127-217](file://internal/knowledge/page.go#L127-L217)（Page.Parse + 必填 + KnownKeys）
- [internal/knowledge/snapshot.go:113-195](file://internal/knowledge/snapshot.go#L113-L195)（BuildSnapshot 排除优先级）
- [internal/knowledge/freshness.go:111-187](file://internal/knowledge/freshness.go#L111-L187)（Evaluate 三层基线 + recordedMatches 晋升）
- [internal/knowledge/protection.go:57-92](file://internal/knowledge/protection.go#L57-92)（Skips 双闸 + --force override）
- [internal/knowledge/run.go:93-144](file://internal/knowledge/run.go#L93-L144)（LockRefresh + ResumePending）
- [internal/cli/cli.go:31-57](file://internal/cli/cli.go#L31-L57)（CodeStale=10 / CodeMissing=11）
- [internal/cli/records.go:898-1109](file://internal/cli/records.go#L898-L1109)（runKnowledge{Scan,Validate,Status,Refresh}）
- [internal/cli/cli.go:318-328](file://internal/cli/cli.go#L318-L328)（codedExit{code} 错误类型）
- [internal/mcp/tools.go:104-107](file://internal/mcp/tools.go#L104-L107)（knowledge_* 工具注册）

**项目接入与配置。** `init` 要求当前目录为 Git 仓库根；先校验已有受管文件，再补建目录和缺失占位文件。`.devsys/.gitignore` 有特殊规则：保留原有行，补齐缺失忽略项。不能把初始化描述为一次跨文件存储事务。`config check` 是普通只读诊断，不加锁、不触发事务恢复。M1 起 `project.yaml` 使用完整 Project 模型（嵌套校验见 `internal/config/nested.go`），领域记录统一走 `internal/domain` 的严格编解码。

章节来源：[internal/project/init.go:41-123](file://internal/project/init.go#L41-L123)、[internal/config/config.go:104-113](file://internal/config/config.go#L104-L113)。

**领域存储（M1）。** WorkItem/Run/记录三类采用「一记录一文件 + CAS 分配 + 乐观并发更新」；事件流按月分片 JSONL 追加；Artifact 版本为不可变文件链（`previous_id` 回链）；`devsys search` 对 `.devsys/` 文本做大小写不敏感扫描，排除 `local/` 与 `.cache/`。

章节来源：[internal/workitem/workitem.go:55-92](file://internal/workitem/workitem.go#L55-L92)、[internal/events/events.go:53-84](file://internal/events/events.go#L53-L84)、[internal/record/record.go:243-325](file://internal/record/record.go#L243-L325)、[internal/cli/cli.go:274-313](file://internal/cli/cli.go#L274-L313)。

**状态机与调度（M2 新增）。** 状态写入只经 `Transition`/`ApplyRepair`：actor+reason 必填、旧快照拒绝、状态与事件（`status_changed`/`repair_applied`）同一事务；调度态与业务状态分离，领取用 `.devsys/scheduling/<id>.yaml` 租约与随机 token fencing，并发领取恰好一个胜者并在同一事务创建 Run。见[状态机与调度](../knowledge/可靠文本存储/状态机与调度.md)。

章节来源：[internal/workitem/claim.go:179-326](file://internal/workitem/claim.go#L179-L326)、[internal/workitem/state.go:46-59](file://internal/workitem/state.go#L46-L59)、[internal/domain/state.go:62-132](file://internal/domain/state.go#L62-L132)。

**对账与修复（M2 新增）。** `devsys doctor` 只读（不建锁、不自动恢复；待恢复事务优先报告且不输出业务事实）；`devsys recover` 先确定性恢复事务，再释放过期/孤儿领取并留事件；`devsys repair --dry-run/--apply` 以无墙钟摘要与写事务内证据复核执行「推断→确认→重写」。见[对账与修复](../knowledge/可靠文本存储/对账与修复.md)。

章节来源：[internal/reconcile/reconcile.go:155-214](file://internal/reconcile/reconcile.go#L155-L214)、[internal/reconcile/reconcile.go:367-380](file://internal/reconcile/reconcile.go#L367-L380)、[internal/cli/cli.go:511-660](file://internal/cli/cli.go#L511-L660)。

**工作流策略、门禁与质量门（M3 新增）。** `.devsys/workflows/<id>.md` 解析为 `Policy`：front matter 行号定位、未知键 warning、结构/类型/必填/交叉引用 error；步骤序列、转换条件白名单（7 字段、`==` `!=` `<` `>` `in` `exists`）、门禁（artifact 名集、最小数量、comment、approval）、质量门（标题/描述/验收标准/引用/验收语言，满分 100）、hook 名枚举（`after_create/before_run/after_run/before_remove`）、`on_reject` ∈ {`block`, `regress:<status>`}、`quality_gate.min_score` 0–100；M3.7 起 `RequireApproval` 由 `approval.MatchingGate` 复核。详见[状态机与调度](../knowledge/可靠文本存储/状态机与调度.md)「M3 工作流实例操作」小节与[架构设计](../knowledge/可靠文本存储/架构设计.md)「workflow / approval / next」表。

章节来源：[internal/workflow/parse.go:40-769](file://internal/workflow/parse.go#L40-L769)、[internal/workflow/condition.go:57-128](file://internal/workflow/condition.go#L57-L128)、[internal/workflow/gate.go:38-73](file://internal/workflow/gate.go#L38-L73)、[internal/workflow/quality.go:33-86](file://internal/workflow/quality.go#L33-L86)。

**记录传播（M3 新增）。** 完成（进入 `done`）事务内 `propagateRecordsTx` 扫描 `decisions/` 与 `findings/` 中 `RelatedWorkItems` 命中的记录，把 `decision://<id>` / `finding://<id>` 追加到父与直接兄弟的 `context_refs`（同事务 CAS、不改对方状态、已存在则跳过 → 幂等）；无 parent 不广播；父子自环（`parent_id == self`）拒绝并回滚。`record_propagated` 事件与 `status_changed` 同批 `events.AppendBatchTx` 提交。

章节来源：[internal/workitem/propagate.go:28-225](file://internal/workitem/propagate.go#L28-L225)、[internal/workitem/state.go:175-181](file://internal/workitem/state.go#L175-L181)、[internal/events/events.go:108-144](file://internal/events/events.go#L108-L144)。

**last-known-good 策略缓存（M3 新增）。** `workflow.Resolve(ctx, root, id)` 是工作流读取统一入口：每次重新 `Parse`（不信 mtime），成功后 `refreshLastKnownGood` 通过 `storage.Write` best-effort 写 `.devsys/.cache/workflows/<id>.md`（同字节不写）；失败回退快照并携带当前 `Issue`；无快照或快照损坏 fail closed（损坏快照视为缺失，绝不作为回退候选）。`workflow check` 仍只读走 `Load`；`claim`（新派发）拒绝在当前策略非法/缺失时执行，即使快照可用；`transition` 门禁允许用快照评估既有任务（既有推进不被配置错误卡死）并打印 `warning: … using last-known-good`；`next` 增 `invalid_policy` 只读风险（含「工作项引用缺失策略」交叉核对）。

章节来源：[internal/workflow/cache.go:47-122](file://internal/workflow/cache.go#L47-L122)、[internal/cli/cli.go:493-506](file://internal/cli/cli.go#L493-L506)、[internal/cli/cli.go:1206-1224](file://internal/cli/cli.go#L1206-L1224)、[internal/next/evaluate.go:140-145](file://internal/next/evaluate.go#L140-L145)。

**工作流实例与 fencing（M3 新增）。** `workflow_instance.go` 五类操作（`start` / `step-complete` / `pause` / `resume` / `cancel`）共用 `workflowWrite`：写事务内 `ReadForExpect` → `bytes.Equal` 校验 `--expect` → 租约活跃时要求 `Owner` + `Token`（与 `UpdateClaimed` 同契约） → `mutate` 重写 `wi.Workflow` 字段 → `PutYAML(ExpectHash)` + `AppendTx` 事件。**不**写工作项状态、不写调度态/租约/`scheduling/` 目录；目标步骤声明 `status` 时当前状态不匹配即拒绝（不隐式改状态）；显式 `--to` 必须命中声明转换且条件成立，自环拒绝；`paused` 拒绝推进。

章节来源：[internal/workitem/workflow_instance.go:108-381](file://internal/workitem/workflow_instance.go#L108-L381)。

**审批服务（M3 新增）。** `.devsys/approvals/approval-<N>.yaml`，ID `^approval-[0-9]+$`；`Request` 写事务内 `findOpenApproval` 防同 (work item, stage, requested status) 重复请求 → `nextNumber` → `PutYAML(ExpectAbsent)` → `approval_requested` 事件；`DecideTx` 重读 + `ExpectHash`、pending → approved/rejected；`ConsumeTx` 仅在调用方写事务（典型是 `changeStatus` 的 Guard）内调用，校验 approved + 未消费 + 同 work item + 同 stage + 当前状态 = `requested_status`，任一不符 abort 整单，写 `consumed_at` + 返回 `approval_consumed` 事件；`InvalidateForStatusTx` 在 `changeStatus` 离开 `from` 状态时把同源未消费审批标记为 `invalidated_at` + 返回 `approval_invalidated` 事件，通过 `Tx.Staged(rel)` 跳过本事务已暂存文件。`scope=stage_gate` 拒绝走 `rejectStageGate` 在 `Transition` 的 Guard 内同时完成 `DecideTx(rejected)` 与状态变更（默认 `blocked`，`regress:<status>` 回退），失败整单回滚。

章节来源：[internal/approval/approval.go:101-533](file://internal/approval/approval.go#L101-L533)、[internal/cli/cli.go:822-976](file://internal/cli/cli.go#L822-L976)。

**就绪判定与 next（M3 新增）。** 纯函数 `next.Evaluate(Input)`：FAIL = 有待恢复事务（不输出业务事实、附 `devsys recover` 修复项）；CONCERNS = 任一风险信号；PASS = 无。风险八类顺序：过期租约 → 孤儿领取 → 不可读调度文件 → 滞留审查（review/verification 且 `updated_at ≤ now − 24h`）→ 阻塞 → 元数据非法 → 策略非法（含「工作项引用缺失策略」）→ 待决审批。推荐六优先级：recover_claim → review → start（dispatch 排序 priority 降序 → created_at 升序 → id）→ start_backlog → milestone_review → report_done。CLI `devsys next` 只读组装 Input（先 `reconcile.Doctor`，再 workitems / pending approvals / 策略与元数据问题），exit 恒 0。

章节来源：[internal/next/evaluate.go:113-316](file://internal/next/evaluate.go#L113-L316)、[internal/cli/cli.go:1323-1437](file://internal/cli/cli.go#L1323-L1437)。

**共享应用服务（M4 新增）。** `internal/app.Service` 是 M4 起所有业务规则的统一持有者：30+ 方法按领域分组（Session/Context/Project/Workitem/Workflow/Approval/Decision/Finding/Artifact/Event/Run/Next/Wire/KnowledgeStatus）。**写方法**统一接收 `expect`（来自对应 `Get*` 的 64-hex `Version` 字段）→ 在写锁内通过 `Tx.ReadForExpect` + `bytes.Equal(raw, expected)` 守卫；**读方法**走 `storage.Store.Read` 共享锁；**只读聚合**（Session/Context/Next/KnowledgeStatus）走 `reconcile.Doctor` + `workflow.Load` + `next.Evaluate` 组合，不创建锁、不触发恢复、不可信状态不输出业务事实（§15.4）。

错误模型 `*app.Error{Kind, Message, Problems}`：9 种 Kind（usage / precondition / internal / invalid / workflow / gate / quality / workitem / approval）由 `Class()` 归一到 4 类（usage / precondition / internal / invalid），CLI 与 MCP 共用此翻译——`toCoded` (CLI) 与 `fail` (MCP) 都从 `*app.Error` 出发。构造器 `Usagef / Preconditionf / Internalf / Invalidf`；`Classify(err)` 包装任意 error。

章节来源：[internal/app/app.go:20-129](file://internal/app/app.go#L20-L129)、[internal/app/workitem.go](file://internal/app/workitem.go)、[internal/app/session.go](file://internal/app/session.go)、[internal/app/context.go](file://internal/app/context.go)、[internal/app/wire.go](file://internal/app/wire.go)、[internal/app/knowledge.go](file://internal/app/knowledge.go)、[internal/app/next.go](file://internal/app/next.go)。

**MCP 服务（M4 新增）。** `internal/mcp` 基于官方 Go SDK `github.com/modelcontextprotocol/go-sdk v1.8.0`（[go.mod](file://go.mod)）：`Config{Root, Profiles, ServerVersion, Instructions, Log}` 由 CLI 通过 `devsys mcp serve --profile <list>` 构造。`ParseProfiles(list)` 拒绝未知名字；`DefaultProfiles = [session, executor]`；`allTools()` 是唯一的工具注册表，每个工具 `toolSpec{name, profiles, register}`，`NewServer` 在注册期 `visible(spec.profiles, cfg.Profiles)` 过滤——未注册的工具既不出现在 `tools/list`，也无法以名称调用。工具处理器是 4–6 行薄适配，业务规则全在 `internal/app`。

错误通过 `toolError{Code, Message, Problems, Notice}` 翻译：`fail / failNotice / usageFail` 把 `*app.Error` 转为 `isError=true` tool result，通过 SDK `StructuredContent` 暴露给客户端。`notice` 字段用于 LKG fallback 等场景。`Instructions` 字段由 CLI 在 `mcpInstructions` 常量中固定——和 `app.Wire` 的纪律块描述一致；agent 的纪律文本由 `devsys wire` 显式注入 `AGENTS.md`，不在 SDK `initialize` 自动推送。

章节来源：[internal/mcp/server.go:1-130+](file://internal/mcp/server.go#L1-L130)、[internal/mcp/tools.go:1-156](file://internal/mcp/tools.go#L1-L156)、[internal/cli/mcp.go:48-84](file://internal/cli/mcp.go#L48-L84)。

**CLI 渲染层（M4 新增）。** `internal/cli` 在 M4 后薄化为三层：flag 解析 → `appService()` + `requireProjectRoot()` → `svc.Xxx(...)` → `render`。**写方法**统一抛 `*app.Error`；`toCoded`（[internal/cli/cli.go:99-117](file://internal/cli/cli.go#L99-L117)）翻译为 `codedError{code, kind, msg, problems}`。**列表输出**支持 `--jsonl`：`writeJSONL[T]`（[internal/cli/cli.go:124-133](file://internal/cli/cli.go#L124-L133)）逐行 encode 列表项，字段名与 `--json` 信封内同名数组一致。**M4 新增命令族**：`project list|get|status|blueprint|update|state-update` / `decision list|get|create|approve` / `finding list|get|create|resolve` / `event list|record` / `artifact list|get|register|update|history` / `run list|get|create|update|heartbeat|log` / `context get|workitem|refresh|compact` / `knowledge status` / `session start` / `wire` / `mcp serve`。**仍保留 CLI-only**：`search / config check / doctor / recover / repair`（无 MCP对应）；`wire`（operator 工具，`方案 §8.2` 无对应）。

章节来源：[internal/cli/cli.go](file://internal/cli/cli.go)、[internal/cli/records.go](file://internal/cli/records.go)、[internal/cli/diagnostics.go](file://internal/cli/diagnostics.go)、[internal/cli/mcp.go](file://internal/cli/mcp.go)。

**AGENTS.md 写入（M4 新增）。** `devsys wire` 把 devsys 纪律块以 `<!-- devsys:begin ... -->` / `<!-- devsys:end -->` 标记注入 `AGENTS.md`；`app.Wire(dryRun bool)`（[internal/app/wire.go:44-77](file://internal/app/wire.go#L44-L77)）保证：①三次运行字节级一致（幂等），②区段外字节保留（含其它工具的管理块与手写内容），③`--dry-run` 预览 diff 不写盘，④块破损（仅有 begin 或仅有 end 或嵌套）拒绝并升 `CodePrecondition = 3`。纪律描述与 MCP `Instructions` 字段、`app.Wire` 注入内容、`mcpInstructions` 常量**完全一致**——同一事实，三个入口。

章节来源：[internal/app/wire.go:1-243](file://internal/app/wire.go#L1-L243)、[internal/cli/cli.go:371-408](file://internal/cli/cli.go#L371-L408)、[internal/cli/wire_test.go](file://internal/cli/wire_test.go)。

**读快照与守卫更新（M4 新增）。** `internal/record/update.go` 把「读 + 守卫写」显式化为可重用 API：`readSnapshot[T]` 泛型（[internal/record/update.go:32-58](file://internal/record/update.go#L32-L58)）共享 `ReadDecision / ReadFinding / ReadArtifact`（[internal/record/update.go:18-29](file://internal/record/update.go#L18-L29)），返回解码对象与原 YAML 字节；`UpdateDecision / UpdateFinding` 在写锁内 `tx.ReadForExpect` + `ExpectHash(HashBytes(expected))` 守卫；`AppendArtifactVersion` 拒绝**并发分叉**——若 `currentID` 已有子版本，立即返回 `ErrSuperseded` 要求 re-read head 再 append（[internal/record/update.go:75-100+](file://internal/record/update.go#L75-L100)）。`internal/run.ReadSnapshot` 同形单态实现支撑 `app.RunUpdate` 的 `expect` 守卫。CLI 的 `--expect <64-hex>` 与 MCP 工具的 `expect` 字段在 `internal/app` 内被反解码为原字节传给 storage 层——保证两入口的乐观并发语义一致。

章节来源：[internal/record/update.go](file://internal/record/update.go)、[internal/run/run.go:156-185](file://internal/run/run.go#L156-L185)、[可靠文本存储 · 读快照与守卫更新](../knowledge/可靠文本存储/读快照与守卫更新.md)。

**可靠文本存储。** `Store.Read` 在共享锁下执行回调；发现未完成事务时释放共享锁、恢复后重试。`Store.Write` 在独占锁下先恢复，再提交回调收集的操作。`Tx.AppendJSONLRaw` 把多行 JSONL payload 一次 append（同 shard 多个事件合并提交）；`tx.Staged(rel)` 查询本事务是否已暂存 `rel`，供 `InvalidateForStatusTx` 跳过本事务已写的审批。此一致性边界适用于经 Store 协调的访问，不自动覆盖任意 `os.ReadFile` 或手工写入。

章节来源：[internal/storage/store.go:108-190](file://internal/storage/store.go#L108-L190)、[internal/storage/txn.go:179-247](file://internal/storage/txn.go#L179-L247)。

版本边界：M5 已落地知识层（`internal/knowledge` 八子文件 + `internal/storage.LockFile` 复用 + `internal/app.Service.Knowledge*` 四方法 + `devsys knowledge { status | scan | validate | refresh }` 四族路由 + MCP `knowledge_*` 三工具 + `context get --task` 装配 + 0/10/11 退出码）；M6 已落地执行层（`internal/harness + internal/workspace + internal/dispatch + internal/retry + internal/prompt` + CLI 七族命令 + MCP `run_prompt/verify/complete/fail/cancel`）；**M7.1** 已落地视图层（`internal/view` 只读聚合 + `devsys workspace view [--json] [--limit N]`，共享锁 → `advisory_unlocked` 降级、pending 事务不渲染业务事实），M7.2 静态构建与 M7.3 `workspace serve` **尚未落地**。`config.yaml` 增可选 `workspace_root` / `dispatch_command` / `knowledge_pages` / `knowledge_generator` 四键。

## 快速链接

- [快速开始](快速开始.md)：正确放置全局参数，初始化并检查配置。
- [开发与故障诊断](开发与故障诊断.md)：扩展约束、验证命令与已知限制。
- [项目接入与配置](../knowledge/项目接入与配置/概述.md)：命令与配置知识卡；M5 增 `CodeStale=10`/`CodeMissing=11` 与 `codedExit{code}`、`--quiet` 下 `Skipped`/`affected`/`Output` 仍可见、`context get --task <id>` 装配。
- [可靠文本存储](../knowledge/可靠文本存储/概述.md)：存储协议知识卡（含 M6 运行事件流 + M5 OS 锁原语）。
- [共享应用与MCP](../knowledge/共享应用与MCP/概述.md)：M4 共享服务与 MCP 服务的知识卡；M5 增 `KindKnowledge` 错误类 + `knowledge_*` 工具；M6 增执行命令族 + 完成校验与人工复核。
- [执行层](../knowledge/执行层/概述.md)：M6 执行层（harness/workspace/dispatch/retry/prompt）知识卡，含适配器协议契约。
- [知识层](../knowledge/知识层/概述.md)：M5 知识层（page 契约与校验 / 索引快照与匹配器 / 新鲜度与基线 / 人工保护 / 生成器契约与适配器 / 断点续跑与锁 / 知识服务接线 / 上下文装配）知识卡；0/10/11 退出码契约。
- [视图层](../knowledge/视图层/概述.md)：M7.1 视图域（`internal/view` 只读聚合 + `devsys workspace view [--json] [--limit N]`）知识卡；信任态 `ok` / `advisory_unlocked` / `pending_transaction` 与「快照非判决」的边界。
