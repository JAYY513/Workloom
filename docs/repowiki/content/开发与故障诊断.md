---
status: stable
type: developer_guide
triggers: [开发流程, 配置扩展, 故障诊断, 事务恢复, 发布构建, 工作流调试, 审批调试, next 调试, MCP 调试, wire 调试, --jsonl 调试, 知识层调试, knowledge status, knowledge scan/validate/refresh, 续跑, OS 锁, context get --task, 视图层调试, workspace view, workspace build, workspace serve, 新鲜度提示, advisory_unlocked, pending_transaction, 只读聚合, 协作层调试, sync status, 接力判定, ProposalNoteUnmerged, dispatch 合并门控, archive, archive events, archive runs, Contrabass, contrabass export, contrabass import, 看板回流, 可选后端, note_unmerged_paths, BOM, release workflow, checksums.txt, install 鉴权下载, default_policy, quality_blocked, retry_pending, 失效审批, stage gate, project blueprint, project_blueprint_get, storeError, --latest, version conflict, invalid workitem id]
description: 跨模块修改约束、真实命令诊断和当前验证边界（M5 commit 7c3fcde：知识层 `internal/knowledge` 八子文件 + `internal/storage.LockFile` 复用 + Page 契约与校验 + 索引快照 + 新鲜度 0/10/11 + 人工保护 + 生成器契约 + 断点续跑 + `internal/app` 四方法接线 + CLI 四族 + MCP 三工具 + `context get --task` 装配；M6 之后执行层；M7 视图层：只读聚合契约 + build/serve 渲染 + 新鲜度提示诊断表与 0444 只读副本验证法；**M8** 协作层：`internal/syncstatus` 只读接力判定（含 NOT ready 七码分类）+ `reconcile.ProposalNoteUnmerged` 人工标注与 `app.mergeConflict` 派发门控 + `internal/archive` 保守归档 + `search` 排除 `archive` + 事件/Run 流双读合并 + `scripts/contrabass/{export,import}.py` 可选看板后端 BOM/无 BOM 写卡问题。**本轮 P1/P2 + 外部审查 + 发布**：子目录解析项目根 (`internal/cli/roots.go`) + `wire --check` 内容比对 / `wire --skill` frontmatter / `--print-mcp codex|claude|opencode` + `devsys` 技能目录 + `prime` 别名 + `--latest`（与 `--expect` 互斥）+ MCP `--tier core|standard` + 提示词 `--by` 与工作流步骤清单 + 安装链 `gh` 鉴权下载 + `install.ps1` 回退构建进入克隆目录 + 校验和容忍 `*` 二进制标记 + `next`/`project status` 与 `claim` 同源质量门（新风险 `quality_blocked`、`retry_queued` → `retry_pending` 风险与补救命令） + `config.yaml default_policy`（`policyForWorkItem` 唯一解析点，未绑定实例按它过门禁；非法或缺失时 claim fail closed；项目有策略文件时未绑定打 warning）+ 阶段门 `require_approval` 未满足报失效审批（`approval.InvalidatedFor` → `workflow.GateEvidence.ApprovalNote`：`approval-N was invalidated at <t> when the work item left "<status>" (方案 §4.9)`） + `project blueprint` 未声明 exit 0 + `no blueprint declared` + MCP `project_blueprint_get` 注册（session 档 / standard tier，返回 `artifact: null`） + `storage: version conflict` 经 `app.storeError` 补出路（重新读取重试 / `--latest`） + `workitem.ReadSnapshot` id 形状校验（非法 id → usage 错 exit 2） + `release.yml` + `scripts/build-release.sh` 严格校验 `checksums.txt`（拒 shasum `*`、要求 GNU 两空格、列全 dist 资产）+ `v0.1.0`（校验和格式教训，已重生成覆盖）与 `v0.1.1`（CI run 35496169845，7 资产）实跑。
source_commit: 2b8b8ce
generated: true
generator: repowiki-gen
---

# 开发与故障诊断

<cite>
**本文引用的文件**
- [README.md:13-128](file://README.md#L13-L128)
- [docs/开发记录.md:35-182](file://docs/开发记录.md#L35-L182)
- [internal/domain/models.go:8-24](file://internal/domain/models.go#L8-L24)
- [internal/domain/serialize.go:14-108](file://internal/domain/serialize.go#L14-L108)
- [internal/storage/serialize.go:35-95](file://internal/storage/serialize.go#L35-L95)
- [internal/storage/txn.go:179-247](file://internal/storage/txn.go#L179-L247)
- [internal/config/config.go:99-152](file://internal/config/config.go#L99-L152)
- [internal/config/validate.go:41-73](file://internal/config/validate.go#L41-L73)
- [internal/config/nested.go:8-29](file://internal/config/nested.go#L8-L29)
- [internal/project/tree.go:42-93](file://internal/project/tree.go#L42-L93)
- [internal/project/init.go:55-110](file://internal/project/init.go#L55-L110)
- [internal/storage/store.go:127-190](file://internal/storage/store.go#L127-L190)
- [internal/storage/txn.go:15-481](file://internal/storage/txn.go#L15-L481)
- [internal/storage/errors.go:6-10](file://internal/storage/errors.go#L6-L10)
- [internal/storage/recover.go:15-345](file://internal/storage/recover.go#L15-L345)
- [internal/storage/jsonl.go:22-95](file://internal/storage/jsonl.go#L22-L95)
- [internal/workitem/workitem.go:55-200](file://internal/workitem/workitem.go#L55-L200)
- [internal/workitem/claim.go:179-326](file://internal/workitem/claim.go#L179-L326)
- [internal/workitem/state.go:23-188](file://internal/workitem/state.go#L23-L188)
- [internal/workitem/propagate.go:28-225](file://internal/workitem/propagate.go#L28-L225)
- [internal/workitem/workflow_instance.go:108-381](file://internal/workitem/workflow_instance.go#L108-L381)
- [internal/workitem/m2_crash_test.go:71-166](file://internal/workitem/m2_crash_test.go#L71-L166)
- [internal/workitem/m3_*.go](file://internal/workitem/m3_propagation_test.go)
- [internal/workflow/parse.go:25-769](file://internal/workflow/parse.go#L25-L769)
- [internal/workflow/condition.go:57-264](file://internal/workflow/condition.go#L57-L264)
- [internal/workflow/gate.go:38-73](file://internal/workflow/gate.go#L38-L73)
- [internal/workflow/quality.go:33-96](file://internal/workflow/quality.go#L33-L96)
- [internal/workflow/cache.go:47-122](file://internal/workflow/cache.go#L47-L122)
- [internal/workflow/template.go:54-233](file://internal/workflow/template.go#L54-L233)
- [internal/approval/approval.go:101-533](file://internal/approval/approval.go#L101-L533)
- [internal/next/evaluate.go:113-316](file://internal/next/evaluate.go#L113-L316)
- [internal/reconcile/reconcile.go:155-380](file://internal/reconcile/reconcile.go#L155-L380)
- [internal/storage/inspect.go:24-96](file://internal/storage/inspect.go#L24-L96)
- [internal/cli/cli.go:361-1437](file://internal/cli/cli.go#L361-L1437)
- [internal/record/record.go:121-325](file://internal/record/record.go#L121-L325)
- [internal/events/events.go:44-264](file://internal/events/events.go#L44-L264)
- [internal/events/events.go:108-144](file://internal/events/events.go#L108-L144)
- [internal/search/search.go:20-127](file://internal/search/search.go#L20-L127)
- [scripts/smoke-m1.ps1:1-64](file://scripts/smoke-m1.ps1#L1-L64)
- [scripts/smoke-m1.sh:1-46](file://scripts/smoke-m1.sh#L1-L46)
- [scripts/smoke-m2.sh:1-106](file://scripts/smoke-m2.sh#L1-L106)
- [internal/cli/cli.go:361-1437](file://internal/cli/cli.go#L361-L1437)
- [internal/cli/cli.go:99-117](file://internal/cli/cli.go#L99-L117)（toCoded）
- [internal/cli/cli.go:124-133](file://internal/cli/cli.go#L124-L133)（writeJSONL）
- [internal/cli/mcp.go:48-84](file://internal/cli/mcp.go#L48-L84)（mcp serve）
- [internal/app/app.go:20-129](file://internal/app/app.go#L20-L129)（Service + Error + Class）
- [internal/app/wire.go:44-77](file://internal/app/wire.go#L44-L77)（AGENTS.md 写入）
- [internal/mcp/server.go:1-130+](file://internal/mcp/server.go#L1-L130)（profile 注册期过滤）
- [internal/mcp/tools.go:108-156](file://internal/mcp/tools.go#L108-L156)（toolError 翻译）
- [internal/record/update.go](file://internal/record/update.go)（ReadSnapshot + Update + AppendArtifactVersion + ErrSuperseded）
- [internal/run/run.go:156-185](file://internal/run/run.go#L156-L185)（Run.ReadSnapshot）
- [scripts/smoke-m4.{ps1,sh}](file://scripts/smoke-m4.ps1)（M4 端到端）
- [scripts/exchange-demo.sh](file://scripts/exchange-demo.sh)（交换协议 demo）
- [scripts/m4helper/main.go](file://scripts/m4helper/main.go)（SDK 客户端 helper）
- [go.mod](file://go.mod)（go-sdk v1.8.0）
- [internal/record/record.go:121-325](file://internal/record/record.go#L121-L325)
- [internal/events/events.go:44-264](file://internal/events/events.go#L44-L264)
- [internal/events/events.go:108-144](file://internal/events/events.go#L108-L144)
- [internal/search/search.go:20-127](file://internal/search/search.go#L20-L127)
- [scripts/smoke-m1.ps1:1-64](file://scripts/smoke-m1.ps1#L1-L64)
- [scripts/smoke-m1.sh:1-46](file://scripts/smoke-m1.sh#L1-L46)
- [scripts/smoke-m2.sh:1-106](file://scripts/smoke-m2.sh#L1-L106)
- [scripts/smoke-m3.sh:1-204](file://scripts/smoke-m3.sh#L1-L204)
- [internal/version/version.go:13-23](file://internal/version/version.go#L13-L23)
- [docs/开发记录.md:99-182](file://docs/开发记录.md#L99-L182)
- [internal/view/view.go:25-105](file://internal/view/view.go#L25-L105)（视图数据契约）
- [internal/view/build.go:33-115](file://internal/view/build.go#L33-L115)（Build 只读装配）
- [internal/view/collect.go:23-152](file://internal/view/collect.go#L23-L152)（共享锁 / advisory / 问题收集）
- [internal/view/view_test.go](file://internal/view/view_test.go)（确定性 / 零写入 / 只读副本）
- [internal/cli/workspace.go:24-228](file://internal/cli/workspace.go#L24-L228)（workspace view/build/serve 命令面）
- [.github/workflows/release.yml:1-131](file://.github/workflows/release.yml#L1-L131)（tag/dispatch 触发的发布工作流、6 平台矩阵、严格校验和自检、`gh release create`）
- [scripts/build-release.sh:1-50](file://scripts/build-release.sh#L1-L50)（矩阵构建 + sha256sum + GNU 文本格式自检）
- [scripts/install.sh:1-80](file://scripts/install.sh#L1-L80)（`gh` 鉴权下载 + 校验和容忍 `*` 标记 + 回退 `git clone --branch` + `go build`）
- [scripts/install.ps1:1-79](file://scripts/install.ps1#L1-L79)（同上 PowerShell 版 + `-AddToPath`）
- [Makefile:1-22](file://Makefile#L1-L22)（`build/install/smoke/release/clean` 五个 operator 入口）
- [docs/发布流程.md:1-88](file://docs/发布流程.md#L1-L88)（release runbook：触发方式、发版前检查、自动/手动/无 Actions 路径、v0.0/v0.1.0/v0.1.1 实跑记录、回退表、常见故障）
- [internal/cli/roots.go:1-46](file://internal/cli/roots.go#L1-L46)（`DEVSYS_PROJECT_ROOT` + 向上查找 `.devsys/` 解析项目根）
- [internal/config/config.go:212-214](file://internal/config/config.go#L212-L214)（`default_policy` 解码分支）
- [internal/app/next.go:60-110](file://internal/app/next.go#L60-L110)（`policyForWorkItem` 唯一解析点 + `default_policy` 缺失/工作项引用缺失策略报错）
- [internal/app/workitem.go:550-615](file://internal/app/workitem.go#L550-L615)（`gateEvidence` 收集阶段门证据，失效审批经 `ApprovalNote` 给出 `approval-N was invalidated at … (方案 §4.9)` 出路）
- [internal/app/workitem.go:447-485](file://internal/app/workitem.go#L447-L485)（`storeError` 把 `storage.ErrConflict` 翻译为「`--latest` 出路」、`workitem.ErrBadID` 翻 usage exit 2）
- [internal/app/skill.go:100-130](file://internal/app/skill.go#L100-L130)（`references/troubleshooting.md` 中 `--latest` / `quality_blocked` / `version conflict` / `default_policy` 文本口径）
- [internal/cli/cli.go:955-1007](file://internal/cli/cli.go#L955-L1007)（`workitem release` / `workitem start` 缺参打 usage；`approval list` 第二列走 `approvalState` 渲染 `consumed`/`invalidated`）
- [internal/cli/cli.go:1396-1440](file://internal/cli/cli.go#L1396-L1440)（`approvalState` 渲染 `consumed` / `invalidated` / `pending|approved|rejected`）
- [internal/cli/cli.go:630-655](file://internal/cli/cli.go#L630-L655)（`devsys project blueprint` 无声明走 `no blueprint declared (project.yaml blueprint_artifact_id is empty)`，exit 0）
- [internal/mcp/tools.go:35-37](file://internal/mcp/tools.go#L35-L37)（`project_blueprint_get` 注册到 session 档）
- [internal/mcp/server.go:31-75](file://internal/mcp/server.go#L31-L75)（`ProfileSession`/`ProfileExecutor` + `TierCore`/`TierStandard` + `ParseTier`）
- [internal/workitem/workitem.go:30-60](file://internal/workitem/workitem.go#L30-L60)（`ErrBadPrefix`/`ErrBadID` 与 `idPattern`）
- [internal/workitem/workitem.go:225-260](file://internal/workitem/workitem.go#L225-L260)（`ReadSnapshot` 走 `ErrBadID` 形状校验）
- [internal/workitem/state.go:120-145](file://internal/workitem/state.go#L120-L145)（租约内 `transition` 报错给出 `devsys workitem release --id … --owner … --token <token from .devsys/scheduling/<id>.yaml> …` 出路）
- [internal/next/evaluate.go:44-49](file://internal/next/evaluate.go#L44-L49)（新风险 `RiskQualityBlocked = "quality_blocked"` / `RiskRetryPending = "retry_pending"`）
## 目录
1. [开发环境](#开发环境)
2. [编码规范](#编码规范)
3. [测试](#测试)
4. [调试](#调试)
5. [发布](#发布)
6. [更新摘要](#更新摘要)

## 开发环境

Go 1.26 与 Git 是当前命令面所需环境。`vendor/` 已提交；干净机器 `GOFLAGS=-mod=vendor GOPROXY=off go build ./...` 即可离线构建（[README.md:120-123](file://README.md#L120-L123)）。升级 YAML 依赖时同步更新 `go.mod`、`go.sum` 与 `vendor/`；M4 起 vendored 依赖新增 `github.com/modelcontextprotocol/go-sdk v1.8.0`（官方 Go SDK，[go.mod](file://go.mod)）。`gopkg.in/yaml.v3` v3.0.1 仍唯一与 YAML 相关的直接依赖。

## 编码规范

### Project 模型与受管文件白名单必须同步演进

M1 起 `internal/domain` 统一八类模型与序列化，`project.yaml` 白名单已扩展为完整 `Project`（[internal/domain/models.go:8-24](file://internal/domain/models.go#L8-L24)）；`config.Load` 通过 `domain.DecodeYAML` 解析为 `*domain.Project`（[internal/config/config.go:142-152](file://internal/config/config.go#L142-L152)）。新增字段必须同步三处：

1. `internal/config/validate.go` 的 `projectSpec` 等白名单（[internal/config/validate.go:41-57](file://internal/config/validate.go#L41-L57)）+ 嵌套结构对应 `fieldKind` 与 `checkNested` 分支（[internal/config/validate.go:17-25](file://internal/config/validate.go#L17-L25)、[internal/config/nested.go:18-26](file://internal/config/nested.go#L18-L26)）。
2. `internal/domain/models.go` 中对应的 struct（M1 起 `domain.Project` 是唯一来源；config/init/CLI 不再重复定义 Project）。
3. `internal/project/tree.go` 的占位 builder（[internal/project/tree.go:60-66](file://internal/project/tree.go#L60-L66)），通过 `domain.EncodeYAML` 序列化保证字段顺序与 struct 一致。

`config.Load` 在写命令前置校验会把第三处漏改的字段立刻暴露为"未知键"；`domain.DecodeYAML` 先调用 `storage.CheckSchemaVersion`，再调用 `storage.DecodeYAML`（开启 `KnownFields(true)` 拒绝未知字段）——schema_version 校验在严格解码**之前**进行（[internal/domain/serialize.go:73-78](file://internal/domain/serialize.go#L73-L78)、[internal/storage/serialize.go:35-53](file://internal/storage/serialize.go#L35-L53)）。`config.yaml` 在 `configSpec`（[internal/config/validate.go:59-77](file://internal/config/validate.go#L59-L77)）下接受 `schema_version`（必填）+ `workspace_root`（可选，§4.8）+ `dispatch_command`（可选，M6.4）+ `knowledge_pages`（可选，M5.1）+ `knowledge_generator`（可选，M5.3）+ `default_policy`（可选，方案 §4.7/§5.3——未绑定实例工作项的门禁解析点；为空时与该键不存在行为一致，工作项若无 instance 也无默认策略则 `claim` 打 `warning:` 但放行；指向不存在的策略文件 → 风险 `config.yaml default_policy %q has no .devsys/workflows/%s.md`）。状态文件 `state/current.yaml`（`summary/risks/blockers/next_focus`）与 `state/milestones.yaml`（`milestones` 序列）已被收紧（[internal/config/validate.go:85-100](file://internal/config/validate.go#L85-L100)）。

### Workflow 策略白名单必须同步演进（M3 新增）

`internal/workflow.Parse` 用 `knownTopLevel` 与各段嵌套 `mapping(known)` 静态校验（[internal/workflow/parse.go:25-31](file://internal/workflow/parse.go#L25-L31)）。新增字段必须同步三处：

1. `internal/workflow/parse.go` 的 `knownTopLevel` 或嵌套 `mapping(known)`（新增字段 + 类型 + 必填 + 形状校验 + 状态/hook 等封闭枚举校验）。
2. `internal/workflow/policy.go` 的对应 struct（让 `Parse` 能解码、让 `Load` 能产出 `Policy`）。
3. 示例 `docs/examples/workflows/*.md`（端到端 `smoke-m3` 覆盖）。

写命令前置校验通过 `workflow.Load` 把 issue 转 `config.Problem`，问题定位保持 `file[:line][: field]: reason` 一致风格。**未知键**（任意层级）= warning（记录、加载成功、`workflow check` 仍 exit 0）；**结构/类型/必填/交叉引用错误** = error（该文件 Policy 为 nil，exit 4）。值域失败（hook 名 / stage / `on_reject` 格式）一律 fail-closed，防止拼错门禁/钩子被静默丢弃（M3Reviewer ②）。

### M4 共享应用服务边界（M4 新增）

所有业务规则在 `internal/app/Service` 收口；CLI 与 MCP 都消费同一份。**写方法**统一接受 `expect`（来自对应 `Get*` 的 64-hex `Version`），写锁内通过 `Tx.ReadForExpect` + `bytes.Equal(raw, expected)` 守卫。**读方法**走 `Store.Read` 共享锁。**只读聚合**（Session/Context/Next/KnowledgeStatus）走 `reconcile.Doctor` + `workflow.Load` + `next.Evaluate`，不创建锁、不触发恢复、不可信状态不输出业务事实。错误统一抛 `*app.Error{Kind,Message,Problems}`：`KindUsage / KindPrecondition / KindInternal / KindInvalid / KindWorkflow / KindGate / KindQuality / KindWorkitem / KindApproval`（9 种）；`Class()` 归一到 4 类（usage / precondition / internal / invalid），CLI `toCoded` 与 MCP `fail` 都从这里出发。**禁止**「CLI 能做 MCP 不能做」的特例——任何新增业务方法必须同时供 CLI 与 MCP 调用，PR review 拒绝例外。

CLI 薄化（[internal/cli/cli.go](file://internal/cli/cli.go)）：每个 `runXxx` 内只做三件事——解析 flag → `appService()` + `requireProjectRoot()` → `svc.Xxx(...)`。**不要**直接调 `internal/{workitem,approval,workflow,...}` 业务包。

MCP 服务（[internal/mcp/server.go](file://internal/mcp/server.go)）：每个工具处理器是 4–6 行的薄适配，业务规则全在 `internal/app`。`NewServer` 在**注册期**过滤——未注册的工具既不出现在 `tools/list`，也无法以名称调用（[internal/mcp/server.go:91-105](file://internal/mcp/server.go#L91-L105)）。

### 领域序列化保留 null 与空集合的差异

`yaml.v3` 默认把 nil slice 编为 `[]`，丢失"未记录"与"空集合"的区别；`domain.EncodeYAML` 在编码前用 `preserveNulls` 走一遍节点，区分 `null` / `[]` / 缺省（[internal/domain/serialize.go:14-52](file://internal/domain/serialize.go#L14-L52)）。`DecodeYAML` 配套使用 `storage.DecodeYAML` 的 `KnownFields(true)` 拒绝未知字段，并在调用严格解码**之前**用 `storage.CheckSchemaVersion` 校验版本（[internal/domain/serialize.go:73-78](file://internal/domain/serialize.go#L73-L78)、[internal/storage/serialize.go:35-53](file://internal/storage/serialize.go#L35-L53)）。

### 分清有效状态读取与诊断

业务一致性读取使用 `Store.Read`，跨文件提交使用 `Store.Write`：读取遇到待恢复事务先恢复，写入先取独占锁并恢复（[internal/storage/store.go:140-190](file://internal/storage/store.go#L140-L190)）。`config.Diagnose` 故意直接读取、不加锁、不恢复，适用于报告无效或未知版本元数据，不能用它替代有效状态查询（[internal/config/config.go:101-104](file://internal/config/config.go#L101-L104)）。

`init` 先调用 `config.Load` 校验既有受管文件，再补建目录并用单文件原子写生成占位；它**没有**把整个初始化过程包装为 `Store.Write`（[internal/project/init.go:55-110](file://internal/project/init.go#L55-L110)）。调用者不要推导出未实现的全局事务保证。

M3 起 `devsys next` 与 `workflow next` / `workflow check` 也是只读：经 `reconcile.Doctor` 或 `workflow.Load` 收集信息，不持锁、不触发恢复、不可信状态不输出业务事实（§15.4）。

M4 起 `devsys session start` / `context *` / `knowledge status` 同样只读；MCP 服务端只暴露只读工具族 (`session` profile)。`devsys wire` 是 CLI-only operator 工具（无 MCP 对应），由 `app.Wire` 实现幂等注入 AGENTS.md。

### 乐观更新与 CAS 分配：守卫源于读快照原始字节

`Reader.Expect`（[internal/storage/store.go:127-138](file://internal/storage/store.go#L127-L138)）只返回两种守卫：目标缺失时 `ExpectAbsent()`，目标存在时 `ExpectHash(HashBytes(data))`——`ExpectAny()` 是 `Expect{}` 的零值，由调用方在知道不关心版本时直接构造（[internal/storage/txn.go:24-37](file://internal/storage/txn.go#L24-L37)）。后续更新若文件被别人改写则 `commitLocked` 在写锁内重读并校验失败（[internal/storage/txn.go:258-280](file://internal/storage/txn.go#L258-L280)）。**文件锁解决协作进程的本机访问协调，不等于 Git 跨设备同时领取仲裁**——设备交接前停写（[README.md:126-128](file://README.md#L126-L128)）。

- **CAS 分配**：`workitem.Create` / `run.Create` / `record.create` / `approval.nextNumber` 共享同一形态：写事务内 `nextNumber` 扫目录 `max+1` → `PutYAML(..., ExpectAbsent())`；`ErrConflict` 即 `continue` 重扫，最多 5 次（[internal/workitem/workitem.go:55-88](file://internal/workitem/workitem.go#L55-L88)、[internal/run/run.go:51-92](file://internal/run/run.go#L51-L92)、[internal/record/record.go:124-153](file://internal/record/record.go#L124-L153)、[internal/approval/approval.go:405-421](file://internal/approval/approval.go#L405-L421)）。`SchemaVersion == 0` 在写事务内改写为 `domain.SchemaVersion=1`。
- **乐观更新**：`Update` 把 `ReadSnapshot` 返回的 raw bytes 原封不动传回 `Write`，由 `ExpectHash(HashBytes(expected))` 守卫（[internal/workitem/workitem.go:156-164](file://internal/workitem/workitem.go#L156-L164)、[internal/workitem/workitem.go:169-195](file://internal/workitem/workitem.go#L169-L195)）；并发写抛 `ErrConflict`，**永不**被静默覆盖。
- **Artifact 不可变链**：`NewArtifactVersion` 用 `tx.ReadForExpect` 在写锁内取当前字节 → `*next = *cur` 浅拷贝 → 重置 `next.ID/Version/PreviousID` → `PutYAML` 写**新文件**（[internal/record/record.go:280-325](file://internal/record/record.go#L280-L325)）。现有版本文件**永不**被改写；`ArtifactHistory` 沿 `previous_id` 反查，环检测用 `seen[cursor]` → `ErrBadID`。M3 起 `record.ListArtifacts`（[internal/record/record.go:243-258](file://internal/record/record.go#L243-L258)）按 ID 升序列出全部 artifact 版本，门禁证据收集从中提取 `Name` 集。
- **事件流追加**：`events.Append` 用事件自身 UTC 时间派生 shard（12 月事件 1 月写仍落 12 月分片），ID 由时间戳 + 4 字节 `crypto/rand`（十六进制输出为 8 hex）派生（M3Reviewer2 ①），**无全局计数器**（[internal/events/events.go:44-93](file://internal/events/events.go#L44-L93)、[internal/events/events.go:150-157](file://internal/events/events.go#L150-L157)）。写路径只校验 `reply_to` 必须是 comment 类型（[internal/events/events.go:69-71](file://internal/events/events.go#L69-L71)）；subject 一致性与二级回复（parent 也是 reply）由 `Comments` 聚合阶段校验（[internal/events/events.go:240-264](file://internal/events/events.go#L240-L264)）。
- **M3 多事件同事务**（[internal/storage/txn.go:179-199](file://internal/storage/txn.go#L179-L199)）：`Tx.AppendJSONLRaw` 接收多行 JSONL payload（拒绝空行、CR、缺终止符），`events.AppendBatchTx` 按 shard 分组一次 append。M3 起 `workitem.changeStatus` 提交批 = `status_changed` + Guard 事件 + 失效事件 + 传播事件，每个 shard 仅一次 append（[internal/workitem/state.go:165-182](file://internal/workitem/state.go#L165-L182)）。
- **M3 事务内已暂存查询**（[internal/storage/txn.go:241-247](file://internal/storage/txn.go#L241-L247)）：`Tx.Staged(rel)` 用于 `approval.InvalidateForStatusTx` 跳过本事务已被消费路径写过的文件，避免 `staged twice` 断言。

### M2 写路径契约：状态、领取与修复

状态变更**只**经 `workitem.Transition` / `ApplyRepair`：`Update` 拒绝状态字段变更，租约活跃时普通 `Update` 被阻断（元数据改用 `UpdateClaimed`）；所有状态与领取变更要求 raw 快照作 `Expected`（CLI 的 `--expect` 版本哈希由 `expectedSnapshot` 校验后转字节），旧视图直接 `ErrConflict`/拒绝（[internal/workitem/state.go:46-59](file://internal/workitem/state.go#L46-L59)、[internal/cli/cli.go:493-511](file://internal/cli/cli.go#L493-L511)、[internal/workitem/workitem.go:169-200](file://internal/workitem/workitem.go#L169-L200)）。事件与记录必须同事务：`events.AppendTx`（[internal/events/events.go:95-110](file://internal/events/events.go#L95-L110)）。修复路径额外要求 `Confirmation`（计划摘要）与 `Guard`（写事务内复核全部证据），`ApplyRepair` 缺任一即拒绝（[internal/workitem/state.go:53-59](file://internal/workitem/state.go#L53-L59)）。

### M3 写路径契约：Guard / 失效 / 传播 / 工作流实例（M3 新增）

`workitem.changeStatus`（[internal/workitem/state.go:66-188](file://internal/workitem/state.go#L66-L188)）在 M2 单一事务基础上承担四件事：

1. **租约 fencing**（M2 沿用）：租约活跃时要求 `Token/Owner/RunID/LeaseUntil` 全部匹配。
2. **Guard**（M3 新增，[internal/workitem/state.go:128-134](file://internal/workitem/state.go#L128-L134)）：`req.Guard(tx) ([]*domain.Event, error)` 在事务内、状态字段改写之前被调用。典型 `approval.ConsumeTx` 在 Guard 内把门禁所依赖的审批标记为 consumed 并返回 `approval_consumed` 事件；`ApplyRepair` 的 Guard 走 `reconcile.RepairApply` 的证据复核路径，不返回事件。Guard 返回事件与 `status_changed` 同批 `AppendBatchTx`。
3. **审批失效**（M3 新增，[internal/workitem/state.go:136-141](file://internal/workitem/state.go#L136-L141)）：离开 `from` 状态时 `approval.InvalidateForStatusTx` 把同源未消费、非失效、非 rejected 的审批标记为 `invalidated_at`，事件并入提交批；通过 `Tx.Staged(rel)` 跳过本事务已暂存文件。
4. **记录传播**（M3 新增，[internal/workitem/state.go:175-181](file://internal/workitem/state.go#L175-L181)）：仅在目标 `done` 且来自非 `done` 时触发 `propagateRecordsTx`（[internal/workitem/propagate.go:28-225](file://internal/workitem/propagate.go#L28-L225)），扫描 `decisions/` 与 `findings/` 中 `RelatedWorkItems` 命中的记录，把 `decision://<id>` / `finding://<id>` 追加到父与直接兄弟的 `context_refs`；无 parent 不广播；父子自环拒绝并回滚。

`workflow_instance.go` 五类实例操作（[internal/workitem/workflow_instance.go:108-381](file://internal/workitem/workflow_instance.go#L108-L381)）共用 `workflowWrite`：`ReadForExpect` → `bytes.Equal` 校验 `--expect` → 租约活跃要求 `Owner + Token` fencing → `mutate` 重写 `wi.Workflow` → `PutYAML(ExpectHash)` + `AppendTx` 事件。**不**写工作项状态、不写调度态/租约/`scheduling/`；目标步骤声明 `status` 时当前状态必须匹配（不隐式改状态）；`paused` / 终态 / `--to` 命中声明且条件成立且不等于当前步均给出明确拒绝面。

### M4 写路径契约：`app.Service.*` 期望守卫 + `ErrSuperseded`（M4 新增）

M4 把所有业务写路径迁出到 `internal/app/`，CLI 与 MCP 共用。每个写方法（`WorkitemUpdate / WorkitemTransition / DecisionUpdate / ArtifactUpdate / RunUpdate / ...`）接受 `Expect string`（来自对应 `Get*` 的 64-hex `Version`），在内部：

1. 解析 hex → 32 字节。
2. 调 `svc.<Store>.Read<Kind>(ctx, id)` → `(*T, []byte, error)`（共享 `readSnapshot[T]` 泛型 或 `Run.ReadSnapshot`）。
3. 把 raw 字节在写锁内做 `bytes.Equal` 校验。
4. 写事务内 `st.Write` → `tx.ReadForExpect(rel)` 再校验 → `ExpectHash(HashBytes(expected))` 守卫。

`record.UpdateDecision / UpdateFinding` 同形。`record.AppendArtifactVersion(currentID, expected, mutate)` **额外**检查 `currentID` 是否有子版本——若有则返回 `ErrSuperseded`（[internal/record/update.go:75-100+](file://internal/record/update.go#L75-L100)），要求 re-read head 再 append。应用服务把 `ErrSuperseded` 包装为 `*app.Error{Kind: KindInvalid}` → CLI exit 4 + stderr「artifact already has a newer version; re-read and retry」、MCP toolResult `isError=true` + `code=invalid`。

CLI 的 `--expect <64-hex>` 与 MCP 工具的 `expect` 字段在 `internal/app` 内被反解码为同一份原字节传给 storage 层——保证两入口的乐观并发语义一致。

### M7.1 视图层只读契约（M7.1 新增，M7.2–M7.4 扩展渲染面）

视图层（`internal/view`）是只读聚合，不参与任何写路径，四条硬约束：

1. **只经 `storage.Inspect` 读状态**：先取既有锁文件上的共享锁，缺锁文件时退回 `storage.InspectUnlocked` 并标 `advisory_unlocked`——**不**创建 `.devsys/local/lock`（[internal/view/collect.go:23-29](file://internal/view/collect.go#L23-L29)），也**不**调 `storage.Read`（它会建锁并写 `.devsys/.cache/`）。
2. **不替调用方做修复**：不恢复事务、不修断尾事件流（只报 `StreamTorn`）、不起生成器进程（[internal/view/build.go:435-443](file://internal/view/build.go#L435-L443)）——修复属于 `devsys recover` / 写入侧 / `devsys knowledge refresh`。
3. **确定性**：模型不带墙钟字段，`Options.Now` 只喂时钟；列表先定序后截断（`capList`）；`Sources` 走 `sortedUnique`——同一状态两次构建逐字节相同（[internal/view/build.go:750-755](file://internal/view/build.go#L750-L755)、[internal/view/collect.go:127-139](file://internal/view/collect.go#L127-L139)）。
4. **错误分层**：只有缺 `.devsys/project.yaml` 才算整体错误；其余缺陷进顶层 `problems` 并只降级对应节（[internal/view/collect.go:142-152](file://internal/view/collect.go#L142-L152)）。

增删视图字段时同步更新[视图层 · 视图数据模型](../knowledge/视图层/视图数据模型.md)——M7.2 静态构建 / M7.3 `serve` 按该 JSON 契约消费（`data/model.json` 与 `--json` 同一模型）；M7.4 新增 hint 行/横幅是纯渲染，不进模型。

## 测试

仓库既有验收命令：

```sh
gofmt -l cmd internal
go vet ./...
go test ./...
```

冒烟剧本用 `DEVSYS_CONFIG_DIR` 把"全局注册表"隔离到临时目录；修改配置后应关注多错定位、未知版本拒写与缺失文件补建；修改存储协议后应关注崩溃恢复、重复重放、版本竞争和锁冲突，而不只检查序列化结果。

M2 回归与端到端：`internal/workitem/m2_test.go` / `m2_extra_test.go`（转换图、并发领取单赢家、token fencing、租约活跃期写拦截）、`m2_crash_test.go`（真实子进程中断，覆盖提交点两侧）、`internal/reconcile/reconcile_test.go`（doctor 只读不改文件、摘要/证据漂移拒绝、恢复幂等）；端到端剧本 `scripts/smoke-m2.ps1` / `scripts/smoke-m2.sh`（领取 → 崩溃 → doctor → recover → 确认修复）。

M3 回归与端到端：`internal/workflow/*_test.go`（解析、条件白名单、门禁，质量门、LKG、模板/环境变量）、`internal/approval/approval_test.go`（请求/决定/消费/失效/拒绝事务编排）、`internal/next/evaluate_test.go`（PASS/CONCERNS/FAIL 与 §7.4 推荐）、`internal/workitem/m3_workflow_test.go` / `m3_propagation_test.go` / `m3_approval_guard_test.go`（实例操作、Guard、传播、失效）；端到端剧本 `scripts/smoke-m3.ps1` / `scripts/smoke-m3.sh`（七段 PASS）。

M4 回归与端到端：`internal/app/*_test.go`（Service 各方法、`*app.Error` 分类、`Class()` 归一）、`internal/mcp/server_test.go`（SDK server 注册期 profile 过滤、`tools/list` 暴露/隐藏、`toolError` 翻译）、`internal/cli/wire_test.go`（`devsys wire` 三次幂等 + 手写保留）、`internal/cli/session_test.go`（`session start` 一体化输出）、`internal/cli/exchange_test.go`（`--jsonl` 各行一条 + 字段名一致性）、`internal/cli/mcp_test.go`（`mcp serve` 启动与 profile）；`internal/record/update_test.go`（`readSnapshot[T]` + `AppendArtifactVersion` + `ErrSuperseded`）、`internal/run/run_test.go`（`Run.ReadSnapshot`）；端到端剧本 `scripts/smoke-m4.{ps1,sh}`（MCP 表面 / CLI-MCP 写一致 / session start / `--jsonl` 流 / wire 幂等 五段 PASS）；`scripts/exchange-demo.sh` 演示 CLI 端读取与 `--jsonl` 流的实际使用。


章节来源：[README.md:31-93](file://README.md#L31-L93)、[docs/开发记录.md:59-74](file://docs/开发记录.md#L59-L74)、[docs/开发记录.md:99-182](file://docs/开发记录.md#L99-L182)。

### `smoke-m1.ps1` / `smoke-m1.sh`（M1.7 完整端到端）

两个剧本从仓库根构建 CLI 与 helper，在新临时 Git 项目中跑：初始化 → 创建任务 → 决策/发现/Artifact 三版本 → 评论回复 → Run + ContextSnapshot → ReadSnapshot/Update → config check/search → 回读验证。Go 与 Git 必须在 PATH。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-m1.ps1 -Keep
```

```sh
bash scripts/smoke-m1.sh --keep
```

成功结束两个脚本都打印 `PASS: M1 create -> record -> complete; all tracked state is UTF-8 text.`（[scripts/smoke-m1.ps1:52](file://scripts/smoke-m1.ps1#L52)、[scripts/smoke-m1.sh:46](file://scripts/smoke-m1.sh#L46)）。`--keep`/`-Keep` 仅保留 `demo-project`，清掉构建产物与临时注册表，并打印 `KEPT_PROJECT=<path>`。`scripts/smoke-m1-helper.go` 同时承担 seed（无 `--verify`）与回读（`--verify`）两件事：seed 按工作流顺序调用 `workitem.Create` → `events.Append` → `record.*` → `run.Create` → `events.Append(comment + reply)`；`--verify` 用 `ReadSnapshot` / `GetArtifact` / `ArtifactHistory` / `Comments` 反向验证，并逐文件 UTF-8 + 非 NUL 校验，`local/` 与 `.cache/` 直接被拒。

Bash 版本检测 `uname -s` 命中 `MINGW*` / `MSYS*` 时自动把 `/c/Program Files/Go/bin` 顶到 `PATH` 头部（[scripts/smoke-m1.sh:6-8](file://scripts/smoke-m1.sh#L6-L8)）。

### `smoke-m3.ps1` / `smoke-m3.sh`（M3 端到端七段）

```sh
bash scripts/smoke-m3.sh --keep
```

七段 PASS：

1. 准备策略与三任务（parent/sibling/child，child 挂 quick-fix，链接 decision + finding）。
2. done 门禁拦截：`workitem transition --to done` exit 4 + `artifact "test-results" is required`。
3. `m3helper evidence` 创建 `test-results` artifact + comment → 推进 done，child 状态 done、父/兄弟 `context_refs` 含 `decision://<id>` + `finding://<id>` 各 1、`record_propagated` 事件 1。
4. `m3helper lowq` 创建 thin 任务（标题"fix"、描述"x"）→ claim exit 4 + `quality_gate.min_score` + 改进项；`m3helper improve` 补足后领取成功。
5. last-known-good：成功 claim 落快照 → 注入坏策略（缺 `id` / `version: "x"`）→ claim 拒绝（exit 4 + `is invalid` + `last-known-good`），`next` 报 `invalid_policy` 风险，`workflow next` 含 `last-known-good` 提示；恢复策略后 claim 成功。
6. 工作流实例：thin 策略（`flow.md`：inspect → implement）→ `workflow start` → `workflow next` 列出 `to=implement` → `--to nope` exit 4 + `not a declared transition` + `allowed next steps` → `--to implement` 成功 → `workflow pause` → `step-complete` exit 4 + `paused` → `workflow resume` → 推进；`.devsys/scheduling/` 哈希前后一致（实例操作不动调度态）。
7. 审批：thin 策略（`approval-flow.md`：in_progress 阶段 `require_approval: true`）→ transition 拒绝 → `approval request` → `approval approve` → transition 成功 + `consumed_at` + `approval_consumed` 事件 → `approval reject --id approval-2` → 工作项 `blocked` + `approval-2 rejected: risk too high`。

`scripts/m3helper/main.go`（[scripts/m3helper/main.go](file://scripts/m3helper/main.go)）是 seed + 回读双模：`setup` / `lowq` / `evidence` / `improve` / `status` / `propagated` 六个子命令按 KEY=VALUE 输出。

### `smoke-m4.{ps1,sh}`（M4 端到端五段）

M4 端到端验证 MCP 服务、CLI/MCP 写一致、session start、`--jsonl` 流与 wire 幂等（[scripts/smoke-m4.sh:1-50](file://scripts/smoke-m4.sh#L1-L50)）。`scripts/m4helper/main.go` 是 SDK 客户端 helper（用同一份官方 Go SDK 作为客户端），按 KEY=VALUE 输出。

```sh
bash scripts/smoke-m4.sh --keep
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-m4.ps1 -Keep

# scripts/exchange-demo.sh 演示 CLI 端读取 + --jsonl 流（无需外部脚本）
bash scripts/exchange-demo.sh
```

五段 PASS：

1. **MCP 表面**：默认 `session + executor` profile 应暴露 `health` / `workitem_list` / `workitem_get` / `workitem_next` / `decision_get` / `finding_get` / `event_list` / `artifact_list` / `artifact_history` / `run_list` / `run_get` / `run_log` / `approval_list` / `approval_get` / `workflow_list` / `workflow_get` / `workflow_step_next` / `context_get` / `context_for_workitem` / `context_refresh` / `context_compact` / `agent_session_start` / `knowledge_status` / `decision_create` / `decision_approve` / `finding_create` / `finding_resolve` / `event_record` / `artifact_register` / `artifact_update` / `run_create` / `run_update` / `run_heartbeat` / `approval_request` / `workflow_start` / `workflow_step_complete` / `workflow_pause` / `workflow_resume` / `workflow_cancel` 等；隐藏 `project_create` / `project_update` / `project_state_update` / `approval_decide` / `decision_approve` (reviewer profile 才暴露)。`m4helper` 调 `health` / `decision_create` / `agent_session_start` 做端到端验证。
2. **CLI/MCP 写一致**：MCP 创建的 decision → CLI `--json decision get` 读到同一个 `version`（`m4helper` 输出 `id` + `version`，CLI 用同 ID 读取断言 `cli_version` 非空）。
3. **会话接口**：单次 `session start` 返回带推荐动作的视图（`m4helper` 在 step 1 已调用）。
4. **交换协议**：`--jsonl workitem list` / `event list` 各行一条 JSON 记录，字段名与 `--json` 信封同名数组一致；`scripts/exchange-demo.sh` 演示完整读取流（`m4helper` 验证两条 workitem 创建后 `--jsonl` 输出两行非空 JSON）。
5. **wire 幂等**：三次 `devsys wire` 后 AGENTS.md 字节级一致；`--dry-run` 预览 diff 不写盘（`m4helper` 不专门测 wire，但 `internal/cli/wire_test.go` 是 acceptance：手写 AGENTS.md 含 `repowiki:begin/end` 与「保留我」段落，`wire` 后手写 + repowiki 块逐字保留；仅 `devsys:begin/end` 存在；连续 3 次 `wire` 字节级一致）。


### 搜索正确性与性能测量

`TestSearchThousandFilesResponsive` 验证千文件命中数量及准确位置，记录耗时但不再设墙钟硬断言；不跳过 Windows（[internal/search/search_test.go:102-111](file://internal/search/search_test.go#L102-L111)、[internal/search/search_test.go:165-177](file://internal/search/search_test.go#L165-L177)）。此前一次全量测试耗时 3.44s 超过原 2s 门限，不能据此认定 Defender 是根因。

性能独立运行：`go test ./internal/search -run '^$' -bench BenchmarkSearchThousandFiles -benchmem -benchtime=3x -count=1`。`FreshFiles` 测新建文件后的首次扫描；`RepeatedFiles` 测同一批文件预读后的重复扫描。数据构造、结果校验及清理排除在计时外；不清空系统缓存。固定环境再比较，2s 保留为交互目标而非任意机器的通过保证（[internal/search/search_test.go:113-144](file://internal/search/search_test.go#L113-L144)）。本轮本机各 3 次平均为 654ms/op 与 12.1ms/op，结果不代表生产算法已优化。

**既有验证边界（不是本次 Wiki 生成的结论）**：开发记录注明本机缺 gcc，未运行 race 检测（[docs/开发记录.md:35](file://docs/开发记录.md#L35)）；macOS/Linux 仅交叉编译、未实机运行（[docs/开发记录.md:37](file://docs/开发记录.md#L37)）；进程中断验收不证明断电持久性（[internal/storage/errors.go:6-10](file://internal/storage/errors.go#L6-L10)、[docs/开发记录.md:36](file://docs/开发记录.md#L36)）。

视图层（M7.1–M7.4）的测试口径与上面同源但更严：固定时钟（`Options{Now: fixedClock}`）+ 两次 `json.Marshal` 逐字节比对（`TestBuildIsDeterministic`）+ 全树文件指纹前后一致（`TestBuildWritesNothing`）+ 整树 0444 的只读副本上仍出完整模型且不建 `local/lock`（`TestBuildOnAReadOnlyCopy`）；pending 事务、断尾事件流、单节损坏、`--limit` 截断各有专门用例（[internal/view/view_test.go](file://internal/view/view_test.go)）；M7.2–M7.4 追加 sitestatic（离线零命中、RenderPage 与 Build 逐字节一致、横幅）与 serve（405 全方法、绑定姿态）用例。M7 收尾 `scripts/smoke-m7.{sh,ps1}` 双平台 exit 0（Git Bash / PowerShell 实跑，断言口径见 [视图层 · 特殊配置与命令](../knowledge/视图层/特殊配置与命令.md#端到端验证)）。

## 调试

| 现象 | 检查路径 |
|---|---|
| `init --json` 返回用法错误 | 全局参数必须前置，改为 `--json init` |
| 非 Git 目录或仓库子目录不能初始化 | 到 Git 根目录执行；`gitRoot` 与 `requireSameDir` 在 [internal/project/init.go:55-142](file://internal/project/init.go#L55-L142) 把关；不要手动绕过根目录前置条件 |
| 配置问题一次输出多条 | 按 `file[:line][: field]: reason` 处理，保留未识别版本文件原样 |
| 未知 `schema_version` | 只读诊断（`devsys config check`）仍可运行并逐条定位（exit 4）；写命令在任何改动前拒绝（exit 4 + `migration must be explicit (实施计划 M9.2)`，零改写）；迁移指引见 `docs/迁移指南.md`，升级/回退流程见 `docs/使用手册.md`（bundle 外用户文档，不做跨包链接） |
| 恢复失败 | 保留 `.devsys/local/txn/` 中日志与 payload；它们不是可随意清理的缓存 |
| Windows 原子替换被目标句柄占用 | 检查长时间持有的读取句柄；实现已有有限重试（[internal/storage/atomic.go:60-63](file://internal/storage/atomic.go#L60-L63)），不代表可以无限等待 |
| `workitem` 转换返回 4 且 stderr 有 allowed 列表 | 目标不在状态图上：按 allowed 选择下一步；`blocked` 恢复需提供 `PreviousStatus` |
| `workitem` 写报 version mismatch | `--expect` 已过期（fail-closed）：重新 `workitem get` 取新版本再试 |
| `repair --apply` 返回 3（摘要不符） | 期间状态/证据已变：重新 `repair --dry-run` 取新 digest 再确认 |
| `doctor` 报 pending transactions | 先 `devsys recover` 完成确定性事务恢复，再信任业务状态 |
| 租约孤儿或过期 | `doctor` 只读列出；`recover` 释放并写 `lease_recovered`；引用缺失 workitem 的租约只报告不释放 |
| `workflow check` 返回 4 + `file[:line][: field]: reason` | 按定位修复 front matter；未知键 warning 不阻断（exit 0）；值域失败（hook 名 / stage / `on_reject`）一律 fail-closed |
| `workflow next` 在过渡中报 `last-known-good` | 当前策略文件非法或缺失，但有 LKG 快照：转换事务仍可推进；`claim` 与 `workflow start` 在策略非法时拒绝（不允许用 LKG 派发新任务） |
| `workflow step-complete --to <step>` 拒绝 + `allowed next steps` | 目标未声明 / 条件不满足 / 等于当前步骤；按列表选择；`paused` 实例拒绝推进 |
| `workitem claim` 报 `quality_gate.min_score` + 改进项 | 标题/描述/验收标准/引用/验收语言任一项不达标：按改进项补足（不修改 `policy.yaml`） |
| `workitem transition` 报 `an approved, unconsumed approval is required` | 策略 `gates.stages[<stage>].require_approval: true` 且无匹配审批：先 `approval request --stage <stage>` → `approval approve --id <approval>`，再重提 transition；离开 `from` 状态后旧审批失效，需重新 request |
| `approval reject` 在 `scope=stage_gate` 后工作项未动 | `rejectStageGate` 失败：查看错误信息；审批保持 pending，状态未改（事务原子回滚） |
| `devsys next` 报 `FAIL` + `devsys recover` 修复项 | 待恢复事务阻塞业务事实：先 `devsys recover --actor … --reason …` |
| `devsys next` 报 `CONCERNS` + `invalid_policy` | 策略非法或工作项引用缺失策略：检查 `.devsys/workflows/*.md`；`workflow check` 看定位 |
| `devsys next` 报 `pending_approval` 风险 | 存在未决审批：决策（approve / reject）或工作项离开当前状态让其自动失效 |
| `devsys next` 推荐 `recover_claim` | 存在过期/孤儿租约：先 `devsys recover` 清理 |
| `devsys next` 推荐 `start` 但无 WLM | 当前没有 ready 工作项：检查 `next` 报告的 risks 是否把 ready 任务阻塞（blocked / pending approval / 检查受限） |
| `devsys next` 报 `quality_blocked` 风险 + 推荐 `workitem update` 命令 | ready 工作项的质量门没过：按 `next` 输出的补救命令补足标题/描述/验收标准/引用/验收语言（任一项），然后再 `claim`；`project status` 同源（[internal/next/evaluate.go:46](file://internal/next/evaluate.go#L46)） |
| `devsys next` 报 `retry_pending` 风险 | `retry_queued` 工作项在等重试窗口；先 `devsys next --queue retry` 看下次尝试时间，或直接 `workitem retry --id … --actor … --reason …` 推进 |
| `workitem claim` 报 `quality gate not satisfied`（exit 4） | 同上：先按 `next` / `claim` 输出里的 `workitem update` 命令补足字段；`devsys next` 用同源判定（[internal/app/next.go:60-110](file://internal/app/next.go#L60-L110)） |
| `workitem claim` 报 `default_policy %q has no .devsys/workflows/%s.md` | `config.yaml default_policy` 指向不存在的策略文件：建文件、把 `default_policy` 改名、或者清空 `default_policy` 让策略走实例绑定；`claim` 在 `default_policy` 非法或缺失时 fail closed |
| `workitem claim` 打 `warning:` 行（exit 0） | 未绑定实例且 `default_policy` 为空，但项目有 `.devsys/workflows/*.md`：门禁没跑（用户显式无门禁策略），不影响通过；如要门禁走绑定实例或 `default_policy` |
| `workitem transition` 报 `an approved, unconsumed approval is required (approval-N was invalidated at <t> when the work item left "<status>" (方案 §4.9); request a new approval with devsys approval request)` | 阶段门 `require_approval` 未满足且最近一次审批已失效：`devsys approval request --stage <stage> --id <workitem>` 重新申请 → `approval approve`，再重提 transition；同源回退（M3.7 起）也会触发（[internal/app/workitem.go:550-615](file://internal/app/workitem.go#L550-L615)、[internal/approval/approval.go:394](file://internal/approval/approval.go#L394)） |
| 写命令报 `storage: version conflict (the file changed after your read: re-read it and retry, or repeat with --latest where the command offers it)` | 乐观并发失败（`storage.ErrConflict`），由 `app.storeError` 翻译（[internal/app/workitem.go:460-477](file://internal/app/workitem.go#L460-L477)）；常见原因是 dispatch child 写 run 时 `run fail` 与当前命令争同一个文件。先 `devsys --json <kind> get <id>` 取新 hash 再 retry，或直接传 `--latest`（适用：`core` / project update / state-update / workitem update / transition / claim / release / start / block / complete / comment / dep / workflow *） |
| `workitem <sub>` / `workflow <sub>` 报 `invalid workitem id "<x>"`（exit 2） | id 不匹配 `^[A-Z][A-Z0-9_]*-[0-9]+$`：修正大小写、`-` 与数字段，或换路径；读 worker 也走同一形状校验（[internal/workitem/workitem.go:35, 230](file://internal/workitem/workitem.go#L35)） |
| `devsys project blueprint` 打 `no blueprint declared (project.yaml blueprint_artifact_id is empty)` | 项目**未声明**蓝图是正常态（exit 0）；如要绑定，先 `artifact create` 后把 ID 写进 `project.yaml` 的 `blueprint_artifact_id` |
| `devsys project blueprint` / `MCP project_blueprint_get` 报 `artifact ... not found` | `blueprint_artifact_id` 写了一个不存在的 ID：`devsys --json artifact get <id>` 看现状，或修正 `project.yaml` |
| `approval list` / `approval get` 第二列显示 `consumed` / `invalidated` | 审批已被消费 / 已失效（不是 `approved`）；不再可二次消费——盘面只读展示原始状态要看 `--json` |
| `workitem release` / `workitem start` 缺 `--id/--owner/--token/--actor/--reason` 任一即打 usage（exit 2） | 补全参数；token 取自 `.devsys/scheduling/<id>.yaml` |
| `workitem transition` 在租约活跃期间被拒，stderr 含 `release the claim before leaving execution: devsys workitem release --id <id> --owner <owner> --token <token from .devsys/scheduling/<id>.yaml> --actor <actor> --reason <reason>` | 状态变更要先把租约放掉；按提示命令 release 后再 transition（[internal/workitem/state.go:127](file://internal/workitem/state.go#L127)） |
| `scripts/install.sh` / `install.ps1` 报 404 / `checksum mismatch` / `checksum entry missing` | 私有仓库匿名下载 404 → 用 `gh auth login` 后重跑；旧版 `checksums.txt` 是 shasum `<hash> *<file>` 格式，安装脚本预过滤不识别 → 自动回退 `git clone + go build`（注意 fallback 走 `-ldflags "Version=$Tag"` 注入，所以 fallback 构建也报告正在装的 tag）；CI release (`v0.1.1`+) 是 GNU `<hash>  <file>` 格式 |
| `devsys --version` 显示 `0.1.0-dev` | 本地构建未注入 `-ldflags`；按 §发布 → 本地构建命令注入；fallback 构建进入克隆目录 + `-ldflags "Version=$Tag"` 自动注入 |

| `mcp serve` 启动后立即退出（exit 1） | 检查 `internal/mcp.Run` 错误：stdin/stdout 不是终端？`nopReadCloser` / `nopWriteCloser` 没传？`Config.Log` 指向 stderr 了吗？ |
| `tools/list` 缺工具 / `tools/call` 报 unknown tool | `devsys mcp serve --profile <list>` 检查：profile 拼错（默认 `session,executor`，加 reviewer/admin 需显式）；未知名字 → CLI exit 2 拒绝启动 |
| `wire` 不写盘 | 检查 `--dry-run` 是否被传；AGENTS.md 不存在时 `--dry-run` 不创建；确认 `.devsys/` 已初始化（`requireProjectRoot`） |
| `wire` 三次运行后报「malformed block」 | AGENTS.md 中存在多个 `devsys:begin/end` 块、仅有 begin 或仅有 end、嵌套。手工合并或删除重复块后重试 |
| `artifact update` 报 `ErrSuperseded` / 「artifact already has a newer version」 | `currentID` 已有子版本被并发 append；re-read head version 后再 append（应用服务在 `ErrSuperseded` 时返回此消息） |
| `--jsonl <list>` 输出 0 行（命令本应非空） | 命令结果就是空（零条记录合法）：用 `--json` 信封确认 `matches` / `decisions` 等数组真为空；区分"空列表"与"管道被截断" |
| `mcp serve` 抛 `unknown profile` 错误 | `internal/mcp.ParseProfiles` 拒绝未知名字：`unknown profile "foo" (expected session, executor, reviewer or admin)` |
| `session start --intent ""` 报 usage | `--intent` 必填非空；CLI 在 flag 解析阶段拦截（`fs.NArg` 或显式非空检查） |

M4 起事务与领取诊断新增命令入口：`devsys session start`（单次给出 SessionView）、`devsys context get|workitem|refresh|compact`（只读聚合；`compact` 模式裁短 payload）、`devsys knowledge status`（**M5 启用**：0/10/11 退出码与 `fresh`/`stale`/`missing` 三态裁决）、`devsys knowledge scan`（M5）、`devsys knowledge validate`（M5）、`devsys knowledge refresh --affected|--full [--force]`（M5，写 `run.json` 与 `refresh-scope.json`）；CLI 与 MCP 共用 `*app.Error` → `toCoded` / `fail` 翻译——脚本只信 `code` 字段。完整命令表（M4 增补 + M5）：`init` / `config check` / `search` / `project *` / `workitem *` / `decision *` / `finding *` / `event *` / `artifact *` / `run *` / `approval *` / `workflow *` / `context *` / `knowledge { status | scan | validate | refresh }` / `session start` / `wire` / `mcp serve` / `doctor` / `recover` / `repair` / `next`。

对账与修复的推荐顺序：`doctor`（只读盘点：待恢复事务、过期/孤儿领取、状态漂移提议）→ `recover`（确定性事务恢复 + 释放过期/孤儿领取；幂等，每次释放写 `lease_recovered`）→ `repair --dry-run` / `--apply --confirm <digest>`（先推断、再确认、后重写；摘要不符或证据漂移即拒绝且不写文件）（[internal/reconcile/reconcile.go:155-380](file://internal/reconcile/reconcile.go#L155-L380)）。

中断语义按提交点划分：commit 标记落盘前崩溃 → 事务丢弃、领取不可见、可重新领取；commit 后崩溃 → 决定已定、完整重放、其余领取者被 token fencing 拒绝——读者永不观察半提交状态。回归用真实子进程在指定阶段 `os.Exit` 注入（`storage.TxnStageHookForTest` 测试钩子，[internal/storage/txn.go:475-481](file://internal/storage/txn.go#L475-L481)、[internal/workitem/m2_crash_test.go:71-166](file://internal/workitem/m2_crash_test.go#L71-L166)）；端到端剧本 `scripts/smoke-m2.ps1` / `.sh`（[scripts/smoke-m2.sh:1-106](file://scripts/smoke-m2.sh#L1-L106)）。

### M3 工作流 / 审批 / next 诊断（M3 新增）

- **LKG 失效**：检查 `.devsys/.cache/workflows/<id>.md` 是否存在；可手动删除强制重建（缓存可重建）。
- **条件白名单报错**：策略编辑期就拒绝；运行时不会因条件语法失败而报错（M3Reviewer5 ②）。
- **门禁证据漂移**：门禁在事务外收集，M3 阶段 `checkTransitionGate` 注释明确 TOCTOU 窗口（M3Reviewer2 ④）；M4 提取为应用服务（`app.WorkitemTransition` 内 `evidenceForTransition` 同步收集）消除此窗口。
- **审批 `invalidated_at`**：离开 `from` 状态自动失效；M3.7 起同源回退也失效；需重新 `request`。
- **`pending_approval` 风险**：`next` 把存在未决审批的工作项加入 `review` 推荐类，理由 `waiting for <approval-id>`。
- **`workflow next` 在 paused 时不输出 `next:` 推荐**（M3Reviewer5 ⑤）：候选仍展示；`resume` 后再次 `next` 才会给推荐。
- **`workflowWrite` 在租约活跃时拒绝**：CLI 必须传 `--owner` / `--token`；无人持有租约时不强制。
- **`workflow check` warning vs error**：未知键 warning；结构/类型/必填/值域错误 error；非 `.md` 文件 warning `ignored`。
- **`next` 缺业务事实**：待恢复事务或缺锁文件时仅打印风险与修复项，不列 workitems（§15.4）。
- **审批 `scope=action` 无消费路径**：M3.7 显式记遗留；M16.2 接入；当前 `Request --scope action` 可成功但不会被自动消费。

### M4 MCP / 共享应用 / 写守卫 / wire / `--jsonl` 诊断（M4 新增）

- **`mcp serve` 启动失败 / IO 错误**：`internal/mcp.Run` 在 SDK `server.Run` 阶段报错；`nopReadCloser` / `nopWriteCloser`（[internal/cli/mcp.go:60-70](file://internal/cli/mcp.go#L60-L70)）必须包住进程 stdin/stdout——SDK 默认会关闭传入的流，没有 no-op 包装会让 CLI 退出后进程异常终止。`Config.Log` 必须指向 stderr（不是 stdout——stdout 是协议 traffic）。
- **`tools/list` 不暴露预期工具**：默认 profile 是 `session + executor`；`project_update / project_create / approval_decide` 等只在 admin / reviewer profile 下暴露。`scripts/m4helper` 验证默认 profile 应含 `health` / `workitem_list` / `decision_get` / `agent_session_start` / `knowledge_status` / `run_heartbeat` 并隐藏 admin / reviewer 工具。
- **`tools/call` 报 `toolError.code="invalid"` + `problems`**：CLI / MCP 都按 `code` 字段分支；`kind` 字段是显示用。`problems` 是 `config.Problem[]`——文件 `:行号: 字段: 原因`。
- **`record update / artifact update --expect` 不匹配**：CLI `--expect <hash>` 是 `decision_get / artifact_get / run_get` 在 `--json` 模式下 `version` 字段值（64-hex sha256 of raw bytes）；哈希不匹配 → exit 3（precondition）；CLI 内部会打印 `version mismatch: <id> changed since your read; rerun <get>`。MCP 工具：传 `expect` 字段（同样 64-hex）；错误在 `toolError.code="precondition"`。
- **`artifact update --id <n>` 报 `ErrSuperseded`**：`AppendArtifactVersion` 在写事务内检查 `hasSuccessor(currentID)`——若有子版本则返回 `ErrSuperseded` 并翻译为 `*app.Error{Kind: KindInvalid}`。CLI exit 4 + 错误说明「artifact already has a newer version; re-read and retry」；MCP `toolError.code="invalid"`。`m4helper` 验证 CLI/MCP 写一致（MCP `decision_create` → CLI `decision_get` 读到同一 `version`）。
- **`devsys wire` 三次运行不一致**：AGENTS.md 在三次 `wire` 之间被修改（人手或 hook 改写）；先恢复 AGENTS.md 到 `wire` 写入的版本再重试。M4 约定管理块标记 `<!-- devsys:begin ... -->` / `<!-- devsys:end -->` 必须配对；手写内容在区段外**逐字保留**（`internal/cli/wire_test.go:27-71` 是 acceptance）。
- **`devsys wire --dry-run` 不输出 diff**：AGENTS.md 已经包含 devsys 块；`spliceWireBlock` 判定为 unchanged，输出 `WireView{Changed:false}`，stdout 仅打印路径。
- **`--jsonl workitem list` 输出非 JSON 行**：检查 CLI 是 `--jsonl` 而不是 `--json`；`writeJSONL[T]` 是泛型，逐行 encode 列表项（[internal/cli/cli.go:124-133](file://internal/cli/cli.go#L124-L133)）。`--json` 输出 `{ok, matches: []}` 信封；`--jsonl` 输出零行（空列表）。
- **`--jsonl` 与 `--json` 字段名不一致**：`writeJSONL` 直接 encode `[]*domain.WorkItem`，字段名是 `id` / `title` / `status` 等（不是 `workitem_id`）；`scripts/exchange-demo.sh` 演示从 CLI 解析这些字段。
- **`session start` 返回空 `current_workitems`**：项目没有 in-flight workitem（`in_progress` 或 `review`）；`inFlight` 函数（[internal/app/session.go:160-185](file://internal/app/session.go#L160-L185)）按 `priority` 降序 → `created_at` 升序取 limit 条。
- **`context` 在不可信状态下不输出 `context_for_workitem`**：`!InspectionOK || len(pending) > 0` 时 `app.Next` 返回空 `WorkItems`（§15.4）；`app.ContextForWorkitem` 仍可单点查询——它直接走 `record` / `events`，不依赖 `Doctor`。
- **`mcp serve` 启动但客户端立即断连**：检查 SDK 版本；`go-sdk v1.8.0` 已 vendored。如果客户端期望更新版本，可能不兼容。MCP 协议握手在 `server.Run` 阶段——若失败会打印 SDK 错误到 `Config.Log`。
- **`devsys knowledge status` 三态裁决（M5 启用）**：`fresh`（exit 0）/ `stale`（exit 10）/ `missing`（exit 11）；`affected` / `unverifiable` 列表在 `--quiet` 下仍可见。`unavailable` 不再返回——无 page 层是 `missing`，有 page 层但 `Affected`/`Unverifiable` 非空是 `stale`。git 失败 / `ErrNoGit` → `Preconditionf("knowledge status: %v", err)`（exit 3）；page 解析 errors → `Invalidf(KindKnowledge, problems, ...)`（exit 4）。
- **审批 `invalidated_at`**：离开 `from` 状态自动失效；M3.7 起同源回退也失效；需重新 `request`。
- **`pending_approval` 风险**：`next` 把存在未决审批的工作项加入 `review` 推荐类，理由 `waiting for <approval-id>`。
- **`workflow next` 在 paused 时不输出 `next:` 推荐**（M3Reviewer5 ⑤）：候选仍展示；`resume` 后再次 `next` 才会给推荐。
- **`workflowWrite` 在租约活跃时拒绝**：CLI 必须传 `--owner` / `--token`；无人持有租约时不强制。
- **`workflow check` warning vs error**：未知键 warning；结构/类型/必填/值域错误 error；非 `.md` 文件 warning `ignored`。
- **`next` 缺业务事实**：待恢复事务或缺锁文件时仅打印风险与修复项，不列 workitems（§15.4）。
- **审批 `scope=action` 无消费路径**：M3.7 显式记遗留；M16.2 接入；当前 `Request --scope action` 可成功但不会被自动消费。

### M7 视图层诊断（M7.1 新增，M7.2–M7.4 扩展）

| 现象 | 检查路径 |
|---|---|
| `workspace view` 报 `trust.state = advisory_unlocked`，业务事实齐全 | 项目从未写过状态（缺 `.devsys/local/lock`，典型是只读副本 / 新检出）：`storage.Inspect` 翻译为 `ErrLockUnavailable` 后走 `InspectUnlocked`（[internal/view/collect.go:23-29](file://internal/view/collect.go#L23-L29)）。视图是快照不是判决；需要共享锁保护时先跑一次写命令或 `devsys recover` 把锁文件建起来。 |
| `trust.state = pending_transaction`，且 `progress.items` / `runs.entries` / `records` 全空 | 设计如此（方案 §15.4）：存在未完成事务时不渲染业务事实，`trust.pending_transactions` 列出事务 ID（[internal/view/build.go:76-80](file://internal/view/build.go#L76-L80)）。按 `progress.readiness.fixes` 的提示跑 `devsys recover --actor … --reason …` 后重读。 |
| `knowledge.status = missing` / `unavailable` | `missing` = 无页面层（生成器未配置或未生成，§12.6 降级）；`unavailable` = 有页面但新鲜度答不出来（典型：无 git，§12.5「答不出来 ≠ fresh」）。两者都与 `devsys knowledge status` 同源；处置走 `devsys knowledge refresh`。 |
| `runs.entries[].stream_torn = true` | run 的事件流断尾（`.devsys/runs/<id>.jsonl` 未完整闭合）：视图只报告不修复，补完属写入侧（[internal/view/build.go:435-443](file://internal/view/build.go#L435-L443)）。 |
| 同一状态连续两次 `--json` 输出不一致 | 不该发生（模型无墙钟字段）。先排查期间是否有写者：并发写会让快照跨在两个状态之间，`trust.state` 与各节 `sources` 是线索。 |
| `workspace build` 缺 `--static` / `--out` 指到 `.devsys/`（非 dist）/根/`.git` → exit 2；缺 `.devsys/` → exit 3 | `ResolveOut` 拒绝矩阵（[internal/sitestatic/static.go:53-88](file://internal/sitestatic/static.go#L53-L88)），调用方映射为 usage（[internal/cli/workspace.go:63-66](file://internal/cli/workspace.go#L63-L66)）。 |
| `workspace serve --host 0.0.0.0` 无 `--allow-remote` → exit 2 并明示风险；写方法（POST/PUT/PATCH/DELETE/OPTIONS）→ 405 + `Allow: GET, HEAD` | `isLoopbackHost` 仅认环回（[internal/cli/workspace_serve.go:131-134](file://internal/cli/workspace_serve.go#L131-L134)）；方法门禁（[internal/cli/workspace_serve.go:44-54](file://internal/cli/workspace_serve.go#L44-L54)）。 |
| stale 只给状态不给命令 / 缺 `--full` 指引 | M7.4 已覆盖：CLI `affected:`/`unverifiable:` + `devsys knowledge refresh`（[internal/cli/workspace.go:196-223](file://internal/cli/workspace.go#L196-L223)），静态站/serve 全站第二横幅 + knowledge 页内联（[internal/sitestatic/static.go:171-185](file://internal/sitestatic/static.go#L171-L185)）；判定复用 `knowledge.Evaluate`，JSON/退出码不动。 |

### M8 故障诊断（M8.1–M8.4 新增）

| 现象 | 检查路径 |
|---|---|
| `sync status` 报 `handoff: NOT ready` + `blocked [uncommitted-devsys]` | `.devsys/` 有未提交改动（事件流追加即触发）。`git status --short -- .devsys` 看脏路径；按 `chore(devsys): …` 前缀提交后重跑。`event record` 这类 append-only 也会让 `.devsys/events/<月>.jsonl` 变脏——即使你刚跑过命令。 |
| `sync status` 报 `blocked [active-leases]` | 还有人在用旧设备领取；在旧设备 `workitem release --id WLM-N --owner <o> --token <t> --actor me --reason handoff-done --expect <hash>` → 提交推送后重跑。`scripts/smoke-m8.sh` 行 79-88 覆盖了这条路径。 |
| `sync status` 报 `blocked [unmerged-paths]` 或 `blocked [merge-in-progress]` | 工作树里有未解决合并（含 `MERGE_HEAD` 等施工痕迹）：按 git 解决（保留双方显式二选一或手动合并），状态提交 `chore(devsys): resolve …` 后重跑；期间 `dispatch` 与 `workitem claim` 都会被门控拒绝。 |
| `sync status` 报 `blocked [pending-transactions]`（带 `leases: audit deferred`） | 待恢复事务阻塞租约审计（partial 快照不发声）。`devsys recover --actor me --reason r` 清 pending → 重跑 → 租约分支就会真的发声。 |
| `sync status` 报 `blocked [expired-leases]` / `[orphan-leases]` / `[unreadable-leases]` | 旧设备忘了释放/崩了/文件不可读：在旧设备 `devsys recover` 释放过期与孤儿；`unreadable` 手工修或删 `.devsys/scheduling/<id>.yaml`。`scripts/smoke-m8.sh` 第 60-88 行覆盖正常分支。 |
| `sync status` 报 `blocked [diverged-from-upstream]` 或 `[no-upstream]` | ahead-behind 不为 0 或分支无上游：`git fetch` 后 push/pull；`no-upstream` 走 `git push -u origin main` 设上游后重跑。 |
| `sync status` 报 `handoff: ready` 但新设备接手后看到旧设备的写入 | 设计如此（[internal/syncstatus/syncstatus.go:1-9](file://internal/syncstatus/syncstatus.go#L1-L9)）：`sync status` 只读本地上游镜像，**不能证明另一台设备已停写**。交接完成前旧设备不得继续写入；新设备接手前应先 `git fetch` 再 `sync status`，避免漏看最新提交。 |
| `repair --dry-run` 与 `sync status` 都看不到的冲突仍让 `dispatch` 拒绝 | 三向合并冲突（`clash.txt`）→ `repair --dry-run` 一定会列 `note_unmerged_paths` 与 `MERGE_HEAD`；`dispatch` 的 `mergeConflict`（[internal/app/dispatch.go:230-263](file://internal/app/dispatch.go#L230-L263)）复用了同一组探针。两者同时看不到说明没冲突；先解冲突再 `dispatch`。复现：`scripts/smoke-m8.sh` 行 90-126 制造 base/side/main 三向 + 断言 `digest` 字节级稳定 + `apply rejected clash.txt` + `dispatch exit 3`。 |
| `repair --apply --confirm <digest>` 报 `rejected <path>: requires human resolution; no automatic merge` | `ProposalNoteUnmerged` 恒被 rejected（[internal/reconcile/reconcile.go:502-509](file://internal/reconcile/reconcile.go#L502-L509)）：git 保留双方、`devsys` 不自动合并、不按时间戳选赢家。`TREE_BEFORE` 与 `TREE_AFTER` 字节级一致——零写入是设计目标。人工 git 解决后再跑一次 `--apply` 清租约残留即可。 |
| `dispatch --dry-run --actor me --reason tick` 直接 `exit 3` + `dispatch blocked` | 合并未解时的派发门控（[internal/app/dispatch.go:90-95](file://internal/app/dispatch.go#L90-L95)）：与 `recover` 之前的优先级冲突——`mergeConflict` 在 `Recover` 之前跑，conflicted tree 持双方、tick 不写任何东西（包括 recovery）。先解冲突；`scripts/smoke-m8.sh` 行 114-119 断言 exit 3 + 字符串匹配 `dispatch blocked`。 |
| `archive events --before 2999-01 --actor me --reason trim` 之后 `event list` 条数变少 | 不该发生：`internal/events.Read`（[internal/events/events.go:192-279](file://internal/events/events.go#L192-L279)）走 `live + archive/events` 双读并按事件时间排序；现役 `.devsys/events/<月>.jsonl` 移走后，archived 分片被纳入。两组都应一致；条数下降说明 manifest 未生成或归档被中断——`cat .devsys/archive/manifest.yaml` 看条目、`ls .devsys/archive/events/` 看分片是否在位。复现：`scripts/smoke-m8.sh` 行 128-156（断言 `live bytes: N -> 0` + `manifest.yaml` 在位 + 前后事件数相等 + `running run refused`）。 |
| `archive runs --id <run-id> --actor me --reason trim` 拒绝并报 `only terminal runs may be archived` | 流未终态（`status` ∈ `succeeded/failed/timed_out/stalled/canceled` 之外）：`devsys run log <id>` 看末条 `exit` 记录的 `status`；先 `workitem release` 或 `run complete/fail/cancel` 终态后再归档。 |
| 归档后体积没缩 | 体积口径是现役 `.devsys/events+runs(*.jsonl)` 字节下降（[README.md:170-183](file://README.md#L170-L183)）；git 对象库不立即收缩（archive 操作 = 文件删除 + 写入，但 git 的 loose object 与 packfile 不会被 GC 重写，直到下次 `git gc` 或 repack）。报告里看 `live bytes: N -> M` 即可，`.git/` 下降不是承诺。 |
| `scripts/contrabass/import.py` 报 `refused` / 不动状态 | 仅 done / review / blocked / closed→cancelled 四态做 `transition --expect`（[scripts/contrabass/import.py:24-29](file://scripts/contrabass/import.py#L24-L29)）；其余 board 状态（open / in_progress / 未知）只搬 `result.summary` 为 comment，不动状态。非法边 / `--expect` 过期版本进 `refused`，不直写 `.devsys/`。先用 `--dry-run` 预览（零写入、`.devsys/` 树 hash 不变）；`scripts/smoke-m8.sh` 行 171-173 断言 dry-run 预览且不留 untracked。 |
| export/import 写卡后 JSON 解析失败 / 卡片里多出 BOM | PowerShell 默认 `Out-File` / `Set-Content` 写 UTF-8 BOM，`json.loads` 在 `strict` 模式不剥离：`smoke-m8.ps1` 用 `[IO.File]::WriteAllText($cardPath, $cardJson, [Text.UTF8Encoding]::new($false))` 写无 BOM UTF-8（[scripts/smoke-m8.ps1:205](file://scripts/smoke-m8.ps1#L205)）。Python 侧 `path.write_text(..., encoding="utf-8")` 也是无 BOM。`open(..., 'w')` 是平台默认（Windows 上有 BOM），smoke 改用 `json.dump(..., open(path,'w'), indent=2, ensure_ascii=False)` 是已知坑。M8-close 起 clash 四写同样走无 BOM `WriteAllText`（[scripts/smoke-m8.ps1:104-116](file://scripts/smoke-m8.ps1#L104-L116)），与 sh 裸字节对等；archive dry-run 零写判读走 `StartsWith('??')` 字面量（[scripts/smoke-m8.ps1:163-165](file://scripts/smoke-m8.ps1#L163-L165)），`Select-String -SimpleMatch` 已弃用。 |
| `import.py` 仍报 `failed to transition verification -> done`，但工作项已在 done | 已 stale：脚本先 `workitem get` 取 `--expect`，人工或前次 import 已推进导致 digest 漂移——`devsys --json workitem get WLM-N` 看 `status`，重跑 export 后再 import。 |
| 真 Contrabass 字段对不上 / board 写入被拒 | `[UNVERIFIED]`（[scripts/contrabass/README.md:66-72](file://scripts/contrabass/README.md#L66-L72)）：本机无 Contrabass、外网不可达，脚本读写的是「中间形态」`.devsys/<board>/issues/<id>.json`。联调真 Contrabass 前先按其原生字段（`.contrabass/board/`）写一层薄转换，或改 export.py 输出 schema。fixture 闭环已验证（[scripts/smoke-m8.sh](file://scripts/smoke-m8.sh) 行 158-189）。 |

**M8 端到端剧本 `scripts/smoke-m8.{sh,ps1}`**：双平台 PASS——sync 三/四类阻断码、conflict 标注 + digest 稳定 + apply rejected + dispatch exit 3、归档 live→0 字节 + 事件仍可查 + running run 拒绝、board 闭环 transition + `[board]` comment。命令字面量见 `scripts/smoke-m8.sh:60-191`；PowerShell 等价在 `scripts/smoke-m8.ps1`。

### P1/P2 / 安装链 / 发布诊断（06f2d8e / 6bd253c / c150007 / 577299a / 98adab3 / d726ed4 / b89ffae / 9987583 / fdf803f / 2b8b8ce 新增）

| 现象 | 检查路径 |
|---|---|
| `devsys` 在 Git 子目录里跑也工作 | `internal/cli/roots.go`（[root]）`DEVSYS_PROJECT_ROOT` 优先；否则向上找 `.devsys/`；到根目录也找不到 → 仍报原 cwd。设 `DEVSYS_PROJECT_ROOT=/path/to/project` 可显式覆盖 worktree 边界（方案 §4.8） |
| `devsys wire` 三态 (`--check` / `--skill` / `--print-mcp`) 行为不符 | `--check` 走内容比对（不是字节相等）— 已含 devsys 块 + 未脏改 → exit 0；`--skill` 写 `.agents/skills/devsys/{SKILL.md,references/cli.md,references/troubleshooting.md}` 三件；`--print-mcp` 接 `codex`/`claude`/`opencode` 三种客户端 snippet；与 `--dry-run` 互斥（[internal/cli/cli.go:411-468](file://internal/cli/cli.go#L411-L468)） |
| `devsys prime` 失败 | `prime` 是 `session start --compact` 别名（[internal/cli/cli.go:304](file://internal/cli/cli.go#L304)）；调用链、退出码、输出同 `session start`；不存在独立解析 |
| MCP `tools/list` 比之前少（`devsys mcp serve --tier core`） | `--tier core` 仅暴露核心工具；默认 `standard`。`ParseTier` 拒未知名（[internal/mcp/server.go:31-75](file://internal/mcp/server.go#L31-L75)） |
| `devsys decision create` 报 `--by` 缺 | 提示词协议：补 `--by <身份>`；仅 `decision create` 这条子命令要 `--by` |
| `scripts/install.sh` / `install.ps1` 报 `checksum entry missing for $asset` | v0.1.0 release 的 `checksums.txt` 是 shasum `<hash> *<file>` 格式（CI 之前本地构建），`grep -E "[[:space:]]\*?$asset\$"` 已经兼容；如仍 missing，说明 release 资产列表与本机 `$asset` 不一致（例如 `darwin/arm64` vs `darwin/amd64`） |
| `scripts/install.sh` 报 `download failed; falling back to git clone + go build` 但 `go` 未装 | 私有仓库匿名 404 走 fallback → 需要 Go + Git；`git -c advice.detachedHead=false clone --branch <tag> --depth 1` + `GOFLAGS=-mod=vendor go build`；安装后 `devsys --version` 应报告 `$Tag`（fallback 自动注入 ldflags） |
| `scripts/install.ps1` fallback 之后 `devsys --version` 仍是 `0.1.0-dev` | 修复前提：fallback 在克隆目录里跑（`(cd "$src"; go build -ldflags "-s -w -X workloom/internal/version.Version=$Tag" ...)`）；仍看到 `0.1.0-dev` 说明你的脚本版本早于该修复（98adab3 之前）——升级到最新 install.ps1 |
| `devsys project status` / `devsys next` 报 `risk: default_policy %q has no .devsys/workflows/%s.md` | `config.yaml default_policy` 写了不存在的策略文件 ID：建对应 `.devsys/workflows/<id>.md`、改正 `default_policy`、或清空 `default_policy`（让未绑定实例走策略文件查找） |
| `devsys next` 推荐 `start` 但 `claim` 拒 + `quality_gate.min_score` | `next` 与 `claim` 同源（[internal/app/next.go:60-110](file://internal/app/next.go#L60-L110)），但 `claim` 在策略非法或默认策略缺失时 fail closed ——把 `default_policy` / 策略文件修好，`next` 报告会同时变更 |
| `devsys approval request --stage <stage>` 之后 `devsys approval approve --id <approval>` 通过但下次 transition 又报缺审批 | 同一 `from` 状态被回退过（M3.7 起，同源回退也失效）；或工作项离开过该 `from` 状态（[internal/app/workitem.go:550-615](file://internal/app/workitem.go#L550-L615)）。看 transition 报错的 `approval-N was invalidated at <t> when the work item left "<status>" (方案 §4.9); ...` 注解——再 request 一次即可 |
| `devsys --version` 不显示 commit | 本地构建没注入 `-ldflags`；release / `make release` 路径自动注入；当前唯一手动入口是 `scripts/build-release.sh` 或 §发布 → 本地构建（[internal/version/version.go:13-23](file://internal/version/version.go#L13-L23)） |
| `gh release view v0.1.1` 缺资产 / checksum 失配 | 看 run 日志 `Verify the artifacts` 段：CI 严格自检拒 shasum `<hash> *<file>`、要求 GNU `<hash>  <file>`、列全 dist 资产。本地 `bash scripts/build-release.sh --version v0.1.1 --out /tmp/d` 自检；格式过关后再 `gh release upload v0.1.1 /tmp/d/checksums.txt --clobber` |
| CI 发布跑挂：`checksums.txt uses shasum's binary marker` | `scripts/build-release.sh` heredoc 用 GNU `sha256sum` 不会写 `*`；如出现说明你 override 了 SHA 工具——回退默认 `sha256sum`，或把 `grep -Eq '^[0-9a-f]{64} \*'` 自检放在你 override 之后（v0.1.1 CI 已经固定这条） |


### 搜索诊断的边界

`search` 是无索引的逐行子串扫描：大小写不敏感、单行截断 200 字节（UTF-8 安全）、单文件最多 5 条命中行（[internal/search/search.go:20-37](file://internal/search/search.go#L20-L37)）。多 worker 扇出但每文件独立槽位保证输出顺序与单线程遍历一致（[internal/search/search.go:70-105](file://internal/search/search.go#L70-L105)）。它**不**解析 front matter、不**识别 YAML 结构**——命中就是 raw 文本里出现的子串，key 与 value 等价、M1 / "M1" / "任务: M1" 等所有出现形式都会被命中。M5.1 知识页面解析接入时按页面 front matter 增强过滤；具体升级时再测性能数字（[docs/开发记录.md:66](file://docs/开发记录.md#L66)）。

### 事件流边界

`events.Append` 把分片路径由事件自身的 UTC `t.Year()/Month()` 派生（12 月事件 1 月写仍落 12 月分片）；ID `ev-<UTC时间戳.微秒>-<8hex>` 由时间戳 + 4 字节 `crypto/rand`（十六进制输出为 8 hex）派生，无全局计数器（[internal/events/events.go:44-93](file://internal/events/events.go#L44-L93)、[internal/events/events.go:150-157](file://internal/events/events.go#L150-L157)）。写路径只校验 `reply_to` 必须是 comment 类型（[internal/events/events.go:69-71](file://internal/events/events.go#L69-L71)）；subject 一致性、悬空 reply 与二级回复（parent 也是 reply）由 `Comments` 聚合阶段校验（[internal/events/events.go:240-264](file://internal/events/events.go#L240-L264)）。`ErrIncompleteTail` 由 `internal/storage/jsonl.go` 的 `InspectJSONL` / `appendJSONL` 透传，事件层不做断尾判定（[internal/events/events.go:27](file://internal/events/events.go#L27)、[internal/storage/jsonl.go:22-95](file://internal/storage/jsonl.go#L22-L95)）。

M3 起多事件同事务由 `events.AppendBatchTx`（[internal/events/events.go:108-144](file://internal/events/events.go#L108-L144)）按 shard 分组一次 `AppendJSONLRaw` 完成；状态事件 + Guard 事件 + 失效事件 + 传播事件同批提交，每个 shard 仅一次 append（违反 `staged twice` 由 `Tx.Staged(rel)` 在失效 pass 跳过本事务已写文件解决）。
- **M7.2–M7.4 视图层（fa2a87d）**：只读契约小节标题扩展渲染面；诊断表新增 build/serve/hint 三行；测试节视图口径追加 sitestatic 12 例 + serve 8 例（离线零命中、RenderPage 与 Build 逐字节一致、405 全方法、横幅）。
- **M8 协作层（8184dea / acb4829 / 6fbb23c / 52294b0）**：编码规范新增「M8 边界」意识（M8.1 sync status 只读：fetch 不许、不建锁、不恢复、阻塞仍 exit 0；M8.2 冲突不自动合并：`ProposalNoteUnmerged` 恒 rejected、`app.mergeConflict` 派发门控在 `Recover` 之前；M8.3 默认保守：read-copy-delete + manifest CAS、`--dry-run` 零写入、running 流拒绝；M8.4 看板非事实来源：脚本只调 CLI、`json.dump(..., ensure_ascii=False)` + 无 BOM UTF-8 写卡）；调试新增「M8 故障诊断」表（NOT ready 七码分类 + `uncommitted-devsys` 排查/恢复、`active-leases` 释放剧本、`expired/orphan/unreadable` 来源、`unmerged-paths/merge-in-progress` 与 `pending-transactions` 处置、`diverged-from-upstream/no-upstream`、`handoff: ready` 但旧设备仍写、`repair --apply rejected` 零写入断言、`dispatch --dry-run exit 3` 优先级、`archive events|runs` 后事件数一致断言、归档体积口径与 git 对象库不收缩、`import.py refused` 分类、PowerShell BOM 实测坑、真 Contrabass `[UNVERIFIED]` 薄转换层）；每条给复现命令 + smoke 行号（`scripts/smoke-m8.{sh,ps1}`）；新增对 `internal/syncstatus` + `internal/archive` + `scripts/contrabass/{README,export.py,import.py}` 的引注。

## 发布

`v0.1.0`（2026-09-19，本地构建，校验和为 shasum `<hash> *<file>` 格式）→ `v0.1.1`（2026-09-20，CI run `35496169845`，3m11s 全绿，7 资产，GNU 校验和）已经走通自动发布闭环；详细命令 / 回退 / 故障见 runbook [docs/发布流程.md:1-88](file://docs/发布流程.md#L1-L88)。下面是发布管线的事实清单与每个事实的物理位置。

### 构建脚本与入口

发布产物走 `scripts/build-release.sh`（[scripts/build-release.sh:1-50](file://scripts/build-release.sh#L1-L50)）：MIME 哨兵 heredoc 列出 6 平台矩阵（windows/amd64、windows/arm64、linux/amd64、linux/arm64、darwin/amd64、darwin/arm64），用 `GOOS`/`GOARCH` 交叉编译，`-ldflags "-s -w -X workloom/internal/version.Version=<tag> -X workloom/internal/version.Commit=$(git rev-parse --short HEAD)"` 注入版本与 commit；最终在 `dist/` 下用 GNU `sha256sum` 生成 `checksums.txt`。本地入口通过 `Makefile`（[Makefile:1-22](file://Makefile#L1-L22)）：`make release VERSION=v0.1.1 OUT=dist` ≡ `bash scripts/build-release.sh --version v0.1.1 --out dist`；`Makefile` 仅暴露 `build / install / smoke / release / clean` 五个 operator 入口。

### CI 工作流：触发方式、矩阵、严格自检

[.github/workflows/release.yml:1-131](file://.github/workflows/release.yml#L1-L131) 声明：

1. **触发**：推 `v*` tag 自动跑，或 `workflow_dispatch` 手动跑并要求 `tag` 已存在、可选 `draft=true`、`notes=""`（自动 `--generate-notes`）。`permissions: contents: write` + `concurrency: group: release-${{ github.ref }}, cancel-in-progress: false`；`runs-on: ubuntu-latest`，单 runner 2-4 分钟完成。
2. **6 平台矩阵**：bash `scripts/build-release.sh --version "$VERSION" --out dist`，与本地 `make release` 走同一脚本。
3. **严格校验和自检**：
   - `dist/devsys-linux-amd64 --version` 必须包含 `$VERSION`。
   - `grep -Eq '^[0-9a-f]{64} \*' dist/checksums.txt` → 命中即 exit 1（拒绝 macOS `shasum` 的 `<hash> *<file>` 二进制标记——`sha256sum -c` 两种都接受，但安装脚本不识别 `*`，`v0.1.0` 就是这样发布的）。
   - `grep -Evc '^[0-9a-f]{64}  [^ ]+$' dist/checksums.txt` → 行数非 0 即 exit 1（拒绝 GNU 文本格式之外）。
   - `(cd dist && sha256sum -c checksums.txt)` + 逐资产 `grep -qF "  $base"` 双重验证：哈希对不对、资产齐不齐同时校验。
4. **发布**：`gh release create "$VERSION" dist/devsys-* dist/checksums.txt --verify-tag --title "$VERSION" [--draft] [--generate-notes | --notes "$NOTES"]`。

本地 `scripts/build-release.sh` 自带同样的 `grep -Eq '^[0-9a-f]{64} \*'` 自检（[scripts/build-release.sh:42-49](file://scripts/build-release.sh#L42-L49)）：本地生成也必须 GNU 文本格式。两条路径共一份校验口径，CI 失败等价的本地态也失败。

### 安装链：鉴权下载、回退释放、格式容忍

`scripts/install.sh`（[scripts/install.sh:1-80](file://scripts/install.sh#L1-L80)）与 `scripts/install.ps1`（[scripts/install.ps1:1-79](file://scripts/install.ps1#L1-L79)）共享三步：

1. **鉴权下载**：优先用 `gh release download "$tag" --repo "$repo" --pattern "$asset" --output … --clobber`（私有仓库资产需要登录）；`gh auth status` 失败则回退匿名 `curl -fsSL --retry 2` / `Invoke-WebRequest` 拉 GitHub release URL（私有仓库匿名会 404，到此步失败）。
2. **回退构建**：下载/校验任一步失败 → `git -c advice.detachedHead=false clone -q --branch "$tag" --depth 1 https://github.com/${repo}.git` + `GOFLAGS=-mod=vendor go build -ldflags "-s -w -X workloom/internal/version.Version=$Tag"`。回退构建进入克隆目录、`-ldflags` 注入 tag，所以 `devsys --version` 报告的版本仍是正在装的版本（v0.1.0 之前的版本默认 `0.1.0-dev`；修复后改 ldflags 让 fallback 也带 tag）。
3. **校验和双格式容忍**：`grep -E "[[:space:]]\*?$asset\$"` 同时匹配 GNU `<hash>  <asset>` 与 shasum `<hash> *<asset>`——`tr -d '\r'` 兼容 CRLF（旧版 PS 构建的 release）。先 `sha256sum -c -`，无 `sha256sum` 时回退 `shasum -a 256 -c -`。

Windows 版额外 `-AddToPath` 把 `%LOCALAPPDATA%\devsys` 加到用户 PATH；`~/.local/bin` 是 bash 版默认 `--prefix`。

### v0.1.0 与 v0.1.1 实跑记录

| tag | 时间 | 路径 | 资产数 | 校验与格式 | 备注 |
|---|---|---|---|---|---|
| `v0.1.0` | 2026-09-19 | 本地 `make release` | 6 | shasum `<hash> *<file>` | 教训：`sha256sum -c` 接受 shasum 标记，但安装脚本 `[[:space:]]\*?` 之前的预过滤匹配不到 → 判「校验项缺失」并回退源码构建。已**仅重生成 `checksums.txt`** 覆盖：6/6 哈希一致，仅 GNU 文本格式修复；不重发二进制（[docs/发布流程.md:79](file://docs/发布流程.md#L79)） |
| `v0.1.1` | 2026-09-20 | CI run `35496169845`，3m11s 全绿 | 7 | GNU `<hash>  <file>` | 第一次走完 tag 推送 → 自动触发 → 6 平台矩阵构建 → 严格自检 → `gh release create` 闭环。`devsys v0.1.1 (fdf803f)` 报告验证；`docs/发布流程.md` runbook 与本卡一致 |

### Runbook（docs/发布流程.md）

7 节：[docs/发布流程.md:1-88](file://docs/发布流程.md#L1-L88)：

- §0 触发方式与成本（tag 推送 vs `gh workflow run` + `draft/notes`）
- §1 发版前检查（`go test ./... -count=1` / `gofmt -l` / `go vet` / `git status --short` 干净）
- §2 自动路径（`git tag -a vX.Y.Z` → `git push origin vX.Y.Z` → `gh run watch` → `gh release view`）
- §3 手动路径（`gh workflow run release.yml -f tag=… -f draft=true` → 验证 → 转正）
- §4 无 Actions 等价手工路径（`make release` → `gh release create … --verify-tag`）
- §5 校验与消费（`bash scripts/install.sh --tag vX.Y.Z` / `powershell … install.ps1 -Tag vX.Y.Z`）
- §6 回退表（tag 指错 / 资产缺失 / 仅改校验和 / 仅改说明 / 用户侧回退）
- §7 常见故障（`Resource not accessible by integration` / `tag … does not exist` / release 已存在 / 版本串不符）

### 本地构建（无 Actions 等价）

```sh
go build -mod=vendor -ldflags "-X workloom/internal/version.Version=0.1.1 -X workloom/internal/version.Commit=$(git rev-parse --short HEAD)" -o bin/devsys ./cmd/devsys
```

未注入版本的本地构建保留 `Version=0.1.0-dev`、`Commit=unknown`；`devsys --version` 在 `Commit=="unknown"` 时只输出 `Version`（[internal/version/version.go:13-23](file://internal/version/version.go#L13-L23)）。这是构建标识能力，不是发布动作。

## 更新摘要

- **source_commit**: 47620c7 → 2735f62（M1 全部子步交付后的统一基线）。
- **Project / 嵌套状态校验**：原"白名单仅 schema_version/id/name/created_at"的描述已陈旧；M1 起 `projectSpec` 扩展为完整 `Project` 模型（13 个字段），`state/current.yaml` 与 `state/milestones.yaml` 通过 `currentSpec` / `milestonesSpec` + `kindScope` / `kindCurrent` / `kindMilestones` / `kindStrings` 嵌套校验；`config.yaml` 现在 `configSpec` 接受 `schema_version`（必填）+ `workspace_root` + `dispatch_command` + `knowledge_pages` + `knowledge_generator` + `default_policy`（`configSpec` [internal/config/validate.go:59-77](file://internal/config/validate.go#L59-L77)），业务键按里程碑分批加入。
- **领域序列化**：明确 `domain.DecodeYAML` 是先 `storage.CheckSchemaVersion`、再 `storage.DecodeYAML`（`KnownFields(true)`）两步——schema_version 校验在严格解码**之前**；JSON 路径则使用 `DisallowUnknownFields`。`EncodeYAML` 通过 `preserveNulls` 保留 `null` 与 `[]` 的差异。
- **快照 / CAS / ReadSnapshot API**：补写 workitem / run / record / approval 共享的 CAS 形态（写事务内 `nextNumber` + `ExpectAbsent`，冲突 ≤ 5 次）、`ReadSnapshot` 配 `ExpectHash` 守卫、`NewArtifactVersion` 通过 `tx.ReadForExpect` 在写锁内安全重读再写**新文件**完成不可变版本链。`Reader.Expect` 实际只返回 `ExpectAbsent` 或 `ExpectHash`，`ExpectAny` 由调用方直接构造零值。
- **搜索诊断边界**：补写"无索引逐行扫描、不解析 front matter、行/文件命中上限、并发扇出保持确定性顺序"。明确 raw 子串命中——key/value 等价、`M1`/`"M1"`/`任务: M1` 都会命中；M5.1 接入时按 front matter 增强过滤，性能数字升级时再测。
- **冒烟脚本**：补写 `smoke-m1.ps1` / `smoke-m1.sh` 的 seed+回读双模、`--keep`/`-Keep` 行为、`DEVSYS_CONFIG_DIR` 隔离；命令字面量改写为可执行形式（`scripts/smoke-m1.ps1 -Keep` / `scripts/smoke-m1.sh --keep`）。
- **性能验证口径 #130**：移除千文件测试的 2s 硬断言，保留准确命中检查，增加 FreshFiles/RepeatedFiles benchmark。此前 3.44s 失败保留为历史观察，不归因于 Defender；最新全量测试通过。未修改生产实现或以预热掩盖首次搜索开销。
- **M3 工作流、审批与 next（cf7b256）**：编码规范新增「Workflow 策略白名单必须同步演进」小节（与 Project 白名单并列，三处同步 + warning vs error 决策）；新增「M3 写路径契约」整节（Guard / 失效 / 传播 / 工作流实例 四件事在 `changeStatus` 与 `workflowWrite` 内的物理落点）；编码规范小节补 `Tx.AppendJSONLRaw`（多行 JSONL payload、拒绝空行/CR/缺终止符）与 `Tx.Staged(rel)`（事务内已暂存查询，供 `InvalidateForStatusTx` 跳过消费后写过的文件）；调试表新增 13 行 M3 现象（`workflow check` 4 + location、LKG warning、step-complete 拒绝 + allowed、quality_gate 拦截、approval gate 拦截、reject 事务回滚、`next` FAIL/CONCERNS/FAIL 推荐类等）；新增「M3 工作流 / 审批 / next 诊断」整节（LKG 失效、条件白名单加载期报错、门禁证据 TOCTOU 窗口、审批失效语义、`pending_approval` 风险、`paused` 不输出 `next:`、租约 fencing 实例操作、`scope=action` 无消费路径）；测试节补 M3 回归与端到端清单；新增 `smoke-m3.{ps1,sh}` 七段 PASS 说明（与 `scripts/m3helper` 子命令映射）；新增「事件流边界」对 M3 `events.AppendBatchTx` 的引用；删除「workflow check 是 M…
- **M4 共享应用服务 / MCP / 写守卫 / wire / `--jsonl`（997c5f8）**：编码规范新增「M4 共享应用服务边界」小节（**所有业务规则在 `internal/app/Service` 收口**；CLI 与 MCP 都消费同一份；写方法统一接受 `expect`，读锁内 `bytes.Equal` 守卫；**禁止**「CLI 能做 MCP 不能做」的特例）；新增「M4 写路径契约」小节（`app.Service.*` 期望守卫 + `record.ErrSuperseded` 防并发分叉 + CLI/MCP `--expect` 反解码为同一原字节）；调试表新增 9 行 M4 现象（`mcp serve` 启动失败、`tools/list` 缺工具、`tools/call` 报 `toolError.code` / `kind` / `problems`、`--expect` 不匹配、`artifact update` 报 `ErrSuperseded`、`--jsonl` 行为、`session start` 返回空、`knowledge status` 永远 `unavailable`、CLI 与 MCP 写不一致）；新增「M4 MCP / 共享应用 / 写守卫 / wire / `--jsonl` 诊断」整节（`nopReadCloser` / `nopWriteCloser` 必要、`Config.Log` 须 stderr、wire 三次运行一致性、`--dry-run` 行为、`ErrSuperseded`、不可信状态下 `context` 不输出业务事实、CLI/MCP 写结果 `code` 必须一致）；测试节补 M4 回归清单（`internal/app/*_…
- **M5 知识层（7c3fcde）**：调试表新增 12 行 M5 现象（`knowledge status` 报 `unknown baseline` / `git check-ignore` 失败 / `ErrNoGit` / page 解析 errors 拒不刷新 / `AuthoredStatuses` 拒手写 `stale` / `sources` 路径形态 / `protected` + `content_hash` 双闸 / `--force` 覆盖仍记录 `Overridden` / `ScopeFile` 与生成器 argv / 锁 `ErrLocked` 与 `refreshHolder` / 续跑 `run.json` 与 `ResumePending` / 无生成器降级 `missing`）；新增「M5 知识层诊断」整节（索引层缺 git 的 `Preconditionf` 而不是降级、生成器命令含空格的 argv 切分、`content_hash` 整文件 vs body hash 的口径差、`Match` 模式在 `**/` 零段语义、`refresh-scope.json` 落地而非 argv、续跑的 checkpoint `Phase=failed` 不删）；编码规范增「M5 知识层边界」小节（**两层分工** + 外部生成器不写 `.devsys/knowledge/state.json` + `Match` 与 ignore 一致 + 整文件 hash vs body hash 口径 + 退出码 0/10/11 由 `codedExit{code}` 承载 + `pageRoots` 合并策略）；测试节补 M5 回归（`internal/knowledge/*_test.go` 9 个文件：`page_test.go` `scan_test.go` `snapshot_test.go` `match_test.go` `freshness_test.go` `protection_test.go` `run_test.go` `adapter_test.go` `generator_test.go` 与 `internal/cli/knowledge_test.go` 含 generatorStub / lockHolderStub / 7 个 knowledge 端到端用例）+ `scripts/smoke-m5.sh` 10 段 PASS。
- **M7.1 视图层（463c2d8）**：编码规范新增「M7.1 视图层只读契约」小节（只经 `storage.Inspect` 读、不替调用方修复、确定性装配、错误分层四条）；调试新增「M7.1 视图层诊断」表 7 行（`advisory_unlocked` / `pending_transaction` 不渲染业务事实 / `knowledge` `missing` vs `unavailable` / `stream_torn` / 双跑不一致 / `--limit 0` 与缺项目退出码 / 只读副本验证法）；测试新增视图层口径（固定时钟 + 双 marshal 逐字节 + 文件指纹 + 0444 只读副本）。
- **M7.2–M7.4 视图层（fa2a87d）**：只读契约小节标题扩展渲染面；诊断表新增 build/serve/hint 三行；测试节视图口径追加 sitestatic 12 例 + serve 8 例（离线零命中、RenderPage 与 Build 逐字节一致、405 全方法、横幅）。
- **M9（d5423fb / 6214330 文档增量，零源码变更）**：未知 `schema_version` 行指向 `docs/迁移指南.md`（bundle 外，不做跨包链接）；M8 故障表 BOM 行引注修正为 `smoke-m8.ps1:205`（写卡）并增 M8-close 两行（clash 无 BOM `WriteAllText` [scripts/smoke-m8.ps1:104-116](file://scripts/smoke-m8.ps1#L104-L116)、archive dry-run `StartsWith('??')` [scripts/smoke-m8.ps1:163-165](file://scripts/smoke-m8.ps1#L163-L165)）；M9.1 `docs/M9-验收报告.md`（17 条逐条验证）与 M9.2 `docs/使用手册.md` 为 bundle 外用户文档。
- **P1 子目录根解析 / wire 三态 / skill / prime（06f2d8e / 6bd253c / c150007）**：`internal/cli/roots.go` `DEVSYS_PROJECT_ROOT` + 向上找 `.devsys/`；`devsys wire --check` 走内容比对（不是字节一致）、`--skill` 写 `.agents/skills/devsys/SKILL.md` + `references/{cli,troubleshooting}.md`、`--print-mcp codex|claude|opencode` 输出对应客户端 snippet；新增 `devsys prime` 别名 ≡ `session start --compact`；MCP 新增 `tier=core|standard` 过滤（`ParseTier` 拒未知名）。CLI help 行同步更新（[internal/cli/cli.go:85](file://internal/cli/cli.go#L85)）。
- **P2 提示词修复与手册配套（577299a / 38f5972）**：`internal/prompt/prompt.go` 决策命令补 `--by <身份>`、无策略兜底文案、工作流步骤清单；`internal/app/skill.go` `references/troubleshooting.md` 把 `--latest` / `quality_blocked` / `version conflict` / `default_policy` 四类口径固化为机器/agent 共读文本。README + 使用手册补「工作流从哪来」说明。
- **安装链与下载鉴权（98adab3 / d726ed4）**：`scripts/install.ps1` 回退构建进入克隆目录并按 `scripts/build-release.sh` 同口径 `-ldflags "workloom/internal/version.Version=$Tag"` 注入版本；`scripts/install.sh` / `install.ps1` 校验项 `grep -E "[[:space:]]\*?$asset\$"` 容忍 shasum `*` 二进制标记（v0.1.0 release 实际就用此格式——`sha256sum -c` 接受但安装脚本预过滤不识别，所以 v0.1.0 装包默认走回退源码构建；同次升级兼容两种）；`gh release download --repo … --clobber` 鉴权优先（私有仓库匿名 404 时仍可下载），`git clone` 加 `-c advice.detachedHead=false`。
- **使用接口修复（d726ed4）**：`workitem create` 新增 `--acceptance a,b`；`approval list` / `approval get` 第二列改走 `approvalState` 渲染（`consumed` / `invalidated` / `pending|approved|rejected`，盘面不再显示可二次消费的「approved」但已消费/失效项）；`workitem release` / `workitem start` 缺 `--id/--owner/--token/--actor/--reason` 任一即打 usage（[internal/cli/cli.go:966, 987](file://internal/cli/cli.go#L966)）；租约活跃期间 `transition` 报错给出 `devsys workitem release --id … --owner … --token <token from .devsys/scheduling/<id>.yaml> …` 出路（[internal/workitem/state.go:127](file://internal/workitem/state.go#L127)）；`workitem.ReadSnapshot` 对 `^[A-Z][A-Z0-9_]*-[0-9]+$` 之外形状在路径构造**之前**报 `ErrBadID`（[internal/workitem/workitem.go:35, 230](file://internal/workitem/workitem.go#L35)）——`storeError` 把 `ErrBadID` 翻 usage、exit 2。
- **next/quality gate/default_policy/失效审批出路（b89ffae）**：`next` 与 `project status` 对 ready 工作项走与 `claim` 相同的质量门判定；不达标即风险 `quality_blocked`（[internal/next/evaluate.go:46](file://internal/next/evaluate.go#L46)），`devsys next` 报告同源并打印推荐的 `workitem update` 补救命令；`retry_queued` 增 `retry_pending` 风险（`next.Evaluate` 仅看 status=retry_queued，[internal/next/evaluate.go:355-359](file://internal/next/evaluate.go#L355-L359)）。`config.yaml` 新增可选 `default_policy`（[internal/config/config.go:111-114, 212-214](file://internal/config/config.go#L212-L214)），`policyForWorkItem` 是未绑定实例工作项的唯一解析点（[internal/app/next.go:60-110](file://internal/app/next.go#L60-L110)）；`default_policy` 指向不存在的策略文件 → `config.yaml default_policy %q has no .devsys/workflows/%s.md` 风险；`config.yaml` 非法或默认策略缺失时 `claim` fail closed；未绑定且无默认策略但项目有策略文件时 `claim` 打 `warning:` 行（门禁未跑但**不阻断**）。阶段门 `require_approval` 未满足时 `gateEvidence`（[internal/app/workitem.go:550-615](file://internal/app/workitem.go#L550-L615)）调 `approval.InvalidatedFor` 拿最近一次失效审批，把 `approval-N was invalidated at <t> when the work item left "<status>" (方案 §4.9); request a new approval with devsys approval request` 灌进 `workflow.GateEvidence.ApprovalNote`（[internal/workflow/gate.go:18-21, 74-75](file://internal/workflow/gate.go#L18-L21)），门禁消息附在 `(...)` 里（[internal/workflow/gate_test.go:89-96](file://internal/workflow/gate_test.go#L89-L96)）。`project blueprint` 无声明 → `ProjectBlueprint` 返回 `(nil, nil)`、`devsys project blueprint` 打 `no blueprint declared (project.yaml blueprint_artifact_id is empty)`、exit 0（[internal/cli/cli.go:630-655](file://internal/cli/cli.go#L630-L655)）；MCP `project_blueprint_get` 补注册（session 档 / standard tier，[internal/mcp/tools.go:35-37](file://internal/mcp/tools.go#L35-L37)），JSON 输出 `{ok, artifact: null}` 形态。`storage.ErrConflict` 经 `app.storeError`（[internal/app/workitem.go:460-477](file://internal/app/workitem.go#L460-L477)）翻译为「`<err> (the file changed after your read: re-read it and retry, or repeat with --latest where the command offers it)`」并定 `KindWorkitem/KindInvalid`；`workitem.ErrBadID` 翻 `Usagef` → exit 2；`internal/app/store_error_test.go:TestStoreErrorNamesTheConcurrentWriteEscape` 固定这条文案。`scripts/release.yml` 与 `scripts/build-release.sh` 严格校验 `checksums.txt`：拒 `^[0-9a-f]{64} \*`（shasum 二进制标记）、要求 `^[0-9a-f]{64}  [^ ]+$`（GNU 两空格）、`sha256sum -c` + 逐资产 `grep -qF "  $base"`（列全 dist 资产）；本地 build-release.sh 与 CI 同口径。
- **发布运行事实（9987583 / 7c2974b / 06f2d8e / fdf803f / 2b8b8ce）**：`.github/workflows/release.yml` 推 `v*` tag 或 `workflow_dispatch` 触发；ubuntu-latest ×1，2-4 分钟；`docs/发布流程.md` runbook 7 节（触发方式 / 发版前检查 / 自动 / 手动 / 无 Actions 等价 / 校验消费 / 回退 / 常见故障）。`v0.1.0`（本地构建，校验和 shasum 格式，已仅重生成 `checksums.txt` 覆盖、6/6 哈希一致，参考 [docs/发布流程.md:79](file://docs/发布流程.md#L79)）→ `v0.1.1`（CI run `35496169845`，3m11s，7 资产，GNU 校验和，`devsys v0.1.1 (fdf803f)`）。`scripts/build-release.sh` heredoc 6 平台矩阵；`scripts/install.sh` / `install.ps1` `gh release download` 鉴权优先 + `git clone --branch + go build` 回退；安装校验项容忍 shasum `*` 标记。README 安装指引指向 `v0.1.1`。
