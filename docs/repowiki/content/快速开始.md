---
status: stable
type: getting_started
triggers: [安装运行, 初始化项目, 首次使用, 离线构建, 关键字检索, 严格配置检查, 工作流, 审批, next, MCP, session start, wire, devsys wire, AGENTS.md, 共享应用服务, --jsonl]
description: 从离线构建到 init / config check / search 与 M2 状态机、对账修复命令、M3 workflow/approval/next、M4 共享应用服务 / MCP / session / context / 记录族 / wire / --jsonl 的上手路径
generated: true
source_commit: 997c5f8
generator: repowiki-gen
---

# 快速开始

<cite>
**本文引用的文件**
- [go.mod:1-4](file://go.mod#L1-L4)
- [README.md:29-86](file://README.md#L29-L86)
- [README.md:120-126](file://README.md#L120-L126)
- [internal/cli/cli.go:31-44](file://internal/cli/cli.go#L31-L44)
- [internal/cli/cli.go:144-180](file://internal/cli/cli.go#L144-L180)
- [internal/cli/cli.go:202-360](file://internal/cli/cli.go#L202-L360)
- [internal/cli/cli.go:361-976](file://internal/cli/cli.go#L361-L976)
- [internal/cli/cli.go:1206-1437](file://internal/cli/cli.go#L1206-L1437)
- [internal/project/init.go:41-124](file://internal/project/init.go#L41-L124)
- [internal/project/init.go:126-159](file://internal/project/init.go#L126-L159)
- [internal/project/tree.go:10-49](file://internal/project/tree.go#L10-L49)
- [internal/config/config.go:29-42](file://internal/config/config.go#L29-L42)
- [internal/config/config.go:95-140](file://internal/config/config.go#L95-L140)
- [internal/search/search.go:20-127](file://internal/search/search.go#L20-L127)
- [scripts/smoke-m1.ps1:1-64](file://scripts/smoke-m1.ps1#L1-L64)
- [scripts/smoke-m1.sh:1-46](file://scripts/smoke-m1.sh#L1-L46)
- [scripts/smoke-m2.sh:1-106](file://scripts/smoke-m2.sh#L1-L106)
- [scripts/smoke-m2.ps1:1-120](file://scripts/smoke-m2.ps1#L1-L120)
- [scripts/smoke-m3.sh:1-204](file://scripts/smoke-m3.sh#L1-L204)
- [docs/examples/workflows/quick-fix.md:1-50](file://docs/examples/workflows/quick-fix.md#L1-L50)
- [internal/workflow/cache.go:47-73](file://internal/workflow/cache.go#L47-L73)
- [internal/next/evaluate.go:113-181](file://internal/next/evaluate.go#L113-L181)
</cite>

## 更新摘要
**变更内容**
- 基于 2735f62 增补 M1 命令：`devsys search <keyword>`（单关键字、文本扫描、`.cache/`/`local/` 排除、零命中仍退出 0）。
- 严格配置层（`devsys config check`）补齐：四份受管元数据逐条定位，未知 `schema_version` 触发退出码 4，`--json` 输出 `error.problems` 数组。
- 更新 `init` 描述与幂等行为：现有文件不重写，仅追加缺失的 `.gitignore` 行；M1 落盘前会先校验已有受管文件，遇到未知版本直接拒绝。
- 关键剧本替换：`scripts/smoke-m1.{ps1,sh}` 是 M1.7 验证入口，PowerShell `-Keep` / Bash `--keep` 保留临时项目。
- `docs/examples/m1-devsys/` 是真实通过验收的 13 文件 `.devsys/` 快照，可复制后用 `devsys init` 补齐注册表。
- 来源基线刷新至 `4ad8f9e`；命令与冒烟流程不变。单独测量搜索性能可运行 `go test ./internal/search -run '^$' -bench BenchmarkSearchThousandFiles -benchmem -benchtime=3x -count=1`；FreshFiles 与 RepeatedFiles 的口径见[开发与故障诊断](开发与故障诊断.md#搜索正确性与性能测量)，不是普通测试的 2s 硬门限（[internal/search/search_test.go:102-144](file://internal/search/search_test.go#L102-L144)）。
- 基于 `85b0de7` 增补 M2 命令：`workitem`（create/get/transition/claim，`--expect` 版本哈希防旧视图写入）、`doctor`（只读对账）、`recover`（显式恢复）、`repair`（摘要确认的状态修复）；退出码语义扩展：非法转换/旧快照 → 4，修复摘要不符 → 3。
- 基于 `cf7b256` 增补 M3 命令：`workflow check/start/next/step-complete/pause/resume/cancel`（租约活跃时强制 `--owner`/`--token` fencing）、`approval list/request/approve/reject`（消费在转换事务的 Guard 内、`scope=stage_gate` 拒绝走 `rejectStageGate` 同事务完成决定与状态变更）、`next`（只读 PASS/CONCERNS/FAIL 与 §7.4 推荐动作，exit 恒 0）；退出码：M3 领域错误 `kind=workflow|approval` code=4 与 M2 同；坏策略（无 LKG）/ 实例操作拒绝 → 4；`--expect` 不匹配 → 3。`scripts/smoke-m3.{ps1,sh}` 七段 PASS。
- **M4（997c5f8）增补接入层**：CLI 薄化为「`runXxx` + `appService()` + `toCoded`」三层——所有业务经 `internal/app` 共享应用服务。新增命令族：`project list|get|status|blueprint|update|state-update` / `decision list|get|create|approve` / `finding list|get|create|resolve` / `event list|record` / `artifact list|get|register|update|history` / `run list|get|create|update|heartbeat|log` / `context get|workitem|refresh|compact` / `knowledge status`（M4 恒 `unavailable`，M5 §12.6 达到）/ `session start` / `wire`（AGENTS.md 纪律块注入，幂等）/ `mcp serve`（基于官方 Go SDK + profile 过滤的 60+ 工具）。新增 `--jsonl`（列表行流，与 `--json` 同字段名）与 `writeJSONL[T]` 泛型。**错误模型**收口到 `*app.Error{Kind,Message,Problems}` + `Class()` 四类归一（usage/precondition/internal/invalid）；CLI `toCoded` 与 MCP `fail` 共用——`code` 字段是分支依据，`kind` 仅供人读。**MCP 入口**：`devsys mcp serve --profile session,executor,reviewer,admin`，未注册的工具既不出现于 `tools/list` 也无法以名称调用（注册期 `visible` 过滤）。**写一致**：CLI 的 `--expect <64-hex>` 与 MCP 工具的 `expect` 字段在 `internal/app` 内被反解码为原字节传给 storage 层——MCP 写的记录用 CLI 读到的 `version` 一致。`scripts/smoke-m4.{sh,ps1}` 五段 PASS（MCP 表面 / CLI-MCP 写一致 / session start / `--jsonl` 流 / wire 幂等）；`scripts/exchange-demo.sh` 演示 CLI 端读取与 `--jsonl` 流的实际使用。**写路径守卫**：`internal/record/update.go` 的 `readSnapshot[T]` 泛型 + `UpdateDecision / UpdateFinding / AppendArtifactVersion`（artifact 版本链线性，`ErrSuperseded` 拒绝并发分叉）；`internal/run.ReadSnapshot` 同形支撑 `app.RunUpdate` 的 `expect` 字段。退出码：M4 新增领域错误（`kind=gate / quality` 等）code=4；知识层预留 10/11（M5 启用）；`devsys wire --dry-run` / `mcp serve --profile` 参数错误 code=2；`mcp serve` 启动失败 / IO 失败 code=1。

## 目录
1. [环境要求](#环境要求)
2. [构建](#构建)
3. [init](#init)
4. [config check](#config-check)
5. [search](#search)
6. [M2 状态机与对账命令](#m2-状态机与对账命令)
7. [M3 工作流 / 审批 / next](#m3-工作流--审批--next)
8. [M4 共享应用服务 / MCP / session / context / 记录族 / wire](#m4-共享应用服务--mcp--session--context--记录族--wire)
9. [典型工作流与剧本](#典型工作流与剧本)
10. [退出码与输出约定](#退出码与输出约定)
## 环境要求

- Go 1.26 工具链（`go.mod` 固定 `go 1.26`）和可在 PATH 调用的 Git。
- 唯一外部 Go 依赖是 `gopkg.in/yaml.v3`，已 `go mod vendor` 并提交 `vendor/`；离线构建时设 `GOPROXY=off` 或在 PowerShell 中设 `$env:GOPROXY='off'`。
- 不需要数据库服务；项目状态保存在 `.devsys/`，随 Git 同步。

章节来源：[go.mod:1-4](file://go.mod#L1-L4)、[README.md:87-93](file://README.md#L87-L93)、[README.md:120-126](file://README.md#L120-L126)。

## 构建

在仓库根构建 `devsys` 二进制：

```sh
# PowerShell / cmd
go build -o bin/devsys.exe ./cmd/devsys
# Bash / Git Bash（路径与产物按平台调整）
go build -o bin/devsys ./cmd/devsys

bin/devsys.exe --version    # devsys 0.1.0-dev
bin/devsys.exe --help
bin/devsys.exe bogus; echo $?   # 2
```

`.exe` 是此仓库 Windows 示例的产物名；其他平台可改为 `bin/devsys`。发布构建需注入版本与短 SHA：

```sh
go build -ldflags "-X workloom/internal/version.Version=0.1.0 -X workloom/internal/version.Commit=$(git rev-parse --short HEAD)" -o bin/devsys.exe ./cmd/devsys
```

章节来源：[README.md:31-35](file://README.md#L31-L35)、[README.md:95-99](file://README.md#L95-L99)。

## init

`devsys init` 是唯一允许修改受管文件的写命令。它把 `.devsys/` 布局落到 Git 仓库根目录、写入 4 份占位元数据、注册项目路径到用户级注册表，并保证 `.devsys/.gitignore` 至少覆盖 `local/` 与 `.cache/`。子目录不是仓库根时会拒绝执行。

```sh
cd /path/to/your/git-repo
devsys --json init
devsys --quiet init
devsys config check
```

文本模式报告初始化目录、项目名称与 ID、实际创建的路径数量及注册表路径；数量取决于已有布局。JSON 模式返回实际创建路径与登记结果。参见下方 CLI 源码引用；不要把示例数量当成固定契约。

幂等行为：

- 缺失的 `.devsys/`、子目录和占位文件会被补建；现存占位元数据不重写。
- `.devsys/.gitignore` 若已存在，只追加缺失的 `local/`、`.cache/` 行，原有内容不变。
- 落盘前先校验现有受管元数据；若已存在的 `project.yaml` / `config.yaml` / `state/*.yaml` 出现未知 `schema_version`、未知键或类型错误，`init` 拒绝改动并报退出码 4。
- 当前目录不在 Git 仓库或 `git` 不可用 → 退出码 3（前置条件），无任何写入。

章节来源：[internal/project/init.go:41-124](file://internal/project/init.go#L41-L124)、[internal/project/init.go:126-159](file://internal/project/init.go#L126-L159)、[internal/project/tree.go:10-49](file://internal/project/tree.go#L10-L49)、[internal/cli/cli.go:180-248](file://internal/cli/cli.go#L180-L248)、[README.md:37-44](file://README.md#L37-L44)。

## config check

`devsys config check` 是只读诊断：不加锁、不写盘、不恢复事务。它遍历四份受管元数据文件，逐条定位问题，并按 `文件:行号: 字段: 原因` 格式化输出。

```sh
devsys config check             # 通过：config ok: 4 files checked
devsys --json config check      # {ok,root,checked,project?,config?}
devsys config check; echo $?    # 0 / 4 / 3
```

四份受管文件（顺序固定）：`project.yaml`、`config.yaml`、`state/current.yaml`、`state/milestones.yaml`。问题示例：

```
project.yaml:5: unknown_field: unknown key
project.yaml:2: schema_version: unsupported version 99 (...)
config.yaml:2: timeout: unknown key
```

`--json` 模式下问题进入 `error.problems` 数组（`code=4`、`kind=invalid`）；通过时返回完整 `project`、`config` 解析结果。`init` 之前的项目目录 → 退出码 3。

章节来源：[internal/config/config.go:29-42](file://internal/config/config.go#L29-L42)、[internal/config/config.go:95-140](file://internal/config/config.go#L95-L140)、[internal/cli/cli.go:292-336](file://internal/cli/cli.go#L292-L336)、[README.md:46-55](file://README.md#L46-L55)。

## search

`devsys search <keyword>` 在 `.devsys/` 下做无索引文本扫描：大小写不敏感子串匹配，单文件最多 5 条命中行，行内容截断 200 字节（UTF-8 安全），排除 `.cache/` 与 `local/`。缺少关键字参数或多于一个位置参数 → 退出码 2。

```sh
devsys search 密码
devsys --json search WLM-2
devsys search no-such-keyword; echo $?   # 0（零命中仍成功）
```

文本输出：`<相对路径>:<行号>: <行内容>`，末尾汇总 `<N> match(es) in .devsys/ for "<keyword>"`。

`--json` 字段：`ok`、`root`、`query`、`matches[]`（`path`、`line`、`text`）、`total`。零命中时 `matches` 是空数组、`total=0`，退出码仍为 0。

章节来源：[internal/search/search.go:20-127](file://internal/search/search.go#L20-L127)、[internal/cli/cli.go:274-313](file://internal/cli/cli.go#L274-L313)、[README.md:57-67](file://README.md#L57-L67)。

## M2 状态机与对账命令

M2 命令作用于 `.devsys/` 的领域记录：`workitem` 读写工作项状态，`doctor` / `recover` / `repair` 负责对账与修复。写命令都要求 `--actor` 与 `--reason`（写入审计事件）；状态转换与领取用 `--expect <64-hex version>` 提交你读到的版本（`workitem get` 输出），旧快照会被拒绝。

```sh
bin/devsys.exe workitem get WLM-1                                   # 打印字段与 version: <64-hex>
bin/devsys.exe workitem create --title 任务标题 --actor me --reason 建任务
bin/devsys.exe workitem transition --id WLM-1 --to ready --actor me --reason 拆分完成 --expect <hash>
bin/devsys.exe workitem claim --id WLM-1 --owner me --reason 开工 [--expect <hash>]
bin/devsys.exe doctor                                               # 只读：过期/孤儿领取、待恢复事务、修复提议
bin/devsys.exe recover --actor me --reason 恢复中断的领取
bin/devsys.exe repair --dry-run --actor me --reason 盘点             # 打印提议表与 digest
bin/devsys.exe repair --apply --confirm <digest> --actor me --reason 确认修复
```

- 非法状态转换（如 `draft → in_progress`）→ 退出码 4，stderr 列出允许的下一步。
- 版本哈希不匹配（旧视图）→ 退出码 4，重新 `workitem get` 后再试。
- `doctor` 是只读对账：不创建锁文件、不触发事务恢复；有待恢复事务时只报告事务、不输出业务事实。
- `recover` 先做确定性事务恢复，再释放过期/孤儿领取（每次释放写 `lease_recovered` 事件）；重复执行不产生重复事件。
- `repair --apply` 需要与当前状态一致的 `--confirm` 摘要；摘要不符或证据漂移 → 退出码 3，不写任何文件。
- M2 完整链路（领取 → 模拟崩溃 → `doctor` → `recover` → 确认修复）见 `scripts/smoke-m2.ps1` / `scripts/smoke-m2.sh`。

章节来源：[internal/cli/cli.go:361-689](file://internal/cli/cli.go#L361-L689)、[internal/reconcile/reconcile.go:155-380](file://internal/reconcile/reconcile.go#L155-L380)、[README.md:92-128](file://README.md#L92-L128)、[scripts/smoke-m2.sh:1-106](file://scripts/smoke-m2.sh#L1-L106)。

## M3 工作流 / 审批 / next

M3 三族命令作用于工作流策略、审批生命周期与就绪判定。示例策略 `docs/examples/workflows/quick-fix.md` 给出最简单的端到端入口；更完整的见 `feature-development.md` 与 `architecture-change.md`。

```sh
# 1) 准备策略与工作项
cp docs/examples/workflows/quick-fix.md .devsys/workflows/
bin/devsys.exe workflow check                       # 只读：issues + policies；exit 0/4
bin/devsys.exe workitem create --title "fix typo" --actor me --reason 建任务
for t in backlog ready; do bin/devsys.exe workitem transition --id WLM-1 --to $t --actor me --reason smoke --expect "$(bin/devsys.exe --json workitem get WLM-1 | jq -r .version)"; done

# 2) 启动工作流实例（租约未激活：可不带 --owner/--token）
V=$(bin/devsys.exe --json workitem get WLM-1 | jq -r .version)
bin/devsys.exe workflow start --id WLM-1 --policy quick-fix --actor me --reason begin --expect "$V"
bin/devsys.exe workflow next --id WLM-1                          # 列出当前步骤候选与首满足推荐
bin/devsys.exe workflow step-complete --id WLM-1 --actor me --reason go --expect "$V"   # 自动取首满足
bin/devsys.exe workflow pause --id WLM-1 --actor me --reason hold --expect "$V"
bin/devsyse.exe workflow resume --id WLM-1 --actor me --reason go --expect "$V"
bin/devsys.exe workflow cancel --id WLM-1 --actor me --reason abort --expect "$V"

# 3) 领取质量门（policy.quality_gate.min_score 拦截 thin 任务）
V=$(bin/devsys.exe --json workitem get WLM-1 | jq -r .version)
bin/devsys.exe workitem claim --id WLM-1 --owner me --reason 开工 --expect "$V"      # exit 0 或 4

# 4) 审批门禁（policy.gates.stages.<stage>.require_approval: true）
V=$(bin/devsys.exe --json workitem get WLM-1 | jq -r .version)
bin/devsys.exe workitem transition --id WLM-1 --to in_progress --actor me --reason go --expect "$V"   # 缺审批 → exit 4
bin/devsys.exe approval request --id WLM-1 --stage in_progress --actor me --reason 关键变更
bin/devsys.exe approval approve --id approval-1 --by boss --comment ok
V=$(bin/devsys.exe --json workitem get WLM-1 | jq -r .version)
bin/devsys.exe workitem transition --id WLM-1 --to in_progress --actor me --reason go --expect "$V"   # 同事务消费
bin/devsys.exe approval reject --id approval-2 --by boss --reason "risk too high"   # scope=stage_gate → 同事务 blocked

# 5) 就绪判定（只读）
bin/devsys.exe next                    # readiness: PASS|CONCERNS|FAIL + reasons + risks + next
bin/devsys.exe --json next             # 完整 Report JSON；exit 恒 0
```

退出码语义（M3 新增）：

- `workflow check` 把策略文件的结构/类型/必填/交叉引用错误升为 `kind="invalid"`、`code=4`；未知键 warning 仍 exit 0；`workflows/` 目录缺失 exit 4。
- 实例操作（`start` / `step-complete` / `pause` / `resume` / `cancel`）失败：`kind="workflow"`、`code=4`；`--expect` 不匹配 → `kind="precondition"`、`code=3`。
- 审批 `request` / `approve` / `reject` 失败：`kind="approval"`、`code=4`（`request --id` 缺失等参数错误走 `kind="usage"`、`code=2`）。
- `next` exit 恒 0；脚本按 `--json next` 输出的 `readiness`（PASS / CONCERNS / FAIL）分支。

M3 完整链路（门禁 → 证据补齐 → 推进；质量门 → 改进 → 领取；last-known-good → 阻断 claim、放行 transition；实例条件分支/自环拒绝/paused 拒绝推进；审批 request → approve → 消费 → 离开阶段失效 → 重新 request）见 `scripts/smoke-m3.ps1` / `scripts/smoke-m3.sh`（Git Bash 版已就绪；PowerShell 版与 M2 同等收尾）。

## M4 共享应用服务 / MCP / session / context / 记录族 / wire
M4 把 CLI 薄化为渲染层，所有业务经 `internal/app` 共享应用服务。MCP 服务端在 `internal/mcp` 基于官方 Go SDK 暴露同款工具；`devsys wire` 把纪律块注入 `AGENTS.md`。

```sh
# 1) 项目元数据与状态（project 族；admin 工具 — 项目创建/更新需 admin profile）
bin/devsys.exe project list
bin/devsys.exe project get
bin/devsys.exe project status          # 计数 + 风险 + 下一步
bin/devsys.exe project blueprint
bin/devsys.exe project update --name T --status active --expect <hash> --actor me --reason r

# 2) 决策 / 发现 / 事件 / 工件 / 运行 五族记录
bin/devsys.exe decision list [--jsonl]
bin/devsys.exe decision get <id>
bin/devsys.exe decision create --title T --decision D --by <author> [--context C] [--reasoning R]
bin/devsys.exe decision approve --id <n> --by <decider> [--expect <hash>]
bin/devsys.exe finding list
bin/devsys.exe finding create --title T --finding D --severity low|medium|high --by <author>
bin/devsys.exe finding resolve --id <n> --by <resolver> [--expect <hash>]
bin/devsys.exe event list [--type <type>] [--subject <id>] [--jsonl]
bin/devsys.exe event record --type <type> --subject <id> --actor me --content <json>
bin/devsys.exe artifact list [--name <name>] [--jsonl]
bin/devsys.exe artifact register --name <name> --kind <kind> --by <author>
bin/devsys.exe artifact update --id <n> --kind <kind> --by <author> --expect <hash>   # ErrSuperseded → exit 4
bin/devsys.exe artifact history --id <head>
bin/devsys.exe run list [--workitem <id>] [--status <status>] [--jsonl]
bin/devsys.exe run get <id>
bin/devsys.exe run create --workitem <id> --by <author>
bin/devsys.exe run update --id <r> --status running|completed|failed --by <updater> --expect <hash>
bin/devsys.exe run heartbeat --id <r> --by <owner>

# 3) 上下文聚合（只读；不创建锁、不写盘）
bin/devsys.exe context get [--limit N]
bin/devsys.exe context workitem --id <WLM>
bin/devsys.exe context refresh [--limit N]
bin/devsys.exe context compact          # 裁短 payload

# 4) 知识层状态（M4 恒 unavailable，M5 §12.6 达到）
bin/devsys.exe knowledge status        # {"status":"unavailable","layer":"index",...}

# 5) 会话定向（单次返回项目状态 + 推荐动作）
bin/devsys.exe session start --harness <h> --agent <id> --intent <i> [--workspace <dir>] [--compact]

# 6) AGENTS.md 注入（幂等；--dry-run 预览）
bin/devsys.exe wire
bin/devsys.exe wire --dry-run
# 三次 wire 字节级一致；区段外字节保留（含其它工具的管理块）

# 7) MCP 服务端（在 stdio 上启动 Model Context Protocol 服务）
bin/devsys.exe mcp serve                                           # 默认 profile = session,executor
bin/devsys.exe mcp serve --profile session,executor,reviewer        # 显式指定
# 未知 profile 名字 → exit 2 + 错误原因
# 协议层（handshake / tools/list / tools/call）由 SDK 处理；业务规则与 CLI 共用同一份 internal/app
```

退出码语义（M4 新增）：

- `decision / finding / event / artifact / run` 写入失败：`kind="invalid" / "workflow" / "gate" / "quality" / "workitem" / "approval"`、`code=4`；`--expect` 不匹配 → `kind="precondition"`、`code=3`。
- `artifact update --id <n> --expect <hash>` 在 `currentID` 已有子版本时返回 `ErrSuperseded` → exit 4 + 错误说明「artifact already has a newer version; re-read and retry」。
- `context` / `session start` / `knowledge status` 只读命令，exit 恒 0。
- `wire --dry-run` 预览 diff 不写盘；`wire` 三次运行字节级一致；管理块破损 → exit 3。
- `mcp serve --profile` 未知名字 → exit 2；进程启动失败 / IO 失败 → exit 1。
- 知识层退出码 10/11 在 M5 启用，M4 保留。

M4 端到端剧本：`scripts/smoke-m4.{sh,ps1}` 五段 PASS：

1. **MCP 表面**：`scripts/m4helper.exe` 作为 SDK 客户端连 stdio；`tools/list` 默认 `session + executor` 应含 `health` / `workitem_list` / `decision_get` / `agent_session_start` / `knowledge_status` / `run_heartbeat`，并隐藏 `project_update` / `project_create` / `approval_decide`；`decision_create` 记录往返。
2. **CLI/MCP 写一致**：MCP 创建的 decision → CLI `--json decision get` 读到同一个 `version`。
3. **会话接口**：单次 `session start` 返回带推荐动作的视图。
4. **交换协议**：`--jsonl workitem list` / `event list` 各行一条 JSON 记录，字段名与 `--json` 信封同名数组一致；`scripts/exchange-demo.sh` 演示完整读取流。
5. **wire 幂等**：三次 `wire` 后 AGENTS.md 字节级一致；`--dry-run` 预览 diff 不写盘。

```sh
bash scripts/smoke-m4.sh --keep
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-m4.ps1 -Keep

# scripts/exchange-demo.sh 演示读取 + --jsonl 流（无需外部脚本，依赖 jq/python）
bash scripts/exchange-demo.sh
```

章节来源：[internal/app/app.go:20-129](file://internal/app/app.go#L20-L129)、[internal/cli/cli.go:99-133](file://internal/cli/cli.go#L99-L133)、[internal/mcp/server.go:91-130+](file://internal/mcp/server.go#L91-L130)、[internal/cli/mcp.go:48-84](file://internal/cli/mcp.go#L48-L84)、[internal/cli/records.go](file://internal/cli/records.go)、[internal/cli/wire_test.go](file://internal/cli/wire_test.go)、[internal/record/update.go](file://internal/record/update.go)、[internal/run/run.go:156-185](file://internal/run/run.go#L156-L185)、[scripts/smoke-m4.sh:1-50](file://scripts/smoke-m4.sh#L1-L50)、[scripts/exchange-demo.sh](file://scripts/exchange-demo.sh)。

## M5 / M6 工作区与执行调度（执行层）

M6 起 `devsys` 真正能跑 attempt：执行层（`internal/harness + workspace + dispatch + retry + prompt`）通过 `internal/app` 接到 CLI 与 MCP 双入口。M5 保留位（`internal/knowledge` 仍未启用，`devsys knowledge status` 仍返回 `unavailable`）。

```sh
# 1) 工作区：创建或复用 git worktree（idempotent）
bin/devsys.exe worktree prepare --workitem WLM-1 \
    --actor me --reason "start dispatch" [--run run-id] [--branch devsys/my-key]
bin/devsys.exe worktree list [--workitem WLM-1]
bin/devsys.exe worktree remove --workitem WLM-1 --actor me --reason cleanup [--force]

# 2) 调度 tick（recover → sweep → plan → claim → spawn）
bin/devsys.exe dispatch --once --actor ops --reason tick       # 一次性
bin/devsys.exe dispatch --watch --interval 30s --actor ops --reason tick  # 循环，Ctrl-C 退出 0
bin/devsys.exe dispatch --dry-run --actor ops --reason preview  # 只 Plan 不创建 run

# 3) attempt：默认 Shell 适配器 / 三种 harness CLI 适配器
bin/devsys.exe run exec --id run-... --actor me --reason "smoke test" -- -- sh -c "echo hi"
bin/devsys.exe run exec --id run-... --harness codex --model gpt-5 \
    --actor ops --reason "acceptance" --timeout 120s

# 4) 提示词预览（不发起 attempt）
bin/devsys.exe run prompt --id run-... [--round N] [--write]    # --write 落 .devsys/local/...

# 5) 完成校验（只读） + 终态
bin/devsys.exe run verify --id run-...                         # advanced | not advanced + reason
bin/devsys.exe run complete --id run-... --actor me --reason "acceptance" \
    [--expect <hash>] [--note "evidence"] [--force --by reviewer]
bin/devsys.exe run fail --id run-... --actor me --reason "tests failed" [--expect <hash>]
bin/devsys.exe run cancel --id run-... --actor me --reason "stop" [--expect <hash>]
```

`DEVSYS_PROJECT_ROOT` 让 agent 在 worktree 内运行时把报告写回主仓库的 `.devsys/`：

```sh
# agent 在 git worktree 内运行
export DEVSYS_PROJECT_ROOT=/path/to/main/repo
devsys --json run exec --id run-... --harness codex --actor me --reason accept
```

退出码语义（M6 新增）：

- `devsys worktree prepare/remove --path` 越界（不在配置根内）→ exit 3（`kind="precondition"`）。
- `devsys run exec --harness <unknown>` → exit 2（`kind="usage"`）。
- `devsys run exec --harness codex` 但 codex 未安装 → exit 3（`kind="precondition"`）。
- `devsys run exec --timeout` 到期 → 整棵进程树被终止（POSIX 进程组 / Windows JobObject），run 终态 `timeout`。
- `devsys run complete` 验证未 advance → exit 4（`kind="workflow"`），run 进 `failed`，workitem 进 `review`；`--force --by reviewer` 接受。
- `devsys run fail|cancel` 在已终态时 → exit 4（不让两个 actor 抢结果）。
- `devsys run verify` / `devsys dispatch --dry-run` 是只读，exit 恒 0。
- `devsys dispatch --watch` Ctrl-C 退出 0（tick 内失败以 notice 回报）。

M6 端到端剧本：`scripts/smoke-m6.{sh,ps1}` 覆盖九段（workspace invariants / dispatch tick / harness adapter chain / completion check / retry sweep / unavailable harness / read-only never dispatch / §17 SPEC 一致性自检 / 真实集成画像）。`scripts/m6helper` 暂无（沿用 `m4helper` 作为 SDK 客户端模板）。

```sh
bash scripts/smoke-m6.sh --keep
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-m6.ps1 -Keep
```

章节来源：[internal/harness/harness.go:1-130+](file://internal/harness/harness.go#L1-L130)、[internal/harness/shell.go:1-318](file://internal/harness/shell.go#L1-L318)、[internal/harness/cli.go:1-180](file://internal/harness/cli.go#L1-L180)、[internal/harness/proc_unix.go](file://internal/harness/proc_unix.go)、[internal/harness/proc_windows.go](file://internal/harness/proc_windows.go)、[internal/workspace/workspace.go:48-315](file://internal/workspace/workspace.go#L48-L315)、[internal/workspace/key.go:36-110](file://internal/workspace/key.go#L36-L110)、[internal/workspace/path.go:23-105](file://internal/workspace/path.go#L23-L105)、[internal/workspace/git.go:36-184](file://internal/workspace/git.go#L36-L184)、[internal/workspace/hook.go:1-198](file://internal/workspace/hook.go#L1-L198)、[internal/dispatch/plan.go:90-209](file://internal/dispatch/plan.go#L90-L209)、[internal/retry/backoff.go:38-99](file://internal/retry/backoff.go#L38-L99)、[internal/prompt/prompt.go:113-328](file://internal/prompt/prompt.go#L113-L328)、[internal/app/dispatch.go:66-454](file://internal/app/dispatch.go#L66-L454)、[internal/app/runexec.go:84-722](file://internal/app/runexec.go#L84-L722)、[internal/app/runverify.go:27-144](file://internal/app/runverify.go#L27-L144)、[internal/app/workspace.go:38-257](file://internal/app/workspace.go#L38-L257)、[internal/app/prompt.go:45-313](file://internal/app/prompt.go#L45-L313)、[internal/cli/worktree.go](file://internal/cli/worktree.go)、[internal/cli/dispatch.go](file://internal/cli/dispatch.go)、[internal/cli/records.go:614-769](file://internal/cli/records.go#L614-L769)、[scripts/smoke-m6.sh:1-198](file://scripts/smoke-m6.sh#L1-L198)、[docs/M6-一致性自检.md](file://docs/M6-一致性自检.md)。


## 典型工作流与剧本

M1.7 提供跨平台回归剧本 `scripts/smoke-m1.ps1` 与 `scripts/smoke-m1.sh`：在临时目录构建 CLI 与 helper，新建独立 Git 项目，跑完 init → 建任务 → 记录决策/发现/Artifact → 乐观更新 → 检索回读的全链路。**注意**：smoke 剧本执行完整的 M1 生命周期，不只是 init/config/search；这里仅作为已就绪的端到端验证入口引用。

```powershell
# Windows PowerShell：保留临时项目
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-m1.ps1 -Keep
# 仅验证：成功后删除
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-m1.ps1
```

```sh
# Bash / Git Bash（WSL 需自备 Go 与 Git）
bash scripts/smoke-m1.sh --keep
bash scripts/smoke-m1.sh
```

剧本成功时打印 `PASS: M1 create -> record -> complete; all tracked state is UTF-8 text.`；`-Keep`/`--keep` 额外打印 `KEPT_PROJECT=<path>` 用于手工回读。

M3 端到端剧本：`scripts/smoke-m3.ps1` 与 `scripts/smoke-m3.sh` 覆盖七段（done 门禁拦截 → 补 artifact + comment 放行 → 父/兄弟 `context_refs` 广播 → 低质量 claim 拦截 → 改进后领取 → 坏策略阻断 claim 但放行 transition via LKG + `next` 报 `invalid_policy` → 工作流实例条件分支/自环拒绝/paused 拒绝推进/无 scheduling 变更 → approval 门禁拦截 → request → approve → transition 同事务消费 → reject 同事务 blocked）。`scripts/m3helper/main.go` 是 seed + 回读双模（`setup` / `lowq` / `evidence` / `improve` / `status` / `propagated` 子命令）。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-m3.ps1 -Keep
```

```sh
bash scripts/smoke-m3.sh --keep
```

`docs/examples/m1-devsys/` 是 13 文件的通过验收 `.devsys/` 快照（不含 `local/`、`.cache/`、用户注册表），可整体复制到新 Git 仓库的 `.devsys/`，再在该仓库执行 `devsys init` 补齐目录与注册表；项目 ID 为 `demo-project`，用于真实项目需一致替换示例 ID 与内容。

8. M4 接入层：`devsys session start` 单次给出项目状态 + 推荐动作；`devsys context get|workitem|refresh|compact` 只读上下文聚合；`devsys knowledge status`（M4 恒 `unavailable`，M5 §12.6 达到）；`devsys wire` 注入 AGENTS.md 纪律块（`<!-- devsys:begin/end -->`、幂等、区段外保留）；`devsys mcp serve --profile <list>` 启动 MCP 服务。`decision / finding / event / artifact / run` 五族记录 CLI/MCP 共用同一份 `app.Error`，CLI `--expect <64-hex>` 与 MCP 工具 `expect` 字段反解码为同一字节给 storage 层。
| 退出码 | 含义 | 触发示例 |
|---|---|---|
| 0 | 成功（含 `search` 零命中、`next` 完成判定、`session start` / `context` / `knowledge status` 完成） | `init` 幂等完成、`config check` 通过、`search` 命中或空、`next` 输出报告、`session start` 返回视图 |
| 1 | 内部错误 | 读取/写入系统调用失败；`mcp serve` 启动失败 |
| 2 | 用法错误 | 未知命令/选项、`search` 缺关键字或多参数、`config` 缺子命令、`workflow` 缺子命令、`approval list --status foo`、`mcp serve --profile <unknown>`、`wire` 缺位置参数 |
| 3 | 前置条件错误 | 不在 Git 仓库、`git` 不在 PATH、未初始化 `.devsys/`、`repair --confirm` 摘要不符、`workitem/approval/workflow/context/decision/finding/artifact/run` 的 `--expect` 不匹配、`wire` 检测到管理块破损（仅有 `begin` 或仅有 `end` 或嵌套） |
| 4 | 受管状态无效或操作被拒 | 未知键、类型错误、缺必填项、`schema_version` 不支持（未知版本仍可只读诊断）；`workitem` 非法状态转换、旧快照；`workflow check` 策略错误、`workflow start` 缺策略/坏策略无 LKG、`workflow step-complete --to` 未声明/条件不满足/自环/paused；`approval` 重复请求/已 `invalidated`/已非 pending/已 consumed/不匹配 gate；`decision / finding / artifact update` 在并发或版本链分叉时（`ErrSuperseded` → exit 4 + 错误说明「artifact already has a newer version; re-read and retry」） |
| 10 / 11 | 知识层（**M5 启用**；M4 保留） | M5 `knowledge status`：`fresh = 0` / `stale = 10` / `missing = 11` |

全局开关（`--json` / `--jsonl` / `--quiet` / `--version` / `--help`）必须放在子命令之前；解析器遇到第一个非选项参数即停止收集开关。`init` 与 `config check` 拒绝多余位置参数。

JSON / JSONL 模式：

- `--json`：成功输出走 stdout（带 `ok:true`），错误信封走 stderr（`{ok:false, error:{code,kind,message,problems?}}`）。
- `--jsonl`：列表类命令按「一行一条 JSON 记录」输出，字段名与 `--json` 信封内同名数组字段一致；空列表渲染为零行（不是空文档）。

M3 起 `kind` 取值扩到 `"workitem"` / `"workflow"` / `"approval"`；M4 起又扩到 `"gate"` / `"quality"`（门禁 / 质量门）。`config check` 不加锁、不写盘；它不替代并发业务读取，只做只读诊断。

章节来源：[internal/cli/cli.go:31-44](file://internal/cli/cli.go#L31-L44)、[internal/cli/cli.go:99-133](file://internal/cli/cli.go#L99-L133)、[internal/app/app.go:20-129](file://internal/app/app.go#L20-L129)、[internal/mcp/tools.go:108-156](file://internal/mcp/tools.go#L108-L156)、[internal/cli/mcp.go:48-84](file://internal/cli/mcp.go#L48-L84)、[internal/cli/wire_test.go](file://internal/cli/wire_test.go)、[internal/record/update.go](file://internal/record/update.go)、[scripts/smoke-m4.sh:1-50](file://scripts/smoke-m4.sh#L1-L50)、[scripts/exchange-demo.sh](file://scripts/exchange-demo.sh)、[scripts/m4helper/main.go](file://scripts/m4helper/main.go)。

下一步：[项目总览](项目总览.md)、[开发与故障诊断](开发与故障诊断.md)、[共享应用与 MCP](../knowledge/共享应用与MCP/概述.md)。
